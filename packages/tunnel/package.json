{
  "name": "@qianmo/tunnel",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Qianmo on-demand tunnels: a connection that exists only while a lease does, with three teardown paths that do not need the peer to cooperate.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@qianmo/protocol": "workspace:*",
    "@qianmo/transport": "workspace:*"
  }
}
