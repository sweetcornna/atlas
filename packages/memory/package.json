{
  "name": "@qianmo/memory",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "description": "Qianmo layered memory: the working / project / baseline tables, their mandatory provenance and dual-time-axis fields, and the sedimentation task that promotes a finished task's working memory into project memory.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  }
}
