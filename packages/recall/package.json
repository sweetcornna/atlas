{
  "name": "@qianmo/recall",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Qianmo memory recall: deterministic retrieval (tags + keywords + time decay), small-scale full injection into the prompt, and the tool-layer citation contract that makes a fabricated source id structurally unusable.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@qianmo/memory": "workspace:*"
  }
}
