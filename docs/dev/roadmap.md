<!-- Copyright 2026 Qianmo AgentNest Team -->
<!-- SPDX-License-Identifier: AGPL-3.0-or-later -->

# 阡陌 AgentNest — 路线图

| 项 | 内容 |
|---|---|
| 文档版本 | **v2.70** |
| 生效日期 | 2026-08-30 |
| 依据 | [`charter.md`](./charter.md) v2.17（范围与验收标准以章程为准；**v2.35 勘误**：此栏自 v2.5 起一直停在 v2.4 未随章程升版，本次对齐） |
| 用途 | M0 部分是排期派活的直接依据；M1 及以后为方向性规划 |

**变更记录**

| 版本 | 日期 | 说明 |
|---|---|---|
| v1.0 | 2026-08-11 | 初稿 |
| v1.1 | 2026-08-11 | 按代码审计结论对齐已落地实现：P0.4 地址格式改为 `qianmo://<node>/<agent>`；「现状基线」与 P0.1 更新（`@qianmo/runtime-adapter` 已创建并通过测试，typecheck + test 全绿 101 用例，S0 剩余工作为 CI/hook/骨架包）；P4.2 防循环改以 hops 查重即时切断、`LIMITS.maxHops=8` 仅作兜底，并区分协议层 600/分钟入站预算与运行时层 20 条/60 秒节流；P0.3 改为评审定稿（源码原文已于首次提交前整改，未入 git 历史） |
| **v2.0** | **2026-08-11** | **基座路线变更**（章程 v2.0 §5）。① 现状基线整体重写：基座 open-claude-code v2.38.3 全量可跑已入库，阡陌自有包为零，旧洁净室三包撤下并保留在历史提交 `67f6081`。② **S0 重定义**为「基座接管」——基座掌握、身份隔离改名、CI 门禁移植/裁剪、协议资产复活评审，不再有 vendor 隔离机制建设。③ **关键路径重新推导**：会话持久化因基座已有而下路径，传输通道升为长杆，新链为 `P0.1 → P0.2 → P1.1 → P2.2 → P3.1 → P4.1 → P6.1`。④ 各期任务包按「基座起点」重新定性，多处从"实现"改为"核验/改造"，任务包编号相应重排。⑤ P7.4 由「vendor 解耦验证」改为「上游同步演练」；P8.4 软著预检按新许可链重写。⑥ M1 出口移除「基座退场」硬门槛。日期结构、排期基础假设、迭代主题节奏**均未改动** |
| **v2.1** | **2026-08-11** | **落实负责人 2026-08-11 决议**（章程 v2.1，**非全员评审结论**，S0 可复核）。① **现状基线更新**：`@qianmo/protocol` 与 `@qianmo/registry` 已**原样复活**并接入基座 workspace，测试随 `bun run precheck` 全绿，"阡陌自有代码为零"不再成立；`@qianmo/runtime-adapter` 仅取设计不取代码，「尚缺」表相应调整（`@qianmo/transport`、`@qianmo/memory` 等仍缺）。② **P0.5 改性质**：由「协议资产复活评审」改为「**复核负责人决议**」，并在同一包内一并定版权头问题。③ **P1.4 备注更新**：AC-5 经决议**不降级**，原"释放工时回填 P2.2"的条款失效。④ **P2.1 交付物**：`@qianmo/registry` 已复活，改为在其基础上补齐跨节点能力。**关键路径、日期结构、产能假设、迭代主题均未改动**——本次决议不改变任何排期 |
| **v2.2** | **2026-08-12** | **落实五路调研 + 六个前置实验的结论与负责人 2026-08-12 的 8 项决议**（依据 [`selection-m0.md`](./selection-m0.md)，章程 v2.2；其中 **D-1 ~ D-3 涉及验收判据变更，章程 v2.2 已写入修订文本但尚待 P0.8 全员评审，未通过则回退至 v2.1 原判据**）。① **关键路径重推**（§0.1）：传输不再是最长杆——基座有约 5,300 行在用传输代码可改造，v2.0「基座在跨机通信这一端给我们的是零」的推导**被代码推翻**；新长杆是 **P2.5 activator + P3.1 休眠**，新链 `P0.1 → P0.2 → P1.1 → P2.2′ → P2.5 → P3.1 → P4.1 → P6.1`。② **现状基线更新**：Linux x86_64 服务器已就位（Dormice + gVisor 已装），前置实验 E1 ~ E6 已实测完毕。③ **S0 重写**：P0.1 ~ P0.5 按实测更新，**新增 P0.6（已完成，前置实验）、P0.7（安全面加固，落实 D-5）、P0.8（判据修订评审，D-1 ~ D-3）**。④ **新增 P2.5 ⭐ 跨节点 activator + 常驻 keepalive**——M0 唯一无先例可抄、同时压着 AC-2 与 AC-6(c) 的组件，此前无任务包。⑤ **P1.1 / P1.2 / P2.2 / P3.1 / P4.1 / P4.2 / P4.3 按实测修订**：两类 ack 区分、`ttlMs` 拆分、恢复入口钉死 `--resume`、常驻宿主改 ACP、休眠形态与测量口径同步 D-1/D-3、防循环粒度同步 D-2、注入面按 T-7 扩展。**日期结构、迭代日期、产能假设、S1 及以后既有 owner 分配均未改动** |
| **v2.3** | **2026-08-12** | **落实负责人 2026-08-12 的分工调整决议**（章程 v2.3 §7.1，**非全员评审结论**，S0 可复核）：**M0 开发主要由负责人喻永昌本人承担，其余四名成员转为辅助开发**。① **owner 栏语义重定义**：自本版起，全部任务包的**主开发均为喻永昌**，各任务包 owner 栏中的原名单改读作「**方向辅助人**」（辅助开发、评审、第二知情人），括号内 backup 为第二辅助——完整表述见「任务包字段说明」一节，**各任务包 owner 行不逐行改写**。② 明确写「全员 / 双人」的流程要件（P0.2 全员任务、P0.8 全员评审、P9.3 双人签字等）**不受本条影响**，仍按原文执行。**本次只调分工：任务包正文、日期结构、迭代日期、产能假设、关键路径、依赖图与文档索引均未逐行改动** |

| **v2.4** | **2026-08-12** | **举证命令勘误 + P0.3/P0.4 落地回写**（章程 v2.4）。① **P8.3 的答辩举证命令修正**为 `git diff 3380c88..HEAD`——旧文的 `848ad8c2..HEAD` 在本仓库不可执行（上游 SHA，本仓库不含上游历史），详见章程 §5.5「举证命令勘误」。② **P0.3 提示按实施时的实清点修正**：facade 实际 **7** 个（基座文档写 6、漏 `searchCredentials`，已逐个清点核实）；基座文档的 `paths.ts` 绕过点数字（12 / 9）指改造前的历史代码、**在本仓库无法复核**，改记 P0.3 当次清点的 26 处及其处置，并把依据从 `selection-m0.md` §2（该节并无此两条，属悬空引用）改为 P0.3 实施审计提交。**排期、日期、产能、任务包正文均未改动。** |

| **v2.5** | **2026-08-12** | **完成状态回写**。新增「完成状态速查」表作为完成状态的**唯一出处**，记录 P0.3 / P0.4 / P0.5 / P0.6 / P0.7 / P1.1 已完成、P1.2 基本完成（AC-1 一条判据需模型凭据待补）、P2.1 前半完成（状态持久化未做），各带合并提交证据；各任务包标题同步标状态、不重复证据。并列出「尚未开始且需要人的」四项（P0.1 / P0.2 / P0.8 / P1.4）。**任务包正文、日期、产能、关键路径、依赖图均未改动**——本次只回写状态。 |

| **v2.6** | **2026-08-12** | **P2.2 落地与报警**。① 完成状态表新增 P2.2（`@qianmo/transport`，三条 DoD 实测通过）与其**报警条目**：v2.2 把 P2.2 降级的依据（基座有 5,300 行可改造传输代码）**经逐行核实不成立**——基座 `Transport` 钉死在 SDK 控制协议类型上、全仓无 WebSocket 服务端、且与会话面硬耦合，**直接 import 行数为 0**，复用的只有设计与常量。降级的**理由不成立、结论侥幸成立**（工时没爆是因为阡陌只需单条 wss + PSK）。**关键路径上 P2.2 的位置请负责人复核。**② 其余任务包与排期未动。 |

| **v2.7** | **2026-08-12** | **P2.5 落地（部分）**。`@qianmo/activator` 交付：两张面孔共用同一个只有 `touch`/`acquire`/`status` 的 daemon 端口，`destroy` 经三层守卫在代码层面不可达（测试带对照组：先用裸 fetch + 同一把 bearer 真的销毁一个沙箱证明路由与凭据有效，再证明经我们的端口够不到且请求未上线）；真 `kill -9` 崩溃恢复保证「要么转发、要么明确失败」，不静默丢弃。**DoD ①②（唤醒转发 10/10、防静默冻结含对照组）需真实 Dormice 沙箱，未测**，已附可执行补测方案（需要什么、怎么跑、判据）。另：P0.7 的「Dormice 端点必须回环」前向守卫此前恒绿（无对象），现已有 2 个文件进入扫描范围。**排期与其余任务包未动。** |

| **v2.8** | **2026-08-12** | **P2.5 真机核实与 API 修正**。主 agent 在实验服务器上核实 Dormice 真实 API，推翻 P2.5 交付时按「合理形状」写的全部假设并已修正落地：API 是 `POST /{methodName}` + JSON body 的 RPC 而非 REST；**`touch` 方法根本不存在**（31 个真实方法里没有），保活改用实测会刷新 `lastActiveAt` 的 `acquireSandbox`；状态取值是 `active` 而非 `running`。**DoD ② 真机验证通过（含对照组）**：不打心跳 t≈45s 冻结并保持，20s 周期心跳全程 120s 不冻。**DoD ① 仍待接线。**另修：`bun.lock` 缺 `packages/activator` workspace 条目，用已提交版本模拟 CI 会直接报 frozen-lockfile 错误（本会红）。**如实记录一处防护丢失**：真实 API 中保活与唤醒是同一操作，原类型窄化保证已不成立（核心属性 `destroy` 不可达不受影响）。 |

| **v2.9** | **2026-08-12** | **P2.2 关键路径复核结论**（实施方按负责人「尽量自主决策」的授权作出）：**链条顺序不改**（P2.2 已交付，回溯调整无决策价值），**改的是估算方法**——本次证明「基座有形状相似代码 ⇒ 工时可下调」这条推理不可靠，它在 P2.2 上给出正确结论却给了错的理由。**前瞻影响**：`P3.1` 的估算建立在同类推理上，其「休眠改核心」的成本从未验证，**开工第一件事应先做可改造性评估并如实报告**；此后任何以「基座已有」为由下调的估算同此纪律。 |

| **v2.10** | **2026-08-12** | **P0.1 DoD 按分工重述并完成主体验证**（实施方决策）。原「5 人各自跑通并留截图」是五人分头开发口径下的写法，分工集中后已成仪式；改按其真正保护的东西判定——**干净检出可复现**，并**已实测通过**（干净 clone + `--frozen-lockfile` + precheck 9217 pass / 0 fail，unused 与 cycles 亦绿）。逐人跑通降级为按需自查，不再阻塞。**服务器 `dor doctor` 仍待做。** |

| **v2.11** | **2026-08-12** | **P0.8 判据修订评审通过**（负责人确认，章程 v2.8）。D-1 / D-2 / D-3 三条修订自此生效，本文各处「待 P0.8 评审 / 未通过则回退至 v2.1」的悬置条件**全部解除**：P3.1 的休眠形态锁定为 frozen（不再按回退方案预留工时）、P4.2 的判环粒度锁定为处理者粒度（反向用例成为硬要求、无降级选项）、P3.1 与 P4.1 的测量口径锁定为独立基准 job。**并新增基础设施登记**：Azure 服务器（Debian 12 / 2 vCPU / 907Mi）已纳入清单，但**明确不承载阡陌负载**——它在跑生产 Supabase 栈，可用内存仅约 115Mi。 |

| **v2.12** | **2026-08-12** | **真实跨机跨架构验证完成 + 第三台服务器登记**。用甲骨文云机器（aarch64/Ubuntu 24.04）与 GCP 机器（x86_64/Debian 12）跑通阡陌传输层的真实公网往返：握手 233 ms、10 条投递 82 ms、重发 2 条被正确判重且未漏进应用层。**此前所有传输测试都是单机 unix socket**，本次补上跨机与跨架构。实验同时暴露并修掉一个健壮性缺陷：**调用方提供的事件 sink 抛异常会把服务端整个带崩**（已改为容纳并记为 `sink_failed`）。Azure 那台仍标注不承载阡陌负载（生产 Supabase、可用内存约 115 Mi）。 |

| **v2.13** | **2026-08-12** | **AC-1 与 AC-5 补测完成**（负责人提供网关凭据后）。**P1.2 转为已完成**：AC-1 最后一条「不重放历史即可续答」真机通过 —— 模型在对话中推出只存在于上下文的代号，多轮途中 `kill -9`，`--resume` 后明确要求不重读文件仍答对，`session_id` 一致。**P1.4 完成**：一致性三项对两条 provider 全绿、同一编程任务两家跑通、「仅改配置」以逐字相同命令行证明；主 agent 另发现网关提供 Anthropic 原生端点并实测通过，**G-1（只有一种 ProviderKind）就此关闭**——原生路径与 openai-compat 是结构上不同的两段适配器。**保留 G-4**（两条路径同一网关）与 **G-2**（`applyCompatRule` 在生产路径零调用方，compat 档案是死代码）。 |

| **v2.14** | **2026-08-12** | **P2.3 落地**。`@qianmo/memory` 交付并合入（`3e6d407`），完成状态表新增该行。三层与基座四类型的关系**定性为上层封装 + 共用文件格式**，三条否决依据均在基座代码中逐条核实（可删条目的提示词、`WHAT_NOT_TO_SAVE_SECTION` 明确排除工作记忆的内容类、基座目录按 git root 分区装不下账户级基线层）。**评审打回一处**：单个损坏文件会让整次召回抛错，健康记录一并失效——这条会在 P3.3 的唤醒注入路径上变成「节点每次醒来一条记忆都召回不到」，撞 AC-6；已改为部分结果 + 显式事件通道。另记一处门禁联动：互操作测试消费了一个此前无人使用的基座导出，`check:unused` 双向棘轮 exports 由 1253 降为 1252，已锁定（`ba8237f`）。**排期、关键路径与其余任务包未动。** |

| **v2.15** | **2026-08-12** | **P3.1 可改造性评估完成，结论：原估算偏低**（按 v2.9 立下的纪律，开工先验成本；实施方按负责人「尽量自主决策」的授权作出裁定）。① **两处前提被代码推翻并已在任务包内勘误**：唤醒半边的「四条现成通道」在 ACP 宿主下**一条都不通**（正解是 ACP 自带的 `session/prompt`，仍为零核心改动）；休眠半边「全仓没有通用 idle 事件」**是错的**（有四个，`sessionActivity.ts` 正是要的形状），真正理由是**基座没有任何进程内扩展点**。两处方向相反，**净效应是增量**。② **最大的一笔是 roadmap 此前零覆盖的**：常驻宿主选 ACP 把 P2.1/adapter 已验收的 **AC-2 最后一跳的读者拿走了**（`useInboxPoller` 是 REPL 专有）。③ 时间跳跃从「造机制」降级为「覆盖面」（闸门已由 P2.5 交付），但覆盖面是本包最大单项。④ **五项决策见下方「v2.15 决策」。**⑤ 真机基准（冻结/唤醒 P95、工作集回暖、idle 进程活定时器枚举、lockfile 冻结行为）**本次未测得**，已备测法。**排期日期、产能、关键路径顺序均未改动**——本次只改 P3.1 的内容与估算依据。 |

| **v2.16** | **2026-08-12** | **P2.5 DoD ① 真机验证通过（10/10），P2.5 转为已完成**。GCP + Dormice + gVisor 上连续 10 轮「休眠 → 唤醒 → ready → 转发」，每轮先确认沙箱 `state=frozen` 再投递；`total` p50 43 ms / p95 423 ms，判据 P95 ≤ 60 s，**低两个数量级**。主 agent 独立核验收件箱 10 行 / 10 个唯一 msgId，确认是真到达而非回执自称。**同时明确两条边界**（见下方「v2.16 决策」）：① 本次**不构成 AC-2 完成**，ack 语义未验；② 这批数字**不能当作 P3.1 的休眠唤醒基准**——被唤醒的是一个 94 KB 的测试节点，不是常驻 occ。**排期与其余任务包未动。** |

| **v2.17** | **2026-08-13** | **P3.1 完成：常驻 occ/ACP 的 Dormice + gVisor 休眠唤醒真机验收通过**。专用 `runsc` 沙箱内跑真实 occ/ACP、文件信箱、transport 与固定 SSE 模型桩；baseline factor 2/2 与 candidate 1.1/1.5 各连续 10 轮，均先确认 `frozen` 再投递、10/10 responsive、P95 首内容分别 4.270 s / 4.361 s（上限 60 s），两组 JSONL 损坏行均为 0。时间跳跃反向判据同时通过：thaw 后新鲜消息 accepted，宽限后正常过期消息恢复 `E_TTL_EXPIRED`；manual 与 3 s timer 两条均从 frozen 出发并完成完整 turn。A/B 结论仅落到两个实测面：resident activity 重连默认 1.1、宿主 resident keepalive 默认 1.5；通用 transport/协议默认仍为 2。**本包完成的是 AC-2 后半「休眠 → 唤醒 → 可响应」；跨节点 ack/result 仍归 P4.1，不在此冒领。** |

| **v2.18** | **2026-08-13** | **P3.2 完成：Atlas 团队仓库上的真实编程闭环 3/3 通过**。一键 runner 对同一 source commit 建三个独立 clone，均先 `bun install --frozen-lockfile`，提交受保护的红色回归 fixture，再由真实 headless 智能体完成「新增 address helper / 修 reconnect jitter 时序 bug / 收紧成功 receipt wire type」；三项 baseline 均非零，模型退出码、focused test 与包级 `tsc` 最终均为 0，生产改动文件与 allowlist 精确相等，受保护测试零改动。命令执行强制 fail-closed sandbox、禁 unsandboxed fallback，工具集只含 Read/Edit/Bash；真实 provider 凭据只在异步回环代理闭包，子进程仅持 dummy，证据扫描零命中。所有配置错误与任务失败统一输出 `qianmo.p32.task-result.v1` envelope，提供 task/phase/code/changedFiles/agent+test exit/artifacts，作为 P5.1 分类器输入；**原因级 `diagnosis` 仍归 P5.1，不在本包提前实现。** |

| **v2.19** | **2026-08-13** | **P4.1 回程链路落地（代码与自动化测试完成，本包仍未完成）**。① **协议**：`ack` payload 收缩为 `{ handler, ackAt }`、`error` payload 去掉原消息 ID——**相关标识只存在于信封**（规则 C-1），`protocol.md` 旧 K-1 要求的 `{ ofMsgId, taskId }` 复制随之取消；新增字段封闭的 `task.result` 联合（成功 `{ outcome, content, completedAt }` / 失败 `{ outcome, code, reason, completedAt }`，运行时逐支精确校验）与稳定码 `E_TASK_FAILED`（`protocol.md` §4.2/§4.6/§11 已同步）。② **传输**：抽出两端共享的 receipt 驱动 outbox 与统一入站处理器（校验 → 两级去重 → handler 抛错即 forget 并回 rejected receipt），握手加入受 HMAC 覆盖的稳定 `channelId`，**同一逻辑通道在物理断线重连后重绑并重放反向 outbox**；业务层只拿到受限 `TransportChannel`（含 `hold()`），不接触裸 socket。③ **activator**：新增有界 task route registry——请求登记 C1 source channel，C2 回来的 ack/result/error 按 `taskId` + 地址 + sandbox 校验后原样沿 C1 转发，ack 保留路由、终态释放；重复 `taskId` 不能串线。**没有第二条反向连接。**④ **resident**：网络任务改为逐条 ACP turn（本地普通消息仍批处理），durable read 翻转后发 ack，turn 终态（正文聚合成功 / 取消 / 异常 / 任务 TTL / ACP 子进程关闭）发封闭 result 并等 receipt。⑤ **门禁**：`packages/` + `src/services/qianmo` + `tests/integration` **2533 pass / 0 fail**（26 skip 为需真实 provider 凭据的 AC-4/AC-5 套件）；根 typecheck、biome、mock hygiene 全绿；cycles 437/2024 在预算；unused 棘轮回到 exports 1251 / types 638（两个新内部 options 接口收回不导出）。**⑥ 本条不构成 P4.1 完成，也不构成 AC-2 完成**：DoD 的「连续 10 次、ack P95 ≤ 60 s、result ≤ 5 min、10/10」按 D-3 跑在独立基准 job 上，**尚未真机执行**；当前证据全部来自本地自动化测试（含真 socket 两跳与真 ACP 子进程），不是 Dormice + gVisor 真机。 |

| **v2.20** | **2026-08-13** | **P4.1 完成，AC-2 达成：跨节点任务消息端到端真机验收 10/10**。GCP + Dormice + gVisor（`runsc`）沙箱内跑本次新构建的常驻 occ/ACP + 文件信箱 + 固定 SSE 模型桩；宿主同时跑 activator（含 activity/keepalive）与 **`@qianmo/registry` 真注册中心**；发送方是一条**只拨出、不监听**的 `TransportClient`。每轮先确认沙箱 `state=frozen` 再投递，链路为「按名解析 → 传输投递 → activator 接住并唤醒 → 沙箱内常驻读入 → ack → 执行 → `task.result`」。**判据**（跑在评审九条修复之后的构建上，与合入 main 的代码一致）：成功率 **10/10**；ack p50 **2.490 s** / **P95 8.022 s**（上限 60 s）；result p50 3.471 s / **max 12.422 s**（上限 5 min）；按名解析 8–11 ms。**P95 等于 max 是因为十个样本的 p95 就落在最大值上**，而最大值是第 1 轮的冷链路建连（8.022 s / 12.422 s）；第 2 ~ 10 轮 ack 在 2.073 ~ 3.483 s。**独立核验（不采信宿主回执）**：进沙箱取常驻侧 timings，10 条 `msgId` 各有 `detected/admitted/read/first_content/turn_completed`，**每条 ack 都晚于 durable read**（§4.5）、**每条 result 都晚于 `turn_completed`**，`turn_failed` 0；沙箱内信箱累计 22 条全部已读、22 个唯一 `msgId`、类型全为 `task.request`；activator 审计 `link.opened=1`、`task-route.registered=10`、`task-reply.forwarded=20`（10 ack + 10 result）——**全程只有一条进沙箱的链路，回程没有第二条连接**。**一键复现**：`demo/p41-task-result.sh`（+ `demo/lib/p41-{registry,send,report,report-core}.ts`），报告不含正文（只留字符数与 sha256 前缀）与凭据。**口径按 D-3**：延迟测量跑在独立基准 job 并留档，未塞进 CI 阻塞位。原始记录、脱敏报告与 sha256 留在验收机用户私有目录（0700，文件 0600）。 |

| **v2.21** | **2026-08-14** | **P4.2 完成，AC-3 达成：防循环与两层限流落地并有一键复现**。① **新包 `@qianmo/router`**——判环键锁死为 `(处理者地址, taskId)`，首次回访即切断，`LIMITS.maxHops` 只做兜底；两层限流是**两个类、两套键、两个上限出处、两类审计事件、两个拒绝码**（协议层 `E_RATE_LIMITED` 在码表内可告诉对端，运行时层 `E_RUNTIME_THROTTLED` **不入码表、永不上线**，且入站判决的返回类型窄化到 `ProtocolErrorCode`，把运行时码回给对端是**编译错误**而不是一条约定）。② **落地时发现并写死了一条本来会当场坏掉的规则**：回复类消息（`ack` / `task.result` / `error` / `pong`）不进判环表——它们按 C-1 带原任务 `taskId` 回请求方，形状与「回访」完全一致，用判环键判它们会在**第一条 ack** 上切断 AC-2 的回程。③ **两个附带缺陷一并处理**：起始节点自我播种（`hops[0]` 与判环键同时播），自动回复乒乓（每圈新 `taskId`，判环与跳数都看不见，**只有运行时令牌桶抓得住**，已有专门用例把这件事说明白而不是假装判环能管）。④ **去重表不重造**：`@qianmo/transport` 的 `DedupTable` 本就以投递时限为表项 TTL，与本包同一口径，再造一张只会让网络有两张表、一份契约。⑤ **协议包顺带补两处**：`isReplyType`（哪些类型是回答属线上契约）与 `advanceTraceparent`（§7.1 早就要求逐跳换 `parent-id`，此前无人实现——原样透传会让每一跳都自称起点的子节点，C-6 要问的「谁转发给谁」就此不可答）。⑥ **接线**：activator 入站处理器（在唤醒与 task route 注册**之前**——唤醒按 E2 是秒级成本，路由要占住一条通道到任务时限，被拒的消息不许花这两笔）、常驻节点 `#receive`（在写信箱与开 turn 之前，规则 L-1）、`residentWake` CLI 出站。⑦ **判据**：`demo/ac3-loop-rate.sh` 一键复现，**十条 check 全 true**——回环在两跳处被切（`maxHops=8`，证明不是兜底救的场）、发送方收到 `error(E_LOOP)`、`loop_detected` 恰 1 条且带 trace-id 段/跳链/判环键、合法 spiral 正常投递且零判环、运行时层第 21 条本地被拒且**没上线**、协议层按**真实 `LIMITS.ratePerMinute` 打满 600 条**后第 601 条回 `E_RATE_LIMITED`（发送方换了 31 个 agent 名字，用来证明这层按**节点**计而非按 agent 计）。⑧ **如实记三条边界**：反向用例在**判环层通过**，但同一 `taskId` 并发派给同一节点的两个 agent 仍会被 **P4.1 的回程路由**拒（key 只有 `taskId`，沙箱回来的 ack 也只带这一个键）——是相关性约束不是判环，码为 `E_BAD_ENVELOPE`，已有用例分别断言；运行时令牌桶在 M0 **没有长驻的生产调用方**（agent 面的发送工具不在 M0 范围内），它的生产接线只有一次性的 `residentWake` CLI 与复现脚本；`withHop` 的「转发追加」半边有实现有用例、**但 M0 没有第三方中转节点**，因此无生产调用方。 |

| **v2.22** | **2026-08-14** | **P4.3 完成：权限分级与授权模型 v0 落地（C-5 的结构性强制点）**。① **新包 `@qianmo/capability`**：每节点一对 Ed25519（`node:crypto`，零依赖）、`<claims>.<sig>` 紧凑令牌、nonce 重放表、三级权限。**签名覆盖的是原样送达的 claims 段**——不重新序列化，也就不需要任何规范化 JSON，那类「同一个对象不同字节」的坑一次都不用踩。② **S-1 是一条签名校验，不是一句约定**：`user-confirmed` 只认本节点私钥签发的，远端签得再好也拒（`E_CAP_INSUFFICIENT`），且这条**在验签之前**就判——它不是「我们没法校验的签名」，是「这个断言我们不从别人那里收」。③ **S-3 变成一条会变红的扫描**：`packages/capability/test/authorization-invariants.test.ts` 扫阡陌全部能看到入站信封的源码，出现基座任何一个**改权限**的 API 即失败，红方向由 fixture 钉住（只会绿的断言等于没有断言）。④ **T-7 攻击用例三族齐备**（伪造凭据 4 条 / 内容夹带指令 5 条 / 已签名令牌里的越权 5 条），**判据一律是「等级没被抬高 + 审计有记录」，一条都不看模型是否被说服**；另有一条**正向对照**（可用的 `write-limited` 令牌确实放行），否则「全都拒绝」的实现也能全绿。⑤ **`protocol.md` §10.1 的已知缺口闭合**：注册中心按节点段核对公钥，在租期内先登记者胜、冲突回 `E_CONFLICT`；不建索引表、从在租记录现算，于是「全部 agent 过期后可重新登记」自然成立。⑥ **节点身份落盘**：`occConfigPath` 派生、目录 0700 文件 0600、`wx` 独占创建**永不覆盖**——密钥就是身份，悄悄换一把等于这个节点悄悄变成另一个节点；文件损坏是报错，不是重新生成。⑦ **顺序偏离一处，记录在案**：S-2 表把入站预算排在判环之前，实现是**判环在前**，理由与代码注释、`protocol.md` §10.1 同文。⑧ **M0 边界如实记**：默认策略 `OPEN_POLICY` **允许未签名的 `task.request`**——可选的只是「是否强制出示令牌」，**出示了就一定全程校验**；必须可选，是因为 M0 没有密钥分发（`--trust <node>=<publicKey>` 是手工配的，没有 TOFU），在拿不到钥匙之前强制签名只会让网络安静。强制版 `SIGNED_TASK_POLICY`（`--require-signed-tasks`）已在包内与常驻集成用例里跑通，M1 上 mTLS 后切默认。 |

| **v2.23** | **2026-08-14** | **P4.4 完成：备份与不可删除保护（AC-6(b)(c) 本机全绿）**。① **新包 `@qianmo/backup`**：快照对象**只增不改**（`wx` 独占创建，id 由 store 从自己的时钟与计数器分配，调用方连「故意撞 id」都做不到），**全包没有任何删除路径**——不是带守卫的删除，是根本没有那个方法。② **为什么不是文件权限**：POSIX 上「删掉一个文件」的权限来自它**所在目录**的写位，与「创建一个文件」是同一个位；sticky bit 收窄成「只能删自己的」，而那正是本例要防的。所以边界不是 chmod，是**一个进程**：store 在宿主侧，沙箱只能经一个没有删除动词的 socket 面够到它，三层守卫（类型 / 运行时 allowlist / import 期形状断言）与 P2.5 同款。③ **两把凭据**：只写凭据能建不能读，宿主凭据能读不能建——一个带 scope 字段的单凭据会把这条性质变成「离写错只差一个布尔」。④ **两个触发**：定时快照 + 任务前快照，`setTimeout` 从**完成**处重排而不是 `setInterval`（E4：漏掉的 tick 不补跑），单飞保证同一工作区不会被并发归档成两个半截状态。⑤ **恢复**：先核对 store 记录的 sha256，再要求目标目录为空——在残留文件上解包会让 `git status` 报出两个状态的并集，而那看起来像「恢复好了」。⑥ **AC-6(b)(c) 十一条 check 本机全绿**（`demo/ac6b-restore.sh`）：真 git 工作区（已提交/已暂存/未暂存/未跟踪/可执行位五种状态）→ 只写凭据经 HTTP 存快照 → `rm -rf` → 宿主取回解包 → `git status --porcelain` 与 `HEAD` **逐字一致**，耗时 **20 ms**（预算 10 min）；随后只写凭据的 DELETE/PUT/PATCH 四次全 405、列表与读取全 403、审计计数对得上、快照仍在。报告里专门有一条 `gitStatusWasNonTrivial`——一个干净工作区恢复回来当然「一致」，那不是证据。⑦ **三条边界如实记**：常驻节点里的**任务前快照不等待**（等它就是把一次 `tar` 挡在 ack 前面，而 AC-2 的 ack 线是已经量过的预算），因此它是「任务开始前后」而不是「任务开始的那一瞬」，要更强的保证得由自己掌握任务生命周期的调用方去 `await beforeTask`；**沙箱边界本身本机测不到**（两侧同进程，测的是凭据与动词面，不是挂载），真机部署仍需把 store 放在沙箱够不到的地方；**M0 不做保留策略与轮转**（N-12），将来加也归宿主侧工具，不归这条入站路径上的任何方法。 |

| **v2.24** | **2026-08-14** | **P5.1 完成：原因级诊断 v0，50 条真实样本盲分类 50/50**。① **新包 `@qianmo/diagnosis`**：五类成因（OOM / 超时 / 缺依赖 / 磁盘满 / 额度耗尽）+ 一个诚实的 `unknown`，每类自带建议动作——诊断不给下一步就只是趣闻。**规则式而非模型式**，三条理由按重要性排：诊断必须可复核（「分类器觉得是 X」没人能查）、模型中立（AC-5；把模型调用放在失败路径上，而失败路径最可能正因为模型不可用）、确定性（同一个失败两次得同一个名字，错了是有位置的 bug 而不是调参问题）。② **先看结构化事实，再看文本**：`exit 137` 是 SIGKILL，**谁杀的都长这样**，靠关键词在超时与 OOM 之间挑一个等于抛硬币；分辨它的是**杀人者当场记下的事实**（`timeoutEnforced` / `oomKillDelta`），而 `@qianmo/sandbox` 早就在记。③ **注入器造的是真失败**：超时真被杀、OOM 真把堆撑爆、缺依赖真去执行不存在的程序、磁盘满真写到写不进去（Linux 用 `/dev/full`，macOS 用 `hdiutil` 建 1 MiB 真卷）、额度耗尽真去请求一个真回 429 的本地服务；**标注只进报告，分类器只看观测**——能看见标签的分类器当然满分。④ **第一次跑就抓到一个真问题**：总体 90%，但 **OOM 只有 5/10**。查下去是实测结论——**Bun 撑爆内存时被系统 SIGKILL 且一个字都不写**，文本规则无从下手。修的不是阈值也不是猜法，是**补一条事实**：「本进程的看门狗没发过这个信号」。据此新增规则「SIGKILL + 不是我们杀的 → OOM（中等置信）」，重跑 **50/50、准确率 100%**。⑤ **报告六条 check**：除总体 ≥80% 外，还有**每类不得低于 50%**（四类满分 + 一类全灭正好凑够 80%，那恰是最该被看见的情况）、**被跳过的类别直接判不通过而不是缩小分母**、不得出现 `unknown`、每条判定必须带依据。⑥ **接上 P3.2**：`observationFromTaskResult` 取**执行顺序上第一个非零退出码**——agent 先跑，agent 死了的话 test 的退出码描述的是一次根本没发生的运行。⑦ **三条边界如实记**：样本是**我们自己造的**失败，比野外样本弱，真机跑批应把 P3.2 的真实失败并进语料；`oomKillDelta` 要 cgroup v2，macOS 上拿不到，所以同一个 OOM 在笔记本上是中等置信、在真机上才是高置信；分类器**故意保守**——裸 ENOENT 不算缺依赖（那是任何打不开文件的任务都会有的），宁可 `unknown`。 |

| **v2.25** | **2026-08-14** | **P5.2 完成：资源协商协议四段式落地并写入 `protocol.md` §13**。① **协议侧**：新增四个消息类型（`resource.request` / `offer` / `grant` / `release`）与字段封闭的 payload，入站校验逐条把关——租约是「花别人机器」的授权，带着本版读不懂的字段就等于没人校验过；新增稳定码 `E_RESOURCE_REFUSED`。② **少的那条是故意少的**：没有「贷方确认收到 grant」。租约的凭据是报价里带的 capability 令牌，借方用它就是在证明持有；一条只为说「真的收到了」的消息，给两端各加一个超时表项却换不到任何可行动的事实。③ **上限不进 `LIMITS`**：`LIMITS` 是全网必须一致的数，而「这台机器愿意借多少内存」逐部署不同、与别人无关；写进去等于把本地决定升格成要改章程才能调的全网常量（§12.2 A 的门槛）。字段在协议里，数值在 `LenderPolicy` 里。**报价永不大于请求**——多给不是慷慨，是带着善意表情的资源泄漏。④ **用户授权在贷方本地发生**：四条消息里**没有任何字段**能表达「我这边用户同意了」，因为借方声称获得授权正是 C-5 点名的 confused deputy 形状，而 S-1 已从密码学上封死它。跨过来的是**贷方自己签的令牌**。⑤ **没有状态能被沉默的对端拖住**（DoD 的第三条）：`requested` 有请求超时、`offered` 有报价超时、`leased` 有租约到期，任一还可被 `release` 提前结束；释放一个已经不在的租约**不算错**——release 与到期擦肩而过是这个竞态的正常形态。⑥ **协商消息算「回复类」**：一次协商在同一个 `taskId` 上来回走，不把后三条算作回复，第二程就会被判环误杀（与 ack / task.result 同理）。⑦ **19 条用例**，两端都由手动调度器驱动——超时不是等出来的，是**触发**出来的；一个测试里触发不了的超时，是没人见过它工作的超时。含反向用例：他人抢占报价、同一报价二次接受、报价过期后到达的 grant、贷方满员、`costLimit ≠ 0`、贷方给得太少时借方**主动退回**而不是占着。 |

| **v2.26** | **2026-08-14** | **P5.3 完成：按需加密隧道，三条拆除路径与「无残留」实测**。① **先说它不是什么**：**没有新造加密层**。章程 N-3 把 M0 钉在 TLS + PSK 上并把这件事记为局限，在这里发明第二套密码学既越界又不如现有的。「按需加密隧道」的实际内容在头两个字——**连接只在租约存在期间存在**：协商之前没有监听，租约结束之后也没有监听、没有为对端保留的逻辑通道、进程里也不再留着那份凭据。② **三条拆除路径，后两条不需要对端配合**：正常释放（借方说完事了）、租约到期（贷方自己的时钟）、对端消失（socket 关闭或超出静默窗口）。第一条不可依赖——崩在半路的借方永远不会发释放，等它的隧道就是永不结束的租约，那正是 AC-7 第六拍要防的。③ **因此隧道客户端关掉了重连**。`@qianmo/transport` 默认重连，这对节点长连接是对的，对隧道恰恰是错的：租约结束后又回来的连接，就是活过了自己条款的租约。④ **「无残留」是**测**出来的**，不是 `stop()` 之后的祝愿：拆除后**再拨一次那个地址并要求拨号失败**。同时如实写明能保证到哪一步——凭据只存在一个对象的一个字段里，那个对象被释放；这是有 GC 的运行时，「从内存里抹掉」不是这个文件能诚实承诺的，所以它不承诺。⑤ **PSK 是门，令牌是租约**：持 PSK 能连上，连上之后没有本租约的令牌一条消息也过不去；宿主按**字节相等**核对自己铸出去的那串（再验签是同一件事绕远一步——只有本节点造得出它，S-1）。⑥ **15 条包内用例 + 3 条集成用例**（协商 → 隧道 → 释放的接缝，AC-7 环节 4 与 6）。集成用例里记下一条边界：**隧道断了不等于租约还了**——把两者混为一谈，会让一次网络抖动提前把别人的机器还回去，所以对端消失时租约仍在贷方账上，直到自己到期或被显式释放。 |

| **v2.27** | **2026-08-14** | **P5.4 完成：边界用例库 v1（29 条，五类齐备），并修好一个「CI 根本没在跑」的缺口**。① **`tests/boundary/` 建库**：五类各一个文件，**每条用例在注释里指明它对应 `protocol.md` §8.3 的哪一行**——看不出对应关系的用例不属于这里。29 条，DoD 要 ≥ 8。② **只做组合，不重复包内细节**：包内用例证明零件对，这里证明**边界发生时装在一起的东西表现对**（例如判环、两层限流三者同时挂在一个路由器上时各管各的；例如租约与隧道在借方消失时的分工）。③ **发现并修掉一个真缺口**：`scripts/test-shards.sh` 的分片列表是 `src/* packages/* tests/integration scripts`——**新目录不在列表里就在 CI 里根本不跑**，而本地 `bun test` 会跑。`demo/lib` 下的报告核心用例（P4.1 起就有）因此**从未在 CI 里跑过**。本次把 `tests/boundary` 与 `demo/lib` 一并加进分片列表，实跑 **53 个分片全绿**。这条缺口的形态值得记住：它让「本地绿」与「CI 绿」同时成立且**第二个绿没有意义**。④ **几条用例本身就是防倒退的**：「同一节点因不同目标地址被再次经过不算环」（D-2 的全部意义）、「处理器抛错必须撤回去重项」（否则一次瞬时故障变成永久静默丢失）、「隧道断 ≠ 租约还」、「`costLimit` 字段还在」（AC 要验的是硬上限能拦住，不是把字段删掉）。 |

| **v2.28** | **2026-08-14** | **P7.2 完成：审计日志与全链路追踪，含一条「做不到就说做不到」的边界**。① **新包 `@qianmo/audit`**：一份**追加式**文件、一种记录形状，各层都往里写。各包自己的环形缓冲留在原地——那是给运行中的进程看的，这份是给三天后的问题看的，而那个问题永远是同一个：**把这次任务的全过程给我，包括被拒的部分**。② **`kind` 原样透传各层自己的事件名**，只有 `outcome` 归一成 `ok / refused / dropped`——运维手里往往正拿着某一层的日志行，一个把名字全改掉的审计只会让他多做一次翻译；而「这件事到底成没成」必须能统一问。③ **「不可改」拆成三句，只认前两句**：**写入方改不了**（fd 是 `O_APPEND | O_NOFOLLOW`，且类里没有任何 seek / truncate / delete 方法）；**外部改动可检测**（每条带前一条的哈希，改一行、删一行、换顺序都会从那里开始断链，读取时报出第一处断点的 `seq`）；**外部改动无法阻止**——有文件写权限的人可以整篇重写并重算哈希，挡住它需要 append-only 挂载或机外见证，M0 两样都没有。**这第三句在测试里有一条专门的用例**，免得有人读完套件以为文件是不可变的。④ **重启续链**：新进程从盘上最后一条接着写，否则每次重启都会留下一个「长得跟篡改一模一样」的断点，而一个天天喊狼来了的完整性检查没人会读。⑤ **`occ audit` 三种查法**：按 trace / 按 agent 或时间窗 / `--verify`；**不带条件不给查**（这份文件永远增长，默认全量打印是它能做的最没用的事）；发现链断时即使没要求校验也会在 stderr 提示。⑥ **DoD 用真链路跑出来**：一次跨节点交换里四条消息四种命运（接收 / **被去重** / 被处理器拒 / **被限流**），用 trace-id 段还原后四条全在——按 `seq` 排序而不是按时间戳，因为两个节点的钟不一致，按时间排会把 ack 排到它回应的消息前面。⑦ 顺带给 transport 的三条消息级事件补上 `traceId`：拿不到相关键的去重记录，没法向「为什么发送方看到两次回执」的人解释。 |

| **v2.29** | **2026-08-14** | **P7.1 完成：边界库 v2、60 分钟混沌跑批与 CI 五连绿全部通过（AC-8）**。① **边界库 v2**：新增 `chaos-recovery.test.ts`——四类混沌注入的**确定性对应物**，边界目录共 39 条（AC-8 要 ≥12、每类 ≥2）。分工写在文件头：混沌跑批找**没想到**的组合，确定性用例防**已知**失效回归；一条跑一小时才碰一次的失效，不该由随机去守。② **混沌注入 `demo/chaos-inject.sh`**：起一套真的本地网络（transport server + 路由闸门 + 落盘审计 + 备份存储 + 被监督的子进程 + 稳定流量），按节拍注入四类故障。**随机带种子**，`--seed` 可原样重放——一条复现不了的红色只是传闻，种子是把传闻变成缺陷报告的那一步。③ **判据的重心不在「没崩」**：什么都不干的一小时同样没有未捕获异常，所以每次注入之后都记「系统又成功处理了多少条」，一次让系统**悄悄停摆**的注入必须判红；此外每条被捕获的失败都要对上已知边界，对不上的一条就判不通过（`unmapped`）；四类里少注入一类也判红——四类里跑了三类的报告不是四类的证据。④ **注入器自己被这套判据抓到两次，都已修**：第一版的 `fill-disk` 往一个**空的** 1 MiB 卷追加 200 字节，每次都成功——**一次什么都没注入的注入，比不注入更糟，它把假的绿色变成真的报告**；现改为先真填满再写，并在写居然成功时主动记一条 `unmapped`。第二版发现随机挑选会让短跑批**整类漏掉**（1 分钟里 11 次注入没有一次 clock-drift），改为**先覆盖每类一次再随机**——否则被测的是跑批时长而不是系统。⑤ **60 分钟正式实测**（seed `2145259399`）：共 **177 次注入**（kill-worker 42 / cut-network 47 / fill-disk 45 / clock-drift 43）、**7,389 条成功投递**；四类 `stalled` 全为 0，**0 未捕获、0 unmapped、审计链完好**，87 条被捕获失败全部是实际 ENOSPC 且正确归到 disk full。恢复时间最大值：kill-worker 103 ms、cut-network 513 ms、fill-disk 112 ms、clock-drift 103 ms；五条报告 check 全 true。⑥ **边界日模板** `docs/dev/boundary-day.md`：四步动作 + 记录表，并写死两条纪律——红了先复现再修；**失败对不上已知边界时不许顺手把它加进 `KNOWN_BOUNDARIES` 变绿**，那张表是「我们理解并接受的失败」，往里加一行等于宣称理解了它。⑦ **CI 五连绿是在修完三处真实缺口之后才算的**：首次干净安装在 typecheck 抓到根包漏声明 `@qianmo/sandbox`，修于 `acf5e57`；下一轮又分别抓到 Knip 不认识两个由 shell 启动的 demo runner（`36ce286`），以及 MCP watcher 正向用例把「收到事件」猜成固定等待 2.6 秒（`a8b06a9`，本地压力回归 20 轮全绿）。最终在同一 SHA `a8b06a9` 上严格串行完成 **5 次 CI 全绿**：`31799841764` / `31800391530` / `31800890960` / `31801288270` / `31801769324`；每次均为 GitHub API 的 `completed / success`，前一次成功后才派发下一次。 |

| **v2.30** | **2026-08-14** | **P6.1 完成，AC-7 达成：数模场景六环节连续三轮正式验收 3/3**。一键链路用同一条常驻双向 transport 承载任务提交、背景流量与 request→offer→grant→release 协商；A 的 Node 32 MB heap 子进程真实 OOM 后得到原因级 `oom` 诊断，B 在本地 `scripted-hook` 授权后用临时 Ed25519 签发并复验 `user-confirmed` capability，再开独立 unix tunnel；A 按计划点发 20 块，B 为每块启动真实 worker 子进程并把结果原子写入 B 侧 store；最后 release、关闭 tunnel、同址再拨失败，常驻链路仍继续投递。正式 seed `6101` 三轮时长 **600,357 / 600,445 / 600,407 ms**，每轮 **20/20 worker**、计算跨度 **509,994 / 509,977 / 510,000 ms**，结果 digest 均为 `cdecda39b681c434fc6c5d524de92610da7f773b53711325c199d8af623bb266` 且与 expected 一致；每轮 600 条背景投递 + 拆除后 1 条、`message_accepted=1210`、lender pending 0、审计链 intact、0 failure / 0 skipped，七项 checks 全 true，`pass=true`、`ac7Eligible=true`。**诚实边界不藏**：授权是脚本化本地同意而非真人点击；A/B 是同 runner 内的逻辑节点，真实包 / 信封 / unix socket / 子进程但不是跨机生产拓扑；unix 演示是 WebSocket + PSK，不冒称 TLS；隧道只 A→B，结果由同一 runner 从 B store 读取；worker 与 runner 共用算法；生产接线仍归 P8.2。`scenario-mcm.md` 草案同时产出，但赛中真实痛点和全员评审尚未补齐，**P2.4 仍未完成**。 |

| **v2.31** | **2026-08-15** | **P6.1 证据补强 + P6.3 完成 + P6.4 草案产出**。① **P6.1 观测做真并 SHA 锚定重跑**：落地前审计发现报告的 `uncaught`/`skipped` 是硬编码字面量（判据因此永远为真，恰违反本项目「测量而非宣称」纪律），已改为真处理器计数与计划/派发差集推导，并加 `QIANMO_P61_KEEP_ARTIFACTS` 证据保留开关与 `versions.bun/node` 元数据字段；随后在提交 `87c4609b` 上重跑正式三轮 **3/3**（seed 6101，时长 600,988 / 600,236 / 600,259 ms，spanMs 509,992 / 509,999 / 510,000，逐轮 20/20 worker、600+1 与 601+1 条背景投递、digest 三轮命中 `cdecda39…`、13 项断言零失败），产物完整保留并打包归档（sha256 `2be9d926…3140`），**scenario-mcm.md §9 的 3/3 表以此为正式记录**（首次 08-14 三轮无 SHA 可锚、无留档，被本次取代）。归档暂存本地私有验收目录；workbench-host 全程 SSH 不可达（TCP 通、无 banner，已排除本机因素），真机副本待恢复后补传。② **P6.3 完成**：两个第三方现成 MCP server（`@modelcontextprotocol/server-filesystem` 2026.7.10、`server-memory` 2026.7.4，精确 pin、npmmirror 同源）实测接入——发现/调用/写读闭环（含 node:fs 旁路复证）/双 server 互不串扰/三类降级（进程不存在、启动即退出、在飞被杀后自动重连自愈）/`request_timeout_ms` 生效，10 条用例进 `tests/integration` 分片（CI 既有分片，零 mock），故障夹具明确不计入「两个现成 server」。实测记录 `docs/dev/p6.3-mcp-conformance.md` 含五条缺口：G-1 第三方对端全是 v1 SDK 只覆盖 legacy 纪元、G-2 **roots 通告会整体顶掉 filesystem server 命令行允许目录**（部署真坑，本次按核验性质不改基座核心，建议先走部署文档）、G-3 只覆盖 stdio、G-4 默认工具超时 ≈27.8 h 等于没有、G-5 `packages/mcp-client` 平行实现未测。**bun.lock 附带一次性规范化**：bun 1.3.13 在装新依赖时把 493 条空解析字段改写为显式 npmmirror URL（零 npmjs.org 条目），属机械变更，之后不再抖动。③ **P6.4 草案产出**：`docs/dev/a2a-gap.md` v0.1——锚定 A2A v1.0.1（proto 为数据模型真源，取证 SHA 入文），15 个概念面逐字段映射、27 条差距全部三选一（M1 收敛 11 / 长期保留 13 / 不适用 3）、三个需人定夺的判断点与 G-18 错误码信息泄漏发现单列待评审；**全员评审未进行，P6.4 未完成**。 |

| **v2.32** | **2026-08-15** | **P6.2 完成：`@qianmo/capacity` 双通路扩容触发器 + 确定性回放，S6 四包全部收口**。零依赖新包，纯桶推进器（时间只从入参来，无 timer 无时钟，7 天回放 0.073 ms）。**双通路语义分开命名不互相冒充**：`scale-up-predicted` 是日历先验（提前量来自日历条目的 `rampBeforeMs`——包头注释写死「这是对人写的日期做算术，不是预测模型」，对外表述纪律同章程 §5.8）；`scale-up-reactive` 是同槽（slot-of-week）中位数+MAD 基线偏离，三条件 AND（z=4 偏离、`minRatio=1.5` 比值下限、连续 3 桶），其中 **`minRatio` 挡的是 MAD=0 时纯 z-score 退化成「周日多了一条任务就扩容」**；冷却 6 h、一窗一次、窗内抑制 reactive，被抑制的判断也写记录（`suppressed`，审计记 `dropped` 不记 `refused`）。**DoD 实测**：判据 1 在 contest 形态 + CUMCM 日历上 `leadMs=405 min ≥ 30 min`，双断言（相对开赛时刻 + 相对负载首越 2×base 的桶）避免「峰值」歧义，且 4 个 seed 的 leadMs 逐字相同——通路 A 不看负载的直接证据；判据 2 在平稳 + 空日历上 **24 个 seed × 7 天计分窗假阳性全 0**（预算逐 seed ≤1、合计 ≤3），并配「探测器确实带电」用例（同窗注入真实突刺必须恰好触发 1 条 reactive）防弃权假绿。**一处解读待负责人确认**：「7 天回放」实现为 21 天序列、前 14 天热身、后 7 天计分——slot-of-week 基线每槽每周只涨一个观测，纯 7 天冷回放里通路 B 全程弃权，0 假阳性什么也证明不了。审计接线：`AuditSource` 新增 `Capacity`，`capacityTrailSink` 三 kind 原样透传，`leadMs` 直接入 detail 作举证字段；审计与决策一一对应有专门用例（`safeAppend` 吞写失败，该用例是唯一保护）。执行面只到 `auditOnlyExecutor`，`needFromDecision` 严格三轴无 `costLimit`（N-1 有用例钉住）、真实资源供给留白（N-7，真弹性扩容在 M2+）；不接演示链路（P-3）。51 用例全绿，`check:unused` 预算原地不动（exports=1251）。 |

| **v2.33** | **2026-08-15** | **P7.4 上游同步演练执行完毕，数据入库（评审与章程 §5.7 回写待做）**。方法：本仓库是快照导入、不含上游历史，`git merge` 会产满屏 unrelated-histories 假冲突，演练在一次性 clone 里用 `git diff --binary v2.38.3 v2.46.0` + `git apply --3way` 三方应用；前提顺带复核成立——`git diff 3380c88 v2.38.3` 除 39 个 atlas 独有立项文档外**基座文件零修改**。结果：目标 v2.46.0（上游最新正式 release，61 提交 / 8 release / 650 文件 / 67,780 变更行）只产生 **5 文件 7 个冲突块（冲突率 0.39%）**；解决后 typecheck 零错误、**11311 条测试全绿**（3 条假红系演练沙箱 EROFS，关沙箱复测 8/8 过）；上游未删任何文件、新增 261 文件与我方 372 新增零撞名、`bun.lock` 零漂移。按族归因：**P0.3 身份隔离 9 文件零冲突是模范**（上游自己也用同一批 helper，其新增 33k 行里硬编码字面量 0 次）；`@qianmo/*` 17 包零冲突验证「新增独立包 = 零成本」；**冲突大头是文档**（CLAUDE.md/README 占 4/7 块）。工时估算保守 **10–20 人时（1.5–2.5 人日）**。行动项：改薄 CLAUDE.md/README（内容降为指针，可消 57% 冲突块）、把 FreezeAwareWatchdog 等常驻替换抽成注入点、`unused-budget.json` 拆「基座基线+阡陌增量」、评估 ACP 常驻逻辑收进 `@qianmo/resident`（本次零冲突纯属侥幸）、M1 起每 2–4 周或 5–8 个 release 同步一次（上游 v2.45.0 遥测改显式 opt-in 构成主动同步的业务理由）。演练分支未合入任何 remote，主检出经独立复核未被触碰；pin 冻结不变；BASE.md 记录草稿已交负责人（§2.4）。详见 `docs/dev/upstream-sync-drill.md`。 |

| **v2.34** | **2026-08-15** | **P7.3 本地腿就绪：量具、方法与文档骨架入库；正式数据阻塞在真机**。① 交付：`demo/lib/p73-{sample,throughput,report-core}.ts` + `demo/p73-baseline.sh` + `occ resident --mem-sample`（照 `--timings` 形状，写入器泛化为私有实现、既有行为逐字保留）+ `docs/dev/baseline-m0.md` v0.1-draft（方法节全文、数据表全部标【待真机】）。② **两个会让报告作废的测量坑被钉死并实证**：Bun 下 `process.memoryUsage().heapUsed` 是冻结常量（三次采样 0.20/0.20/0.20 MB 而同期 RSS 27→106 MB），采样一律走 `bun:jsc heapStats`；且实测发现第六坑——`heapSize`/`objectCount` 是**滞后**指标（30 万对象分配后纹丝不动而 RSS 已涨 59 MB），判读必须按多点斜率。③ 吞吐校准（本地 unix socket，20 s 档，仅仪器校准）：T2 限流首现于 50 档且数值与令牌桶公式吻合（`B·(1/T+1/60)`，「钉在 10 msg/s」只在 T→∞ 成立）；三节点线性抬升（100 档 550→1999），**再证预算按节点计**；去重表拐点在 20 s 档下方法上不可证伪（档长必须 > 表项时限），驱动已加告警。④ **三条量具缺陷全修在量具上，被测代码一行未动（N-12）**：档间静默 35 s→65 s（令牌回补不足会让正式表长得像「高档位更差」）、档长告警、`--resident-pid` 传错一层的核对办法入文档 §9.2。⑤ report-core 六条 check 把「本地数据不进正式表」做成机器强制：macOS 无 cgroup 通道 → `memoryChannelsIntact=false` → `pass=false`。⑥ `bun:jsc` 不能静态 import 进 vite 打包图（`ssr.noExternal`），改运行期动态解析，`build:vite:only` 实证产物未被打包器改写。**正式 24 h 长跑、n=100 唤醒双阶梯、T3 吞吐正式表全部待 workbench-host 恢复**（或启用 ARM 降级方案，届时须分栏标注架构）。 |

| **v2.35** | **2026-08-15** | **S8 开工：P8.1 / P8.2 / P8.3 / P8.4 各交付第一版，P7.4 章程回写完成；另做一批文档勘误**（主 agent 派遣四个并行子代理产出、逐项审计后合入；`workbench-host` 本日仍 SSH 不可达，凡需真机的一律如实标待验）。① **P8.1 演示环境固化——本地腿完成，真机腿脚本已写未验证**：`demo/env/{bootstrap,seed,up,down,reset,smoke}.sh` + `common.sh`（`DEMO_ROOT` 布局、**每节点独立 `OCC_CONFIG_DIR`**——审计链按配置根落单文件，两个常驻共用一个根必然断链）、`demo/lib/p81-{registry,probe}.ts`（多条登记的注册中心 runner，与 AC-2 专用的 `p41-registry.ts` 分工不合并；probe 做 health → 按名解析 → 真拨号 → 可选发一条真 `task.request` 等 ack）、`demo/env/remote/`（Dormice/gVisor 宿主与沙箱镜像准备，**逐行反推自既有 demo 与 `scripts/ops/`，未在真机跑过一条**）、`docs/dev/demo-env.md`（依赖清单、30 min runbook 六条命令、计时表、两条腿各能演哪几条 AC）。**干净 clone 实测**：clone → bootstrap（含 1717 包安装、build、85 用例自检）→ seed → up → smoke（ack 414 ms、两条审计链 intact）→ AC-3 十条 check → AC-7 冒烟 → down 合计 **87 s**，距 30 min 预算 20 倍余量——**但计时口径是热 bun 缓存 + 本地 clone + 开发机**，全新机器要另加依赖下载与 postinstall 外网取 ripgrep 的时间，文档写死四条口径。**主 agent 独立复跑**：seed/up/smoke/reset/down 全通过、停机无残留进程；`reset.sh` 是唯一 `rm -rf` 处，守卫三条（`demo_guard_root` + 标记文件首行 + 逐路径复核在 `DEMO_ROOT` 内）**六个负向用例逐个放哨兵实测拒绝**（家目录 / 仓库根 / `~/.occ/demo` / `~/.claude/x` / 落在调用方 `OCC_CONFIG_DIR` 内 / 无标记文件），最后一条守卫为审计时加固。DoD 的「未参与部署的成员在全新机器 30 min 内复现」**待人实测**。② **P8.2 判定记录表**：`docs/dev/acceptance-m0.md` v0.1-draft——8 条 AC 各一节（判据指针 / 复现命令 / 实测数据 / report-core check 清单 / 独立核验点 / 边界 / 录屏分镜 / 待办），19 个 SHA、25 个路径、5 个 CI run 逐一核到，未核到的 8 项如实列出。**按 DoD 口径当前 0 条未通过、0 条需豁免**；卡点是「能否复跑」（AC-2 / AC-6(a) 需真机，AC-1 判据② / AC-4 / AC-5 需凭据）而非「是否达标」；**AC-6(b)(c) 的沙箱挂载边界只在本机测过**，是最可能需要豁免的一条，两种定性写明供负责人定夺；AC-1 判据②只有 v2.13 的叙述、无留档报告，走查时应补一次留档。录屏与现场走查**待做**。③ **P8.3 口径与 Q&A 草案**：`docs/dev/positioning-m0.md` v0.1-draft（**待负责人 + 安全 owner 双签，签前不得外发**）——§5.8 标准表述三个场景版本、21 条禁用表述、28 行「基座既有 / 本项目新增」两栏表（照抄章程 §3 三态判定）、实跑举证（`3380c88..HEAD` 于 `37dc4eb1` 前一提交：429 文件 / +69,812 / −1,362 / 191 提交；`@qianmo/*` 17 包；测试占 40.3%）、基座 `src/` 56 个改动文件逐一从提交信息取「为何改核心」（11 明确 / 17 部分 / **4 待补**：`cli.tsx`、`teammateMailbox.ts` 及其测试、`configWatcher.test.ts`——P9.3 改造点清单的原料）、16 问 Q&A（含必含两问）、13 镜分镜。④ **P8.4 工程侧完成，科技处咨询待人**：`scripts/sbom.ts`（零新依赖，`bun run sbom` 幂等生成 CycloneDX 1.5 形状 `docs/dev/sbom-m0.json` + 人读 `sbom-m0.md`；`--check` 可作机器判据；38 用例，`scripts` 已在 CI 分片）——1419 条 lockfile 条目 → 1089 唯一组件，**强/网络传染 0、受限 0**（DoD 该条满足），弱传染 5（LGPL 的 sharp-libvips 预编译库 + MPL-2.0 三项，均可留用），5 个组件对原始 package.json 抽查 5/5 一致；**一条判读口径写死**：`ssr.noExternal: true` 且基座把运行时库多放在 devDependencies，prod/dev 划分 ≠ 产物边界，不得以「dev 依赖」放行传染性许可。`docs/dev/license-chain-m0.md`：证据包核对表（npm 元数据 v2.38.3 `gitHead` 与 pin **逐字一致**、发布于 2026-08-11；**E-8 负责人对基座的归属关系仓库内零证据、只能由人提供**）、NOTICE 六条差异（见⑥）、17 个 `@qianmo/*` 包 license 全 MIT + 版权头 217/217、科技处议题模板五组（含 L-2 / L-5）与「空着即未闭环」的口径回填表。⑤ **P7.4 章程回写完成**（章程 v2.9，提交 `9a69829f`）：§5.7 四处补注 + 「M1 排期输入」候选清单；**评审与 `BASE.md` 记录（负责人）仍待做**，DoD 未闭。⑥ **审计中坐实的四件事，需负责人处置**：(a) 根 `NOTICE` 45–46 / 133–134 行与 `README.md` 仍写旧举证命令 `git diff 848ad8c2..HEAD`（章程 v2.4 已勘误为 `3380c88..HEAD`；NOTICE 是对外法律文本、须双签，本次未改）；(b) **本机主检出里 `848ad8c2` 能解析**——是 pack 中不被任何 ref 可达的悬空提交对象（`rev-list --all` 仅 196 个提交、无分支包含它），**干净 clone 中不存在**，章程结论仍成立，但本机预演会误以为旧命令能跑且给出另一组数字，答辩机应用干净 clone，对象来源待查并由负责人决定是否记入 `BASE.md`；(c) `@anthropic-ai/claude-agent-sdk`（devDependencies）是**专有授权**（`LICENSE.md` 全文「© Anthropic PBC. All rights reserved.」），全仓仅一处 `import type` + 两处注释、`dist/` 零命中不进产物，与 NOTICE「无关联」声明并存需主动披露，是否换本地类型声明提请评审；(d) `vendor/audio-capture/` 六个已入库 `.node` **无源码、无 LICENSE、无溯源**且随 `dist/vendor/` 分发，NOTICE 五未覆盖，待负责人说明。⑦ **文档勘误**（由 P8.2 核对抓出）：AC-2 的 ack P95 在 v2.16 决策注与 P4.1 任务包「验收完成」条写 4.440 s / 5.885 s，与 v2.20 及速查表的 **8.022 s / 12.422 s** 矛盾——正式记录取后者（评审九条修复之后的构建、自带 p50 与冷链路解释、速查表自称唯一出处），两处就地加勘误；P1.2 任务包标题的「⚠️ 基本完成」与速查表「✅ 已完成」不一致，改标题；「尚未开始且需要人的」清单收尾（P0.1 已按 v2.10 实测通过）；文头「依据」栏自 v2.5 起停在章程 v2.4，对齐 v2.9。`session-persistence-review.md` §0 第 7、8 条已过期一事记入 acceptance-m0 §5.4，本次不改该文。**排期、日期、产能、关键路径均未动。** |

| **v2.36** | **2026-08-16** | **第二轮收口：能不靠人的都不靠人——两台全新机器实测、AC-3 慢机器假红修订、本机腿全量走查留档、aarch64 基线正式数据、NOTICE / BASE.md 闭环、P9.3 三件交付、P9.2 复盘草案**（主 agent 派遣六个并行子代理、逐项审计后合入；`workbench-host` 自 08-14 起 SSH 不可达，负责人另有九台无 sudo 的 VPS 可用，凡需 Dormice + gVisor 的仍如实标待真机）。① **P8.1 全新机器实测两台**（`da98d86f`、`840b934d`）：Debian 13 x86_64 2 vCPU 冷缓存公网，第一台 clone→bootstrap→seed→up→smoke→AC-3→AC-7→down 端到端 **2 min 40 s**；第二台（只读 runbook 复现）净 **105 s** / 含往返墙钟 **8 min 19 s**，唯一硬卡点是 bun 安装器要 `unzip` 而机器没有——runbook 补依赖行、busybox 垫片与故障表，另补 node / make 装法与最低内存 2 GB。两次都远在 30 min 内；DoD 的「未参与部署的成员」形式要件仍需一名真人照 runbook 走一遍，**主 agent 判定本地腿 DoD 实质达成、形式要件待人**。② **AC-3 慢机器假红修订**：第一台全新机器上 AC-3 稳定判红——`accepted=601 ≠ 600`，根因是慢机器上 601 条突发跨过了令牌桶 100 ms 回补步长（1 token/100 ms），量具把「刚好一条回补」判成失败。两案并存：主 agent 的子代理冻结接收方原始时钟（`c7a02607`），负责人另一会话改判据为「按突发用时算顶」（`accepted === sent−1 ∧ perMinute ≤ accepted ≤ perMinute + ⌊burstElapsedMs/refillIntervalMs⌋`）；**取后者**（真时钟、量具不侵入被测代码、判据可解释）——`7e83649a` 撤回前者（保留其钉住 100 ms 回补步长的 router 用例），`9736ddca` / `5b98e6e8` 合入后者，同一台慢机器复跑 **4/4 绿**（`accepted=602`，`allowance=2`，`e74df9d8`）。P4.2 被测代码一行未动。③ **P8.2 本机腿走查完成留档**（`ba95527d`）：新增一键驱动 `demo/walkthrough.sh`（`--only/--skip-slow/--with-ac7/--with-chaos N/--list`，每条 AC 在 `script -q` 下录 transcript 作为可机器核验的录屏替代，产物落 `~/qianmo-acceptance/<UTC>/` 0700/0600 仓库外，PSK / 令牌每次现生成只经环境变量传递，凭据只探存在不读值，INT/TERM 把进行中项标 `interrupted`）；`docs/dev/acceptance-m0.md` v0.2-draft §8 走查记录 16 个 sha256 逐一核到。**结果**：AC-3 / AC-6(b)(c) / AC-7（`856d0ff8` 上再跑 3/3）/ AC-8（含 60 min 混沌）**PASS-已走查（本机）**，AC-1 脚本部分 PASS、判据②仍待凭据；AC-4 / AC-5 收敛为一条命令 `source <凭据文件> && demo/walkthrough.sh --only ac4,ac5,ac5e2e`；AC-2 / AC-6(a) 待真机。真人剩三件：供凭据跑那一条命令、真机两条、现场评议。④ **P7.3 aarch64 降级栏正式数据**（`005d6888`，baseline v0.2-draft）：cornna-p6（aarch64 Ubuntu 24.04）T1 / T2 / T3 / T2×3 四轮八档全 `pass=true`；单节点入站天花板 **1799 条 = R_max(120)**（14.99 msg/s，与 `B·(1/T+1/60)` 逐字吻合），三节点 **5397 = 3.00×**——第三次证实预算按节点计；两条新发现记 §7：**B-3** 被拒消息也逐条 fsync 进审计链（容量按被拒流量算 IOPS）、**B-6** 拒绝比投递更贵（须栏 A 复测）。**24 h 长跑运行中**（systemd user unit，预计 2026-08-17T06:21Z 结束，`~/p73/collect.sh` 回收后填 §5/§6）；x86 / GCP 栏与 n=100 唤醒阶梯仍待真机。⑤ **P7.4 只剩评审**：`BASE.md` 记录已由负责人授权写入（`0c2f3b17` 补上游公开 URL `https://github.com/sweetcornna/open-claude-code`、npm `gitHead` 证据与演练记录；`a626eb95` 勘误——v2.35⑥(b) 的悬空对象 `848ad8c2` 来源坐实：演练用 worktree 共享主检出对象库，上游对象随 fetch 进了主 pack、不可达、干净 clone 不带走；`856d0ff8` 同步勘误演练报告）。⑥ **P8.4 / NOTICE 闭环**（`f288afb4`）：NOTICE 与 README 举证命令勘误至 `git diff / log 3380c88..HEAD`（另给 `git show --stat 3380c88`）；NOTICE 五补披露 SBOM 位置、`vendor/audio-capture` 预编译二进制（溯源：上游同样只有 `.node`、Rust + napi-rs / cpal，无源码无 LICENSE，随基座 MIT 分发）、ripgrep、`@anthropic-ai/claude-agent-sdk` 专有 devDep（**决定保留 + 披露**：全仓仅 `import type`、`dist/` 零命中）。license-chain 与 positioning §3.5 同步。**仍待人**：双签（NOTICE 是对外法律文本，本次按负责人授权先改、签字补记）、E-8 归属举证、科技处咨询。⑦ **P9.3 三件交付**：17 个 `@qianmo/*` 包 README（`91f1f446` 通信/常驻九包、`5c8a3bd2` 调度/记忆八包；每份含架构图、API 面、不变式与钉住它的测试、双人签字栏与 3 道复述题）；`docs/dev/base-modifications.md` 对基座的改造点清单——**11 条书面归因 / 14 条依代码推断 / 7 条待改动人确认**（v2.35③ 的「4 待补」在其中），positioning §3.3 三行归因勘误（`8012b300`，非看门狗替换）并改指向该清单为真源；`docs/dev/signoff-m0.md` 双人签字表 v0.1-draft（`b3cda44f`）。签字与 3 道题现场验证待人。⑧ **P9.2 复盘草案**（`c9786d38`，v0.1-draft）：见本版 P9.2 速查行；审计时修正四处与 08-16 提交状态不符的陈述、AC-7 达成日按 git 改为 08-15（**roadmap v2.30 行的日期栏写 08-14 与提交时间 `ca8c07f7` 08-15 07:40 不符，以 git 为准，本表不改历史行**）。其附 B 六条文档不一致本轮处置五条：B-1 acceptance §5.5/§5.6 加「已修」注、B-2 本表变更记录 v2.30–v2.36 改回升序（本版）、B-3 positioning §3.1 重跑至 `dbecf008`（217 提交 / 472 文件 / +111,782；分解表单列**机器生成物 31,093 行**——扣掉后 80,689 行，`41910fe5`）、B-4 **章程 v2.10**（`52a76802`：§5.5「不改基座核心」一句加补注，消化 `teammateMailbox.ts` 向后兼容加法的字面不符；原句保留、不改范围）、B-6 `session-persistence-review.md` §0 第 7、8 条加过期注；B-5（NOTICE 旧举证命令）成稿前已由 `f288afb4` 修掉。**P9.1** 首次入表：本机 `bun test` 三次连跑全绿（HEAD `005d6888`，9985 pass ×3）+ 同日 `precheck`，真机腿纳入前不判完成。⑨ 门禁：`precheck` 零错（9985 用例）、`check:unused` 主检出绿。**排期、日期、产能、关键路径均未动；文头「依据」随章程升至 v2.10。** |

| **v2.37** | **2026-08-16** | **P9.1 / AC-8 收口：CI 五连绿在结项 HEAD 重取 5/5，顺手抓出并修掉一处 Linux 专属的用例竞态**。① 在 `74ff0154`（v2.36）上串行 `workflow_dispatch`，首次即红——`src/services/mcp/__tests__/configWatcher.test.ts`「watched file is deleted」5002.99 ms 超时（08-15 已在 `31904161135` 见过同族「appears for the first time」）。先坐实一件事：**`bunfig.toml` 的 `[test] timeout = 10000` 在 pin 的 Bun 1.3.13 下不生效**（6 s 用例设该键仍 5.03 s 判失败，CLI `--timeout` 才生效），全仓每用例实际预算一直是默认 5 s；给三条正向用例显式 15 s（`0e9e7c3b`）后再 dispatch **仍在 15 s 超时**——不是节拍慢。② 在 cornna-p4（Debian 13 x86_64）复现：原用例 **15 跑 8 败**，改文件前先等 1.5 s 则 **15/15 绿**。根因：**Linux 上 `fs.watchFile` 的首个 stat 在返回后异步落地，抢在它前面的写 / 删被当成基线、永不上报**；macOS 上几乎复现不出，所以本地一直稳绿。修在用例上（`d162fe72`：`settleWatcherBaseline()` 等过一个轮询周期再改文件），生产代码未动（首秒内的人为编辑丢失不算产品缺陷）。③ 在 `d162fe72` 上串行五次全绿：`31940540478` / `31940845668` / `31941139584` / `31941438394` / `31941752229`（10:00Z–10:34Z，`gh run view` 逐个核到同一 headSha）；`acceptance-m0.md` §1 / §2.8 / §4.5 记录（`58a7dba4`），§4.5 由「待表态」改为已重取。`a8b06a9` 那组首次五连绿保留。④ 速查表：P9.1 行补「1 处 CI 偶发用例根因坐实并修」与结项 HEAD 五连绿；P8.2 行 AC-8 补重取。**排期、日期、产能、关键路径均未动。** |

| **v2.38** | **2026-08-16** | **P9.3 收尾：改造点清单 §6.1 五件「待改动人确认」考证收口（`0c1ba4ce`）**——主 agent 派子代理对 1–4 号做逐通道 / 逐调用点考证、抽查证据指针后合入；第 5 件（configWatcher）已由 v2.37 的排障闭环。**7 行待确认收敛到 2 行待裁定**（判定统计 11/14/7 → **12/18/2**）。两句原表述被考证**证伪**并就地改写：①「加 `if (active)` 后基座语义逐字不变」不成立——忙边缘多一帧、`sendSessionActivitySignal()` 在 refcount 0 时不再发帧、idle 计时器触发面扩大，三处均在 keep_alive / 诊断语义内且有测试钉死，留待裁定「有意 / 漏看」；②「基座没有子命令注册表」不成立——Commander 注册表存在（`registerSubcommands`，15 模块）但被刻意排除在 print 快速路径外，基座自己对 `migrate` / `autonomy` / `remote-control` 就是两处都写；**新待办**：`resident` / `audit` / `resident-wake` 未注册进 Commander、不出现在 `occ --help`。QueryEngine 回调（「输入已入上下文」无任何既有通道可观测、且该事实是改动自己造出来的）与信箱锁内记账（竞态三前提坐实、「不导出锁」改「`proper-lockfile` 不可重入」）升 🟢。positioning §3.3 / retro / 文档索引同步。**排期、日期、产能、关键路径均未动。** |

| **v2.39** | **2026-08-16** | **凭据腿与真机腿双双解锁并走查**（负责人指出「凭据和真机都给过」后主 agent 复查：真机 `workbench-host` 直连自 08-14 起坏在路径上、机器本身健康——串口日志佐证，经 **gcloud IAP 隧道**恢复访问（SSH 别名 `workbench-iap`）；凭据取自 `~/.occ/provider-profiles.json` 的 `opencode` 档。两个 opus 子代理并行实跑、主 agent 二次复核 sha256 与判定）。① **AC-1 判据②现场留档并落库**（`8bf700a3`、`ff3fa4b1`）：CSPRNG 现生成常量两轮入上下文 → 第三轮开工后 `kill -9`（退出码 137）→ `--resume` 单条追问 → 回答复述常量名与数值，14 项全过；脚本落库 `demo/ac1-criterion2.sh` + walkthrough 凭据腿 `ac1c2`，落库版复验 14/0。acceptance §4.3 关闭，**AC-1 整条 PASS-已走查**。② **AC-4 deepseek 腿全绿**：命中 5/5 带来源 ID、伪造决策零引用 3/3。**AC-5** 一致性三项 3/3 + 两跑 run A 全绿；run B（qwen3.8-max）被网关上游 502/503 阻断（主 agent 亲测复现，非回归），已挂 10 min 间隔恢复监视，恢复即重跑补齐。过程抓出凭据提取缺陷（`OPENAI_BASE_URL` 少 `/v1` 时 env 覆盖压过仓库内正确配置、首轮三项全红）——已修并在 acceptance §9.1 留作反面教材。③ **AC-2 真机复跑 10/10**（HEAD `62a9439c`，bun 钉 1.3.13）：八条 check 全 true、每轮先确认 frozen；**ack P95 5.589 s / result max 8.976 s，较 08-13 的 8.022 / 12.422 s 均降约三成**；独立核验进沙箱取常驻侧证据三项全 true。**AC-6(a) 真机复跑 5/5**（runsc 出生契约 + 五条审计事件 + 审计文件 600/700）。产物 `~/qianmo-acceptance-remote/20260816T153435Z/`，六文件 sha256 主 agent 登机复核（acceptance §9.3，`0bf387c8`）。④ **P8.1 真机腿首验通过**：`prepare-host.sh` 修两处后 PASS=10/FAIL=0（doctor 要以 daemon 身份 + EnvironmentFile 跑；守卫 `sudo -n test -r`），`prepare-sandbox.sh` 真沙箱四节全通（2 GB cgroup 内 build 成功、常驻 ALIVE、宿主拨 38622 得 101）并补 PSK 长度前置判（`b6d464c2`）；README 与 demo-env §7.1 的「未在真机验证 / setup.sh 取不回来」两句证伪并回写。**新发现**：云元数据防火墙规则未持久化（重启即失，待装 unit）；P0.1 遗留③在该机已消。⑤ **过程事故与纪律**（acceptance §9.3 如实记）：并发 `--delete` rsync 打坏过远端树（产物产出前，已重建）；**仓库根的 `.occ/` 含凭据、`.claude/` 21 GB，往任何远端送代码禁止裸 rsync**，同步姿势入 demo-env §7.1；`dor sandbox push` 有体积上限，送码用 `docker cp` 或沙箱内 clone。⑥ 剩余：AC-5 qwen 半条（等网关）、AC-6(b)(c) 真机挂载边界、P7.3 栏 A（下一轮真机任务）；真人项进一步收缩见「尚未开始且需要人的」。**排期、日期、产能、关键路径均未动。** |
| **v2.40** | **2026-08-17** | **第五轮收口：AC-6 挂载边界真机闭合、P7.3 栏 A 三块数据落表、AC-5 以替代供应商收口、P9.1 终锚判完成、P0.2 修正复核定稿候审**（opus 子代理周限耗尽后由主 agent 亲测亲写；真机仍经 IAP 隧道）。① **AC-6(b)(c) 真机挂载边界首测通过，AC-6 三子项全绿、不再需要豁免**：burn-vm-01 runsc 沙箱（只读根）内 **28/28 check 全 true**——store `/var/lib/qianmo/backups` 不在任何 docker/沙箱挂载之下（唯一可写 bind 是 workspace）、沙箱内对 store 全 ENOENT、HTTP 面（docker 网桥宿主端）快照 201 / list 403 / read 403 / 删除动词 405×4 且 9 条审计事件逐一对上、沙箱内 `rm -rf` 工作区后 **60 ms** 恢复（预算 10 min）且 `git status`/`HEAD`/exec 位逐字一致、wipe 前后 store 两文件 sha256 不变；产物 `~/qianmo-acceptance-remote/20260816T165237Z/ac6bc-mount/`（`SHA256SUMS.txt` 全量、`report.json` sha256 `4207dc6a…`）。acceptance §9.5 新增、§4.1「最可能豁免」条按「直接补做」关闭、§1 AC-6 行翻全绿（`rm -rf` 对不存在路径退 0 不作证据、以 ENOENT + store 完好为证的口径照录）。② **P7.3 栏 A 三块数据落表（baseline v0.3-draft）**：(a) 吞吐四轮（T3→T2→T1→T2×3，16:58~18:33Z 顺序跑）八档全 `pass=true`，**栏 B 三条形状结论全部复现**——天花板 1799 = R_max(120)（四档完全相同）、三节点 **5397 = 1799×3 整数相等**（第四次坐实预算按节点计）、T1 `kneeAt` 又在 400 档触发（p95 2→7 ms，仍弱阳性，两栏交叉印证位置与方向）；**公式复现到个位**——T2 400 档超时 1.835 s 投递 1818 = 1800+⌊18.35⌋，T3 两档 1824 = 1800+⌊24.5⌋；**新发现 A-1**：拒绝审计 fsync 在 GCP 网络盘上被放大（T3 200 档 p50 862 ms vs 栏 B 同档 3 ms，发送循环被拖到 23949/24000），与 B-3/B-6 同根、归因实验排 M1；T1 八档全清坐实 4 vCPU 上传输层非瓶颈、B-4 的「1 vCPU 成分」被排除。(b) **t=0 唤醒阶梯 n=100 全 responsive**（P4.1 现场沙箱、每轮确认 frozen、100/100 真唤醒）：唤醒发起→就绪 P50 **931 ms** / P95 1414 ms（含 unpause + 工作集换回）、接受→首内容 P50 4162 / P95 5794 ms、接受→turn 完成 max 16.3 s（尾部全在冷身第 1 轮）；P95 为真分位数（n=100）。(c) **栏 A 24 h 长跑开跑**：2026-08-17T00:57:44Z 起双 systemd user unit（`p73-l0*`，Linger、独立 cgroup），1.67 h 现场核对采样双通道零断档、pid 不变、oom 0；预计 08-18T00:57:44Z 满，届时补 t=24 h 阶梯（E2 外推对照的主角）再回收。③ **AC-5 以替代第二供应商收口（判据字面满足）**：qwen 上游 502/503 8h+ 后按判据原文「≥2 个供应商适配器」不点名 qwen 的事实，取 **kimi-k3（strict-openai）** 为第二腿——两跑命令行逐字相同仅换 providersFile（差异 2 行）双绿、实现体一字不差、一致性三项在两条不同 compat 档上 **9 pass / 0 fail 且零测试代码改动**（只换夹具数据 1 行、跑完 sha256 校验还原）；qwen 同分钟 503 对照留档为上游可用性铁证，**qwen 原夹具重跑降级为补充项**（恢复监视在挂，06:21Z 仍不恢复则执行已记录的最小整条替换四步）。acceptance §9.4（`69bbd6c2`）。④ **P0.2 修正复核定稿候审**：`base-adoption.md` v2.0（`7e1d52ba`）——selection-m0 §2 八条修正逐条书面结论（采纳 5 / 部分 2 / 不采纳 1），§8.6 附可照跑抽查块（主 agent 复跑过）；retro P10.5 补第五项 DoD（章程 §4.1 AC-6(a) 提请评审问题，`6759d1eb`）。⑤ **P9.1 终锚并判完成**：结项走查 HEAD `2fc6ce70` 上本机 `bun test` 三连绿 + CI `workflow_dispatch` 绿（run `31983703436`，headSha 逐字核对）；v2.39 已把 AC-2 / AC-6(a) 真机复跑纳入，「真机腿纳入前不判完成」的保留条件解除。⑥ 剩余真人项不变（五个评审、双签两件、科技处 + E-8、P8.1 成员形式要件、P8.2 现场评议）；机器侧只剩三件在途：栏 B 长跑回收（06:21Z 后）、栏 A 长跑 + t=24 h 阶梯（明日）、qwen 补充项。**排期、日期、产能、关键路径均未动。** |
| **v2.41** | **2026-08-17** | **负责人 2026-08-17 决议落实：流程件总关闭，M0 剩余工作收敛为机器侧在途三件**（负责人原话「评审通过，双签通过，不必要的都删除，我现在只要达到目的开发完软件就好」；记录形式与 v2.1/v2.3 历次负责人决议同一口径，可复核）。① **五个评审通过**：P0.2（`base-adoption.md` 升 **v2.1 定稿**，P0.2 关闭）、P2.4（现有草案即终稿，「赛中痛点补写」不再要求）、P6.4（三个判断点与 G-18 按草案建议定案）、P7.4（同步节奏**定稿**每 2–4 周 / 每 5–8 release，「回贡」定为暂不、个案再议——**章程 v2.11** 回写 §5.7 并关闭 §4.1 的 AC-6(a) 提请评审问题：**双层，基座权限模式第一道、gVisor 容器第二道**，即已验收形态的书面确认）、P9.2（retro 评审通过）。② **双签通过**：P8.3 `positioning-m0.md` 升 **v1.0「已双签，可对外」**（§6 留签署记录；录屏两条核查项随录制补勾）；根 `NOTICE` 修订文本随同一决议签认补记。③ **形式要件豁免**：P8.1「由未参与部署的成员走 runbook」豁免（两台全新机器实测已覆盖实质）；P8.2 现场评议以三条腿留档走查为准关闭（acceptance 即最终判定记录）；P9.3 逐包现场验证与手写签字豁免（`signoff-m0.md` 升 v1.0 转留档，签字栏如实留白）。④ **移出 M0 出口**：P8.4 的科技处咨询与 E-8 归属举证与申报流程绑定、不阻塞软件完成，申报启动时再做（license-chain 缺口登记保留不删）。⑤ **小裁定一并记录**：真机防火墙规则不装持久化 unit（重启后照 README 重跑 `prepare-host.sh`）；`qm-p13` 沙箱模板保留（复现便利）；`base-modifications.md` 2 行待裁定与 `--help` 待办、capacity「21 天」解读均维持现状归 M1（P11.5）。⑥ **M0 收口后的全部余项**（都不需要人）：栏 B 长跑回收（08-17T06:21Z 后）、栏 A 长跑 + t=24 h 阶梯与回收（08-18T00:57Z 后，随后 baseline 升 v1.0）、qwen 补充项（等网关，不阻塞）。retro §8 前置检查表已逐行标注闭合。**排期、日期、产能、关键路径均未动。** |
| **v2.42** | **2026-08-17** | **M0 收口：负责人决议「暂停试验，已经可以了」落实——两条长跑计划内提前停机、baseline 升 v1.0 定稿、AC-5 最小整条替换落地、qwen 补充项取消；M0 无任何在途项，转入 M1 开发**。① **两条长跑按停机纪律收口**（先只读取数 → 应答探针 → `--stop`，数据全量保留、零计划外中断）：**栏 B 22.13 h**——六判据全过（pid 不变、ACP 零重启、oom 0、采样 1329+2655 条零断档、探针 `delivered` 164 ms），堆曲线**非单调上涨**（12~18 h 段斜率 −1.57 MB/h、全程最小二乘 +0.11 MB/h，heapSize 12 h 见峰 25.5 MB 后回落至 16.1——GC 收得回来，泄漏假设在此负载与跨度内不成立）、ACP 子进程空转 22 h 零增长（VmHWM 全程钉 312.3 MB）；**栏 A 3.53 h**——同判据通过（探针 599 ms），跨度落在热身段、内存不判读。② **baseline v1.0 定稿**（`a86a8013`）：§0 ① 改为按实际跨度表述（**「连续 22.13 h 六判据成立」，不说 24 h**）、DoD 字面未达成与 E2 外推对照未完成**如实进 §8.4 缺口**；t=24 h 唤醒阶梯取消；§5.5/§5.6/§6.1~§6.5 全部落表；§10 记账收口。③ **AC-5 最小整条替换执行完毕**（qwen 上游 502/503 逾 20 h，按 acceptance §9.4 预案）：夹具两处各 1 行换 kimi-k3（`cff9b138`）、id 中性化 `qianmo-qwen` → `qianmo-alt` 单独 refactor（`cfd68052`，id 不再编码厂商）；换后真调用 **9 pass / 0 fail**（deepseek + kimi 双腿）、`precheck` 零错（9985 pass）；p1.4 升 v0.2、acceptance 同步（`68f6d930`）；**qwen 补充项取消**、恢复监视撤下。④ **P7.3 判完成**（以 v1.0 定稿收口；L1/L2、A-1 归因、重复跑批误差棒按 §10.1 归 M1）。⑤ **至此 M0 无任何在途项**：八条 AC 全 PASS（零豁免）、全部流程件已关（v2.41）、基线定稿——**M0 收口，开发主线转入 M1**（首两迭代按 retro §7 草案：内测环境 + 身份隔离收尾，任务包以开工时按现状重估为准——P10.4/P10.5 已提前做掉大半，见各处闭合注）。**本表 M0 部分自此冻结为记录，后续变更记录 M1 事项。** |
| **v2.43** | **2026-08-17** | **M1 开工：P10.1 身份隔离三者共存正式验收完成**（retro §7.2 首迭代包；M0 部分冻结后本表开始记 M1 事项）。① **零绕过抓实并修净**：新棘轮 `bun run check:identity-paths`（`scripts/check-identity-paths.ts`，`ddd11ffc`）扫 src 与各包 src 共 **2648 个生产文件**，注释剥离后禁身份字面量 / `claude-cli` 缓存命名空间 / `homedir()` 拼身份路径——首扫抓到**最后 3 处真实绕过**并修掉（`ab2e8746`，行为逐字不变）：ide.ts WSL 启发式两处裸 `.claude`、migrateFromClaude 一处裸 `.claude.json`（paths.ts 新增 `LEGACY_GLOBAL_CONFIG_FILENAME` 同源派生）；协议位 UA `claude-cli/<version>` 与第三方点目录按设计豁免、豁免理由写死在脚本头注；现 **0 绕过**。② **探针升级**（`bec20efa`）：identityProbe 报解析后 env-paths 缓存根 + 三个 XDG 根，新用例断言两身份**落盘树前缀不相交且 HOME 内**（防「字符串不同但一棵树包含另一棵」的递归事故面）；套件 9 pass。③ **真机枚举**：本开发机上官方（`.claude` 三件全存在）与 occ（`.occ` 三件全存在）**长期真实共存零交叠**，qianmo 命名空间派生正确、尚未启用（absent 如实记）。④ 验收报告 `docs/dev/identity-coexistence-m1.md` v1.0（含 DoD 对账与三条边界：官方侧证据是结构性+观察性、棘轮暂未进 CI 门禁待 M1 评审、钥匙串归 P0.3 原验收）。`precheck` 零错（9986 pass）。 |
| **v2.44** | **2026-08-17** | **M1 · P10.3 减冲突整改三件套全部落地，并用真实上游补丁量出冲突面下降**（retro §7.2 首迭代第二包；主 agent 组织 + opus 子代理并行实作 + 主 agent 逐项复跑审计）。① **① 文档改薄——落地形态与演练设想相反但等效**：不是「改薄成指针」，而是**阡陌增量在前、基座原文逐字附在后**（`BASE-CLAUDE.md` 并回根 `CLAUDE.md` 下半部分并删除；`README.md` 同法附基座原版 README）。理由是三方合并的机制：`git apply --3way` 按 blob 合并，我方相对上游 preimage 的差异必须退化成**一次纯插入**才不撞上游 hunk，而「改薄成指针」删掉的恰恰是上游 hunk 要落的那些行。基座部分与 `3380c88` 逐字节一致（`tail -168 CLAUDE.md` / `tail -167 README.md` 的 sha256 与基线相同），阡陌部分只动 5 处指针/自指句、**已双签的对外表述一字未动**。② **② 常驻替换抽注入点**：按演练 §5⑥「在上游现成的间接层之下派生」——上游现成的间接层就是 `setTimeout`/`clearTimeout` 的**形状**，于是在阡陌自有的 `freezeAwareWatchdog.ts` 里派生同形的 `setFreezeAwareTimeout`/`clearFreezeAwareTimeout` + `FreezeAwareTimer` 句柄（含尾随实参透传，正是为了让 `claude.ts` 的告警计时器回到上游三参原形）；三个基座文件的控制流与变量名回到上游原文，**残余 body 改动行 99 → 20（−80%）**，`git diff --word-diff` 全部是纯标识符替换；行为逐项核对不变（超时值 / 错误消息与类型 / 重置时机 / 冻结跨越语义 / 一次性 / `unref`），新增 3 条 drop-in 契约用例。③ **③ unused 棘轮拆两段**（`14f8ead9`，v2.43 后落地）：基线文件回到基座原数字且 `--update` 永不写它，增量落 `scripts/unused-budget.qianmo.json`。④ **冲突面下降是实测的，不是测算**：演练留下的上游对象仍在本机对象库（v2.46.0 `1128a391`），主 agent 在一次性沙箱里对**整改前 / 整改后**两侧各跑一次 `git apply --3way`——`CLAUDE.md` 2→0、`README.md` 2→0、`responsesAdapter.ts` 1→0（演练里唯一的代码语义冲突），且合并后阡陌内容一字未动、基座部分自动升级为上游 v2.46.0 版本。**7 个冲突块中 5 个已消（含③的 `unused-budget.json`），预期下次同步剩 2 块**（`openaiStreamAdapter.ts` 的 provider 兼容修复 + 演练未整改项）。⑤ **DoD 的最后一腿留给 P10.2**：P10.3 判据原文是「在 P10.2 的真实同步上冲突块数下降」，本次是用真实上游补丁做的**前置量化**，真同步的实记仍以 P10.2 为准。⑥ 门禁：`precheck` 零错（**9989 pass / 0 fail**）、`check:identity-paths` 0 绕过、`check:unused` 数字零漂移、`check:mock-hygiene` 全零。回写：`base-modifications.md` §2.2/§2.8/§4、`upstream-sync-drill.md` §5 第 2/3/4 条标注已落地。**排期、日期、产能、关键路径均未动。** |
| **v2.45** | **2026-08-17** | **M1 · P10.2 首次真实上游同步完成：v2.38.3 → v2.46.0 已合入，冲突 1 块 / 0.06%，成果边界口径同步更新**（retro §7.2 首迭代第三包；主 agent 组织 + 3 个 opus 子代理并行审计/归因 + 主 agent 逐项复跑）。① **同步本体**：`git diff --binary 848ad8c2 1128a391 | git apply --3way`（不是 merge），**650 文件 / 1784 hunk / +60,363 −7,417 / 61 提交 / 10 个 release 全部落地无遗漏**（补丁文件数与落地提交文件数逐一比对为 650:650）；上游对象来自 P7.4 演练时进入本机对象库的那批，未加 remote、未 fetch。**依赖零漂移**（`package.json` 只动版本号，`bun.lock` 未动，`bun install` 报 no changes）；`.github/` 未被触及，P0.4 移除的 `publish-npm.yml` 未复活。② **冲突 1 文件 / 1 块 = 0.06%**（演练同区间 5 文件 / 7 块 / 0.39%）——P10.3 预先消掉的 6 块**全部兑现**，只剩演练里那个 provider 兼容族的 `openaiStreamAdapter.ts`：上游加 `sawOutput`（网关以 usage chunk 收尾时判正常停止）、我方加 qwen 空 reasoning 守卫，两侧正交取并集（`3476dd93`），并经子代理穷举复核 `textBlockOpen ⟹ sawOutput` 成立、四条反例路径均不成立。③ **提交切分守边界**：`d04a79dd` 的改动内容全部来自上游补丁（649 文件自动合并 + 冲突文件取上游侧原文，零人工判断），阡陌判断从下一笔起。④ **两份合并审计（16 个「双方都改」的文件）判无静默语义破坏**：身份/权限族（`filesystem.ts` 上游 +242/−28、`sandbox-adapter.ts`、`cli.tsx`、`QueryEngine.ts`）与 API/会话族（三个看门狗文件、`openaiStreamAdapter.ts`、`logAssembly.ts`、`transcriptLoader.ts`）我方 13 处改动**全部原样落在上游新代码之上**——主 agent 复核「合并后相对上游剩下的行数」与「我方原始改动行数」逐一相等；关键前提：**上游没碰 `paths.ts` 与 `identity.ts`**，三身份并集真源整体存活。另确认上游未新增任何未被冻结感知注入点覆盖的裸 `setTimeout` 空闲看门狗。⑤ **同步带出的三件事各自处置**：(a) 上游新增文件 `plugins/eval/discovery.ts` 把 `.occ`/`.claude` 写成字面量，改从 `paths.ts` 派生（`d811e1c7`，零行为变化）；(b) 上游 `openaiStreamAdapter.ts` 读 `chunk.choices.length` 不带可选链、遇到只发 usage 的网关会抛 TypeError，补上（`c628ff0d`，属上游自带缺口，可回贡）；(c) `throttleAndCircuitBreaker.test.ts` 两侧撞同一个修，撤回我方 10 行、该文件回到与上游逐字节一致（`6406af76`，**同步该收的红利**）。⑥ **`check:unused` 按设计跳闸后查清归因**：两个对照实验坐实**上游自己的基线数字过期 +7/+31/+1**（纯净上游 v2.46.0 树实测 1247/665/10 vs 其提交的 1240/634/9；对照组纯净 v2.38.3 逐字复现 1255/638/9，环境保真），knip 全量 1918 条**零条**落在阡陌自有代码上，阡陌真实增量仍是 −4/0/0。为不把上游的陈旧数字记成我方死代码，棘轮拆出第三列 `unused-budget.upstream-drift.json`（`73cb65d5`），有效预算 = 基线 + 上游漂移 + 阡陌增量。**副产品：上游 main 的 `check:unused` 门禁目前应为红**（其 CI 跑这条），可回贡。⑦ **全量门禁绿**：`typecheck` exit 0、`bun test` **11344 pass / 0 fail**（3 条 EROFS 系执行沙箱写 `/mock`，关沙箱 8 pass，与演练同一现象）、`check:cycles` **443/2054 恰在上游新预算上**（我方零环增量）、`check:identity-paths` 2771 文件 0 绕过、`check:mock-hygiene` 全零、`check:prompt-purity` 在预算、`biome ci` exit 0、`build:vite` + `check:bundle`（679 chunk）通过、双入口 `--version` 均报 2.46.0。⑧ **成果边界口径更新（后果最重的一条）**：上游 6 万行进入本仓库历史后 `git diff 3380c88..HEAD` 会把工作面虚报成 1,111 文件 / +173,478 行。已为当前基座树打**无父提交的零改动快照**标签 `base-snapshot/v2.46.0`（提交 `972b01d8`，树与上游 `1128a391` 逐字节一致，**不引入上游 git 历史**，干净 clone 可解析）；工作面改用 `git diff base-snapshot/v2.46.0..HEAD` = **509 文件 / +113,674 / −310**。章程升 **v2.13**（§5.5 新增条 + L-1 对策② + §5.8 答辩口径），`BASE.md` 记同步与新口径，已双签的 `positioning-m0.md` **只加「签署后事实性附注」§0′、正文一字未改**。⑨ **P10.3① 的设计被真同步验证**：`CLAUDE.md`/`README.md` 的基座原文部分**自动升级为 v2.46.0 版本、零冲突**，阡陌部分一字未动。⑩ 回写：`upstream-sync-drill.md` 新增 **§7 真实同步实记**（与演练逐项对照 + 演练没预测到的四件事 + 被验证的两条设计判断）、`base-modifications.md` 口径变更与撤回行。**排期、日期、产能、关键路径均未动。** |
| **v2.46** | **2026-08-17** | **M1 · 阡陌控制面板（`@qianmo/console` + `occ console`）落地**——负责人 2026-08-17 决议直接立项（章程 **v2.14** 记录 N-5 解禁及其边界；范围依据是本表 M1 节「注册发现产品化」：智能体列表、状态、消息链查看，出口判据「内测用户无需接触 CLI 即可完成注册与查看」，另加常驻节点唤醒与限额视图）。主 agent 组织与逐项审计，4 个子代理分批实作（HTTP 面 / 视图层 / CLI 接线 / UI 重构），打回与升级记录见本条⑦。① **形态**：新 workspace 包 `packages/console`，`Bun.serve` + 手写路由 + **零第三方依赖**，服务端渲染纯函数（可被 `bun:test` 直测）+ 约 400 行无框架客户端脚本；`createConsoleHandler` 不绑端口即可测（照 `@qianmo/registry` 的既有形态）。**不引前端框架与第二套构建管线**（v2.14 解禁边界）。② **鉴权**：双 bearer token 分权（view 只读 / admin 可写，`timingSafeEqual`、admin 是 view 严格超集、空 token 显式堵死、**角色判定先于方法判定**、401/403 不回显 token）；`resolveTokens` 纯函数承载全部策略——环回未给则自动生成，**非环回必须显式给两个 token 否则拒绝启动**（fail closed；理由：注册中心 HTTP v0 自身零鉴权，面板敞开等于注册中心敞开）；刻意不用 cookie（环境凭据会把每条 admin 路由变成 CSRF 靶，「必须出示外部源读不到的 token」即防线）。③ **端口注入架构**：包内只定义 `RegistryPort/AuditPort/WakePort/LimitsSnapshot` 接口（`deps.ts` 契约），生产实现在 host 侧 `src/cli/handlers/consolePorts.ts`（fetch 注册中心带 5s 超时、`@qianmo/audit` 的 readTrail/queryTrail/reconstructChain、限额从 `LIMITS`/`RUNTIME_RATE`/`DEFAULT_TTL_MS` 真常量 import、唤醒复用 `executeResidentWake`）；端口失败一律 `{ok:false}` 降级不 500，**注册中心挂掉页面仍 200** 并渲染不可达（e2e 实测）。唤醒面无 PSK 时不注入、页面渲染禁用态并写明原因。④ **UI 两轮定稿**：先按「颜色只表示状态」的克制版做底（文案纪律：无「花名册」、无 emoji、无外链、无说明句，全部做成单测门禁），随后按负责人指定**参照本机 Dormice 项目重构**——shadcn oklch token 逐字照抄（蓝 primary / destructive 红 / 10px 圆角 / 暗色 10% 白边框）、15rem 侧栏壳（无顶栏，实例/时钟/刷新/令牌收进侧栏底部）、总览 StatCard 四卡、租约 Meter（健康蓝/过半琥珀/过期红三段，单测钉边界）、destructive Alert、跳数路径消息链；Inter-first 系统字体栈不引外部字体。⑤ **验收（主 agent 独立复核，非采信报告）**：包内 **212 测试 / host 侧 33 测试全绿**；独立 e2e **21/21**（真注册中心+真控制台+真审计文件：鉴权矩阵、页面内注册后注册中心侧真有记录、链断裂如实报出、限流两层不混、kill 注册中心后页面存活、XSS 转义、用词纪律）；`typecheck` 0 错、`biome` 干净、`check:identity-paths` 2787 文件 0 绕过、`check:unused` 零漂移、`check:mock-hygiene` 全零（测试全用手写假端口，零 `mock.module`）；明/暗/消息链三张截图目检。⑥ **审计揪出的真缺陷两个**：注册表单端点占位符教的「主机:端口」会被注册中心 400（`isValidEndpoint` 只收带 scheme 的 URL），改为 `ws://主机:端口`；`residentArgs` 的 `--port=` 经 `Number('')` 静默解析为 0 绑临时端口（波及 resident 全部数值选项），取值处一次堵死（`64451a7e`，独立提交）。⑦ **过程记录**：视觉重做一轮因会话限额中断后由 Dormice 重构批次接续完成（代码已落地未损失）；UI 重构按模型分级派 Sonnet 一次通过，未发生两次打回升级。已知边界写在 `docs/dev/console.md` §7：无账号体系（N-2 仍有效）、注册中心自身无鉴权故面板绝不可把注册中心暴露出去、审计「外部改动无法阻止」缺口归 P11.4。**排期、日期、产能、关键路径均未动。** |
| **v2.47** | **2026-08-17** | **M1 · P11 批次：三份设计件交付待评审 + P11.5 遗留收口完成**（retro §7.3 第二迭代四包；主 agent 组织 + 逐项审计，3 个 opus + 1 个 sonnet 子代理并行；自主模式下裁定类决策由主 agent 按授权定案并逐条留痕，负责人可复核推翻）。① **P11.5 收口完成**（`31dcf795` + `f918e367`）：`base-modifications.md` §6.1 #2 三处会话活跃度差异裁定**有意为之、维持现状**（依据 `sessionActivity.test.ts:60-80` 把 `cb(refcount > 0)` 钉为有意语义——空闲发 keep_alive 才是谎报；核证行号记入台账），全表 ⚠️ 归零（✅12/🟢20/⚠️0）；#3 `--help` 待办定夺**补第二半**并落地——新增 `commands/qianmo.tsx` 照 `remoteControl.tsx` 形状注册 `resident`/`audit`/`resident-wake`/`console` 四命令，fast-path 一行未动，cli-golden `ROOT_COMMANDS` 恰多四行（30/30 主 agent 亲测），并更正台账旧说法「付 `main.tsx` 全量 bootstrap 代价」（barrel 本就只在非 print 路径动态加载）。② **P11.1 `docs/dev/beta-env.md` v0.1-draft**（`7f85eaf8`，646 行）：八条定案——4 节点一机一节点（上界=控制台单链人肉可管、下界=跨机跨架构+沙箱冻结态）、不解禁 N-2 用一枚共享 view + 2 人 admin 且 **token 显式固定不用自动生成**、审计链按节点分文件不集中（三条独立成立的理由）、九类数据保留表 + 备份间隔内测期 15→60 min、五级回滚 L0–L4 各带 RTO（L2 第⑤步「审计链尾巴挪走要留痕」单独强调）、一期 PSK 每节点一把不等 mTLS、升级顺序注册中心→节点→控制台、端口 38620–38629 与演示段不重叠。主 agent 审计：**八处代码引用逐一核验零失实**（含 §9.1「注册中心代节点心跳，机器关机名册仍显示在线」这条一期最会咬人的现状限制）；三个待验证点（H 的 443 未实拨、备份体量 5 MB 系估算、「用户=观察者」可能低于预期）已转成落地包 DoD 或标注待评审；落地包①~④草案（包③与 P11.3 后半合并；包④按需且触及 N-2 边界须先裁定）。③ **P11.4 `docs/dev/audit-witness.md` v0.1-draft**（`6b01995c`，440 行）：三选一定案**定期锚定摘要**——append-only 挂载在 9/10 无 sudo 机器上机制不存在，机外见证服务要新建常驻组件且把审计写入变成在线依赖（与「头两迭代不排新功能」冲突）；最小验证 **14 项主 agent 独立复跑全绿**：整篇重写重算哈希后链内 `intact=true`（复现 `claim 3` 缺口）而机外见证 `tampered=true`；变体 D1 证明**承重墙是第二位置只增不删而非签名**，变体 C 把锚定窗口盲区跑成显式「未覆盖 N 条」输出；落地包 A/B 合计 18–32 人时，超原估算差额（控制台接线，P11.4 立项时控制台不存在）已说明，包 A 可单独达成 DoD。评审注意：60 s 锚定周期是论证值非实测值、见证方与显示方同机是替评审做的范围裁定、**九台 VPS 是否共用 SSH 私钥待负责人确认**（共用则失陷域前提不成立）。④ **P11.2 `docs/dev/key-distribution.md` v0.1-draft**（`973d37c5`，610 行）：**十项 Bun TLS 探针实测定地基**，其中 F-5（Bun 不收 Ed25519 叶证书）+ F-8（应用层读不到对端证书）判死「同一把密钥两用 / mTLS 顶替握手」两个最自然方案——信任根定**离线自签单 CA**（带外分发仅 CA 根证书一件，`--trust` O(N²)→O(1)），节点身份留在应用层握手并改**双向** Ed25519 签名（服务端也签 `ready`，把端点重定向 T-B′ 从「降级为拒绝服务」提升为「结构上不可能」）；TOFU 含弱化形态一并否决（沿 `nodeIdentity.ts` 既有立场）；吊销用 CA 签名 RL 不用 X.509 CRL，RL 过期 **fail-closed 到 `--trust`** 而非全断；`SIGNED_TASK_POLICY` 切默认五条可测判据（S-1~S-5）+ 观察模式三段式，回滚零代价（`policy.ts` 结构保证已签消息两个方向都不改变命运）；N-3 建议**部分解除**（端到端加密与密钥托管两半继续有效），K-1~K-14 可勾选清单；落地包 P12.1~P12.4 合计 40–80 人时、顺序不可交换，P12.3 必须先看 `frames.ts:169` 严格版本相等（迁移只能走 v1 内可选字段，否则整包返工）。主 agent 审计：**F-5/F-6/F-8 三条承重实测独立复现成立**（本机 Bun 1.3.13）、六处承重代码引用抽查命中、修正四处探针计数（八项→十项，第二轮补测 F-9/F-10 后的遗留）。⑤ **评审待办汇总（负责人）**：三份 v0.1-draft 均「评审通过才生效」；需单独表态——K-13（`--trust` 退役 = 失去两条降级路径）、K-14（T-E CA 私钥新单点 + 90/30 天两个数字）、beta-env 包④算不算账号体系、audit-witness 的失陷域前提。⑥ **P11.3 后半（保留与轮转宿主侧工具）压住未派**：策略数字在 beta-env §5，待该文档评审通过再实现，避免按未定稿数字写工具。**排期、日期、产能、关键路径均未动。** |
| **v2.48** | **2026-08-17** | **M1 · 控制台对话面（`/chat`）落地**——承接 v2.46 控制台批次（范围依据同条 M1「注册发现产品化」出口判据的直接延伸：内测用户无需接触 CLI 即可与常驻 agent 对话；主 agent 组织 + 逐项审计，实作跨会话接续——代码与测试由上一会话子代理完成、本会话独立复核通过，文档与修正批次本会话 1 opus + 1 sonnet，零打回）。① **形态与回程**：`/chat` 独立页面（会话轨道/转录/composer，不动主页滚动模型），`ChatPort` 成为 `deps.ts` 第五个端口，包内零传输层知识，生产实现 `src/cli/handlers/consoleChat.ts`；**回程走既有 `task.request → ack → task.result`，三者都在控制台自己拨出的已认证 WebSocket 上**（P4.1/AC-2 判据排除「回程另开连接」），控制台是纯拨号方——不起监听端口、不注册进注册中心、不持节点私钥；「做成可寻址对等体」三条理由否掉（协议无 agent 主动消息类型/判据排除第二出处/三样新攻击面），代价如实入档：只能答被问的（console.md §6.2）。② **分权与鉴权**：名字从注册中心来（发现）、能不能拨从 `--chat-url` 允许名单来（授权）——注册中心自身零鉴权故两者必须分开；PSK 只从环境变量；**聊天面全部 admin-only 含只读路由**（两条独立理由：花对面模型预算/转录是控制台唯一自由文本面），无通道时 `/chat` 404、`/v0/chat/*` 501 且 **admin 判定先于存在性判定**防探测（console.md §4.5）。③ **投递状态链**：pending→delivered（回执）→read（ack）→done，failed 任一步出口，页面渲染成三个独立事实不收成一个灰勾；状态秩单调不降（回程四事件不保序，回环上 result 可先于回执）；本地兜底计时器 `ttl+min(15s,ttl)` 防「永远停在已读」；任务期限 5 分钟与 P4.1 `RESULT_LIMIT_MS` 同源不另立。④ **落盘与实时**：NDJSON append-only（`occConfigPath('qianmo','console','chat.ndjson')`，0700/0600，turn 全量重写 replay last-write-wins，坏一行只损一行）；SSE 事件只带 `{sessionId, revision}` 不带内容（远端输出进 DOM 只留服务端渲染一条路）；**15 s 心跳与 `Bun.serve` idleTimeout 写成算术关系**（实测 Bun 1.3.13 默认 10 s idle 每十秒杀流，表现为 ERR_INCOMPLETE_CHUNKED_ENCODING 重连风暴）；EventSource 不可用降级 2 s 轮询，`?stream=off` 强制。⑤ **验收（主 agent 独立复核，非采信报告）**：console 包 + host 侧 **351 测试全绿**（对话面新增 78：视图转义/三 pill 独立/「不在名册」vs「名册不可达」、路由鉴权矩阵/404-501/verb 拒绝/SSE 回收、hub 回程四事件乱序竞态/孤儿回复丢弃/重放/撕裂行、入口按角色）；近 CI `verify` 全绿（biome ci/typecheck/prompt-purity/mock-hygiene/cycles/分片全量测试/build:vite/check:bundle/check:unused，unused 主检出零漂移）、`check:identity-paths` 2793 文件 0 绕过（转录路径从 `occConfigPath` 派生）、mock 卫生全零（手写假端口，零 `mock.module`）；真模型回环浏览器实测有快照证据（`已投递 · 回执 accepted · 13ms` → `已读 · 13ms` → 回复 `用时 12s`），明/暗截图目检，`stream=off` 降级路径实测。⑥ **审计揪出并修正**：主页「对话」入口未按角色渲染——`http.ts` 模块注释与 §4.5 论证都要求 view 连「接没接聊天」都不可分辨，而代码只判有无通道，**改代码贴合注释**并加测试钉住；`chatClient.ts` 模块小标题残留改设计前的「navigation, not a swap」说法，与 `openSession` 实现相反，订正；「四个端口」两处过时改五个；文档重编号后三处代码注释章节引用失实（§7.2→§8.2、§6.2→§7.2）同步修正。⑦ **存量测试顺序性失败归因（非本批次）**：FileReadTool 分页两用例整仓不分片必红、单跑 8/8 全过、**干净 HEAD 复现**（stash 归因实验）——已立独立任务卡不并入本批次；CI 用的分片模式（`test-shards.sh`）下全量全绿，分片硬隔离正是它不咬 CI 的原因。文档：console.md 补 §6 对话面八小节 + §4.5 + §3/§5/§9 三表，旧 §6/§7/§8 重编号为 §7/§8/§9。`.playwright-mcp/`（浏览器实测产物）入 `.gitignore`。**排期、日期、产能、关键路径均未动。** |
| **v2.49** | **2026-08-17** | **测试可靠性收口：v2.48 ⑦ 立案的存量失败当批根治（负责人指令「把风险清除」；主 agent 组织 + 逐项审计，2 个 opus 并行，零打回）**。① **FileReadTool 分页整仓必红的根因不是分页**（`1be556c9`）——`share-projectdir` 与 `launchAutofixPr` 两套件顶层 `mock.module` 把进程全局 cwd 钉成 `/mock/cwd` 且 no-op 掉 setter（旧手写 stub 残留，套件自身一处都不读它）；受害链 `services/vcr.ts` 以 `getCwd()` 为 fixture 根 → 命不中走录制分支 → `mkdir '/mock'` EROFS，恰只有 2/8 用例到得了 `countTokensWithAPI` 所以恰红两个；单跑绿、CI 分片绿（污染者与受害者不同进程）、整仓必红，`/cd` 测试早前已被同一污染逼得放弃环境 cwd（`validation.ts` 注释为证）。修在污染源：删两处 cwd 簇覆盖，机理入 `tests/mocks/state.ts` 新增「NEVER override the cwd / projectRoot cluster」节，调用点留指针（指针不复制）；**负对照**（恢复一行即复现、四文件组合 59/59）坐实。② **三个 5 s 超时偶发是两种病**（`933440a0`），判别器先行——Bun 打不断同步代码，报「恰 5000 ms」证明事件循环是空的：(a) keychain delete 两用例**真慢**：`beforeEach` 每次新写 stub `security`，macOS syspolicyd 对新建可执行首次 exec 做评估（实测新建 ~78 ms vs 复用 ~3 ms，负载下单次尾部 3.1 s），改 `beforeAll` 一次创建 + 真正 exec 的用例显式 30 s 预算（引「Bun 1.3.13 不读 bunfig [test] timeout」已知坑）；(b) logout 搜索用例**不该慢**：stale token 触发机会式 `refreshTokens()` 打真网 `auth.openai.com`（实测 2.3–4.9 s，只因请求失败被吞才一直「通过」），装拒绝式 offlineFetch（既有 `savedFetch` 机制两处恢复，不新增全局泄漏），顺带堵住同文件另外三处静默真网调用。③ **验收（主 agent 独立复核）**：整仓不分片**连续两轮 11646 pass / 0 fail**（此前必红模式；修复前基线 11644/2）、11 轮单文件含 6 轮三路并发负载全绿、typecheck 0 错、mock-hygiene 全零；keychain 文件时长 1.6–5.5 s → ~320 ms。④ **新浮现疑点立卡不并入**：cwd 不再被毒化后 skillLearning `llmObserverBackend` 的 VCR 开始真录制且有错误被吞（此前恰被 EROFS 掩盖），已立独立任务卡待查。⑤ 「agent 不能主动开口」裁定**不属可清除风险**：协议新消息类型 = 章程范围变更，无书面变更不开工（§2.1），维持 console.md §6.2 入档形态，立项权在负责人。**排期、日期、产能、关键路径均未动。** |
| **v2.50** | **2026-08-17** | **`s4/p11-console` 首过 CI 双平台全绿 + resident 拆机真缺陷修复**（v2.49 收口的自然延伸：推送后发现该分支从未跑过 CI，首跑连揭四个存量问题，逐轮收窄至全绿，run 32050180684；全部问题经同树/旧顶对照证明先于本会话存在）。① **typecheck 双平台 OOM**（`13f31273`）：tsc 峰值实测 ~2.7 GB（旧顶 bf3441e7 数值全同——随批次累积增长非单次回归），超 Node 默认 ~2 GB 堆上限，CI 两个 Type check 步骤显式 `--max-old-space-size=4096`。② **zip bomb 用例超时**（`3ea33645` + 注释常量化 `eeb64693`）：level 9 同步压 320 MB 在 2 核带覆盖率 runner 上实测 7 s，照 v2.49 模式显式 30 s 预算；期间发现 lint-staged 的 biome fix 会把 test 第三参前的行注释搅乱语序并造成格式漂移（三跑唯一失败），注释一律写成常量形态规避。③ **resident 拆机排空漏洞——产品代码真缺陷**（`9b8cbf58`，Opus 诊断）：`#settleTask` 在首个 await 前出表，`#failActiveTasks` 扫不到、`transport.stop()` 把**已送达回复**的回执等待拒绝成「transport server closed before receipt」——onError 收到一条没发生过的故障，CI 慢机器掷硬币复现（同树对照铁证：与绿轮只差一段注释）；本机 20 轮满载打不红，注入 250 ms 延迟必红坐实交错，修后 3000 ms 仍绿；新增 `#settling` 集合 + `#drainReplyReceipts()`（`#failActiveTasks` 与 `transport.stop()` 之间，同预算封顶，两条拆机路径共用的 stop 闭包单点覆盖），线格式/协议数值/`@qianmo/transport` 零改动，测试断言一字未动；满载 12/12、qianmo+transport+resident 124 测试全绿。④ 手动触发 CI 沿用 s4 分支 `workflow_dispatch` 先例。**排期、日期、产能、关键路径均未动。** |
| **v2.51** | **2026-08-17** | **内测环境 H 腿上线 + 控制台对外可用面收口（文档回写批次）**。现场把数据面从「38625 直连」改成「H 经 SSH 正向隧道到各节点回环」——三台节点的 38625 被云厂商安全组挡死（另探十个端口全不通，只有 22/80/443 通），隧道因此不是可选功能，而是当前现场数据面唯一的落地形态；随之落地的还有：控制台登录页与 `HttpOnly` 会话 cookie（配套 `X-Qianmo-Console` 头 + 零 CORS 头的 CSRF 防线、登录失败退避限流）、两枚 token 的三入口与优先级（文件 > 环境变量 > 命令行，文件入口自带 0600 权限门禁，补上了「密钥不走命令行」这条此前只写在文档里而实现违反的纪律）、审计镜像（5 min timer、单向只读、原子替换，控制台 `--audit` 改指镜像并在页头标注滞后）、节点侧 `authorized_keys` 受限 key（`command=` + `restrict` + `permitopen`）、以及新增设计件 `docs/dev/node-provisioning.md`（控制台填 SSH 凭证自动装机与接网，十二项探针实测）。本版**只回写文档**：`console.md`（鉴权模型、路由表与三个保护等级、选项表与 token 出处、已知边界）、`beta-env.md` 升 **v1.1**（八条定案一条未改，改的是前提——H 成为数据面单点、L4 多三件事、跨公网那一段现为 SSH 隧道套 PSK）、两处相关文件指针。**排期、日期、产能、关键路径、任务包正文与完成状态标注均未动。** |
| **v2.52** | **2026-08-18** | **内测隧道与审计镜像收进落地包 ①（`f7caa7d2`）+ 两份设计件回写**。**这是包 ① 范围内的补齐，不是新范围**：四条 `ssh -L` 与四条单向只读审计镜像本来就是 `beta-env.md` §2.6 的数据面与 §4.3 的一期做法，此前是现场手搓、仓库里没有出处，改一处要在两处对齐；现在统一由 `peers.conf` 的 **`node` 坐标行**派生（`node <名> user= host= port= local-port= remote-port= trail= key=`，键值对、顺序无关；老格式第一字段必以 `qianmo://` 开头，`node` 关键字不可能与合法老行相撞，**存量文件一字不改照读**），`ops/` 三个 systemd `--user` 模板 + `mirror-pull.sh` 全是占位符、由 `beta-up.sh --role host` 派生进 `<内测根>/ops/` 与 `~/.config/systemd/user/`，**真源在仓库不在 H 上**（手改会被下一次派生覆盖）。① **三条断言来自现场踩过的坑，各配负向自验**：**改端点前挪开注册中心落盘表**（`announce()` 里 `heartbeat` 成功即 `continue`、不比端点，于是旧端点原样回来而 `--register` 被整条跳过，症状是「smoke 全红而 `peers.conf` 明明是对的」；处置分两半——落盘表**改名挪开不删**，**活表不一致必须重起注册中心**，内存里那张表挪文件管不着，这是 `beta-up.sh` 唯一一个会重起在跑组件的场合）；**隧道口的 TCP 探测恒为真**（转发建成而节点侧无人监听时 `ssh` 照样在本地 LISTEN 并把每条连接 accept 后立刻关掉，判据换成真读一次应答字节——节点回 `426 Upgrade Required`，中间只转发不应答的链路伪造不出来；`beta_tcp_open` 被喂隧道口**直接 die**）；**审计路径与页头标签持久化进 `<内测根>/console.conf`**（0600，`AUDIT_NODE`/`AUDIT_PATH`/`LABEL`/`WAKE_NODE` 四键，env > 文件 > 派生默认且**胜出者回写**；此前只在环境变量里，控制台一重起就静默丢成**空审计视图而不报错**、页头也丢掉「镜像」标注）。② **镜像只能是拉取式**：H 那把 key 在各节点 `authorized_keys` 里带强制命令，`rsync` 协商不到远端 `--sender`，必然挂在握手上——保留 `MIRROR_METHOD=rsync` 分支只为让「有人想写回去」**当场报错并说明理由**；另有「拉到零字节不覆盖」这道守卫（`cat` 给空内容而 `ssh` 退出码仍是 0，覆盖上去等于用一次网络抖动抹掉整条镜像）。③ **起停重置三个脚本现在都认识链路**：`beta-up.sh` 建/收敛/收残且**绝不重起在跑的单元**（文件变了而实例在跑时在末尾列出并给出维护窗口里的 `systemctl --user restart`），`beta-down.sh` 停并取消自启（给节点名时只切那一条），`beta-reset.sh --purge-links` 只删生成物、**`mirror/` 一条不动**——`beta-env.md` v1.1 §6 L0 那句「两个脚本不认识它们」「隧道断了 `beta-up.sh` 修不好」**就此作废**。④ **文档回写（本轮只改文档，不碰代码）**：`beta-env.md` 升 **v1.2**——§2.3 拓扑图的 rsync 标注、§2.6 隧道本地口改为 `local-port=` **单一出处派生**（38631–38634 是现场取值不是常量，三处派生物 + `beta_assert_peers_match_tunnels` 带行号拦下不一致）、§4.1 目录布局补 `peers.conf`/`console.conf`/`mirror/`/`ops/`/`secrets/peers/`、§4.3 镜像的落地形态与 `console.conf`、§5 保留表、§6 L0、§6.5 改为**原理说明**并补上原文缺的活表那一半、§7.4 label 持久化、§9.1 与 §9.8 两条假绿互指、§9.8 三判据表（真读应答 / 真握手 / TCP 探测），§10 包①「不含」栏（**「不含任何功能代码改动」仍成立**，作废的是「不做镜像」）、§11 补 README 指针；**八条定案一条未改**。`node-provisioning.md` §3.2/§3.3 三处「审计镜像 rsync」改指单向只读拉取并写死那条硬结论、§5.7 补落地实现指针。⑤ **README 与 `beta-env.md` 按「指针不复制」分工**：操作细节（怎么敲、变量表、负向用例复跑命令）留 `demo/env/beta/README.md`，拓扑与决策理由留 `beta-env.md`，两边互指。**排期、日期、产能、关键路径、任务包正文与完成状态标注均未动。** |
| **v2.53** | **2026-08-18** | **阡陌节点自己的命令名 `qm` 落地（第四个 `bin`）+ 两份文档回写**。**这是 P11 范围内把「阡陌节点有自己的命令名」做实，不是新范围**：范围依据是本表 M1 节「基座关系收敛」行的「身份隔离改名完成并验证」，且 `identity-coexistence-m1.md` §2 表自 v1.0 就把 `qm` 登记为节点态的 CLI 名——此前那一格是**名义上的**（`BIN_NAME` 按身份切，但装得出来的三个命令没有一个会把身份设成 `qianmo`），本次把它变成真的可安装。**retro §7.3 的 P11.1~P11.5 五条 DoD 无一涉及命令名或身份**，措辞无需改动。① **形态**：`package.json` 的 `bin` 增第四项 `qm` → `dist/cli-qianmo.js`（原有 `occ` / `occ-bun` / `open-claude-code` 三项一字未动），由 `scripts/entrypoints.ts` 与另两个入口**同源生成**（新抽出的 `writeEntrypointFiles` 被 `build.ts` 与 `scripts/post-build.ts` 共用，两个 builder 生成不出不一样的入口）。② **身份写在文件内容里，不从调用名推断**：入口首行 `process.env.OCC_IDENTITY ??= "qianmo"`。**曾经有过一版让 `argv[1]` 的 basename 参与身份解析的实现，已撤掉**——调用名这条信号在三条路径上都会丢（Bun 在填 `argv[1]` 之前解析软链、Windows `.cmd` shim 传的是 `.js` 路径、bundled 子进程那一格是 CLI 参数），每丢一次节点就静默落回 occ 的配置根，正是三方隔离要防的那件事；理由写死在 `src/constants/brand.ts` 的 `invokedBinName()` 注释里。**身份判定点仍然只有一个**（`identity.ts` 的 `IDENTITY_MODE` 读 env），`qm` 只是那个 env 的另一个**写入方**。③ **两处看着像风格、实则承重**：农场必须 `await import` 而非静态 import（ESM 把静态 import 提升到模块体语句之前求值，农场会赶在赋值之前按 occ 的根初始化——用**真跑一次打桩农场**的用例钉住，不是读文本）；必须 `??=` 而非 `=`（`OCC_IDENTITY=occ qm …` 得仍然是 occ）。④ **顺带修一处真缺陷**：`cli.tsx` 的 `--version` 快路径此前硬编码 `(Open Claude Code)`，于是阡陌节点自称 occ 而同一次调用写的是 `.qianmo`——改走 `DISPLAY_NAME`（commander 那条一直用的就是它，快路径是同一事实的第二个打印处，漂了）。⑤ **身份名只许拼一次**：`identity.ts` 新增 `IDENTITY_ENV_VAR` / `NODE_IDENTITY_MODE`，build 脚本与 `resident.ts` 里钉 ACP 子进程身份那处都从 roster 取，字面量 `'qianmo'` 从这两处消失。新增用例 `scripts/__tests__/entrypoints.test.ts`（7 条）与 `src/constants/__tests__/invokedBinName.test.ts`（6 条，跨进程、用真实文件而非软链做 shim）。⑥ **两条残留如实记，不粉饰**：(a) **`qm` 硬依赖 Bun**（bun shebang，因为 `console` / `resident` 都强制 Bun），代价是 `qm --version` 这类本来纯 Node 就能跑的快路径也一并绑上 Bun——**顺带查实一条此前没写下来的事实**：`occ` 指向 node shebang 的 `dist/cli-node.js`，在 Node 下 `assertConsoleRuntime()` 必抛，**`occ console` 本来就起不了**，已安装的名字里能起 console 的只有 `occ-bun` 与 `qm`；(b) `OCC_IDENTITY=occ qm console --help` 打的是 `Usage: occ console`（Bun 把 `argv[1]` 解析成 `cli-qianmo.js`，显示层拿不到 `qm` 这个字），**已决定不修**——那是把 env 与命令名故意拧反的调用，修它要新开一条只为显示服务的 env 通道。⑦ **文档回写**：`console.md`（前置条件行、§2.1 两种写法与 Bun 代价、§3 显示名的出处与那处不准、§9 指针）、`identity-coexistence-m1.md` 升 **v1.1**（§1 的观察方式补第二条并写明「这不是第二个身份判定点」、§2 表与其下新增一段、复现补 `invokedBinName.test.ts`；**验收结论与 §3 / §4 / §5 一字未动**）。**排期、日期、产能、关键路径、任务包正文与完成状态标注均未动。** |
| **v2.54** | **2026-08-18** | **M1 第 3 迭代立项：常驻 agent bot 化（P13.1~P13.7）**，依据 `docs/dev/resident-botization.md`（v0.1-draft，评审通过才生效）。参照 hermes-agent 的 bot 模式与常驻原理做了逐条采纳判定（A~F 共 **47** 条：采纳 19、改造后采纳 12、已成立或登记备查 6、本轮不做 10；另 G 表 6 条复核仍不适用）。**四条设计主线**：① 协议先行长出唯一一个 agent 主动发起的类型 `notify`（fresh `taskId` + `contextId` 归组，**不给 `isReplyType` 豁免**——复用 taskId 会让同一值守作业的**第二条**通知被判 `E_LOOP`，第一条永远是绿的）；② 传输回执与协议 ack 分层（回执提前到 durable 信箱写入之后，**ack 的发出点一行未动**，AC-2 不变式相容性论证见设计件 §4.2）；③ `(agent, contextId) → sessionId` 多会话（**协议 / 适配器 / 基座三处零改动**——`contextId` 已随整条信封落进信箱 `text`）；④ 定时完全反转到中枢，节点侧零值守状态（节点内 ticker 会让节点永不冻结，抵消 R-3）。**`LIMITS` 新增三项**（`defaultNotifyTtlMs` / `notifyRatePerMinute` / `maxQueuedTurns`），charter §3.3 C-4 同步回写（charter v2.15）。**编号让号**：`P12.1~P12.4` 已被 `key-distribution.md` 占用（mTLS 落地线，P11.2 设计、P12.x 落地包），本批次用 `P13.x`；两条线在 `frames.ts` v1 可选字段上有交集，**排期不得并行**。**M0 排期、日期、产能、关键路径均未动。** |
| **v2.55** | **2026-08-20** | **P11.4 包 A/B 已集成，P12.1~P12.4 机制实现收口。**P11.4 的锚定发送、append-only 见证端点、`occ audit --verify --witness` 与控制台三态已落地，代码/测试与唯一状态见 `audit-witness.md` §8/§9；**两台无 sudo VPS 真机 DoD 未采集**，不以回归用例替代。P12 代码默认 `SIGNED_TASK_POLICY`，真实 beta 舰队留在阶段①——**但那不是显式选择的结果，此处原写「仍显式 `--open-policy` + `--audit-signed-tasks`」是错的**（2026-08-23 生产实查回写，见本表 v2.58 与 issue #10）：四台节点的 `/proc/<pid>/cmdline` 里两个开关一个都没有，它们处在阶段① 只是因为线上产物是 P12.4 翻默认之前（2026-08-17）的构建，默认值恰好还没翻。起法脚本 `demo/env/beta/beta-up.sh` 自 2026-08-20（`92ba7e82`）起确实显式写着这两个开关，线上进程只是还没滚到那一版；因此**滚新产物前必须先按该脚本补齐启动参数**，否则同一条命令行会静默翻成要求任务签名。K-5 仍 **1/5**，K-11/K-12 未完成，准确证据与提交序列见 `key-distribution.md` §9.1/§12。未新增正式 S13 章节，M0 排期、日期、产能、关键路径与既有任务包正文均未动。 |
| **v2.56** | **2026-08-22** | **S14 安全跟进：P12 握手凭据链路三处机制修正，另收口观察者错误契约与测试出网隔离。**① **握手 proof claim 与 effective credential 分层落地**：同一 node 同时有显式 `--trust` 与 CA 证书时，第二证明按 `certificate/<fingerprint256>` 校验，连接绑定为 effective `explicit/<node>`。此前这个组合必然握手失败。② **`CertificateDirectory` 显式分支由「查不到即拒」改为「有矛盾才拒、缺数据不拒」**：原行为在注册中心不可达时重启常驻节点会拒掉 `--trust` 名单上的证书型对端，把 `key-distribution.md` §6.4 的「fail-closed 到 `--trust`」做成了 fail-shut；同时写明那几条事实查验从来不是安全边界（持显式私钥者把 selector 写成 node 段即可绕开并拿到逐字节相同的凭据），准入靠的是那把私钥的签名。③ **保留通道按认证身份冻结，并给合法凭据轮换留换道自愈**：通道在首次认证后冻结 `{peerNode, authentication 档位, effective credential, 签名公钥}` 四元组，避免 F2、另一把 key、降级到 PSK 的连接继承 F1 尚未收 receipt 的 payload；但冻结的第一版用 4003 拒绝，而 4003 在本包里按契约是永久的，于是「监听方目录在运行中变化」（新增/移除 `--trust`、registry refresh 引入显式条目）会让一个字节都没改的拨号方永久下线——该回归由三组对照实验（HEAD 基线／仅分层／分层+冻结）坐实。修法：新增 `CLOSE_CHANNEL_CONFLICT`(4004) 与专用拒因 `channel_identity_mismatch`，拨号方换新 channelId 重连（上限 3 次，调用方显式传入的 id 不轮换）；旧通道原封不动按 retention 过期、不移交，两侧各留一条痕（监听侧 `AuthRejected` 带 `channelId` 与 `pending`，拨号侧新事件 `ChannelRotated`）。刻意不采用「丢弃旧通道再重建」，因为那让任何能以同 node 通过 L1 的弱身份都能按需摧毁签名通道尚未送达的回复。④ **两处 fail-open 形状收口**：第二证明校验缺 `proofCredential` 时原会静默跳过校验并放行，改为缺失即拒，并把 `ResolvedSigningKey` 改成联合类型让该状态编译不过；客户端致命关闭路径补 `outbox.close(真实错误)`，此前挂在 `sendAndWait`/`waitForDrain` 的调用方只能等自身超时、拿到「未在 Xms 内收到回执」而非真实死因。⑤ **skill-learning 观察者错误契约收窄 + 测试离线化**：只有预期的观察者不可用（超时、认证失败、可重试 API 失败、无结果）才退回启发式后端，配置/VCR/文件系统错误改为上抛给 post-sampling hook 既有的错误契约；并新增 `/fixtures/` 根锚定忽略（VCR 卡带默认落点是 cwd，本地跑测试会往仓库根落未预期的真实录制，曾留下两个残留文件）。⑥ **门禁**：`bun run typecheck` 零错误；`scripts/test-shards.sh`（该脚本即 CI 用的那条路径）**61 个分片全过**；`check:cycles` runtime 443/total 2054、`check:unused` exports=1243/types=665/duplicates=10、`check:prompt-purity`、`check:mock-hygiene` 均 at budget 或零。**执行端只保留了最后 4 个分片的汇总输出，因此不写全量通过数**——定向数字有据的是 transport 137、qianmo 136、skillLearning 93、依赖包（activator/resident/tunnel）463，均 0 fail。判据原文与完整论证见 `key-distribution.md`（§6.4 吊销与 `--trust` 的硬边界、§7.1 selector 矛盾查验与通道身份绑定、§8.3 `--trust` 反方向代价）。**本轮全部是机制侧改动与本机门禁，无任何真机/舰队证据。**M0 排期、日期、产能、关键路径与既有任务包正文均未动；未新增任务包编号。 |
| **v2.57** | **2026-08-22** | **S14 合并前的独立对抗性审查与修复，另补两轮定点变异测试。**审查由无前置参与的独立代理按 7 个攻击面执行，结论：三条核心机制（proof claim 与 effective credential 分层、通道身份冻结 + 4004 换道自愈、两处 fail-open 收口）在构造攻击下均站得住，**未发现可利用安全缺陷**；其中「缺数据不拒」的等价性由独立探针真跑握手坐实——用显式 `--trust` 目录 + 一个从未被任何 CA 签发的 selector，监听方确实接纳，而该 selector **不出现在监听侧或拨号侧任何一条事件记录里**，只进过 `verifyBytes` 的签名内容。审查产出 8 条发现：5 条在本分支修掉，2 条越界另开 issue，1 条判为等价变异保持原样。① **4004 轮换预算由「终生」改为「按一次连续冲突串」**：原实现不随成功握手复位，而它自陈的触发源是「监听方目录在运行中变化」——那是运维驱动、会反复发生的事件，不是「这个 client 坏了」的症状，第 4 次 `--trust` 编辑会让一个健康的长命节点进终态。改为在 `onReady()` 里与 `schedule.succeeded()` 并列归零；「一串」仍只有三长，因为两次轮换之间必然夹一次完整再准入（服务端只在 auth 阶段发 4004，ready 之后不再发，这是结构保证而非约定）。② **`giveUpAfterMs: 0` 的拨号方不再记幻影轮换、不再报错误死因**：该配置是 `@qianmo/tunnel` 的实际配置，其 `next()` 首次调用即 give-up，于是原实现会为一次**从未发生**的重拨记下一条 `ChannelRotated`（文档定义为「一次有账可查的丢失」，两侧对账必然对不上），并以「重连预算耗尽」收场而不是 4004——正是本轮第 ④ 项「让 parked 的调用方拿到真实死因」要修的那类问题漏在了这条路径上。改为 4004 分支**一次读钟、两处消费**（`rotationRefusal(now)` 与 `scheduleReconnect(onFatal, now)` 共用同一时刻），并给 `ReconnectSchedule` 新增只读探测 `willRetry(now)`（与 `next()` 共用私有 `survey()`，三种情况下 `elapsed`/`attempts`/`startedAt` 的落定逐格等价）。③ `TransportChannel.id` 的**共享接口契约**补上换发例外——此前只在 `TransportClientOptions.channelId` 与 getter 上写了，而消费方（`activator/routes.ts` 拿它作幂等键）真正编程依据的是那条契约。④ **生产代码里的 `NODE_ENV === 'test'` 分支移除**：该分支会让任何 shell 里带着 `NODE_ENV=test` 跑 occ 的用户静默降级到启发式后端，并使三条用例结构性空洞（在 `bun test` 下只会走到早返回，任何熔断器状态下都不可能红）。改为模块级 seam `setLlmObserverQueryForTest`（导出面净零，`createLlmObserverBackendForTest` 同时删除），出厂路径无条件走 `queryHaiku`；三条用例改为断言调用计数并各有变异红证。**并修掉审查者漏掉的一处 hazard**：`observerBackend.test.ts` 里有一处 `analyze()` 未 await 就抛的悬空 promise，守卫移除后那就是一次真实 API 请求。另判定原「`existsSync(fixtureRoot) === false`」断言不仅是装饰品，其产生的红还**顺序依赖**——`globalThis.MACRO` 在 `bun test` 下是进程全局且约 15 个文件会设它，分片里谁先加载决定失败形态是瞬时 `ReferenceError` 还是 5 秒挂起加一次真实请求。⑤ **`key-distribution.md` 三条不实陈述校正**：「本包其它所有拒绝统一 4003」不成立（`CLOSE_CAPACITY` 1013、`CLOSE_REPLACED` 4000、`CLOSE_PROTOCOL_ERROR` 1002 三个可区分码都早于 4004 存在，原注释写的是「in this file」被放大成了「本包」；且 `UnexpectedFrame` 同一个 rejection 值在认证前映射 4003、认证后映射 1002，按阶段切不干净，故收窄为「凭据类拒绝」）；「channelId 不出两端」不成立（`activator/routes.ts` 三处把它写进事件 detail，`auditTrail.ts` 整份抄进 `trail.ndjson`，即 `occ audit` 与控制台读的那份，泄露可接受的依据改为「它是拨号方自己生成的、换发只对同一 node 生效」，并记下审计流水将来外送时需另议脱敏）；「旧通道按 retention 时钟过期」不成立（`#removeIfIdle()` 在 `#holds > 0` 时直接取消回收，实际占用是 hold 时长加一个 retention 周期，`maxChannels` 须按这份更长的占用估算）。另 `vcr.ts` 的卡带缺失提示对齐 `/fixtures/` 忽略规则（改为 `git add -f`）。**变异测试两轮共 9 次定点变异**，均在一次性 clone 上执行、不触碰主检出：第一轮 4 次验三条核心机制，三次杀死（签名公钥腿、4004 早于 bind、`?? credential` 回退分别转红 1／1／8 条用例）；第四次（删 `verifyAuthAttempt` 的 `proofCredential === undefined` 合取项）**存活但经两条独立证明判为等价变异**——静态上该值的唯一产出方 `resolveHandshakeCredential` 以对象字面量兜底、永不为 nullish，运行时在该点插 `throw` 探针跑全套从未触发，故**保留代码、不补用例**（公开面上没有测试杀得死它，补测等于假绿）。第二轮 5 次逐腿变异冻结四元组，挖出审查者未发现的问题：**`peerNode` 腿零覆盖**——删掉它 137 pass / 0 fail 无一转红，根因是测试脚手架 `rawHandshake()` 把节点名写死、没有用例构造得出该场景（运行时探针：全套 13 次判定调用全是 `node-a` vs `node-a`），而该腿在 PSK 档上是**唯一**分隔器（该档 credential 与签名公钥皆空，另三条腿逐字节相同；MAC 虽覆盖节点名但任何持 PSK 者都能为任意名字算出）。区分输入已写出并跑通：冒名者不仅被准入，还真的收到了保留 outbox 里那条未送达 envelope——正是该文件头注声称要守的性质。已补两条用例（PSK 档，以及目录对两个节点名返回同一把公钥的 signature 档变体，后者把 `peerNode` 腿与签名公钥腿隔离，否则后者会替它兜住），变异下各 5 句独立断言转红。`#authentication` 腿同样判为等价变异（`verifyAuthAttempt` 只发三种形状、不存在「无键有凭据」，故档位是另两条腿 null 性的全函数；且全套已跑遍六种有序档位错配，每次都是键腿或凭据腿同时也不同），判断保留在代码里作为将来第四种档位形状的廉价守卫。**越界两条不并入本 PR**，另开 issue 记录：#7（CA 化节点无法与纯 `--trust` 节点握手——拨号方发 credential、监听方目录不实现 `handshakeCredentialOf` 即 `UnknownSigner` + 4003 永久拒绝，即使 `--trust` 里钉着对方公钥；`hasCredential ? … : …` 无回退三元在 base 分支逐字相同，非本轮引入，但它让 §8.2 阶段①共存期被单向切断，需就「有意/无意」表态）、#8（冻结四元组的 `credential.source` 子腿无覆盖，经今天的目录实现不可达，因两类 credential 的 id 命名空间不相交）。**门禁**：`bun run verify` 整体 exit 0——`biome ci` 4352 文件无待修、`check:prompt-purity` 棘轮成立、`check:mock-hygiene` 0 inline + 0 unreset + 0 shim、`check:cycles` runtime 443 / total 2054 双向在预算、`scripts/test-shards.sh` **61 个分片全过**、`build:vite` 8364 模块、`check:unused` 七类全零且 exports=1243 / types=665 / duplicates=10 at budget。定向：transport 由 137 增至 143。**本轮仍全部是机制侧改动与本机门禁，无任何真机/舰队证据**；4004 换道自愈在四节点 beta 上的实际表现未经观察，①那条预算复位在长跑中的行为亦无观测数据。M0 排期、日期、产能、关键路径与既有任务包正文均未动；未新增任务包编号。 |
| **v2.58** | **2026-08-23** | **S14 生产环境验证：首次为 P12 传输层改动取得真机证据，并挖出三条既有生产缺陷。**本轮**无任何代码改动**，只有勘查、实验与记录。**① 现场事实（此前无人完整实查过）**：四台常驻节点 `/proc/<pid>/cmdline` 逐字读出共 17 项，**没有 `--sign-handshake` / `--trust` / `--trust-ca` / `--cert` / `--registry`**，节点自报 `{"requireSignedTasks":false,"trusts":[]}`；注册中心 `/v0/agents` 字段集恰好 7 个，**无 `publicKey`、无 `certificate`**。**生产跑的是纯 PSK 档，证书那条线在真机上不存在。**四台 PSK 互不相同，数据面是 **H 经四条 SSH 隧道单点辐射**（`127.0.0.1:38631-38634`，注册中心登记的正是隧道口），节点之间握不上手。四台 38625 上 **established 连接数为 0、从未被拨过**，live 配置根连 `audit/` 目录都没有——当前这一代节点没做过任何协议工作。线上部署的是 **2026-08-17 12:31 UTC** 一次构建的产物，早于 S14。**② 真机 A/B（beta-1，aarch64）**：在生产机器上用**独立端口、独立配置根、自备 PSK**起两个构建（对照组是从该机 `~/atlas-beta/dist` 只读复制的**生产在跑的那一份**），以一个只依赖 `node:crypto` 与全局 WebSocket 的独立协议探针跑保留通道身份冲突场景。结果：**旧构建 close 4003 `unauthorized`，新构建 close 4004 `logical channel identity conflict`**，两版 `sawEnvelope` 均 false（负载都未泄漏），两版 `retainedChannelConfirmed` 均 true。与本地 `linux/arm64` 容器结果**逐字段零处不一致**。探针的有效性有三重佐证：两个构建结论不同（不是在测握手）、`impostorFreshChannelDial` 两版皆 `ready`（拒绝是通道范围而非凭据范围）、`--skip-pending` 负对照两版皆翻转（判据可证伪）；「保留」也是观测到的而非假设——owner 重新接管后**同一个 `msgId`** 从 outbox 被重放。**③ 结论必须按档位说准**：PSK 档下本改动**不是堵一个开着的洞**——合并前 base 的 `server.ts:691` 已有 `existing.peerNode !== result.node` → `refuse(BadChannel, CLOSE_UNAUTHORIZED)`，旧构建照样拒、照样不泄漏。它在这一档实际改变的是三件事：给出**可区分**的关闭码（旧版 4003 与「密钥错」同码，运维分不出配置错还是代码错）、审计记录带上 `channelId` 与 `pending`（两侧对账才有数）、以及 **4003 按契约永久而 4004 可自愈**。另两条腿（认证档位、effective credential）只在签名/证书档下才绑得住，**生产没有那两档**。**且 4004 在当前生产形态下正常运行中基本不可达**：两个触发源各缺前提——没有生产调用方会复用 `channelId`（`resident-wake` 不给 `TransportClient` 传它，用随机 128 位 id），而「监听方目录运行中变化」需要证书或注册中心凭据，生产两者皆无。探针能触发它，是因为它刻意复用了同一个 channelId。**④ 回归证据**：用真正的拨号侧入口 `resident-wake`（内部即 `TransportClient`）对新旧构建各跑一次端到端唤醒，两版均 `receipt:"accepted"`，两个节点落盘的会话 transcript **同字节数、同行数、同内容**，`msgId`/`taskId` 与回执一一对应，链路完整走通（握手 → 帧收发 → 路由 → 回执 → 拉起 ACP 子进程 → envelope 进入 agent 会话 → 产生应答轮）；全程无 4003/4004，节点 stderr 全 0 字节。**未覆盖最后的模型往返**——隔离配置根内无模型凭据，且按纪律不读生产 `secrets/model-env`。**⑤ 挖出三条既有生产缺陷，均非本 PR 引入，已各自开 issue**：**#10** 部署会静默把舰队从 open policy 翻成要求任务签名——代码默认早已是 `resident.ts:329` 的 `let requireSignedTasks = true`，而生产 argv 里**既无 `--open-policy` 也无 `--audit-signed-tasks`；同一条命令行旧构建起出 `false`、新构建起出 `true`（实测）。**舰队现在处在阶段① 靠的是「跑着旧构建」而非显式配置**，本文 v2.55 那句「仍**显式** `--open-policy` + `--audit-signed-tasks`」与现场不符，需回写。缓解办法 `--open-policy` 已实测有效。**#9** 审计镜像链路每 5 分钟失败一次（四条 `qianmo-mirror@*` 全 failed，远端 `trail.ndjson` 源文件根本不存在），而控制台把空链显示为 `{"records":[],"intact":true,"total":0}`——**管道断了与链本来就空在控制台上长得一样**。**#7** CA 化节点无法与纯 `--trust` 节点握手（base 分支就有的形状，阶段①共存期被单向切断）。**#8** 冻结四元组的 `credential.source` 子腿无测试覆盖。**⑥ 纪律与副作用**：全程**未触碰任何线上进程与生产目录**——两轮实验前后线上 resident 的 PID / `lstart` / 监听 / 38625 established(恒 0) / 日志字节数全部未变，`find ~/qianmo-beta ~/atlas-beta -newermt <开始时刻>` 两次均为空；未读取生产 secrets，实验 PSK 现场生成；按 PID 关闭自建进程（未用 `pkill`），工作目录已删、磁盘余量复原。**未升级任何生产节点**——升级的前置条件是先按 #10 补齐策略开关，属配置变更，留给负责人决定。**⑦ 附带记下的两条**：新构建启动横幅字段集比旧版多四个（`auditSignedTasks`/`mtls`/`signedHandshake`/`requireSignedHandshake`），对横幅做严格 schema 断言的巡检会一起碰到；生产 `~/atlas-beta/dist` 里有 14 个 macOS `._*` 伪文件（未影响运行，下次部署建议清理）。**方法论一条**：`nc -z` / `/dev/tcp` 在这套环境里对被安全组挡住的端口会**假阳性**（三次复现 OPEN 而应用层无响应），判可达性必须用应用层探测（qianmo WS 端点对普通 GET 回 `426`）。M0 排期、日期、产能、关键路径与既有任务包正文均未动；未新增任务包编号。 |
| **v2.66** | **2026-08-29** | **仓库许可由 MIT 改为 GNU AGPL-3.0-or-later（落实章程 v2.16）。**① **这是负责人决议，不是工程判断**——两条待定项（vendor 二进制可否分发、版权采取何种协议）由负责人 2026-08-29 定为「**可以分发**」+「**采取比较严格的开源协议**」，选型定为 **AGPL-3.0**、vendor 二进制「**一并纳入 copyleft 并补源码**」。② **改的是阡陌自有那一层，不是整仓**：`git mv LICENSE LICENSE.base` 保住基座 MIT 原文（与 `base-snapshot/v2.46.0:LICENSE` 逐字节相同），`LICENSE` 换成 AGPL-3.0 正文（34,523 B，与 gnu.org 原文逐字节相同，**正文前不加任何前言**——GitHub 按全文比对识别许可，加一行说明就会被识别成 Other，说明文字一律放 `NOTICE`）。③ **两层的判据是 SPDX 文件头，不是目录**：阡陌文件 **564 个**带 `SPDX-License-Identifier: AGPL-3.0-or-later`，基座文件**一个 SPDX 头都没有**——这个边界本来就物理存在，转许可只是给它标了值，因此**源文件层面与上游零重叠、同步时不会冲突**。一条命令即可举出 AGPL 那一层（`git grep -n … | awk -F: '$2<=5'`，**限定文件头**——不限定会多出 8 个正文引用该串的文档，数目对不上会让判据看起来是错的）。④ **为什么是 AGPL 而不是 GPL**：产品线②是网络型产品，最可能被无偿占用的方式是做成托管服务对外提供，这恰是 GPL-3.0 管不着、AGPL-3.0 §13（Remote Network Interaction）管得着的那一格。**代价如实记**：§13 对**我们自己**同样成立——p11 上那个对公网开放的控制台从此负有向使用者提供 Corresponding Source 的义务，这是本次变更唯一新增的持续运营负担，已列为咨询议题 T-4.1b。⑤ **落点清单**：`LICENSE`/`LICENSE.base`、564 个 SPDX 头、`scripts/sbom.ts` 的 `SPDX_HEADER` 常量、根 `package.json` 与 **19 个 `@qianmo/*` 包**的 `license` 字段、`NOTICE` 一/1 重写、`README.md` §许可、`CONTRIBUTING.md` 的 CLA 段与「优先发上游」段、`CLAUDE.md` §0 许可行、章程 §5.2①/§5.3/§5.5 就地加 v2.16 修正、`license-chain-m0.md`（D-5 关闭、新增 D-8、T-4.1 换题、新增 T-4.4/T-5.3）、`upstream-sync-drill.md` §9（新增）。⑥ **`upstream-sync-drill.md` §9 是本次最容易被忽略的一条**：上游 `LICENSE` 路径没变，所以上游一旦改它，三方应用会把 AGPL 正文当成「我方的本地修改」去合并，**大概率干净应用、不报冲突，得到一份两种许可搅在一起的文件**。这是**会静默成功的错误**，不像 §8 那类 apply 失败。处置：同步前把 `LICENSE` 剔出上游 diff，上游许可正文若有变更手工落到 `LICENSE.base`。⑦ **一个口子没堵上，如实记**：`vendor/audio-capture/` 六个预编译 `.node` 随 `dist/vendor/` 出货，**源码位置负责人尚未提供**。MIT 下「许可待确认」可以挂着，**AGPL 下挂不住**——copyleft 要求随分发提供 Corresponding Source。因此**没有把它们标成 AGPL**（标了却给不出源码，等于让仓库违反自己的许可，比「许可待定」严重得多），`NOTICE` 与 `license-chain-m0.md` D-8 如实写成「许可待定的随附二进制」，并给了两条收口路径：补源码，或从分发中摘除（删 `build.ts`/`post-build.ts` 的复制步骤 + 装载层降级；M0 演示本就不用音频，章程 N-9，这条路可行）。**必须二选一，不能长期挂着。**⑧ **不可追溯**：此前按 MIT 取得副本者的授权不受影响，AGPL 只适用于此后发布的版本；三处（`NOTICE`/`README.md`/章程 v2.16）都写明了这一点。⑨ **方向性提醒**：MIT 可并入 AGPL，反向不成立——**基座那层的改动要回贡上游必须单独授权**，基座与本仓库同属一位负责人、具备双授权条件但不自动发生；`CONTRIBUTING.md` 已把「基座改动优先发上游」补上这条新理由。回贡节奏仍按 v2.41「暂不、个案再议」。⑩ **历史记录一行未改**：章程 v2.5 变更记录行、`protocol-asset-review.md` 的头格式代码块、`license-chain-m0.md` §3.1 的原核对结论，**都保留 MIT 原文**并另加带日期的新行——批量改写一度把它们也改了，已逐条回退。改历史会让变更记录变成谎话。**范围、AC 判据、`BASE.md` 一字未动。** |
| **v2.67** | **2026-08-30** | **直接收口 v2.66 第 ⑦ 条留下的口子**：`vendor/audio-capture/` 六个预编译 `.node` 源码此前完全缺失，现已入库。① **新增 crate `packages/audio-capture-napi/native/`**（Rust + napi-rs 2.16、cpal 0.15.3），**从零重写，不是反编译原二进制**——10 个源文件全部带 `Copyright 2026 Qianmo AgentNest Team` + `SPDX-License-Identifier: AGPL-3.0-or-later` 两行头，落在本仓库的 AGPL 层内。② **契约来源**：实现了 `packages/audio-capture-napi/src/index.ts` 早已声明的全部八个函数——`startRecording` / `stopRecording` / `isRecording` / `startPlayback` / `writePlaybackData` / `stopPlayback` / `isPlaying` / `microphoneAuthorizationStatus`；该文件与装载/降级逻辑**未改动**。③ **配套入库**：`scripts/build-audio-capture.sh`（构建脚本）与 `docs/dev/audio-capture-native.md`（源码、契约与构建说明）。④ **实测结论（4/6 已验证替换，均为真加载验证，非结构性推断）**：`arm64-darwin` 从零构建（删 `target/` 后纯 `cargo build --release`）成功，`clippy --all-targets -- -D warnings` 干净，crate 自带单元测试通过；**本机 Bun 真机验证**录音（16 kHz / 16-bit LE / mono，50 ms 分块，三轮一致，15584–15600 Hz）与播放（起停/幂等/重启），八个导出齐全，`microphoneAuthorizationStatus()` 返回 3，与原厂二进制、独立 Swift 探针三方一致——**该 triple 的 `vendor/` 二进制已替换成自建产物**。`x64-darwin` 通过 **Rosetta 2 + 真实 x86_64 Node v26.3.0**、与原厂二进制**并排加载对比**，八个导出齐全、`microphoneAuthorizationStatus()` 返回 3、状态位一致，**已替换**。`x64-linux` 通过 **Docker `linux/amd64` + `node:22-slim`**（装 `libasound2`）与原厂并排对比，逐项一致，**已替换**。`arm64-linux` 通过 **Docker `linux/arm64` 原生容器**与原厂并排对比，逐项一致，**已替换**。**跨平台安装走的是 `--allow-unverified-install`，但不是没验证**——该标志的判据只是"本机能否原生 `require()` 加载"，这三个产物是先在容器或 Rosetta 2**目标架构上真实加载**、并排对比原厂逐项一致之后才装的。**一条运行时依赖等价性证据**：`readelf -d` 比对 `NEEDED` 段，原厂 Linux 二进制依赖 `libasound.so.2`/`libpthread.so.0`/`libc.so.6`/`libdl.so.2`/`ld-linux-x86-64.so.2`，自建产物依赖以上全部外加 `libgcc_s.so.1`/`libm.so.6`——**ALSA 这个硬依赖原厂本来就有，不是自建产物引入的**，多出的两个在任何 glibc 系统上必然存在，运行时依赖面无实质变化。⑤ **新增的构建前提**：crate 需要 **Rust ≥ 1.85**（`coreaudio-sys` 要求 edition2024），已用 crate 内 `rust-toolchain.toml` 钉住 `1.85.0`、`Cargo.toml` 写 `rust-version = "1.85"`。⑥ **仍未完成，不粉饰**：仅剩 `arm64-win32` / `x64-win32` 两个 triple 本机无法构建（需要真实 MSVC 工具链），待 Windows 机器或 CI。**在这两个 triple 替换完成之前，`vendor/` 里仍混有原厂来源、许可不明的二进制**——v2.66 第 ⑦ 条的口子因此判**部分收口**，不是收口。⑦ **落点**：`NOTICE` 一/1 与五/5、`license-chain-m0.md`（D-9 由「仍待人」转「部分处置」、D-4 升级注补一句现状、T-5.3 与 U-4 补现状、文首状态块补注）同批改写；`build.ts` / `scripts/post-build.ts` 的 vendor 复制步骤**未改动**——六个 vendor 目录结构不变，只是内容逐个 triple 换绑。⑧ **编号勘误**：`license-chain-m0.md` 里 vendor 这条原编号 **D-8**，与既有「`SECURITY.md` 与 issue 模板对外化」那条 **D-8** 重号——重号是 v2.66 批次引入的疏漏（当时新增时插在 D-5 与 D-6 之间，未查重号）。本次把 vendor 这条**重编为 D-9** 并移到既有 D-8 之后，恢复编号单调；`NOTICE`（中英两侧）与本文档内部全部指向它的引用同批改名。**v2.66 行本身不改**（已合并，属历史记录）——v2.66 行里提到的「`license-chain-m0.md` D-8」与「新增 D-8」，现在都应读作 **D-9**。**范围、AC 判据、`BASE.md` 一字未动。** |
| **v2.68** | **2026-08-30** | **直接收口 v2.67 第 ⑥ 条留下的口子**：`arm64-win32` / `x64-win32` 两个 triple 本机无法构建（需真实 MSVC 工具链），现已由 CI 用同一份仓库内源码构建并装入 `vendor/`。① **新增手动 CI 路径**：`.github/workflows/build-audio-capture-windows.yml`，**只配 `workflow_dispatch`**（不进 push / pull_request 阻塞门禁——那些 runner 不带 Rust，而原生模块缺失时装载层本就优雅降级，这条可选能力不该卡住阻塞门禁），`windows-latest` runner，矩阵两个 job 对应 `x86_64-pc-windows-msvc` 与 `aarch64-pc-windows-msvc`。跑了两次：`33291590858`（在 main 上，两 job 均成功，但产物有依赖面退化，**这批没有装进仓库**）与 `33292271322`（在本分支上，带静态 CRT 修复，**这批才是装进 `vendor/` 的产物**）。② **第一次构建暴露的退化，以及为什么它严重**：自建产物比原厂多出四项依赖——三个 `api-ms-win-crt-*` 转发器（UCRT，Windows 10+ 自带，无所谓）与 **`vcruntime140.dll`**（有所谓：它来自 Visual C++ 可再发行组件包，**既不随 Windows 出厂、也不随 Node.js 安装程序分发**）。目标机缺它时 `require()` 抛错，而 `packages/audio-capture-napi/src/index.ts` 的 `loadModule()` 会吞掉 `require` 错误静默降级——**故障表现是语音模式无声消失、不留任何报错线索**，不是一条会被发现的红。原厂产物本就是静态链接 CRT 构建的（`bcryptprimitives` + `ntdll` + `dbghelp` 正是 Rust 静态 CRT 二进制的典型形态），所以这条依赖是本次重写引入的退化。③ **修法与"为什么按 target 写"**：新增 `packages/audio-capture-napi/native/.cargo/config.toml`，在 `[target.x86_64-pc-windows-msvc]` 与 `[target.aarch64-pc-windows-msvc]` 下分别打开 `-C target-feature=+crt-static`。**不能写成 `[build] rustflags`**——那会波及已经在目标架构上真加载验证过的 macOS / Linux 产物，等于用一次没验证过的重建换掉四个已验证的产物。工作流无需改动（它没有 `env:` 块、没有 `RUSTFLAGS` / `CARGO_HOME` / `--config`，`working-directory` 就是 crate 目录，cargo 会自己向上找到这份配置）。④ **两个 triple 的证据等级不同，明写**：`x64-win32` **在目标平台上真加载验证过**——工作流在 windows-latest（x86_64）runner 上用 Node 真 `require()` 加载刚构建出的 `.node`，逐个断言八个导出均为函数，并调用 `microphoneAuthorizationStatus()` 返回 3（落在 0..=3 的合法区间内）。**`arm64-win32` 从未在任何机器上被加载或执行过**——`windows-latest` runner 是 x86_64，结构上加载不了 aarch64 的 DLL；**工作流对此不假装通过**，而是打印一行显式跳过理由。它取得的证据只有 `cargo build --release` 成功、`cargo clippy --release -- -D warnings` 干净，加上结构性核对（PE 机器类型 `0x8664` / `0xaa64` 与 triple 相符、characteristics 带 DLL 位 `0x2022`、`napi_register_module_v1` 存在、八个导出名字符串齐全）。**这条不许被"六个 triple 已收口"盖过去**：它欠一次真实 Windows on ARM 机器上的加载验证，**那是验证缺口，不是许可缺口**——许可上它与其余五个同出一份 AGPL 源码。⑤ **依赖面等价性证据，这次是零差异**：提取被引用的 DLL 名（用 `strings -a` 读出全部 `*.dll` 引用名，排除 crate 自身的模块名——原厂是 `audio_capture_napi.dll`、自建是 `audio_capture.dll`，那是重命名前的产物原名，不是外部依赖）逐项对照，原厂与自建**同为 8 项、`diff` 无输出**：`advapi32.dll` / `api-ms-win-core-synch-l1-2-0.dll` / `bcryptprimitives.dll` / `dbghelp.dll` / `kernel32.dll` / `ntdll.dll` / `ole32.dll` / `oleaut32.dll`，`vcruntime140.dll` 已消除。**这条比 v2.67 ④ 那条 Linux 证据更强**——Linux 自建产物比原厂多出 `libgcc_s.so.1` / `libm.so.6` 两个，Windows 这两个一个都不多。⑥ **装进仓库的指纹**：`x64-win32` sha256 `d0d79b7a…`（809984 B）、`arm64-win32` sha256 `d619e905…`（752640 B）；被替换掉的原产物 `2a8267f4…` / `98386089…` 仍可由 `git show 5459375f:vendor/audio-capture/<平台>/audio-capture.node` 取回。⑦ **结论**：**六个 triple 现在全部由本仓库自有的 AGPL-3.0-or-later 源码构建，`vendor/` 里不再有来源不明、许可未定的二进制**——`NOTICE` 里那条"例外收窄为两个 Windows triple"就此不存在，`license-chain-m0.md` 的 **D-9 由 🟡 部分处置 转为 ✅ 已处置**（处置历程保留：新增于 08-29 → 部分处置 → 已处置，不抹成一步到位）。⑧ **落点**：`NOTICE`（中英两侧同批，一/1 与五/5）、`license-chain-m0.md`（文首状态块新增一段而不改写前一段、D-9 条、D-4 升级注、T-5.3、U-4）、`docs/dev/audio-capture-native.md` §3.2、`packages/audio-capture-napi/native/.cargo/config.toml`、`.github/workflows/build-audio-capture-windows.yml`；`build.ts` / `scripts/post-build.ts` 的 vendor 复制步骤与 `packages/audio-capture-napi/src/index.ts` 装载层**均未改动**。**范围、AC 判据、`BASE.md` 一字未动。** |
| **v2.69** | **2026-08-30** | **收口 D-9 之后补齐收口半径**：D-9 本身关得住，关联面没跟上。① **由来**：D-9 收口后做了一次**独立对抗性审查**（审查者无前置参与）。结论分两半——**工程内核扎实、诚实性那一关过得住**：`NOTICE` 中英两侧、`license-chain-m0.md` D-9、`audio-capture-native.md` §3.1/§5 逐条写明 `arm64-win32` 没有运行时证据，**没有任何一处把这个缺口盖过去**；**问题全出在收口半径**——v2.68 ⑧ 的落点清单本身漏了几处，仓库里还有多处文本停在收口前的状态，其中几处**与 `NOTICE` 直接矛盾**。本批只修这些，不动 D-9 的任何结论。② **章程升 v2.17**：v2.16 ⑥ 是**规范文件的最新表态**，写着 vendor 六个 `.node` 在源码到位前「**不标 AGPL**」，而 `NOTICE` 已把它们划进 AGPL 层——两份文件对同一件事给出**相反的规范判断**，而 `CLAUDE.md` §2.1 写明「章程 §5 是强制条款，违反的提交一律回退」。**v2.16 那一行一字未改**（已定稿的历史记录，改它会让变更记录变成谎话——先例见本表 v2.66 ⑩），改法是**新增 v2.17 行**记源码入库 / 六个 triple 全部换绑 / v2.16 ⑥ 的口子已闭 / 该行里的指针 `D-4/D-5` 应读作 **D-9**，并把头部「文档版本」与「生效日期」同步到 v2.17 · 2026-08-30。③ **`README.md` 两处，这是同一个坑第二次踩在同一个文件上**：首页许可段写着「`vendor/audio-capture/` 六个预编译二进制的**待定状态**，详见 `NOTICE`」——一边说待定、一边把读者送去说相反话的 `NOTICE`；同段代码块注释里的「**564 个文件**」实测已是 **580**。本表 **v2.62 ④** 刚记过同类事故（pin 出现在 `BASE.md`(2) + `NOTICE`(2) + `README.md`(1)，**后三处不在任何门禁视野里**，`check:*` 全绿也不提示）。处置：「待定状态」改为「来源与逐个的证据等级」；数字**直接删掉**而不是照 `NOTICE` 那样加时间锚——README 是最常被读、最少被维护的一面，而那条判据命令就印在注释下面三行，把一个会漂的数字留在这里等于给同一个坑第三次机会；带日期锚的实测值统一以 `NOTICE` 一/1 为准。④ **`license-chain-m0.md` 五处**：**(a) T-5.3 重号**——vendor 那条新增时误取了已被「弱传染许可」占用的 **T-5.3**（PR #128 引入），上一批正好编辑了它却没查重，而 v2.68 ⑧ 的落点又写作「T-5.3」，**有两个时这个指针是歧义的**；vendor 这条**重编为 T-5.4**（先来的那条不动），文内两处指针同批改名，**v2.68 行本身不改**（已合并，属历史记录）。**(b) D-4 转 ✅**：标题还挂着 🟡 部分处置，而本条正文的更新块已经说「本条描述的『原厂二进制、许可待定』**已不复存在**」，该文 §3.2 图例又自己规定「每条前缀即当前状态」——D-4 只剩 audio-capture 许可归属这一项未闭（ripgrep 那半 2026-08-15 起溯源就是清楚的、`NOTICE` 侧同日补齐），该项已随 D-9 收口，故转 ✅ 并保留处置历程。**(c) 三处活结论过期**：§1「DoD 中机器可判的两条」里的「D-4 的许可归属仍是待人项」、§3.2 引导块末句「未标已处置的条目（D-4 的许可归属、D-5）仍是待人项，`LICENSE` 与根 `package.json` 未动」（**三重过期**：D-5 已 ✅、那两个文件 08-29 都改过、D-4 那半也闭了）、D-4 里的「咨询议题 T-5.2 保持不变」——按现状改写，原句照录保留。**顺带还发现一处不在审查清单里的同类过期**：未决事项表的 **U-9**（「`LICENSE` 是否增列阡陌版权行」）仍写 ⏸ 仍待人 + 「本次未动 `LICENSE`」，而 D-5 已于 08-29 转 ✅、`LICENSE` 当天就换成了 AGPL-3.0 正文、原题 T-4.1 也已作废——同批转 ✅ 并保留原状可读。**(d) T-5.2 补更新注**：它问的「需不需要在申报前移除、或由基座作者补齐源码与许可」早已有答（走的是补齐那条），此前 T-5.4 加了两条更新注而 T-5.2 一条都没有；仍需科技处答的只剩「源码在手但未经运行时验证的随附产物要不要单独说明」一问。**(e) U-4 重定性**：标题/责任人/依据三列仍是「源码位置与许可说明」「负责人（以基座作者身份）」「证据包完整性；咨询 T-5.2」，而现在剩的是**工程验证待办、不是负责人待办**——三列重写为「`arm64-win32` 的一次运行时加载验证」「谁有 Windows on ARM 机器谁跑（当前无人）」「无（只影响对外表述的准确性）」，状态由「保持 ⏸ 因为仍需一台我方没有的机器」改为**无限期挂起、未排期**。⑤ **依赖面证据：结论没变，方法与数字都错了**。`NOTICE` 中英两侧与 `audio-capture-native.md` §3.2 都用 `strings -a` 加一条 `grep -oiE` 正则提取被引用的 DLL 名，报「**共 8 项**、逐项零差异」并把那 8 项列为门禁判据。`strings` 是启发式，只能**过度包含**——分不出装载期导入、延迟导入和碰巧出现的字符串。改用 PE **导入表**复核四个文件（新旧 × x64/arm64）：**装载期导入是 7 项、延迟导入 0 项，四者完全相同**（`advapi32` / `api-ms-win-core-synch-l1-2-0` / `bcryptprimitives` / `kernel32` / `ntdll` / `ole32` / `oleaut32`）。**`dbghelp.dll` 不在任何一个产物的导入表里**——它是 Rust std 的 backtrace 在 panic 时 `LoadLibrary` 的符号化目标，只作为字符串存在于二进制中，被 `strings` 法误算成了第 8 项。判据改为导入表法并给两条可复现命令（x64 用 `x86_64-w64-mingw32-objdump -p <文件>` 读其中的 `DLL Name:` 行、aarch64 用系统 `objdump` 同样读法，**两个架构的 objdump 不通用**）；**「这条证据比 Linux 那条更强」复核后仍成立**——结果更强（Linux 自建多出两个库、Windows 零差异），且改用导入表后方法也与 `readelf -d` 的 `NEEDED` 对等了，不再是权威读法对启发式。另补一处遗漏：§3.2 只说要排除自建产物的模块名 `audio_capture.dll`，**没说原厂那个叫 `audio_capture_napi.dll`**（v2.68 ⑤ 两个名字都写了），照文档命令去比对取回的原产物会多出一个没解释的条目。`license-chain-m0.md` D-9 里那条同批加**带时间锚的补注**，**原记录一字未改**。⑥ **`audio-capture-native.md` §3.2 补两条此前缺的提醒**：**`RUSTFLAGS` 环境变量会把 `.cargo/config.toml` 里的 `target.<triple>.rustflags` 整条丢弃**——cargo 的四个来源互斥、命中第一个就不再看后面（`CARGO_ENCODED_RUSTFLAGS` > `RUSTFLAGS` > `target.*` > `build.*`），工作流本身是干净的（无 `env:` 块、无 `RUSTFLAGS`），但开发者 shell 里设了它（哪怕内容无关）就会**静默**退回动态 CRT、重新带上 `vcruntime140.dll`；**静态链接 CRT 对一个被 Node `dlopen` 的 N-API 插件为什么安全**——`/MT` 最经典的「跨 CRT 堆不匹配」在这里**结构上不成立**：Rust 在 Windows MSVC 上的 system allocator 走 `HeapAlloc(GetProcessHeap(), …)` 而**不是** CRT 的 `malloc`（判据是导入表，新旧产物都导入 `GetProcessHeap` / `HeapAlloc` / `HeapFree` / `HeapReAlloc`），进程堆是进程级的、不随 CRT 副本分裂，且 cpal 与 `windows` crate 都是纯 FFI，不跨边界交换 CRT 对象。**残留风险明写**：两份 CRT 各有各的 `errno` / stdio 缓冲 / locale；**将来若引入一个把 `malloc` 指针（或 `FILE*`）交给调用方的 C 依赖，上面第二条前提会静默失效**——编译照过、加载照过，崩在释放的那一刻。⑦ **SBOM 生成器与构建脚本指引不在本批**：`scripts/sbom.ts`、`docs/dev/sbom-m0.md` / `sbom-m0.json`、`scripts/build-audio-capture.sh` 由同一分支的另一批改动处理，本批一个字未写。⑧ **`arm64-win32` 仍无运行时证据，且这一项无限期挂起**：本项目没有 Windows on ARM 机器，**负责人 2026-08-30 决定不做，不排期**。本批每一处改动都保住这条不被盖过去——章程 v2.17 ④、U-4 三列、T-5.2 与 T-5.4 的末问都明写它是**验证缺口而不是许可缺口**（许可上它与其余五个同出一份 AGPL 源码）。⑨ **顺带修一处本表自身的记录错误**：头部「文档版本」停在 **v2.67**，而 v2.68 行已经在表里——本次一并升到 **v2.69**（同类错误 v2.62 ⑤ 修过一次）；「依据」栏的 `charter.md` 版本同步到 **v2.17**（该栏的用途就是跟着章程走，v2.35 勘误正是为此）。**范围、AC 判据、排期均未动；`BASE.md` 一字未动。** |
| **v2.70** | **2026-08-30** | **把双许可的文件头判据补成真的，并记下它为什么会漏。**① **补头本身**：66 个阡陌自有文件缺 `Copyright 2026 Qianmo AgentNest Team` + `SPDX-License-Identifier: AGPL-3.0-or-later` 两行，提交 `03967930` 补齐，判据值由 **580** 升到 **646**（`git grep -n "SPDX-License-Identifier: AGPL-3.0-or-later" | awk -F: '$2<=5' | cut -d: -f1 | sort -u | wc -l`）。**范围判定不能用文件头自己**——那是循环论证：漏加头的文件恰恰就是判据看不见的那些。用的是 CLAUDE.md §2.5 那条权威判据「文件在不在 `base-snapshot/v2.46.0` 那棵零改动快照里」，快照之外 = 阡陌自有。漏加的里面有 `src/constants/identity.ts` —— CLAUDE.md §2.3 点名的**身份 roster 唯一真源**，不是边角文件。`BASE.md` 同样缺头，但 CLAUDE.md §2.4 写死「只有导入与上游同步两类事件才写它，功能 PR 里顺手改一律回退」，故**有意跳过**，所以是 66 不是 67。三个 shebang 文件（`demo/ac1-restart.sh`、`demo/ac2-wake-forward.sh`、`scripts/check-macro-guards.ts`）的头加在 shebang **之后**，SPDX 落第 3 行，仍在判据的前 5 行窗口内。② **根因不是手滑，是一条已经过期的成文决定**：**仓库里根本没有版权头门禁**。实测 `package.json` 的 11 条 `check`/`check:*`（`check` / `:fix` / `:bundle` / `:unused` / `:unused:raw` / `:docs-i18n` / `:cycles` / `:prompt-purity` / `:mock-hygiene` / `:macro-guards` / `:identity-paths`）没有一条查版权头；`scripts/check-*.ts` 八个脚本里 SPDX 只出现在其中两个**自己的文件头**里，不是判据。这是章程 §5.5 的明文决定：「此后新增的 `@qianmo/*` 源文件一律带头，**不为此新增 CI 断言**（漏加由 PR 评审兜）」。**那条决定作出时（v2.5，2026-08-12）覆盖面是 `protocol` / `registry` 两包共 15 个 `.ts`；今天判据覆盖 646 个文件、横跨整个仓库，约 43 倍。**「由 PR 评审兜」这一道这次一口气漏了 66 个 —— **漏加不是偶发，是那条「不设门禁、靠评审兜」的决定在覆盖面涨了 40 多倍之后失效了。** ③ **由此留一条开放项给负责人（本批不落地）**：判据现在是机器可判的，做成棘轮成本很低（现成对照命令就是 `comm -23 <(git ls-files) <(git ls-tree -r --name-only base-snapshot/v2.46.0)` 再筛前 5 行），且本次之后「**形态上带得了注释头却没带的阡陌文件**」已归零 —— 正是钉零的时机，与 `check:mock-hygiene` 从 241 清到 0 之后设棘轮防回潮是同一个形状。**但这条不能由本 PR 自行落地**：反转章程 §5.5 的成文决定属**章程级决定、不是工程判断**，而 CLAUDE.md §2.1 写明范围变更必须回写章程并升版本号、无书面变更不开工。故本次**只记证据、不建门禁**，是否设棘轮由负责人定。门禁若要设，得先想清楚三类**不能算漏**的文件怎么豁免：**83 个**形态上就带不了注释头的阡陌自有文件（48 `.json` / 27 `.jpg` / 3 `.png` / 2 `.pdf` / 2 `.docx` / 1 `Cargo.lock`）、**4 个**有意不加的（`BASE.md` / `LICENSE.base` / `NOTICE` 与 `packages/audio-capture-napi/native/.gitignore`）—— 前两类合计 **87**，即「阡陌自有却无头」的全集；以及**另一条独立的限制**：**180 个基座文件带着阡陌的改动而有意不加头**（带头会抹掉上游 MIT 溯源，且每次上游同步多 180 处冲突），**它们的来源层仍是基座、不是「无头 ⇒ 基座」的反例**，其中阡陌写的行由 git 历史举证（`git diff base-snapshot/v2.46.0..HEAD`）。均为 2026-08-30 实测，命令见 `NOTICE` 一、许可。**这里有一个前一版口径踩过的坑**：用 `comm -12` 取「快照里且被改过」会得到 **315**，那把 **135 个被我们删掉的基座文件**（`publish-npm.yml`、`scripts/release.ts` 等发布面，见 CLAUDE.md §0）也算成了「被改造过的」—— 对已删除的文件谈带不带头没有意义。正确拆分是 `git diff --name-only --diff-filter=M/D/A base-snapshot/v2.46.0 -- .` = 180 改 / 135 删 / 733 增，合计 1048，与 `git diff --shortstat` 的文件数对得上；那 180 个里带 AGPL 头的实测为 **0**。**另注**：§5.5 那条的头格式原文写的是 `SPDX-License-Identifier: MIT`，v2.16 转 AGPL 后未回改 —— 照它建门禁会断错值，**本批未改章程**，一并留给负责人。④ **补头的连带伤害：行号引用整体下移**。被引用文件各多 3 行，同批修正 **15 个**行号引用（分布在 **11 行**、`base-adoption.md` / `base-modifications.md` / `resident-botization.md` / `roadmap.md` **4 份文档**）。核对时另发现 **8 个**引用**在本次改动之前就已失准**（按「若原本准确则应恰好位移 3 行」反推，实际偏差 3 / 3 / 16 / 16 / 30 / 30 / 64 / 96 行），一并改到真值。**偏差最大的一对**在本文件 §「v2.15 决策（P3.1 开工前置，实施方裁定）」那段 D-2 论证里，指向 `protocol.md` 的 `:311`·`:594` —— 真值是 `:378`·`:693`，实际位移 67 / 99 行，故偏 64 / 96。**指错对象最明显的一对**是 `base-adoption.md` 里指向 P0.2 交付物/DoD 的 `roadmap.md:344`·`:345`（各偏 30）—— 偏得不是最多，但它当时实际指着的是排期表的 S5/S6 两行，**连表都指错了**，而那两个数看上去正好像是「交付物在前一行、DoD 在后一行」，读的人不跳过去核根本看不出来。**这正是 ② 那条开放项的另一面**：行号引用同样没有门禁。**并且本条自己就再演示了一遍**：本表新增 v2.70 这一行，使 `roadmap.md` 该行以下全部下移 1 行，于是刚改准的四个引用又差了 1 —— `base-adoption.md:390` 的 `roadmap.md:378`·`:377` 应为 `:379`·`:378`，`resident-botization.md:812` 的 `roadmap.md:852`·`:855` 应为 `:853`·`:856`。**这四个已在本批一并改到真值**——四处都是三位数换三位数的**等长替换**，不改变任何文件的行数，因此不会再引发新的连锁位移（2026-08-30 实测，复核用 `sed -n '378p;379p' docs/dev/roadmap.md` 与 `sed -n '853p;856p' docs/dev/roadmap.md`）。**任何往本表加行的批次都会重犯这一条** —— 这也是把行号引用做成机器可判的另一个理由。⑤ **`resident-botization.md:189` 加带日期的补注**：那条「`protocol.md` 写 `wake` 需要 ack、实现里不产生 ack」的文档实现差，已由 P13.5 定夺（`114436d6`，2026-08-19，**改文档不改实现**），原发现记录一字未改。⑥ **`NOTICE` 三处修正**（同分支另一批落地）：**(a)** 计数与「判据尚未覆盖全部」段落按补头后的现状改写（剩 87 个无头文件 = 83 个形态上带不了注释 + 4 个有意不加，原文说的「应补而未补的源文件与文档正由单独一批提交补齐」已完成）；**(b)** 新增一段说明那 **180** 个被改造过的基座文件为何有意不带头；**(c)** **修正「成果边界」的举证命令 —— 这是一次把我方工作量向下修正的勘误**：原文写 `git diff 3380c88..HEAD`，但 2026-08-17 首次上游同步后它把上游 v2.46.0 的内容一并算成我方工作（实测 **292,973** 插入行，而正确的 `git diff base-snapshot/v2.46.0..HEAD` 是 **234,068** 行，**虚报约 58,905 行**）。章程 §5.5 的 v2.13 更新早已改用后者，`CLAUDE.md` §0 也写明「不要再用 `git diff 3380c88..HEAD`」，**只是从未同步到 `NOTICE`** —— 又一处与 v2.62 ④ / v2.69 ③ 同型的「同一事实散落多处、后几处不在任何门禁视野里」。⑦ **`license-chain-m0.md` D-5 计数刷新**：`580` → `646`；`564` 作为 2026-08-29 当天的处置记录**原样保留**。同处那句「不限定前 5 行会多出 **8** 个」重测后是 **4**（`CLAUDE.md` / `CONTRIBUTING.md` / `NOTICE` / `README.md`）—— **变小不是因为哪份文档删了引用**，而是因为 `charter.md` / `license-chain-m0.md` / `roadmap.md` / `sbom-m0.md` 这四份自己也补上了文件头，于是两种算法都数得到它们。该差值**两头都会动**，故改写成带日期锚 + 现跑现算的命令，不再是永久断言。⑧ **SBOM 重新生成，并堵上一个会静默回退本批成果的坑**：`docs/dev/sbom-m0.md` 由 `bun run sbom` 整体覆盖，而 `03967930` 给它手加的那两行头**生成器并不输出** —— 实测先跑一次 `bun run sbom`，diff 恰好是那 3 行被删、其余零变化，判据计数会从 646 **静默**掉到 645，且**没有任何门禁会报错**（见 ②）。处置：**让生成器自己写出这两行**（`scripts/sbom.ts` 的 `buildMarkdown` 开头，附注释说明为什么不能只存在于产物里）；`sbom-m0.json` 不加也加不了，`.json` 形态上带不了注释头。另按补头后的现状改 `scripts/sbom.ts` 三处**已被自己证伪**的陈述：落在 `sbom-m0.md` §7.2 的「反向不成立（**没有文件头既可能是基座文件，也可能是漏加头的阡陌文件**）」—— 结论仍成立但**举例已不存在**，反例改写成那 87 个阡陌自有却无头的文件，并把 180 那条**另起一句写成独立限制**（把它当反例是错的：那些文件本来就是基座层，推断在它们身上成立）；以及 `napiPackageProvenance` 的文档注释与 `baseSnapshotVerdict` 的兜底串里同一处措辞，其中前者点名的实例 `src/constants/identity.ts` 当天正好被补上了头。**产物差异仅此一句**，`sbom-m0.json` 字节未变、各包「版权头」比例列未动（66 个里只有 1 个落在 `packages/` 下，且是 `packages/console/README.md`，而该列只扫 `.ts`）。**范围、AC 判据、排期均未动；`BASE.md` 一字未动。`docs/dev/charter.md` 只在补头那笔多了两行文件头（它本身也在那 66 个里），条款与决定一字未改——本条 ② 与 ③ 提到的两处章程问题都只是记录，没有回改章程。** |
| **v2.65** | **2026-08-29** | **清掉挂着的四条 issue（#102 / #111 / #113 / #116），其中两条的原诊断被实测推翻。**① **#111 节点重启静默丢尾参**：拓扑类参数由脚本按 `--role`/`--node` 推导所以每次都对，`--` 之后的东西只存在于当初那一次命令行里。于是「停机 → 换产物 → 起机」这个最常做的动作会静默改变节点的策略面（08-26 在 cornna-p3 上，beta-2 起来后 argv 少了一个 `--trust`，其余一字不差）。**丢的是策略不是显示项**：节点照常启动、照常在线、拨号照常 426，所有存活判据全绿，只有真发一个签名唤醒任务时才失败。尾参落进 `<root>/state/<节点>.passthrough`（0600，一行一个）。**方向与控制台腿相反且是有意的**——控制台「不给就撤掉」撑得住是因为开机由 systemd 读 `console.env`，节点这半边每次换产物都要人重跑，同一个默认在这里等于把这条 issue 变成默认行为。一行一个参数而不是 `KEY=值`：那是 console.env 迁就 systemd 分词才有的形状，也是它对含空白尾参只能 WARN 的原因。**② #113 真机腿起法不在仓库里**：`grep -rn "QIANMO_ACCEPTANCE_PSK_"` 在 `*.md` / `*.sh` 上是空的。后果不是「跑不起来」而是**跑出来的结果无人能复核**——少接一把 PSK 的表现不是报错，是那个节点的场景整片 skip 或红，两者都容易被读成「环境问题」。新增 `ops/fleet-acceptance.sh`：现取 PSK、校验四把、exec runner；值不打印不落盘，通过时打 sha256 前 8 位。用例除了钉那两件会静默出错的事，还加了一条防漂——壳里的缺省对应表与 `DEFAULT_FLEET_HOSTS` 必须一致，漂了的表现是去另一台机器取 PSK、取到了、然后整条腿对着错的节点跑。**③ #116 影子拒绝**：原 issue 问「控制台该出示什么能力」，**那件事已经定了也已经实现**——`SIGNED_TASK_POLICY` 要求 wake 出示 `write-limited`，而 `--wake-sign`（2026-08-23 落地）签发的正是这一档，整条链路由 `console/wake-sign-round-trip` 钉着；08-27 那台控制台只是没带那个开关。缺的是**判据**，两条补上：一条钉机制（开放策略 + `--audit-signed-tasks` 时那条影子拒绝确实被记下、且说得出要什么出示了什么），一条是判据本身（链上一条影子拒绝都不该有，**在 open policy 下照样会红**，08-27 那天就会红）。空链记 skip 不记 pass——本地腿附着的是刚起来的节点，「一条都没有」无条件成立，那是一条什么都没看的绿。**④ #102 的诊断有一半是错的，实测推翻**：它把 `bootstrap-preconditions.test.ts` 与 `beta-retain.test.ts` 归成同一个病根（首次执行策略扫描）。前者成立且 `a6eb47e9` 已治好（实测 6 条 0.69 s、最慢 0.41 s，5 s 预算的 12 倍余量）；**后者不成立**——那个文件一个可执行文件都不写，它跑的是 `bash <仓库里那份脚本>`，永远同一个 inode，脚本还被 bash 当数据读，压根不走 exec 策略那条路。「机器忙着扫描所以这里也慢」也直接测了并证伪：一个热 inode spawn 100 次，空闲 146/132 ms、8 路扫描压力下 138/140/141 ms（**没有影响**）、8 路纯计算压力下 205/204 ms（1.5×）。**那笔钱只在新 inode 上收，不拖慢热二进制的 spawn。**这条结论已写进用例文件头，供下次分辨。真正撑不住的是两处结构性的东西：`runPaused` 自己等检查点等 5 s，**恰好等于**整条用例的预算，于是它那句点名检查点的诊断永远轮不到抛、用例先死在 Bun 的匿名 TimeoutError 上；以及 `beta-retain.ts` 的 apply 锁自己定了 10 s 上限，用 5 s 去装一件实现说要 10 s 的事是预算写错了。改为按实现给出的上限算（两个下界取大：锁 10 s、2 × 检查点等待 6 s＝12 s → 20 s），检查点等待严格小于用例预算好让诊断先抛。**这不是 issue 禁止的「调大超时盖住偶发红」**——那条禁令针对的是**没有上限**的扫描代价，而这里的上限是实现自己写死的数。**⑤ 顺带**：写 #113 的用例时当场撞上 #102 的真病根（`不带 --` 那条在忙起来的机器上 5400 ms 超时），按模块作用域预建 + 预热改掉——懒到 `place()` 里写不够，第一条用例仍要独自扛那条尾巴。**范围、判据、排期均未动。** |
| **v2.64** | **2026-08-29** | **常驻在 ACP 起不来时降级而不退出——落实 v2.63 ⑧ 记下的那条待办。**① **这不是 CI 的事。**v2.63 是从 `acceptance-local` 那条红查到它的，但那条红只是症状：**真实部署里节点凭据一旦失效，整台节点会以同样的方式消失**——ACP `session/new` 被拒 → supervisor 连试五次后 park → `run()` 返回 → 进程退出。对面看到的现象是「拨不通」，与断电、掉线、机器没了完全一样，而事实是这台机器活着、审计链完整、投递账本还在，只是不能跑模型。「agent 不可用」与「节点没了」是两件事，网络有权知道是哪一件。**② 根因是所有权**：监听口原先建在 `#startAcp()` 的**末尾**（`sessions.start()` 之后），生命周期整个嵌在 ACP 子进程里面。于是两个后果，都是结构决定的——每次 ACP 重启都会把监听口一起拆掉（重启窗口内拨过来的对端看到的是「节点没了」）；而一次都起不来的时候，那行代码**根本没被执行到**，监听口从未绑上。**③ 修法**：监听口上提到 `run()`，在 agent 之前绑、活过每一次重启与 park（`#startTransport`）；`supervisor.run()` 返回不再等于结束——若不是被 `stop()` 叫停，就是 park，此时报一条「已降级」的错误并**停在那里继续服务**，直到真的有人 `stop()`。投递侧新增 `#runtimeForDelivery()`：agent 正在起来就短等（3 s，**卡在 5 s 回执预算之下**——等得比发送方久是最坏形状：发送方超时放弃了，这边却照样收下并跑完，活干了没人知道），park 或正在停止则**立刻**拒绝。两句拒绝措辞**分开**：一句说「重试」，一句说「别重试」，合成一句会让一次普通重启看起来像需要人介入。**④ 顺带修掉一个只有解耦之后才存在的缺陷**：监听口活过 ACP 之后，对端可以在「子进程已死、新一代还没起」的缝里投递进来，而正在死的那一代的 `#failActiveTasks` 会把这条**尚未交给任何一代**的任务结算成 `task.result{failed}`——随后它在新一代上真的跑完了，结果却因为任务已离开两张表而被丢掉，发送方连 `ack` 都收不到（实测：第二条消息只有 `task.result`，没有 `ack`）。改为跳过仍在 `deliver()` 里的任务。**这个窗口以前不存在，因为以前那种时刻根本没有监听口。**⑤ **用例**：新增「agent 永远起不来的节点仍然可寻址」——每一代都立刻退出，监听口照样绑上、`run()` 不返回、对端连得上、`task.request` 在回执预算之内拿到 `E_UNDELIVERABLE` 与带原因的 `error`、收件箱**零写入**（跑不了的活不该占一个信箱位）。既有的「ACP 重启后投递不中断」用例同步改写：不再等对端察觉断链（链路根本没断），改等第二个子进程；阶段集合**仍然是精确相等**，因为 `child.kill()` 同步置 `killed`，`#runtimeIsLive` 当场就是 false，那一轮被留给下一代而不是喂给尸体，`turn_failed` 不会出现。**⑥ 新增一个生产选项 `acpRestart`**（重启退避与 park 阈值）。它不是纯测试钩子：park 从「结束进程」变成了「进入一种持续状态」，多久进入那种状态因此成了运维选择；顺带让用例不必等完 2/4/8/16 s 的生产梯子。缺省仍是 supervisor 自己的常数。**⑦ `acceptance-local` 仍然只手动跑**，但注释里的理由换了：那条「没凭据就整个死掉」已经不成立，留下的是**另一条**没被这次修复动到的——115 条 × 38 s ≈ 73 min 装不进 job 预算。而降级之后每条要多久是**未测的**（那 38 s 全花在等一个永远不会绑上的监听口，这条路径没了）。**在拿到一次 `workflow_dispatch` 的真实计时之前，不要把它挂回 push / pull_request。** **范围、判据、排期均未动。** |
| **v2.63** | **2026-08-29** | **仓库转公开后 CI 第一次真跑起来，三条红全部查清；`acceptance-local` 改为只手动跑，并记下一条真实部署隐患。**① **背景**：此前 CI 每次都是 `steps: 0` / 1–2 s——Actions 配额耗尽、没有 runner 接单，于是**没人看见过它的结论**。转公开后配额限制消失，第一次真跑就红了三处，其中两条测试分别在 08-25 / 08-26 落地，在 Linux/CI 上红了三四天无人知晓。② **`beta-deploy` 固件的 `exec` 竞态**：守卫按 `ps` 里 argv 含不含树路径判定，而固件是 `#!/bin/sh` + `exec sleep 120`——`exec` 把 shell 换掉，argv 从此是 `sleep 120`，树路径没了。macOS 的 `/bin/sh` 启动慢，`ps` 常抢在 exec 之前拍到旧 argv（实测 `argv = /bin/sh /…/tree/old-form`），Linux 的 dash 立刻 exec 一次都抢不到。改成把 `sleep` 拷进树里直接跑：没有 shell、没有子进程、argv[0] 恒是树里的路径。**CI 已验证修好。**③ **`raw-source-macro-defines` 从来不是「Linux 才红」**：`vcr.ts:139` 是 `env.isCI && !VCR_RECORD` 才抛，判据是 `CI` 这个环境变量、不是操作系统。开发机上 fixture miss 会落到真调用（用例自己起的 loopback 端点），CI 上直接抛，请求根本没发出去、`systemPrompt` 停在 null。本机 `CI=true` 一次复现。补 `VCR_RECORD=1` 恢复 fall-through，盒带落在 per-run 的 scratch 里随目录消失。**先让断言把子进程 exitCode 与两条流带上，再用那份现场把它修了**——上一版只吐一句 `Received: null`，那是查不动的红。**CI 已验证修好。**④ **就绪等待加快速失败**：常驻 ACP 连试五次后 park 退出，而等待照旧轮询到预算耗尽，把「它死了、这是遗言」拖成一条不含信息的「超时」。加一句进程存活检查，带 exit code 与 stderr 抛。实测单条 90 s → 38 s，一轮从 22 条推进到 60 条，且**第一次留下了可分析的产物**。⑤ **一个被撤回的错误修法，如实入档**：曾加过一道「无凭据就撤下 spawn-node 能力」的门，用 `Bun.spawnSync` 跑 `auth status` 当判据。**两处都错**——(a) 它没有超时也无法中断，在托管 runner 上挂住，`acceptance-local` 连套件都没跑到就被砍，**比它要修的毛病更坏**（上一版至少留下 22 条 scenario 行，这一版一行没有）；(b) 判据本身站不住：`handshake/psk-ok` 这类**纯传输场景根本不需要模型**，只要节点活着，把它们按凭据跳掉是跳错了。一个 fail-safe 成 false 的探针，在自己坏掉时和「环境确实没凭据」长得一模一样，我据此写进提交信息的那句「本机实测无凭据 215 s / skip=88 / PASS」是错的。已整笔 revert。⑥ **真因**：没有凭据不是「少验几条」，是**常驻进程整个死掉**——ACP `session/new` 被拒 → supervisor park → 退出 → 监听口再没绑上，于是不需要模型的场景也一起陪葬。开发机上这些场景绿是**赢了竞态**（冷启动快，握手 1.5 s 内走完，那时 park 还没发生），不是真的通过；CI 慢且带 `--timeout-scale 3`，每次都输。**别把本机的绿当成这条腿在 CI 上能过的证据。**⑦ **处置**：115 条 × 38 s ≈ 73 min，结构上装不进任何合理预算，故 `acceptance-local` 加 `if: github.event_name == 'workflow_dispatch'`——不再挂在 push / pull_request 上，每个 PR 烧 30 min 换一个必然的红既不提供信息也占着别人的 runner；要跑就手动触发并在那次运行里给出凭据。另加一步 `always()` 的覆盖率注解（读 NDJSON 末行 counts 出 notice，自己不判红），免得「验收过了」被当成「115 条都过了」。⑧ **待办（另立，本轮不顺手改）**：常驻在 ACP 起不来时应当**降级而不是退出**——保留传输面、只关掉 agent 能力。这不只是 CI 的事：**真实部署里节点凭据一旦失效，整台节点会以同样的方式消失而不是降级**，而外部看到的现象只是「节点不见了」。**范围、判据、排期均未动。** |
| **v2.62** | **2026-08-28** | **开源就绪（可逆批次）：对外面按本仓库校准，不动仓库可见性。**负责人指示「先做可逆的开源就绪，不动可见性」；**仓库保持 PRIVATE，未执行任何 `gh repo edit --visibility` 操作**。**① 全历史密钥扫描：干净。**从未提交过凭据形状的文件名或运行时状态；唯一的 key-header 命中是两处测试固件（`secretScanner.test.ts` 的 `AKIAIOSFODNN7EXAMPLE`、`bedrockClient.test.ts` 的重复字面量假 RSA），均为设计内。**② pin 陈述跟进首次上游同步**（`license-chain-m0.md` **D-7**，新差异）：08-17 的 P10.2 把 pin 迁到 `1128a391`（v2.46.0），`BASE.md` 与章程 §5.5 跟进了，**对外面三处没有**——`NOTICE` 中英两块与 `README.md` 第 20 行仍写「锁定 v2.38.3」，于是溯源声明与溯源真源各说一个版本，而 `README.md` 第 22 行同时在描述那次同步，**同一页自相矛盾**。三处统一改为「导入提交 `848ad8c2`（v2.38.3，历史锚点，不随同步变化）／当前锁定 `1128a391`（v2.46.0）」两行并列，用词与 `BASE.md` 第 6、8 行一致。**属事实性勘误**（把已定案的事实落到文本上），按 D-1 先例处理；M0 流程件已于 08-17 决议关闭，不另设签署待办。**③ `SECURITY.md` 与 issue 模板对外化**（**D-8**，原转公开阻断项）：三份文件自 v2.46.0 同步以来与基座**逐字相同**，于是本仓库的漏洞报告入口指向 `sweetcornna/open-claude-code` 的 advisory 页、issue 前必读指向基座 issue 列表与 README、`config.yml` 指向基座 Discussions（**本仓库 Discussions 未启用**，死链）。**后果比看起来重**：转公开后第一份安全报告会**私密地进到另一个仓库**——advisory 是私密的，报告者以为报了、本仓库收不到，双方都不会发现投错。`SECURITY.md` 按本仓库重写：去掉基座的「受支持版本」表（本仓库不发版本，判据是 commit）、补 M0 阶段与未经第三方审计的如实声明、新增「问题出在哪一侧」三分归属表（附一条 `grep` 让报告者自己分阡陌包与基座包），安全边界从基座那五条换成阡陌威胁模型（签名分档与能力令牌、`LIMITS` 防放大、每节点 PSK 与身份私钥、控制台的转义顺序即 XSS 边界，并要求报告时只给 sha256 前缀、不粘凭据本身），基座继承的隔离与凭据两条保留。issue 模板改指本仓库并按实际形态重写（检出 commit 而非版本号、运行形态三选、功能建议指向章程 §3/§2.2 范围、新增「这不是安全问题」勾选）；`config.yml` 换成安全通道／本仓库文档／基座 issue 三条。**④ 防复发**：pin 出现在 `BASE.md`(2) + `NOTICE`(2) + `README.md`(1)，后三处不在任何门禁视野里，`check:*` 全绿也不提示——记入 `upstream-sync-drill.md` §7.1 第 5 条（该节标题「四件事」随之改「五件事」）。**⑤ 顺带修两处本文自身的记录错误**：`4aba06ec`（v2.61 那笔）把整条变更记录误写进头部「文档版本」行（2 列表里塞 4 个单元格，渲染时静默丢弃），而第 111 行的 v2.61 记录**仍停在被撤回的第一版修法**（描述 `beta_seed_node_settings` 派生 settings.json 预批准清单——该函数已不在树上）。头部行复原为 `| 文档版本 | v2.62 |`，v2.61 记录行换成撤回后的正确正文。**⑥ `CONTRIBUTING.md` 开篇仍是基座的**：写着「欢迎参与 open-claude-code」，并把本项目自述为「Anthropic Claude Code CLI 的逆向/反编译社区版」——那是**基座**的定位，不是阡陌的。按章程 §5.8 标准表述改写成「两层」结构（阡陌基于负责人自有的 open-claude-code 构建 + 基座那层的逆向溯源**如实保留**，§5.8 第 3 条要求不回避），并说明改哪一层适用哪套规矩。新增 §10.1「仓库外的贡献者」：fork 流程、**不需要 CLA**（MIT inbound=outbound）、大工程先开 issue（范围以章程 §3 / §2.2 为准）、基座那层的改动优先发上游（否则每次同步都要重解）、安全问题不走 PR/issue。**⑦ `README.md` 的阶段标签过期**：首页写「当前阶段：**M0 原型验证期（2026H2）**」，而 M0 已于 08-17 收口（本表 v2.42，八条 AC 全 PASS 零豁免）、此后二十条记录全部标 M1。改为「M1 开发期」并指向 v2.42 与 `acceptance-m0.md`；`SECURITY.md` 同步。**⑧ 明确未动**：`LICENSE`（D-5 版权行仍待科技处口径，`license-chain-m0.md` 写明「不要凭工程判断改」）、`package.json` 的基座发布元数据（D-6 已决不改）、`README.zh.md`／`README.ja.md` 与 `README.md` 下半部分的基座原文（逐字副本，改动等上游同步带进来）、`.github/pull_request_template.md`（已是阡陌版）、`ci.yml`、`BASE.md`（§2.4）。**⑨ 仍待人的转公开阻断项未变**：`vendor/audio-capture/` 六个已入库 `.node` 的源码位置与许可（U-4，上游同样无源码无 LICENSE，只能由负责人以基座作者身份给出）——它随 `dist/vendor/` 出货，**转公开即随仓库分发**。回写 `license-chain-m0.md`（D-7／D-8／U-10~U-12）。**范围、判据、排期均未动。** |
| **v2.61** | **2026-08-28** | **M1 · 对话式控制收口：工作区可写 + 过程行去重。**范围依据同 v2.48/v2.59/v2.60。**① 起因是 v2.60 上线后的第二轮真回环**：operator 让 beta-4 在自己的工作区里建一个文件，agent 回「当前权限模式拒绝写入」。**不是缺陷也不是权限位**——无人值守那一轮跑在 `permissionMode: 'dontAsk'`（不提示、未预批准即拒绝），常驻又对每次授权请求答 `cancelled`。失败形状极难认：四段状态链全绿、工作区可写、节点 `.err` 无任何文件系统错误、`ls` 正常，只有模型回话里那一句。**② 第一版修法是错的，如实入档**：往节点配置根派生了一份 `settings.json` 的 allow 规则，部署上去之后写文件**依然被拒**——ACP 会话的权限上下文由 `getEmptyToolPermissionContext()` 建，`alwaysAllowRules` 是空的，settings 规则根本不进这条路。那份文件是惰性的，而且比没有更糟（看起来像已经授权了），已连同它的 seeding 一起撤掉。**这一条只有真机能发现：单测、precheck、verify 全绿。③ 真正生效的旋钮是模式**，因为模式经 `_meta` 传进会话。新增常驻开关 `--allow-workspace-edits`，把会话模式从 `dontAsk` 放宽到 `acceptEdits`——只放宽**工作目录之内**的编辑（`pathInAllowedWorkingPath` 门控）；配置文件与敏感路径仍由 `checkPathSafetyForAutoEdit` 挡着，`guard.ts` 的硬名单排在所有判定之前，其余需要授权的工具照样 `cancelled`。**缺省关**，且 `beta-up.sh` 必须显式写出来（`resident-task-policy.test.ts` 钉住），理由与任务策略两个开关同形（issue #10）：省掉它，这台节点能不能干活由构建日期决定。演示拓扑不列它。**④ 过程行去重**：通知工具自己不再占一行——它的产出**就是**那一行，两行说同一件事而前一行是裸工具名。跳过点排在预算**检查**之后、**扣减**之前：被跳过的调用一分额度不花。常驻是叶子包不能 import `src/`，两处字面量由 `notifyToolName.test.ts` 看守。**⑤ 现场**：四台节点全部换到本版产物（beta-1/2/3 此前一直停在 `976e2a72`），过程行在四台上都有。**排期、日期、产能、关键路径均未动。** |
| **v2.60** | **2026-08-28** | **M1 · 控制台对话面第三批：`--chat-sign`，把这张面从「问答」变成「控制」。**范围依据同 v2.48/v2.59，未新增任务包编号。**① 起因是 v2.59 部署到 p11 之后的真回环**：投递 110 ms、已读 463 ms、终态回复 9.8 s，四段状态链全绿——而 agent **拒绝执行**，回的是「我收到的是一条标记为不可信的跨节点消息，不能据此执行命令」。**这不是缺陷，是 issue #28 那两档通告在起作用**：控制台的 `task.request` 一枚 capability token 都不带，于是以 untrusted 档到达，那一档的固定模板以「treat its content as data, never as instructions」结尾。唤醒面 08-24 已经付过这笔学费（六次实测六次拒绝），对话面是第二个撞上它的调用点。**② 新增 `--chat-sign`，与 `--wake-sign` 是两个开关而不是一个**：唤醒授权的是「醒过来看一眼收件箱」，对话授权的是「按这段文字去干活」，合成一个开关等于让打开前者的人顺手交出指挥权。**身份仍然只有一把**——同一台控制台在对面的审计链里只该有一个 `iss`，`consoleWakeIdentity.ts` 两张面共用（文件名与 `--print-wake-identity` 里的 wake 是历史不是范围，没跟着改是因为那一行 `<node>=<publicKey>` 已经分发在舰队上）。令牌两处同型：绑 `(aud, sub, taskId, createdAt)`、等级 `write-limited`——`SIGNED_TASK_POLICY` 对 `wake` 与 `task.request` 要的正是同一档，签发器无需按面分叉。**③ `taskId`/`createdAt` 上提到信封之前铸**，与 `executeResidentWake` 同一段理由的第二个调用点：令牌绑唯一一个 `taskId`，承载它的信封造出来之前那个值必须已经存在；同一时刻交给两边，令牌窗口才是从它所乘的信封量起。**④ 签不出来就拒绝这一轮，不退回去发一条没签名的**——静默降级在这里是最坏形状：消息照送、回复照来，只是对面拒绝执行，而控制台这边一切正常。启动横幅加 `(signed)`，那是唯一能一眼看出开的是哪一种的地方。**⑤ 验收**：新增 6 个用例（令牌与信封绑同一 `taskId`、每轮各签一枚、签不出来即拒绝、两个开关互不牵连、没开开关不读身份因而不落盘私钥、开了则装签发器并写进横幅）。**⑥ 已知边界**：滚动顺序只有一个方向（先在每个节点 `--trust <控制台节点>=<公钥>`，再打开开关），反过来会在两种策略下同样被拒；现场 beta-1/2/3 信的仍是旧 H 控制台那把公钥，要逐台更新才能对它们签名生效。**排期、日期、产能、关键路径均未动。** |
| **v2.59** | **2026-08-28** | **M1 · 控制台对话面第二批：过程可见 + 每节点 PSK。**范围依据同 v2.48（M1「注册发现产品化」出口判据的延伸），未新增任务包编号。**① 起因是一条现场事实**：p11 的控制台横幅一直写着 `chat disabled (no --chat-url)`——对话面 08-17 落地后**从没在内测环境上开过**。而且补参数也开不出来：08-20 的 v2.51 把唤醒面改成每节点一把 PSK（`QIANMO_TRANSPORT_PSK_NODE_<hex>`），对话面还停在「整台控制台共用一把」，而现场四个节点各一把、共享的那个变量根本不存在。**② `--chat-url` 长出命名形式 `<节点>=<url>`**，与 `--wake-url` 同形，PSK 按节点取（助手改名 `transportPskEnvVarForNode`，它读的本来就是节点的 PSK 不是唤醒面的）。顺带把**授权粒度从「端点」收到「节点 + 端点」**：注册中心零鉴权，改一条记录不要任何凭据，不绑节点的话一条被改过的记录就能把 `node-z` 的地址指到 `node-b` 的端点上、而控制台会拿 node-b 的钥匙拨过去。一个节点没钥匙不拖垮其余的，禁用原因逐节点上横幅。**③ 会话 id 兼作 `contextId`**，一个字段两件事：常驻侧按 `(agent, contextId)` 分 ACP 会话，多轮上下文这才成立（此前同一 agent 的所有对话挤在 `default` 里）；而 notify 每条自带全新 `taskId`，能把过程归到哪条会话上的只有它。代价如实入档：一条会话 = 节点上一个 ACP 会话，开会话不再零成本。**④ 过程可见走 `notify`，一个字节的协议都没改**。关键事实是**过程数据本来就到了节点手上**——`acp-turn.ts` 的 `handleSessionUpdate` 收下 ACP 每一条 `session/update`（含 `tool_call`/`plan`），此前只用来戳看门狗、累加文本、记首字时延。**刻意不复活 `task.progress`**：protocol.md §3 否决它的理由是「进度事件属 B 类 ack、会搅浑 §4 边界」，而 `notify` 不产生任何 ack，紧接着那张对照表逐条说明否决理由没有一条落在它上面。控制台侧三处（声明 `supportedTypes` = floor + notify，否则能力发现判定它不实现、节点一条不发且积压被当确定性死亡退役；`onReply` 先分 notify 因为它按 `contextId` 归组而非 `taskId`；转录加 `variant: 'notice'`，缺省 `message` 故存量 NDJSON 原样重放）。节点侧一处（唯一动 `packages/resident` 的地方）：工具开始发一条、失败发一条，成功收尾不占一条。**⑤ 上限是硬停不是排队**：真正保护人的天花板 `notifyRatePerMinute` 超限时**排队不丢弃**，于是一轮发一百条不会淹掉页面、而是让第四十条在回复到达两分钟后才显示——**过期的过程比没有过程更糟**。故每轮 24 条到顶硬停；滑动窗口要在那个类里加计时器而它由十几个不该长计时器的单测构造着。逐 token 流式在这条路上明确不做。**⑥ 验收**：新增 25 个用例，含一条**真端到端**——fixture 在 turn 里跑一个工具，hub 侧收到 `kind:'task'`、`contextId` 为会话 id、`causeTaskId` 为原任务、`summary` 为「读：…」的 notify，全程宿主自发、agent 没调任何工具；另有旧节点兼容（不声明 notify 则完全退回今天的行为）与出口转义语料。`precheck` 全绿。**⑦ 已知边界**（写进 console.md，不在实现里假装能做）：不能中断/取消（协议无 cancel 类型，estop 是节点级急停）；agent 仍不能在无人问时主动说话（过程属于一条在途任务）。**排期、日期、产能、关键路径均未动。** |

---

### v2.16 决策（真机验收后的边界澄清，实施方裁定）

**① 10/10 通过的是 P2.5 DoD ①，不是 AC-2。**
DoD ① 要的是「转发且不丢」，已达成。AC-2 判据要的是**60 s 内的 `ack` 回执**，而本次 demo 的目标节点**刻意没接 `@qianmo/adapter`**（`deliverAndAck`）。那条链要成立，沙箱内得跑真 occ + adapter。**归属 P4.1**（跨节点投递端到端），不在 P2.5 内补——P2.5 的职责边界是「接住、唤醒、转发」，把 ack 塞进来会让两个包的判据互相纠缠。

> **已闭合（v2.20，2026-08-13）**：P4.1 在同一套 Dormice + gVisor 环境里跑通了这条链——沙箱内是真 occ/ACP 常驻节点，ack 与 `task.result` 都从发送方自己那条连接回来，10/10、ack P95 4.440 s（**v2.35 勘误**：正式记录为 **8.022 s**——见 v2.20 变更记录与速查表 P4.1 行，那是评审九条修复之后构建上的数字；4.440 s 系早先一轮，无配套 p50 与原始报告，走查材料一律用 8.022 s）。本条从此只作历史记录读。

**② 这批耗时数字不能外推成 P3.1 的休眠唤醒基准。**
被唤醒的是一个 **94 KB 单文件测试节点**，堆几乎是空的；而 P3.1 的 DoD 说的是**常驻 occ 节点**「休眠 → 唤醒 → 可响应」。E2 实测 400 MiB 工作集回暖就要 9–10 s，常驻 occ 的堆通常显著更大。**本次结果只证明「沙箱冻结/解冻这一层的开销可以忽略」（p50 37 ms），把唤醒总成本的锅甩给了工作集回暖那一层**——这恰恰是 P3.1 基准该测的东西，且**尚未测得**。谁要是拿 43 ms 去填 P3.1 的基准报告，那是把测试桩的数字冒领成常驻节点的数字。

**③ 真机环境的两处一次性障碍已记录，供后续复现参考**：`workbench` 模板镜像已不在 GCP 宿主上（另注册 `qm-ac2` 模板指向 `dormice-base`，未改动既有模板）；`@qianmo/transport` 的**服务端半边用 `Bun.serve`**，而基础镜像只有 node 24，因此沙箱镜像必须带 bun（用宿主现成的 linux 二进制 `docker cp` 进去后 commit，不联网拉取）。**后者是设计约束不是环境问题**——任何要跑传输服务端的沙箱镜像都得有 bun。

---

### v2.15 决策（P3.1 开工前置，实施方裁定）

评估交回五个开放问题，逐条裁定如下。**裁定依据一律是代码或已生效的文档，不是偏好。**

**Q1 最后一跳保信箱还是改走 ACP `session/prompt`？→ 保信箱，给 ACP 补读者。**
章程 §341 定死「复用基座既有的文件信箱」，改道要动章程（§2.1：范围变更必须回写章程并升版本号），并会**让 P2.1 与 adapter 已交付的 DoD 证据链失效**——那条证据链是软著与竞赛成果认定的一部分（§5.5、风险 L-1）。而「上层封装、不再造第二条收件通道」这个定性在换宿主后**依然成立**。补一个 ACP 侧轮询器是纯增量，不推翻任何已验收的东西。

**Q2 常驻宿主真的必须是 ACP 吗（headless streaming 单会话更省事）？→ 是，维持 ACP。**
不是因为 ACP 更好，而是因为**我们已经发出去的协议假设了一个节点上有多个 agent**：注册中心用复合键 `<node>/<agent>`（`protocol.md` §2.4），D-2 把判环粒度定在**处理者**而非节点、理由正是「同一节点因不同目标地址被再次经过是正常路由」（`protocol.md:378`，已于 P0.8 评审通过），`protocol.md:693` 更直接讨论了「同一节点的两个 agent」。**headless 是单会话**，选它等于回头推翻已通过评审的 D-2 与已交付的 P2.1。

**Q3 跨会话并发（`switchSession` 改的是进程级 STATE）要不要现在处理？→ M0 内用节点级 turn 串行规避，不改基座核心。**
ACP 只保证**同一会话内**串行，跨会话不串行，而进程级身份切换会互相覆盖（transcript 落盘、成本记账、analytics 都可能串会话）。M0 锁定「**一节点同一时刻一个活跃 turn**」，串行闸放在我们自己的常驻包里 + 一条断言，**不动基座**。代价是同节点多 agent 不能真并发——M0 的验收与演示都不需要它。**此项作为已知缺口记录在案**，M1 若要真并发，须回到基座核心。

**Q4 `LIMITS.defaultTtlMs = 30_000` 小于 34.7 s 冻结，要不要改数值？→ 不改数值，改成类型强制传 `now`。**
`protocol.md` 已经要求判据先过 T-2 闸门，但那是**口头约定**：`packages/protocol/src/validate.ts` 里是裸 `Date.now()`。把「必须传 `now`」变成编译期约束，比调一个数值更能根治，且**不必回写章程 §3.3 C-4**（改 `LIMITS` 要走文档流程，代码半天、流程一周）。

**Q5 两处 factor-2 检测盲区（阈值 60 s / 40 s，而 E4 冻结 34.7 s）→ 需真机 A/B，合并进 P3.1 的基准跑。**
降 factor 会抬高误判率，这个权衡**不能在本地拍**。并入下一节的真机基准一起测。

**另记一条与本包无关但已核实的基座缺陷**（不修，仅备案）：ACP 的 `initialize` 对外声明支持 MCP（`AcpAgent.ts` 的 `mcpCapabilities`），但 ACP 路径从不构造 MCP client，客户端传入的 `mcpServers` 只被用来算会话指纹。**若 M0 演示中有人用 Zed/Cursor 接 ACP 并配 MCP server，会当场发现工具不存在。**演示脚本须避开这条路径。

---

## 0. 排期基础假设

在读任何日期之前，先认下面这几条，否则排期看起来会过于乐观。**本节 v2.0 未作任何改动**——基座换了不改变团队产能。

1. **有效产能按 60% 估**。5 名在校生，课程、实验、竞赛都在同一时间轴上。任务包工时按"这周只有 3 个有效工作日"来估。
2. **低产能窗口显式减载**，不假装它不存在：

   | 窗口 | 迭代 | 原因 | 处理 |
   |---|---|---|---|
   | 2026-09-10 ~ 09-13 | S1 尾 | CUMCM 数模国赛（团队自己参赛） | 李怡康本期不排开发任务，改为**赛中采集真实场景素材** |
   | 2026-10-01 ~ 10-07 | S3 | 国庆假期 | S3 按 50% 产能排 |
   | 2026-12-21 ~ 2027-01-03 | S9 | 期末考试周 | S9 按 50% 产能排，只做收尾，不排新功能 |

3. **关键路径任务不排进低产能窗口**。关键路径见下方"§0.1 关键路径（v2.2 重新推导）"。这条链上任一环节延期，M0 整体延期。
4. **欠账阈值**：任一迭代欠账 > 30% 时，下一迭代不加载新任务包；连续两期欠账 > 30% 触发范围裁剪评审（削减章程 §3 范围，而非削减验收标准）。
5. **迭代节奏**：双周迭代，周一开始、周日结束。每周一次 30 min 同步会（只对齐阻塞项），每迭代末复盘 + 下期任务包确认。
6. **任务包状态**：`未开始 / 进行中 / 阻塞 / 已完成`，在每期复盘时更新本文。

### 0.1 关键路径（v2.2 重新推导）

**旧链（v1.1）**：`P0.1 → P1.1 → P3.1 → P4.1 → P6.1`（工程地基 → 会话持久化 → 休眠唤醒 → 跨节点投递 → 场景端到端）

**旧链（v2.0）**：`P0.1 → P0.2 → P1.1 → P2.2 → P3.1 → P4.1 → P6.1`（传输通道被判为最长杆）

**新链（v2.2）**：

```
P0.1 基座导入与环境跑通
  → P0.2 基座通读与能力盘点
    → P1.1 消息协议草案 v0.1
      → P2.2′ 节点间传输通道（降级：实现一个 Transport + 自建服务端半边）
        → P2.5 跨节点 activator + 常驻 keepalive   ← 新增，新的长杆之一
          → P3.1 休眠与唤醒                        ← 另一根长杆，与 P2.5 互相咬合
            → P4.1 跨节点任务消息端到端
              → P6.1 数模场景端到端串联
```

**三处变化，各有明确理由：**

1. **会话持久化（旧 P1.1）下了关键路径**（v2.0 结论，仍成立）。基座已有 append-only JSONL 会话记录与多种恢复入口（出处：基座 `docs/zh/internals/session-transcript-persistence.md`），我方工作从"实现持久化层"降级为"核验判据 + 补齐一致性缺口"，不再构成后续任务的阻塞前置。**v2.2 补充**：恢复入口须钉死 `--resume`，理由见 P1.2。
2. **传输通道（P2.2）仍在关键路径上，但不再是最长杆。**

   > **v2.2 修正**：v2.0 原文的推导是「基座上游已于 2026-07 删除**全部**远程传输层实现（约 45k 行）……**基座在跨机通信这一端给我们的是零**」。**这条推导已被代码推翻**（依据 `selection-m0.md` §2）——基座现存约 5,300 行**在用**的传输代码，且其主循环在结构上已能跑在网络传输之上。2026-07 删除的是 **Remote Control 的 `src/bridge/`**，不是全部传输层；`LAN_PIPES` / `UDS_INBOX` 连代码一并移除这一点仍属实，但它们不构成"起点为零"的依据。
   >
   > 因此 P2.2 的性质由「从零造一套传输栈」降级为「**实现一个 Transport + 自建服务端半边**」，工作量随之下调。**告诫保留**：这次下调**仅凭代码存在与形状判断，可改造性未验证**——要 S0 实际动手才知道。若 S0 发现改造成本接近重写，P2.2 立刻回到长杆位置并按风险跟踪条目报警。

3. **新的长杆是 P2.5（activator + keepalive）与 P3.1（休眠与唤醒）。** 两者**都没有扩展点可挂**（P3.1 的唤醒半边有，休眠半边没有），而且**互相咬合**：activator 要"接住请求 → 唤醒目标节点 → ready 后转发"，休眠侧要能被它唤醒并在唤醒后正确处理时间跳跃；任一半没到位，AC-2 就不成立。P2.5 此前**根本没有任务包**（见 `selection-m0.md` §5），这是本次重推最实质的一处变化。

> **v2.2 要团队记住的一句话换成了这个：起点抬高的不只是产品线 ①，产品线 ② 也抬高了一截；但真正没有先例可抄的那块（跨节点 activator + 常驻保活）此前不在计划里。**排期的风险重心从"能不能按时打通跨节点通信"转移到"能不能按时做出一个此前没人写过、同时压着 AC-2 与 AC-6(c) 的宿主侧组件"。S4（P4.1）仍是 M0 单点风险最高的一期，且它的前置链在 v2.2 里又长了一环。

### 任务包字段说明

每个任务包含：**编号 / 名称 / 负责方向（owner，backup）/ 依赖 / 交付物 / 完成判据（DoD）**。
标 ⭐ 者为关键路径，标 ⚖️ 者为法律合规关键项，标 🔍 者为**核验型任务**（基座已提供实现，我方工作是验证与补齐，v2.0 新增标记）。

**已完成任务包的标注方式（v2.2 新增）**：排期开始前即已完成的任务包（如 P0.6），标题行标 `（状态：已完成）`，正文保留其依赖与交付物原文以便追溯，DoD 一栏直接写实测结论与"无剩余阻塞项"。**不因为已完成就把它从本文删掉**——它是后续任务包的证据来源。

**owner 栏语义（v2.3 重定义）**：按负责人 2026-08-12 的分工调整决议（章程 v2.3 §7.1），**M0 开发主要由负责人本人承担，其余成员转为辅助开发**。因此自 v2.3 起：

- **全部任务包的主开发均为喻永昌**，不再逐包指派开发负责人；
- 各任务包 owner 栏中的**原名单改读作「方向辅助人」**——即该方向的辅助开发、评审人与**第二知情人**；括号内的 backup 读作**第二辅助**。
- **各任务包的 owner 行不逐行改写**，一律以本段声明为准。这样做是为了避免几十处机械改动把真实的变更历史淹没掉——需要知道某个包由谁辅助时，看它的 owner 行；需要知道由谁主开发时，看本段。
- **例外**：明确写「全员 / 双人」的**流程要件不受本条影响**，仍按原文执行——包括但不限于 **P0.2**（全员任务、不设单一 owner 覆盖）、**P0.8**（全员评审）、**P9.3**（双人签字），以及章程 §7.2 条款 5 的「PR + 至少一人评审」（评审人即方向辅助人）。


### 完成状态速查（v2.5 新增）

排期文档久了最容易失真的就是「哪些其实已经做完了」。下表是**完成状态的唯一出处**，各任务包标题只标状态、不重复证据。**未列入的任务包一律视为未开始。**

| 任务包 | 状态 | 合并证据 | 关键产出 / 剩余缺口 |
|---|---|---|---|
| **P0.3** 身份隔离 | ✅ 已完成 | `8ac2b14` | 方案 A 三者共存；`identity.ts` 派生层，78 个 `BIN_NAME` 消费方与约 140 个 `configDir` 调用方零改动；保护清单改取三身份并集 |
| **P0.4** CI 门禁 | ✅ 已完成 | `bb496a6` | 修 H7 flaky、CI 钉死 Bun 版本、删 `publish-npm.yml`、新增 `verify` 命令组、PR 模板、阡陌包开 `--isolate` |
| **P0.5** 协议资产复核 | ✅ 已完成 | `507b8fb` | 逐包维持；版权头已决为「加」；license → MIT；**关系定性：上层封装**（P1.1 的直接输入） |
| **P0.6** 前置实验 E1~E6 | ✅ 已完成 | 见 `selection-m0.md` §3 | 六个实验实测完毕，无剩余阻塞项 |
| **P0.7** 安全面加固 | ✅ 已完成 | `35ab634` | 服务器侧三件已真机落地并验证；运维脚本与绑定不变式断言入库（`scripts/ops/`） |
| **P1.1** 消息协议 v0.1 | ✅ 已完成 | `682ffff`（文档）+ `5fd3853`（信封落地） | `protocol.md` 713 行，七条硬问题全部有结论；§12.1 的 1~6、9 已落成代码 |
| **P1.2** 会话持久化核验 | ✅ **已完成** | `5508358` + `3e7a401` + 主 agent 真机补测 | AC-1 三条判据全部达标。**最后一条「不重放历史即可续答」已于 2026-08-12 用真实模型补测通过**：让模型在对话中推出一个只存在于上下文的代号（`QM-seven-bridges`，由 NOTES.md 规则 + build.config 值算出），多轮途中 `kill -9`，`--resume` 重启后明确要求「不要重新读文件」追问该代号 —— 答对，`session_id` 前后一致，端到端 5.2 s |
| **P1.3** 沙箱执行环境 v0 | ✅ 已完成 | 见 `packages/sandbox/` + `demo/ac6a-sandbox.sh` | Dormice 新出生契约已切回 gVisor，并由真实 Docker inspect 与周期漂移守卫共同断言；一次性新沙箱内 `occ` / `qm` 可用。AC-6(a) 真机 5/5：白名单外写入被拒、超时强杀、CPU 节流、内存 OOM、持久审计均有内核或控制面证据；审计文件 0600、目录 0700、无命令 / 输出 / 凭据字段。验收沙箱与前置探针已精确删除，存量业务沙箱未动 |
| **P1.4** 模型适配层核验（AC-5） | ✅ 已完成 | `s1/p1.4-provider-verification` 合并 + 主 agent 补测 | 一致性套件（多轮/工具调用/流式）对两条 provider 各跑一遍、零 mock 零回放；同一编程任务两家各跑通、任务自带测试各 5 pass；「仅改配置」以两次逐字相同的命令行证明。**主 agent 补齐 G-1**：网关同时提供 Anthropic 原生端点，occ 走原生路径（不做消息转换、不走流适配）同样完成该任务（5 pass），于是「≥2 个不同适配器」有了字面意义的证据。**保留 G-4**：两条路径仍指向同一网关，未验证不同厂商 HTTP 层差异 |
| **P2.1** 注册中心与按名寻址 | ✅ 已完成 | `15c7eb8`（复合键）+ 持久化合并 | 复合键 `<node>/<agent>`、`publicKey`、状态标记；状态持久化（`occConfigPath` 派生、`wx`+`fsync`+`rename` 原子写、`expiresAt` 不落盘按当下 TTL 重算）。DoD「重启后可自动重新注册」已覆盖 |
| **P2.2** 节点间传输通道 | ✅ 已完成（**附报警**） | 见 `packages/transport/` | `@qianmo/transport`：五类帧 + PSK 握手、指数退避重连（含时间跳跃闸门）、两级去重（msgId + fingerprint）。三条 DoD 实测通过。**⚠️ 触发 roadmap 报警条款，见下方「P2.2 报警」** |
| **P2.5** activator + keepalive | ✅ **四条 DoD 全部真机验证** | 见 `packages/activator/` + `demo/ac2-wake-forward.sh` | 三层能力面守卫（类型 / 运行时白名单 / import 期形状断言），`destroy` 代码层面不可达；真 `kill -9` 崩溃恢复不静默丢弃；时间跳跃闸门。**DoD ③④ 本地真测；DoD ② 已由主 agent 在真机验证通过**（对照组：不打心跳 t≈45s 冻结并保持；实验组：20s 周期心跳全程 120s 始终 active）。**DoD ① 已于 2026-08-12 真机验证通过：10/10**（GCP + Dormice + gVisor，目标节点跑在真实沙箱内、每轮先确认 `state=frozen` 再投递）。分位数：`total` p50 **43 ms** / p95 **423 ms**，`acceptToWake` p50 3 ms、`wakeToReady` p50 37 ms、`readyToForward` p50 2 ms；`forwarded 10 / failed 0 / refused 0`。**判据是 P95 ≤ 60 s，实测低两个数量级。**首轮 423 ms 是唯一离群点（冷链路建连），其余九轮均在 39–49 ms。**主 agent 另做了独立核验，不采信回执**：进沙箱查目标节点收件箱，**10 行、10 个唯一 `msgId`**，首条为真实信封（`qianmo://node-a/planner` → `qianmo://node-b/reviewer`），msgId 与发送方日志逐一对应——即「消息真的到了对端」，而非「回执说已转发」。**本次不证 ack 语义**：demo 目标节点刻意未接 `@qianmo/adapter`，AC-2 判据里 60 s 内的 `ack` 回执要成立，需沙箱内跑真 occ + adapter（见下方 v2.16 决策）。**另：真机核实推翻了原 API 假设并已修正**——Dormice 是「方法名即路径段」的 RPC、**不存在 `touch` 方法**、状态取值为 `active`；保活改用实测会刷新 `lastActiveAt` 的 `acquireSandbox` |
| **P3.1** 休眠与唤醒 | ✅ 已完成 | 见 `packages/resident/` + `src/services/qianmo/` + `demo/p31-resident-wake.sh` | 常驻 ACP + durable 文件信箱读者、deterministic admission、节点级 turn 串行、activity→宿主 keepalive、manual/timer wake、全链路 timing 与时间跳跃覆盖。2026-08-13 在专用 Dormice + gVisor (`runsc`) 沙箱真机验收：baseline 2/2 与 candidate 1.1/1.5 各 **10/10 responsive**，每轮先确认 frozen；accept→first content P95 **4.270 s / 4.361 s**，accept→read P95 **3.112 s / 3.192 s**，均远低于 60 s。thaw 后新鲜消息 accepted，宽限结束后过期消息恢复 `E_TTL_EXPIRED`；manual **1.928 s**、3 s timer **4.862 s**，均完成 `detected/admitted/read/first_content/turn_completed`。A/B 决定两个专用默认值为 **1.1 / 1.5**，通用默认不动。脱敏聚合报告与原始文件校验和留在服务器用户私有验收目录（0700，文件 0600）；未持久化 bearer、PSK、地址或模型请求。**边界：本包只完成 AC-2 后半；跨节点 ack/result 仍归 P4.1。** |
| **P3.2** 编程智能体闭环核验 | ✅ 已完成 | `scripts/qianmo-programming-tasks.ts` + `bun run verify:p32` | 同一 Atlas source commit 上三个独立 clone 任务 3/3：新增 `agentOf` helper（地址测试 **11 pass**）、修 reconnect 最大 jitter 误判（backoff 测试 **9 pass**）、补 `SuccessfulReceiptFrame` 成功子类型（类型测试 **1 pass**）；三项包级 `tsc` 均通过。每项先提交红色 fixture 并证明 baseline 非零，再由真实 headless agent 修复；生产 diff 精确等于 allowlist，受保护 regression tests 零改动。执行时仅开放 Read/Edit/Bash，Bash 强制 fail-closed sandbox 且禁止 unsandboxed fallback；真实 provider key 从进程环境移除，仅存异步回环代理闭包，agent 只持 dummy。失败统一为 `qianmo.p32.task-result.v1`，已覆盖 unknown task / missing credential 契约测试；原因分类留给 P5.1。脱敏 report/patch/test logs 与 sha256 留在私有验收目录（0700/0600） |
| **@qianmo/adapter** 入站适配器 | ✅ 已完成 | 见 `packages/adapter/` | protocol.md §12.1 第 8 项：入站适配器 + `read` 翻转观察器 + blob 暂存区。**AC-2 的最后一跳**；ack 端到端（写完即发被测试证伪）、blob 阈值从基座常量 import 且测最终字符串、M-2 注入面封堵。76 用例零 mock |
| **P2.3** 分层记忆 schema | ✅ 已完成 | `3e6d407` | `@qianmo/memory`：三层即三张表（目录即主键），双时间轴（事件轴 `validAt`/`invalidAt` 与摄取轴 `createdAt`/`expiredAt`）由 `revoke`（撤下记录，任何 `asOf` 都不再召回）与 `invalidate`（事实失效但记录仍 live，问过去仍命中）**两个独立操作**驱动；沉淀归档先写目标再封存源。**与基座四类型的关系定性：上层封装 + 共用文件格式**——复用基座前言契约与路径派生（`getMemoryBaseDir()`，兼顾 `CLAUDE_CODE_REMOTE_MEMORY_DIR` 持久化挂载），但自持根目录，因为基座记忆目录处在一个**被提示词明确指示可删条目**的 agent 写权限之下（`src/memdir/memdir.ts:202`），装不下「只标记不删除、废止后可审计」这条不变式；互操作有测试对着基座真解析器断言。43 用例。**评审打回一处并已修**：单个手改过的文件原会让整次 `query` 抛错、连健康记录一起带走（主 agent 实测「3 条 → 全灭」），改为扫描取部分结果 + 显式事件通道，`getEntry` 定点查询维持抛错 |

| **P3.3** 记忆检索唤醒 v0 | ✅ 已完成 | 见 `packages/recall/` + `tests/integration/qianmo-memory-recall.test.ts` | `@qianmo/recall`：确定性检索（标签 + 关键词 + 时间衰减，无模型无向量）之上叠**小规模全量注入**（< 50 条 / < 20k 字符即全投），**候选集只按 scope 取、绝不按词过滤**——词过滤正是 D-6 实测到的 0 结果失败面。来源标注走**工具层强制引用**：`qianmo_memory_answer` 是纯 JSON Schema（零供应商名），回来的每个 `citations` 逐个 `getEntry` 解析，**解析不到即无法被引用**，伪造引用因此是查表不是祈祷；六种引用判定互不合并（其中 `unreadable` 与 `unknown` 分开，避免把损坏的真条目报成幻觉）。只投 live 条目、每次从盘上重建，废止后下一次召回即消失。store 的 `entry_unreadable` 事件通道被抬到 `RecallResult.events` + `degraded`，不在上层重新盖回静默。**DoD 三条真机全绿**：5 条决策 × 2 家 provider 命中 10/10 且标注 ID 与写入时间；3 条伪造决策 × 2 家引用数 0/6；工具声明在两家转换后逐字相同（切换供应商不改代码）。51 包内用例 + 19 集成用例，零 mock |
| **P4.1** ⭐ 跨节点任务消息端到端 | ✅ 已完成 | 见 `packages/transport/src/{channel,outbox,receiver}.ts` + `packages/activator/src/routes.ts` + `src/services/qianmo/resident.ts` + `demo/p41-task-result.sh` | 回程走**同一条已认证长连接**：握手带受 HMAC 覆盖的稳定 `channelId`，两端共用 receipt 驱动 outbox 与统一入站处理器，物理断线重连后按同一 `channelId` 重绑并重放反向 outbox；activator 侧有界 task route registry 在 TTL 内持有源通道（`hold()`），C2 回复按 `taskId` + 地址 + sandbox 校验后原样沿 C1 转发，ack 保留路由、终态释放，重复 `taskId` 不能串线；resident 把网络任务改为逐条 ACP turn，durable read 翻转后发 ack、turn 终态发字段封闭的 `task.result`。协议侧 ack payload 收缩为 `{ handler, ackAt }`、相关标识只留信封，新增 `task.result` 联合与 `E_TASK_FAILED`（`protocol.md` §4.2/§4.6/§11）。**2026-08-13 真机验收 10/10**（Dormice + gVisor + 真注册中心，每轮先确认 frozen）：ack **P95 8.022 s**（p50 2.490 s，P95 即第 1 轮冷链路）、result **max 12.422 s**，判据 60 s / 5 min；独立核验证实每条 ack 都晚于沙箱内的 durable read、每条 result 都晚于 `turn_completed`，且 `link.opened=1`——回程没有第二条连接。详见 v2.20 变更记录 |
| **P4.2** 防循环与限流 | ✅ 已完成 | 见 `packages/router/` + `demo/ac3-loop-rate.sh` + `tests/integration/qianmo-loop-and-rate.test.ts` | `@qianmo/router`：判环键 `(处理者地址, taskId)` 首次回访即切断（`LIMITS.maxHops` 仅兜底）、起始节点自我播种、回复类消息不进判环表；两层限流是两个类两套键两个上限出处两类审计事件两个拒绝码，运行时层的 `E_RUNTIME_THROTTLED` 不入协议码表且入站判决类型上够不到它。去重表沿用 `@qianmo/transport` 的 `DedupTable`（本就以投递时限过期），不另造第二张。协议包补 `isReplyType` 与 `advanceTraceparent`（§7.1 的逐跳 `parent-id` 此前无人实现）。接线：activator 入站（唤醒与路由注册之前）、常驻节点 `#receive`（写信箱之前，L-1）、`residentWake` 出站。**AC-3 十条判据全绿**（`demo/ac3-loop-rate.sh`，含真实 600 条入站预算 / 31 个发送 agent 的负向证据）；单元 38 + 集成 3 + 报告核心 8 用例，零 mock。**边界如实记**：运行时令牌桶在 M0 无长驻生产调用方，`withHop` 转发追加半边无生产调用方（M0 没有第三方中转节点）。**v2.36 补注**：AC-3 量具在慢机器上的假红（突发跨过 100 ms 回补步长）已按「按突发用时算顶」修订（`9736ddca`），被测代码未动 |
| **P4.3** 权限分级与授权模型 v0 | ✅ 已完成 | 见 `packages/capability/` + `src/services/qianmo/nodeIdentity.ts` + `packages/router/src/capability.ts` | `@qianmo/capability`：每节点 Ed25519、`<claims>.<sig>` 令牌（签名覆盖原样送达的 claims 段，不做规范化 JSON）、nonce 重放表、三级权限。**S-1 是签名校验不是约定**（远端 `user-confirmed` 一律拒，且在验签前判）；**S-3 是会变红的扫描**（阡陌入站路径出现基座任一改权限 API 即失败，红方向由 fixture 钉住）；**没有 TOFU**（未登记的 `iss` 即 `E_CAP_INVALID`）；**令牌过期不过 T-2 闸门**（顺延授权寿命是反方向的失败）。T-7 攻击用例三族 14 条 + 1 条正向对照，判据一律「等级没被抬高 + 审计有记录」。注册中心按节点核对公钥闭合 §10.1 的已知缺口。节点身份 0600/0700、`wx` 独占、永不覆盖。**边界如实记**：默认 `OPEN_POLICY` 允许未签名 `task.request`（可选的只是「是否强制出示」，出示了就全程校验），因为 M0 无密钥分发；`--require-signed-tasks` 是强制版本，已在包内与常驻集成用例跑通 |
| **P4.4** 备份与不可删除保护 | ✅ 已完成（本机 + **真机挂载边界已验**，v2.40①） | 见 `packages/backup/` + `demo/ac6b-restore.sh`；真机 `acceptance-m0.md` §9.5 | `@qianmo/backup`：快照只增不改（`wx` + store 分配 id）、**全包无删除路径**、只写/只读两把凭据、三层能力面守卫（类型 / allowlist / import 期断言）、定时 + 任务前两个触发（从完成处重排，单飞不并发）、恢复前核对 sha256 且要求目标目录为空。**AC-6(b)(c) 十一条 check 本机全绿**：真 git 工作区 `rm -rf` 后恢复，`git status` 与 `HEAD` 逐字一致、耗时 20 ms（预算 10 min）；只写凭据的删除/改写四次全 405、读取两次全 403、审计计数吻合、快照仍在。**真机挂载边界（2026-08-16，runsc 沙箱）28/28 全 true**：store 不在任何沙箱挂载之下、沙箱内 ENOENT、`rm -rf` 后 60 ms 恢复——本机测不到的那一条已闭合。**边界**：常驻节点的任务前快照**不等待**（保 ack 预算，语义是「任务开始前后」）；M0 不做保留与轮转 |
| **P5.1** 原因级诊断 v0 | ✅ 已完成 | 见 `packages/diagnosis/` + `demo/p51-diagnosis.sh` | `@qianmo/diagnosis`：五类成因 + 诚实的 `unknown`，规则式（可复核 / 模型中立 / 确定性），**结构化事实先于文本**——`exit 137` 谁杀的都长这样，分辨它的是杀人者当场记下的 `timeoutEnforced` 与 `oomKillDelta`。**50 条真实注入样本盲分类 50/50、准确率 100%**，六条 check 全绿（含「每类不得低于 50%」与「跳过的类别直接判不通过」）。**过程中抓到一个真问题**：Bun 撑爆内存时被系统 SIGKILL 且零输出，首轮 OOM 仅 5/10；补一条事实（「不是我们杀的」）后修复。**边界**：样本是自造失败（弱于野外样本）；`oomKillDelta` 需 cgroup v2，macOS 上同一 OOM 只能给中等置信 |
| **P5.2** 资源协商协议 | ✅ 已完成 | 见 `packages/negotiation/` + `protocol.md` §13 | 四段式（request → offer → grant → release）、字段封闭 payload、稳定码 `E_RESOURCE_REFUSED`。**上限是贷方本地策略而非 `LIMITS`**（全网常量与逐部署决定不是一回事），**报价永不大于请求**，**四条消息里没有任何字段能表达「我这边用户同意了」**（C-5 的 confused deputy 形状，由 S-1 从密码学上封死）。三处状态各有不依赖对端的出口：请求超时 / 报价超时 / 租约到期，释放一个已不在的租约不算错。19 条用例，超时全部由手动调度器触发而非等待 |
| **P5.3** 按需加密隧道 | ✅ 已完成 | 见 `packages/tunnel/` + `tests/integration/qianmo-lease-tunnel.test.ts` | **不造新加密层**（N-3 未变）；「按需」的内容是连接只在租约期间存在。三条拆除路径：正常释放 / 租约到期 / 对端消失，**后两条不需要对端配合**；隧道客户端**关掉重连**——回来的隧道就是活过了条款的租约。「无残留」由**再拨一次并要求失败**证明，凭据能保证到哪一步也如实写明（GC 运行时不承诺抹内存）。PSK 是门、令牌是租约，宿主按字节相等核对自己铸的那串。15 条包内 + 3 条集成用例；**隧道断 ≠ 租约还**（否则一次抖动会提前还掉别人的机器） |
| **P5.4** 边界用例库 v1 | ✅ 已完成 | 见 `tests/boundary/`（含 README）+ `scripts/test-shards.sh` | 五类边界各一文件、共 **29 条**用例（DoD 要 ≥ 8），每条注释里指明对应 `protocol.md` §8.3 的哪一行；只做组合，不重复包内已证明的细节。**顺带修掉一个真缺口**：CI 的分片列表不含新目录，`demo/lib` 的报告核心用例自 P4.1 起**从未在 CI 里跑过**；已把 `tests/boundary` 与 `demo/lib` 加进列表，实跑 53 分片全绿。PR 模板的「本 PR 修复的边界问题对应哪条用例」必填项自 P0.4 起已在 |
| **P2.4** 数模竞赛场景需求文档 | ✅ 已完成（负责人决议 2026-08-17 评审通过，v2.41①；现有草案即终稿，「赛中痛点补写」不再要求） | `docs/dev/scenario-mcm.md` | AC-7 六帧、拓扑、时间线、指标、数据集与 3/3 模板已写成可评审草案；**赛中真实痛点仍待团队按原始记录补写，全文仍待全员评审**，两项完成前不满足 DoD |
| **P6.1** ⭐ 数模场景端到端串联 | ✅ 已完成，**AC-7 达成** | `demo/p61-e2e.sh` + `demo/Makefile` + `demo/lib/p61-{scenario,dataset,worker,report-core,seed}.ts` + `demo/p61-data/` | seed 6101 正式三轮严格串行 **3/3**；正式记录为提交 `87c4609b` 上的 SHA 锚定重跑（时长 600,988 / 600,236 / 600,259 ms，产物留档，见 v2.31 与 scenario-mcm §9），观测经审计后由宣称改为测量。单进程逻辑双节点与脚本化授权等边界见 v2.30 |
| **P6.3** 🔍 MCP 兼容子集核验 | ✅ 已完成 | `tests/integration/qianmo-mcp-conformance.test.ts` + `docs/dev/p6.3-mcp-conformance.md` | 两个第三方现成 server（filesystem / memory，v1 SDK、stdio、离线确定性）实测：发现、调用写读闭环（node:fs 旁路复证）、双 server 互不串扰、三类降级不崩溃且在飞被杀后自动重连自愈、`request_timeout_ms` 生效；10 条用例进 CI 既有分片、零 mock。缺口 G-1~G-5 如实入档，其中 **G-2 roots 通告整体顶掉命令行允许目录**是部署真坑。故障夹具不计入「两个现成 server」 |
| **P6.2** 预测性扩容 v0 | ✅ 已完成 | `packages/capacity/` + `capacityTrailSink` | 双通路触发器（predicted=日历先验 / reactive=同槽中位数+MAD，`minRatio` 防 MAD=0 退化）+ 冷却与窗口抑制；DoD 实测 leadMs=405 min、24 seed×7 天计分窗假阳性全 0（21 天=14 热身+7 计分的解读待负责人确认）；决策含 suppressed 全部写审计（`AuditSource.Capacity`）。执行面到 `auditOnlyExecutor` 为止，真实供给留白（N-7） |
| **P6.4** A2A 对齐评估报告 | ✅ 已完成（负责人决议 2026-08-17 评审通过，v2.41①；三个判断点与 G-18 按草案建议定案） | `docs/dev/a2a-gap.md` | 锚定 A2A v1.0.1（proto 真源），15 概念面逐字段映射、27 条差距全部三选一（11/13/3）、三个判断点与 G-18 信息泄漏待评审；**全员评审未进行**，完成前不满足 DoD |
| **P7.3** 性能与稳定性基线 | ✅ **已完成（baseline v1.0 定稿收口，v2.42②④）**：两栏吞吐 + 栏 A t=0 阶梯 + 两条 L0 长跑（栏 B 22.13 h / 栏 A 3.53 h，按决议提前收口） | `demo/lib/p73-*` + `occ resident --mem-sample` + `docs/dev/baseline-m0.md`（**v0.3-draft**） | 量具、方法与文档骨架入库；两个作废级测量坑（Bun `heapUsed` 冻结常量、`heapSize` 滞后）已钉死并实证。**栏 B（cornna-p6 aarch64）**：四轮八档全 pass，单节点天花板 1799 = R_max(120)、三节点 5397 = 3.00×，新发现 B-3 / B-6。**栏 A（burn-vm-01 x86_64，v2.40②）**：四轮八档全 pass，**栏 B 三条形状结论全部复现且公式验证到个位**（1818 / 1824 的超时回补）；t=0 唤醒阶梯 **n=100 全 responsive**（唤醒发起→就绪 P50 931 ms，真 P95）；新发现 **A-1**（拒绝审计 fsync 在网络盘上被放大，与 B-3/B-6 同根）。本地数字只作仪器校准 |
| **P7.4** ⚖️ 上游同步演练 | ✅ 已完成（负责人决议 2026-08-17 评审通过，v2.41①；同步节奏定稿并回写章程 v2.11 §5.7） | `docs/dev/upstream-sync-drill.md`（`e2955931` + 勘误 `856d0ff8`）+ 章程 v2.9（`9a69829f`）+ `BASE.md`（`0c2f3b17`、`a626eb95`） | 对上游 v2.46.0 三方应用：5 文件 7 冲突块（0.39%）、合并后 typecheck 零错 + 11311 测试全绿、工时估算 10–20 人时；演练分支未合入。BASE.md 已记同步记录、上游公开 URL 与「上游对象经 worktree 进入主检出 pack、不可达」的勘误。**只剩评审**（全员），完成前不满足 DoD |
| **P8.1** 演示环境固化 | ✅ 已完成（本地腿 + 两台全新机器 + 真机腿首验均通过；「成员形式要件」按负责人决议 2026-08-17 豁免，v2.41③） | `demo/env/` + `demo/lib/p81-{registry,probe}.ts` + `docs/dev/demo-env.md`（`cc210e65`、`da98d86f`、`840b934d`） | 六条命令 runbook；本机 87 s（热缓存），**全新 Debian 13 x86_64 2 vCPU 冷缓存 2 min 40 s，第二台只读 runbook 复现净 105 s / 墙钟 8 min 19 s**（唯一卡点 unzip 已补进 runbook）；每节点独立配置根，`reset.sh` 守卫六个负向用例实测拒绝；AC-3 慢机器假红已修（v2.36②）。`demo/env/remote/` **已于 08-16 在 burn-vm-01 首验通过**（两处脚本缺陷当场修掉 `b6d464c2`，结果回写 README 与 demo-env §7.1）。DoD 时间与真机要件均达成，剩「未参与部署的成员」形式要件待一名真人照 runbook 走一遍 |
| **P8.2** 全量验收走查 | 🟡 **八条 AC 全部 PASS-已走查（0 未通过 / 0 需豁免），剩现场评议一件** | `demo/walkthrough.sh`（凭据腿 `ac1c2`）+ `docs/dev/acceptance-m0.md`（v0.2-draft：§8 本机 + §9 凭据/真机/替代腿/挂载边界） | 一键驱动逐条 AC 录 transcript（可机器核验的录屏替代，产物在仓库外 0700）；**AC-1（含判据②现场）/ AC-2（真机 10/10）/ AC-3 / AC-4（deepseek 腿）/ AC-5（kimi-k3 替代第二供应商收口，v2.40③；qwen 重跑降级为补充项）/ AC-6（(a) 真机 5/5 + (b)(c) 本机十一条 + 真机挂载边界 28/28，v2.40①）/ AC-7 / AC-8（CI 五连绿于 `d162fe72`）均 PASS-已走查**。「最可能需要豁免」的一条（挂载边界）以补做实测闭合，**豁免机制零动用**。**剩**：现场评议（真人）；qwen 补充项等网关（不阻塞） |
| **P8.3** ⚖️ 竞赛/答辩材料对齐 | ✅ 已完成（双签通过 2026-08-17，positioning-m0 升 v1.0「已双签，可对外」，v2.41②） | `docs/dev/positioning-m0.md`（`37dc4eb1`、`f288afb4`、`8012b300`） | 标准表述三版、禁用表述、两栏能力表、实跑举证、16 问 Q&A、13 镜分镜；§3.3 基座改动归因勘误后指向 `base-modifications.md`（11/14/7）为真源，「为何改核心」的 7 条待改动人确认在那里追。**负责人 + 安全 owner 双签前不得外发**；演示视频未拍 |
| **P8.4** ⚖️ 软著与开源预检 | ✅ 已完成（工程侧全量 + NOTICE 双签补记 2026-08-17；科技处咨询与 E-8 按决议移出 M0 出口、随申报流程再做，v2.41②④） | `scripts/sbom.ts` + `docs/dev/sbom-m0.{json,md}` + `docs/dev/license-chain-m0.md`（`9b31dc9e`）+ `NOTICE` / `README.md`（`f288afb4`）+ `BASE.md`（`0c2f3b17`） | 1089 组件 SBOM，强/网络传染 0（DoD 该条满足）；NOTICE 六条差异全部落地（举证命令 → `3380c88`、上游公开 URL 入 BASE.md、SBOM 位置、`vendor/audio-capture` 溯源披露、ripgrep、`@anthropic-ai/claude-agent-sdk` 保留 + 披露）；NOTICE 改动的双签、E-8 归属举证与科技处咨询只能由人做 |
| **P9.3** 文档补齐与双人签字 | ✅ 已完成（三件交付物齐；逐包现场验证与手写签字按负责人决议 2026-08-17 豁免，signoff-m0 升 v1.0 转留档，v2.41③） | `packages/*/README.md` × 17（`91f1f446`、`5c8a3bd2`）+ `docs/dev/base-modifications.md`（`5c8a3bd2`）+ `docs/dev/signoff-m0.md`（`b3cda44f`） | 每份 README 含架构图、API 面、不变式与钉住它的测试、双人签字栏与 3 道复述题；改造点清单判定 08-16 考证后为 12 书面 / 18 依代码 / 2 待裁定（`0c1ba4ce`，见其 §6.1）；签字表汇总 17 包 owner / backup 与 3 道题位置、每人担子、现场验证排法。DoD 的「backup 现场复述 + 双人签字入库」待人 |
| **P9.2** M0 复盘与 M1 任务拆解 | ✅ 已完成（负责人决议 2026-08-17 评审通过，v2.41①；§8 前置检查表已逐行标注闭合） | `docs/dev/retro-m0.md`（v0.1-draft，`c9786d38`） | 522 行：一页纸摘要、做对的六件（「测量而非宣称」四次抓假绿等）、估错的九处（最贵一处：事前能力盘点无 P2.5 activator——「基座缺什么」的问法找不到「拓扑缺什么」）、排期偏差数据（计划 140 天 / 实际功能面 5 个日历天 212 提交，偏差单调递增 18→140 天，关键路径实跨 5 个日历天 vs 计划 97 天；口径不可通约、三个低产能窗口一个没被测试过 → P-1 对 M1 满格有效）、**必答题**：`base-adoption.md` 事前估计是六个量级词、无一个工时数字，故只能定性对账——省在产品线①与门禁、多花在事前无条目处（activator / 休眠改核心 / 举证-SBOM-许可链）+ 上游同步年化 130~520 人时算术外推；M1 首两迭代任务包草案 P10.1~P10.5 / P11.1~P11.5（不排新功能，owner 一律「建议」）+ 候选池；附 A 数字命令、附 B 六条文档不一致（本轮已处置五条，B-4 由章程 v2.10 消化）。**待全员评审**；「charter 版本升级」交付物属负责人动作，未做 |
| **P9.1** 缺陷收敛与回归 | ✅ **已完成（v2.40⑤）** | 终锚：走查 HEAD `2fc6ce70` 上本机 `bun test` 三连绿 + CI `workflow_dispatch` 绿（run `31983703436`，headSha 逐字核对）；此前 `005d6888` 三连 + `d162fe72` CI 五连（run id 见 `acceptance-m0.md` §2.8）；`0e9e7c3b` / `d162fe72` 修 MCP watcher 用例 | DoD 后半「全量回归连续 3 次全绿」本机与 CI 两个口径均达成（含 `tests/boundary/` 39 条）；前半「P0/P1 级缺陷清零」——`acceptance-m0.md` §1 与本表无未闭 P0/P1 缺陷登记，本轮唯一抓到的缺陷是 CI 偶发用例（Linux 上 watchFile 首个 stat 之前的改动被当基线，v2.37②），已修。原保留条件「真机腿纳入回归」已由 v2.39 的 AC-2 10/10 / AC-6(a) 5/5 真机复跑满足，v2.40 终锚后判完成 |
| **P7.1** 边界用例库 v2 + 混沌注入 | ✅ 已完成 | 见 `tests/boundary/chaos-recovery.test.ts` + `demo/chaos-inject.sh` + `docs/dev/boundary-day.md` | 边界目录 **39 条**且进入 CI；60 分钟正式跑批 seed `2145259399`，177 次四类注入、7,389 条投递、0 停摆 / 0 未捕获 / 0 unmapped，审计链完整。CI 先抓到并修复根 workspace 依赖、Knip 入口图和 watcher 固定等待三处缺口；最终 SHA `a8b06a9` 上 **5 次严格串行 CI 全绿**（`31799841764` / `31800391530` / `31800890960` / `31801288270` / `31801769324`）——**AC-8 达成** |
| **P7.2** 可观测性：审计日志与全链路追踪 | ✅ 已完成 | 见 `packages/audit/` + `src/cli/handlers/qianmoAudit.ts` + `tests/integration/qianmo-audit-chain.test.ts` | 一份追加式、带哈希链的审计文件，各层同形状写入（`kind` 原样透传，只有 `outcome` 归一）。**「不可改」拆成三句只认两句**：写入方改不了（`O_APPEND`、无 seek/delete 方法）、外部改动可检测（断链定位到 `seq`）、**外部改动无法阻止**（需 append-only 挂载或机外见证，M0 没有——并有专门用例把这条钉住）。`occ audit` 支持按 trace / agent / 时间窗查询与 `--verify`。DoD 用真链路跑出：四条消息四种命运（接收 / 去重 / 处理器拒 / 限流）在同一 trace 下全部还原，按 `seq` 而非时间戳排序。**六层映射函数齐备**（路由 / 传输 / activator / 协商 / 隧道 / 备份），**常驻节点生产路径已完整接好**（`occ resident` 开 trail，路由拒绝 + 传输消息事件两半都写），后四层的 sink 有实现有用例、接进启动路径留给 P8.2 的演示拓扑 |

**⚠️ P2.2 报警（v2.6 记录）**：roadmap v2.2 曾把 P2.2 从「从零造」**降级**为「实现一个 `Transport` + 自建服务端半边」，依据是基座有约 5,300 行在用传输代码。**实施时逐行核实，该依据不成立**：

- 基座 `Transport` 是**会话上行**接口，`write(message: StdoutMessage)` 钉死在 occ 自己的 SDK 控制协议 zod 联合类型上，**信封不可能是它的成员**——实现该接口只能靠强制转换（`src/cli/transports/Transport.ts:1, :8`）。
- **基座全仓没有任何 WebSocket 服务端**：仅有的两处 `Bun.serve` 都在 `src/ssh/SSHAuthProxy.ts`，该文件出现 `websocket` **零次**（主 agent 独立复核确认）。`WebSocketTransport` 的 808 行全是拨号路径。
- 它还与基座会话面硬耦合（analytics、诊断日志、mtls 环境变量），且其重放机制是**上游服务方的契约**（`X-Last-Request-Id`），不是我们的。

**直接 import 的行数 = 0。**真正复用的是**设计**：退避公式与常量、10 分钟预算、「休眠检测阈值 = 2× 上限」（正好对上 E4 的时间跳跃闸门）、永久关闭码、以及「代理不数控制帧、必须发 keep-alive 数据帧」这条硬经验。

**结论**：**降级的理由不成立，降级的结论侥幸成立**——工时没有爆炸，但原因不是基座帮上了忙，而是阡陌只需要单条 wss + PSK，不需要基座那套多传输选路 + 代理 + 遥测。

**关键路径复核结论（v2.9，实施方判断）：链条顺序不改，但改的是估算方法。**

- **不改顺序**的理由：P2.2 已交付（45 用例、三条 DoD 实测），它在链上的位置已成既成事实，回溯调整不产生任何决策价值。
- **要改的是方法**：本次证明「**基座里有形状相似的代码 ⇒ 工作量可下调**」这条推理不可靠——它在 P2.2 上给出了正确结论，但给的是**错的理由**，属于侥幸。真正决定工时的是**范围**（阡陌只要一条 wss + PSK），不是复用率。
- **前瞻性影响（这才是本条的价值）**：**`P3.1` 的估算建立在同一类推理上**——roadmap 说它「唤醒挂扩展点、休眠改核心」，其中「休眠改核心」的成本**从未验证过**，而基座全仓没有通用 idle 事件这一点只说明「没有现成的」，不说明「改造要多久」。**P3.1 开工第一件事应当是像 P2.2 那样先做可改造性评估并如实报告**，而不是直接按既有工时排。同一条纪律适用于此后任何以「基座已有」为由下调的估算。

**尚未开始且需要人的**（v2.41 收尾）：**无。**全部人项已由负责人 2026-08-17 决议关闭（五个评审通过、双签通过、形式要件豁免、科技处/E-8 移出 M0 出口——逐项见 v2.41）。验收面零欠账（八条 AC 全 PASS-已走查、零豁免）；机器侧三件在途已于 v2.42 全部收口（长跑提前停机取数、t=24 h 阶梯取消、qwen 补充项以整条替换落地后取消）——**M0 就此收口，主线转入 M1**。P0.1 已按 v2.10 以「干净检出可复现」口径实测通过，P0.8 已于 v2.11 通过，P1.4 已完成，不再列。


---

## M0：原型验证（2026-08-17 ~ 2027-01-03，10 个迭代）

**出口条件**：章程 §4 的 AC-1 ~ AC-8 全部通过（豁免 ≤ 2 条）；上游同步演练完成、`BASE.md` 记录完整。

### 现状基线（2026-08-11 首版，v2.0 重写；v2.1 按负责人决议更新「阡陌自有代码」与「旧洁净室三包」条目；**v2.2 按 2026-08-12 的调研与实测更新**）

排期前先认清仓库实际状态，S0 不重复造、也不假装已有：

| 已落地 | 内容 |
|---|---|
| **基座** | **open-claude-code（`occ`）全量入库**，pin `848ad8c2c8daca9f5aa2410da555553e07700f5d` = tag `v2.38.3`，零改动快照，fork 布局。功能完整可运行 |
| 基座许可 | MIT（基座根 `LICENSE`）；npm 包名 `@sweetcornna/open-claude-code`（基座 `package.json` 核实） |
| 基座工具链 | Bun runtime、TypeScript strict、Biome、Vite 构建（code splitting）、`bun run precheck`（typecheck + lint + test）、循环依赖棘轮、mock 卫生棘轮 |
| 基座能力（与阡陌相关，逐项出处见 `base-adoption.md`） | 会话持久化与恢复、分层文件式项目记忆与三级上下文压缩、多模型供应商流适配器、子 agent 与后台 agent、Agent Teams 单机信箱通信、MCP client/server、ACP、工具权限模型与 hooks |
| 溯源 | 根 `BASE.md`（上游、pin、导入日期、零改动声明） |
| **阡陌自有代码（v2.1 更新）** | **`@qianmo/protocol`（信封 / 寻址 / 限额 / 校验）与 `@qianmo/registry`（注册发现 + HTTP v0）已原样复活**，接入基座 workspace（根 `package.json` 的 `packages/*` glob 天然覆盖，未改 workspace 配置），测试随 `bun run precheck` 全绿。依据：负责人 2026-08-11 决议（章程 v2.1 §5.5），**非全员评审结论，S0 的 P0.5 可复核** |
| **实验/演示服务器（v2.2 新增）** | **Linux x86_64 服务器已就位**：Debian 12、4 vCPU、15 Gi 内存、18 G 可用盘，cgroup v2 + 13 Gi swap；**Dormice 与 gVisor（runsc `release-20260622.0`，systrap 平台）已安装**。原先占用它的服务已于 2026-08-12 迁走。**遗留三项（P0.1 承接）**：① `dormice-base:20260718` 镜像为**重建件**（apt 层浮动，与原件不保证逐字节一致）；② `workbench:0.7.10` 模板镜像缺失，任何 `template: workbench` 的 acquire 会失败；③ `dormice.service` 为 active 但 **disabled**（重启不自起）。依据：负责人 2026-08-12 决议 D-4，见 `selection-m0.md` §1 |
| **第二台服务器（v2.11 登记，⚠️ 不承载阡陌负载）** | Azure，Debian 12 / x86_64 / **2 vCPU / 907 Mi 内存 / 63 G 盘（46 G 可用）**，已装 Docker 29.7.1 与 Node 18，**未装 Bun、未装 Dormice**。**它在跑一整套生产服务**（自托管 Supabase：db / auth / rest / realtime / kong / functions，外加监控与代理，已连续运行 10–11 天），**可用内存仅约 115 Mi**（另有 4 Gi swap 已用 844 Mi）。**结论：不在其上部署任何阡陌组件。**理由：occ 常驻堆很大（见 P7.3 补注记录的堆驻留问题），在这台机器上运行极可能把生产容器 OOM 掉；跨机 e2e 所需的「第二个节点」应另找一台空闲机器，或复用已为阡陌腾空的实验服务器做双沙箱拓扑。**登记在此是为了让它的存在与约束都可见，不是为了使用它。** |
| **第三台服务器（v2.12 登记，✅ 可用作第二节点）** | 甲骨文云，Ubuntu 24.04 / **aarch64（ARM）** / 1 vCPU / **5.8 Gi 内存（可用 3.8 Gi）** / 45 G 盘（27 G 可用）。也跑着别的服务（gitea、postgres、sub2api），但**有真实余量**，与 Azure 那台不同。**已用它完成真实跨机跨架构验证**（见下）。已装 Bun 1.3.14（用户目录 `~/.bun`，未动系统）。 |
| **真实跨机验证（v2.12 新增，已完成）** | **首次在两台真实机器、跨架构、跨发行版、走公网**跑通阡陌传输层：甲骨文（aarch64/Ubuntu 24.04）→ GCP（x86_64/Debian 12），端口 9443 直连。结果：PSK 握手成立（`auth_accepted:1`、`auth_rejected:0`，握手 **233 ms**）；10 条信封全部投递（`message_accepted:10`，应用层收到 10 条、去重后仍 10 条，**82 ms**）；重发的 2 条被正确判重（`message_duplicate:2`）且**未漏进应用层**；连接干净收尾。**此前所有传输测试都是单机 unix socket**，这条补上了跨机与跨架构两个维度。实验痕迹已清理，两台的 `~/.bun` 保留供后续复用。 |
| **前置实验（v2.2 新增）** | **E1 ~ E6 已于 2026-08-12 实测完毕**（沙箱→宿主可达性、唤醒到全速耗时、自主工作是否被静默冻结、长驻进程穿越 freeze 的完好性，以及两项 Bun 测试侧实验）。实测数据与局限的**唯一真源是 `selection-m0.md` §3**，本文只引用影响排期的结论。对应任务包 P0.6（状态：已完成） |
| 阡陌立项文档 | `docs/dev/charter.md`、`docs/dev/roadmap.md`（本文）、`docs/dev/base-adoption.md`、**`docs/dev/selection-m0.md`（v2.2 新增：调研结论、实测记录与 2026-08-12 决议）** |
| 申报材料 | `docs/` 下计划书、申请书、品牌与图表素材（已有，不受本次变更影响） |

| 尚缺（S0 起补齐） | 内容 |
|---|---|
| **阡陌自有代码 · 仍缺部分（v2.1 更新）** | **不再为零**——`@qianmo/protocol` 与 `@qianmo/registry` 已复活（见上表）。**仍缺**：`@qianmo/transport`（P2.2 交付，**v2.2：工作量已降级，不再是最长杆**）、`@qianmo/memory`（P2.3 交付），以及**跨节点 activator 与常驻 keepalive**（v2.2 新增，P2.5 交付，**新的长杆**）、休眠唤醒、沙箱、备份、调度层等尚无对应包的能力 |
| 旧洁净室三包 | **处置已决**（负责人 2026-08-11 决议，章程 v2.1 §5.5）：`@qianmo/protocol` / `@qianmo/registry` **原样复活**；`@qianmo/runtime-adapter` **仅取设计不取代码**，其代码继续留在历史提交 `67f6081`，**不复活**（它抽象的旧绿地运行时已被基座本体取代）。P0.5 相应改为**复核本决议**。注意：v1.1 记载的"101 用例全绿"是那个历史提交的状态，**不是当前仓库状态**——当前两个复活包自带 75 个用例（5 个文件），已进 `bun run precheck`，排期仍不得据历史数字免测 |
| 身份隔离 | 阡陌节点态仍完全等同基座（CLI 名 `occ`、配置目录 `~/.occ`）。改名未做（P0.3） |
| CI | 基座 workflow 未按阡陌需要裁剪：发布类流程（`publish-npm.yml`）需移除，测试门禁需保留并接入本仓库（P0.4）。**v2.2 新增：三处继承自基座的缺陷**——① CI 从未 pin Bun 版本（workflow 写 `bun-version: latest`，与 `.tool-versions` 的 `bun 1.3.13` 不一致）；② 一条已定位根因的 flaky 用例；③ 测试未开 `--isolate`。均在 P0.4 顺手处理 |
| 阡陌根文档 | 根 `README.md`、根 `CLAUDE.md`、根 `NOTICE` 需按新事实落地（随本次立项一并交付） |
| 通信全栈（**v2.2 修正**） | 协议、注册中心、防循环限流、加密隧道仍为自研。**但"基座为零起点"这一整体判定已被代码推翻**——传输一端基座有约 5,300 行在用代码可改造（依据 `selection-m0.md` §2），P2.2 相应降级，详见 §0.1 与 P2.2。**可改造性未验证**，S0 动手才知道 |
| **跨节点 activator + 常驻 keepalive（v2.2 新增）** | **此前完全不在计划里**：内核 accept queue 的缓冲只在同机有效，Dormice daemon 只听本机、不提供这一层（已实测确认）。**必须自研，无先例可抄**，且 AC-2 与 AC-6(c) 同时压在它上面。由新增的 P2.5 承接 |

**约定**：包命名沿用既有 `@qianmo/<domain>` 单段命名，M0 内新增包遵循同一约定，不再引入第二套命名。阡陌包作为 workspace 包接入基座既有 workspace，不另建 monorepo。

### 迭代总览

> 日期结构与主题节奏沿用 v1.1，未作调整。S0 主题因基座变更而重命名。**v2.2 只在 S2 主题上追加「+ activator」字样以反映新增的 P2.5，日期与产能一律未动。**

| 迭代 | 日期 | 主题 | 产能 |
|---|---|---|---|
| S0 | 08-17 ~ 08-30 | **基座接管与工程地基**（原"工程地基与法律护栏"） | 100% |
| S1 | 08-31 ~ 09-13 | 单节点常驻雏形 | 80%（数模国赛） |
| S2 | 09-14 ~ 09-27 | 通信骨架 + 记忆骨架 **+ activator** | 100% |
| S3 | 09-28 ~ 10-11 | 休眠唤醒 + 编程闭环 | 50%（国庆） |
| S4 | 10-12 ~ 10-25 | ⭐ 跨节点投递打通 | 100% |
| S5 | 10-26 ~ 11-08 | 调度层：诊断 / 协商 / 隧道 | 100% |
| S6 | 11-09 ~ 11-22 | 场景整合与压测 | 100% |
| S7 | 11-23 ~ 12-06 | 稳定化与可观测性 | 100% |
| S8 | 12-07 ~ 12-20 | 验收走查与演示固化 | 100% |
| S9 | 12-21 ~ 01-03 | 收尾与 M1 拆解 | 50%（期末） |

---

### S0 · 2026-08-17 ~ 08-30 — 基座接管与工程地基

> 本期不写业务功能。**本期的产出是"团队真的能在这个基座上干活"，不是代码行数。**
>
> 与 v1.1 的 S0 相比，法律护栏类工作（vendor 隔离机制、pre-commit 拦截、vendor-guard）**整体消失**，取而代之的是基座掌握与身份隔离。这是本次路线变更在排期上最直观的体现。
>
> **v2.2**：前置实验已在排期开始前跑完（P0.6，状态：已完成），S0 相应减负；同时按调研结论**新增 P0.7 安全面加固与 P0.8 判据修订评审**。本期任务包数由 5 个变为 8 个，但其中 1 个已完成、2 个是小包，**日期与产能未动**。

**P0.1 ⭐ 基座导入与开发环境跑通**
- owner：董宗岳（backup：陈子轩）
- 依赖：无
- 现状（**v2.2 更新**）：① 基座已零改动快照导入仓库根，pin `848ad8c2` = v2.38.3，`BASE.md` 已建；② **Linux x86_64 实验/演示服务器已就位，Dormice 与 gVisor 已装、base 镜像已重建**（见现状基线，依据负责人 2026-08-12 决议 D-4）。**原「缺一台 Linux 机器」的阻塞项已解除**
- 交付物：全员本地开发环境跑通（`bun install` → `bun run dev` → `bun run precheck`）；环境差异清单（Bun 版本、OS、已知坑）；`@qianmo/*` 包在基座 workspace 下的接入方式验证（建一个空骨架包证明 workspace 能识别）；**（v2.2 新增）服务器侧遗留三项的处置**——补齐缺失的模板镜像或明确记录"不使用该模板"、`dormice.service` 设为开机自起、base 镜像为重建件这一事实写入环境差异清单（它影响一切以镜像为基准的复现结论）
- DoD（**v2.9 按分工调整重述**）：原文要求「5 人各自在自己机器上跑通并留截图」，那是 v2.2 之前「五人分头开发」口径下的写法；章程 v2.3 把开发集中到负责人之后，逐人截图变成仪式，**它真正保护的是「这个仓库在一台干净机器上能复现」**。改按此判定：
  - **干净检出可复现（已验证 ✅）**：从 HEAD 做干净 `git clone` → `bun install --frozen-lockfile` → `bun run precheck` 全绿。**实测（2026-08-12，提交 `9a26433`）**：1683 包安装无错，precheck **9217 pass / 0 fail**；另跑 `check:unused` 与 `check:cycles` 亦绿。这条判据能抓住真问题——它正是 `bun.lock` 缺 workspace 条目那类缺陷的照妖镜。
  - 环境差异清单入库；**服务器上 `dor doctor` 全绿**（仍待做）
  - **逐人跑通**降级为「参与开发者按需自查」，不再作为阻塞判据

**P0.2 ⭐ 基座通读与能力盘点（全员任务）**
- owner：喻永昌（协同：全体）
- 依赖：P0.1
- 说明（**v2.2 改性质**）：这是**全员任务，不设单一 owner 覆盖**（对策 P-2、T-6）。基座的工程约定里有大量"历史上真实踩过的坑"，不读就会重踩。**本包的重心从"从头盘点"改为"复核 + 补齐"**——2026-08-12 的调研已产出一批对能力盘点的**修正**（见 `selection-m0.md` §2），其中数条直接推翻了 base-adoption.md 与章程 §3 的既有判定。本包先逐条复核这些修正（复核的是结论是否站得住，不是重做一遍调研），再补齐剩余未判定项。**凡文档与代码冲突，一律以代码为准**——这是本次调研最大的收获来源，也是本包的工作方法
- 交付物：① 每人通读基座根 `CLAUDE.md`（必读，非选读）；② 按方向分工通读基座关键文档并向全员讲解——运行时与记忆（董宗岳）、通信与权限相关既有机制（陈曦宇）、多模型与 provider（陈子轩）、场景与可观测（李怡康）、总体架构与发布/门禁（喻永昌）；③ **（v2.2）逐条复核 `selection-m0.md` §2 的能力盘点修正**——每条给出「采纳 / 部分采纳 / 不采纳」结论，不采纳的必须给出与之冲突的代码依据；④ `docs/dev/base-adoption.md` 定稿（含上述复核结论）；⑤ **章程 §3 功能清单的「基座起点」三态判定逐项落实**，每项给出改造入口（目录或扩展点）
- DoD：base-adoption.md 全员评审通过；`selection-m0.md` §2 每条修正都有书面复核结论；章程 §3 每一项都有 `已有 / 部分 / 自研` 判定与改造入口，无"待定"；每位成员能口头回答本方向的"基座给了什么、缺了什么"；§4.1 的提请评审问题给出团队结论并回写章程

**P0.3 身份隔离改名（阡陌节点态）**（状态：✅ 已完成，见「完成状态速查」）
- owner：陈子轩（backup：董宗岳）
- 依赖：P0.1
- 说明：目标是**阡陌节点、occ 本体、官方 Claude Code 三者能装在同一台机器上互不干扰**。基座已经为此建好了机制（唯一真源 `src/config/paths.ts`），本任务是沿着它派生一套阡陌身份，**不是新造机制**
- **提示（v2.2 新增，v2.4 按 P0.3 实施时的实清点修正）**：**动手前一律以代码实际清点为准**，不要照文档数字做"改完 N 处即完工"的判断。已核实的两条：① **facade 实际 7 个**（`packages/tool-runtime/src/` 的 register 函数逐个清点），而 `BASE-CLAUDE.md` 写"现有 6 个"并漏列 `searchCredentials`；② 基座文档提到的 `paths.ts` 绕过点数量（12 / 9）指的是**基座隔离改造前**的历史代码，那棵树不在本仓库 git 历史里，**在 atlas 无法复核**——P0.3 实施时的当前清点为 26 处带身份字面量的生产代码位置，其中 1 处真正打穿身份切换（已修）、3 处保护清单取错范围（已修）、22 处有理由保留。依据：P0.3 实施审计（提交 `189268d`~`4f26214`）
- 交付物：CLI 名 / 进程名 / socket 前缀、用户配置根、全局状态文件、项目内资产目录、缓存树与 XDG 子目录、凭据条目——全部经基座 `src/config/paths.ts` 与 `src/constants/brand.ts` 派生；**「明确不改」清单严格遵守**（系统提示词前缀、`claude-code/<ver>` User-Agent、OTel `service.name`、`CLAUDE.md`/`AGENTS.md` 文件名、`CLAUDECODE=1`，见章程 §5.2③）
- DoD：三者共存实测——分别登录互不覆盖凭据、配置互不读写；**零处绕过 helper 直接拼路径**（用基座既有的隔离测试 + 一次全仓 grep 自查）；改动集中在派生层，未散落到调用点

**P0.4 CI 门禁移植与裁剪**（状态：✅ 已完成，见「完成状态速查」）
- owner：陈子轩（backup：喻永昌）
- 依赖：P0.1
- 说明：基座自带成熟门禁，**本任务是移植与裁剪，不是从零搭 CI**（这是路线变更净收益最明确的一项，对策 P-4）。**v2.2 补充**：实际要处理的 workflow 只有 2 个，`@qianmo/*` 包在分片上天然各自成片、零改动
- 交付物：保留并接入——typecheck、lint、分片测试、循环依赖棘轮、mock 卫生棘轮；**移除**——npm 发布流程（`publish-npm.yml`）、release 脚本相关门禁、与上游发布面绑定的冒烟步骤（章程 N-14）；PR 模板（含"本 PR 修复的边界问题对应哪条用例"必填项，为 AC-8 流程约束预埋）
- **交付物追加（v2.2 新增，三件顺手做掉的事，均已定位根因，依据 `selection-m0.md` §2/§4）**：
  - ① **修掉那条 flaky 用例**：`src/services/skillLearning/__tests__/throttleAndCircuitBreaker.test.ts`，根因已定位为用例漏写超时覆盖参数，一行可修。不修它 = 让 CI 长期带着一次随机变红，负向自测（下面那条 DoD）就失去判别力
  - ② **CI 钉死 Bun 版本**：现有 workflow 共 **三处**写 `bun-version: latest`，与仓库 `.tool-versions` 里钉的 `bun 1.3.13` **完全脱节**（其中一处在本包一并移除的发布类 workflow 里，实际需改的是保留下来的那两处）。改为从 `.tool-versions` 读取。这是**从基座继承来的缺陷**，不是本仓库引入的；它意味着上游发新版即可在零代码改动下改变构建结果，**直接威胁 P8.1「30 min 全新机器复现」的 DoD**
  - ③ **测试开 `bun test --isolate`**：实测可消灭 mock / env 的跨文件污染（基座 CI 曾因这类污染连红），且在 `@qianmo/*` 自有包上**零开销**。这是本次调研里性价比最高的一条工程改动
- **DoD（v2.2 重写）**：
  - PR 上自动触发且全绿；发布类 workflow 已移除并在 PR 描述中记录移除清单
  - **故意提交一处类型错误 / 一条脏 mock，CI 各变红一次**（负向自测，保留）
  - **定义并入库一条真正等价于 CI 的「提交前必跑」命令组合**——原判据「本地 `bun run precheck` 过 = CI 过」**已被实测推翻**：`precheck` 不含 `check:cycles` / `check:unused` / `check:bundle`，且其中的 lint fix 步骤**会写文件**（不是只读检查）。新判据：在干净工作区上跑这条组合并全绿后推 PR，CI 不应因门禁类原因变红；组合内容写进根 `CLAUDE.md` 的常用命令段与 PR 模板
  - 三件追加交付物各自可验证：flaky 用例连续 10 次构建不再随机变红；CI 日志中打印的 Bun 版本与 `.tool-versions` 一致；`--isolate` 已生效且测试总时长无明显劣化

**P0.5 ⚖️ 协议资产处置复核**（v2.1 改性质：由「评审决策」改为「**复核负责人 2026-08-11 决议**」；状态：✅ 已完成，见「完成状态速查」）
- owner：喻永昌（backup：陈曦宇）
- 依赖：P0.2
- 说明：三包的处置**已由负责人决议定案并已落地**（章程 v2.1 §5.5）——`@qianmo/protocol` / `@qianmo/registry` 原样复活并接入 workspace，`@qianmo/runtime-adapter` 仅取设计不取代码。该决议**不是全员评审结论**，因此本包仍然保留：性质从"做决定"变为"**复核这个决定**"。复核的实质问题没变——基座的 Agent Teams 已有一套单机信箱与按名寻址的既成形状，**两者的关系必须是一次自觉的决策，而不是默认叠加**
- 交付物：① 逐包复核结论——**维持 / 推翻**（推翻时给出替代处置与回滚成本）；② 已复活两包与基座既有机制的关系定性（替换 / 并存 / 上层封装），作为 P1.1 的直接输入；③ **版权头问题在此一并定**（是否给 `@qianmo/*` 源文件加统一版权头，章程 §5.5「许可」条，本次复活未加头）；④ 顺带确认两包 `package.json` 遗留的 `Apache-2.0` license 字段按章程 §5.5 的 S0 清理项处理
- DoD：复核结论全员评审通过并回写章程 §5.5；若推翻，**同一迭代内完成回滚**，不得让工作区与章程长期不一致；版权头问题有明确结论（加 / 不加），不留"待定"；结论直接成为 P1.1 的输入

**P0.6 前置实验 E1 ~ E6**（**v2.2 新增；状态：已完成**）
- owner：董宗岳（backup：陈曦宇）——**本包已完成，owner 仅为归档记录，负责人可调**
- 依赖：无（在排期开始前于实验服务器上完成）
- 说明：在动第一行业务代码前，把四件"只能推断"的事变成实测数字——沙箱能否够到宿主 daemon（AC-6(c) 的唯一依靠）、唤醒是否等于"恢复到全速"、沙箱内自主工作会不会被静默冻结、长驻进程能否穿越 freeze 而不损坏；另有两项 Bun 测试侧实验。**结果推翻了原定的常驻默认配置，并改写了 P1.1 / P2.5 / P3.1 三个包的设计输入**
- 交付物：E1 ~ E6 的实测记录与局限声明——**唯一真源是 `selection-m0.md` §3**，本文不复制数据
- DoD：**已于 2026-08-12 实测完毕，全部完成，无剩余阻塞项。**下游任务包引用实测结论时一律指向 `selection-m0.md` §3，不得在别处另记一份数字；引用外推值（非实测点位）时必须连同"未实测"这一标注一起引用

**P0.7 🔒 安全面加固**（**v2.2 新增**，落实负责人 2026-08-12 决议 D-5；状态：✅ 已完成，见「完成状态速查」）
- owner：陈曦宇（backup：董宗岳）
- 依赖：P0.1
- 说明：E1 实测的结论是"当前**够不到**"，但**基础比预期薄**——AC-6(c)（智能体删不掉自己的备份 / 逃不出沙箱边界）目前**只靠一件事成立：Dormice daemon 绑在 127.0.0.1**。宿主防火墙对沙箱网段没有任何限制规则，且 API key 无权限分级（销毁沙箱与执行命令用同一把凭据）。**单点依赖不是"已经安全"，是"还没出事"**——本包把它变成纵深防御，趁 S0 还没有业务代码压着时做，成本最低
- 交付物：① **防火墙加规则**：禁止沙箱网段（docker0）访问宿主上的 Dormice daemon 端口，使 AC-6(c) 不再单点依赖绑定地址；② **处理那两份 nginx vhost**：`sites-available` 下现躺着一份指向 daemon 的 `proxy_pass` 配置（含其同步副本），**启用即等于把含销毁沙箱在内的全量 API 对外打开**——移出目录或改名加 `.disabled` 后缀；③ **把「daemon 只绑 127.0.0.1」写成 CI 可断言的不变式**，而不是靠人记
- DoD：**从沙箱内重跑 E1 的五地址探测，全部被拒**；③ 的断言进 CI 且常绿；**负向自测：人为启用那份 vhost 后，该断言必须变红**——只会在正常态变绿的断言等于没有断言；加固动作与验证脚本入库（不含任何主机名 / 地址 / 凭据，用变量或占位符）
- 备注：本包只做**部署面**加固，不改变章程 §3 范围，也不替代 P1.3（沙箱执行环境 v0）与 P4.4（备份保护）——那两个包做的是产品内的爆炸半径控制，本包做的是它们所依赖的宿主前提

**P0.8 判据修订评审**（**v2.2 新增**，D-1 ~ D-3）
- owner：喻永昌（全员参与）
- 依赖：P0.2、P0.6
- 说明：本次调研查出**三条验收判据本身有问题**——两条是判据与实测机制对不上，一条是判据会误杀合法行为。负责人已于 2026-08-12 拍板并写入章程 v2.2，但**验收标准调整按章程 §7.3 属"全员评审"档**，因此拍板之后仍要走完全员评审才算定案。单列任务包的理由是**必须在 S0 内解决，不许拖到用得着它的那一期再说**——拖到 S3/S4 才发现判据要改，等于让 P3.1 与 P4.2 照着错的目标做完一遍
- 待评审三条（每条的完整事实依据与倾向见 `selection-m0.md` §1）：
  - **D-1**：AC-2 的"休眠态"认不认沙箱的 frozen 形态（实测：进程仍在、页被换出、状态字节级完好，与判据字面写的"进程不在内存"不一致）
  - **D-2**：AC-3 的"首次回访**节点**即切断"是否改为**处理者地址 + 任务标识**粒度（节点粒度会误杀合法路由，业界已有同样设计被定性为规范级 bug 的先例）
  - **D-3**：AC-2 的"连续 10 次测量 P95 ≤ 60 s"进不进 CI 阻塞门禁（延迟门禁在共享 runner 上的假阳性率高到会让门禁失去意义）
- 交付物：三条各一份书面评审结论（通过 / 否决 / 附条件通过）；通过的**回写章程 §4 并升版本号**，同时同步本文对应任务包（D-1 → P3.1、D-2 → P4.2、D-3 → P3.1 与 P4.1 的测量口径）
- **注意评审的起点**：章程 v2.2 **已按拍板结论写入修订后的判据文本**。因此本包评审的是**已在纸面上的新判据能否留下**，不是从空白处起草——否决的后果是回退，不是搁置。
- **结果（v2.11）：✅ 已通过**。负责人于 2026-08-12 确认 D-1 / D-2 / D-3 三条修订通过，章程 v2.8 记录在案；各处「未通过则回退至 v2.1」的悬置条件**自此解除**
- DoD：**三条各有书面结论，无"待定"**；未通过的**回退至 v2.1 原判据并记录理由**——记录理由是硬要求，否则同一个问题会在 S3 再吵一次；回退时同步修订本文中标注"同步 D-1 / D-2 / D-3"的各处任务包

---

### S1 · 2026-08-31 ~ 09-13 — 单节点常驻雏形（80% 产能）

> 09-10 ~ 09-13 数模国赛，李怡康不排开发任务，改为赛中记录真实痛点（P2.4 的输入）。

**P1.1 ⭐ 消息协议草案 v0.1**（状态：✅ 已完成，见「完成状态速查」）
- owner：喻永昌（backup：陈曦宇）
- 依赖：P0.2、P0.5、**P0.6**（实验结论是本包的设计输入）、**P0.8**（D-2 / D-3 的评审结论直接决定字段定义）
- 说明：v1.1 中本包排在 S0 且无依赖。v2.0 移到 S1 并加了两个前置——协议必须在**看清基座既有通信形状之后**再定稿，否则会与 Agent Teams 的信箱语义打架，或重复定义已经存在的东西。**v2.2 又加了两个前置**：P0.6 的实验结论是本包若干字段的直接依据；P0.8 的评审结论决定 D-2 / D-3 相关字段怎么定——**判据没定就先写字段，等于赌一次**
- 交付物：`docs/dev/protocol.md`——消息信封结构、地址格式 `qianmo://<node>/<agent>`、**消息生命周期状态机**（created → queued → sent → delivered → acked → completed / failed / dropped，含 loop_detected、rate_limited、timeout 等终态）、错误码表、hop / trace / fingerprint 字段定义；**新增一节**：与基座既有单机信箱机制的关系（替换 / 并存 / 封装，依 P0.5 结论）
- **设计输入追加（v2.2 新增，逐条见 `selection-m0.md`）**：
  - **D-2 / D-3 / D-9 的结论**：防循环粒度、AC-2 测量口径、以及"自研协议 + 概念对齐，不采用 A2A 子集"的定性
  - **必须显式区分两类 ack**：**触碰旧工作集的 ack** 与**不触碰的 ack**——实测两者代价差两个量级。60 s 的 ack 预算能不能守住，取决于协议把 ack 定义成哪一类；**这是本包要定的事，不是 P3.1 实现时再看着办的事**
  - **`ttlMs` 拆成两个字段**：**投递时限**与**任务时限**。现状是同一个字段被两处语义共用，直接导致 `LIMITS` 里的默认存活时长与 AC-2 的 ack 回执线**互相矛盾**——按默认值，消息会在回执线到达之前就先过期。**拆字段是修根因，改数值只是绕过它**；拆分后两个字段的默认值仍以 `@qianmo/protocol` 的 `LIMITS` 为唯一出处
  - **`trace_id` 采用 W3C `traceparent` 格式**：MCP 规范已把它写进标准（SEP-414），格式对齐是免费的生态互通
  - **capability 由每节点 Ed25519 签发，预共享密钥仅作接入门禁**（D-8）。理由：纯对称密钥下，持有它的任何节点都能伪造任意节点签发的令牌——包括伪造"用户已确认"凭证，于是 C-5「消息不能替用户授权」会从**结构性保证**退化为**约定**。签发与校验落在本包定义、P4.3 落地
  - **注意**：`withHop` 在 `packages/` 内**当前零生产调用方**——协议落地时需要真正接线，不能因为"函数已经存在"就认为跳数机制已生效
- DoD：全员评审通过；五类边界问题（触发时机、超时、消息风暴、额度耗尽、异常退出）在状态机上**都能找到对应的迁移路径**，无"待定"；协议级数值上限只有一处出处（章程 §3.3 C-4）；**上述设计输入逐条在文档里有对应落点**（两类 ack、两个时限字段、traceparent、capability 签发），无遗漏、无"后续再定"

**P1.2 🔍 会话持久化能力核验与常驻缺口清单**（状态：✅ 已完成，见「完成状态速查」；v2.35 勘误——此前标题停在 v2.5 的「基本完成」，速查表自 v2.13 起已为已完成）
- owner：董宗岳（backup：陈子轩）
- 依赖：P0.1、P0.2
- 说明：**性质从"实现"变为"核验"**。基座已有 append-only JSONL 与多种恢复入口，本包要回答的是"它满不满足 AC-1"，而不是"怎么把它写出来"
- **v2.2 修正（两处，均以代码为据，见 `selection-m0.md` §2）**：
  - **恢复入口钉死 `--resume <id>`，不用 `--continue`。**后者**没有持久化索引**，每次恢复都要 readdir + stat 项目下每个会话文件并读取其尾部，**成本随历史线性增长**——AC-1 的"启动到可收消息 ≤ 10 s"会被它撞线，而且是**跑得越久越容易撞**这种最难在验收现场解释的失效方式。两者都保持 `session_id` 不变，换入口不影响 AC-1 的第一条判据
  - **工作内容改写**：原定的"补一个容错解析器"**不需要做**——半写行在读取侧已有容错。真正的缺口是**语义**：崩溃瞬间可能丢失一个极短时间窗内的消息，**要定义"丢了这一小段"意味着什么**（是否重投、发送方如何得知、是否计入 at-least-once 的保证范围），并与 P1.1 的状态机对齐。**这是协议问题，不是解析器问题**
- 交付物：AC-1 三条判据的实测结果（`session_id` 一致性 / 不重放历史即可续答 / 启动到可收消息 ≤ 10 s，**统一以 `--resume <id>` 为入口测量**）；`kill -9` 三种崩溃点（写事件中 / 快照中 / 工具执行中）的一致性实测与补齐；**丢失窗口内消息的语义定义**（回写 `protocol.md`）；`demo/ac1-restart.sh` 一键复现脚本；**常驻化缺口清单**（基座会话是交互进程内的，常驻节点需要什么额外状态，输出给 P3.1）
- DoD：`demo/ac1-restart.sh` 可一键复现且三条判据全部达标；三种崩溃点各有用例；**"≤ 10 s"在会话历史积累后复测仍达标**（至少测两个历史规模点位，防止线性成本在验收后才暴露）；丢失窗口语义已回写 `protocol.md`；缺口清单交付 P3.1 → **AC-1**

**P1.3 沙箱执行环境 v0**（状态：✅ 已完成，见「完成状态速查」）
- owner：董宗岳（backup：陈曦宇）
- 依赖：P0.1
- 说明：基座有工具权限模型与 hooks 可作第一道，**容器化与路径白名单仍需自研**（章程 §4.1 为此留了一个提请评审）
- **选型已定（v2.2，章程 §3.1 A-1）**：沙箱用 **Dormice + gVisor**，且**架构钉死为「occ 跑在沙箱内」**——不是"沙箱作为 occ 的一个受限执行区"。理由是硬的：daemon 的凭据无权限分级（销毁沙箱与执行命令同一把），**AC-6(c) 只能靠"智能体在物理上够不到 daemon"成立**；把 occ 放在沙箱外，这条验收标准直接不成立。**本包不再重新比选方案**，宿主侧前提由 P0.7 保障
- 交付物：容器化工作区；路径白名单；宿主敏感路径只读挂载；命令超时与 CPU / 内存上限；越权操作产生审计事件（优先复用基座既有审批与审计链路）
- DoD：向白名单外路径写入被拒并留审计记录；超时命令被强杀；资源超限被拦。用例进 CI → **AC-6(a)**

**P1.4 🔍 模型适配层核验与供应商选定**
- owner：陈子轩（backup：董宗岳）
- 依赖：P0.1、P0.2
- 说明：**性质从"实现"变为"核验"**。基座已内置多供应商流适配器（出处：基座 `docs/zh/features/providers.md`），AC-5 的"仅改配置不改代码"在结构上已经满足
- 交付物：选定 2 家供应商并跑通；适配器一致性测试用例（多轮 / 工具调用 / 流式 三项）；配置切换的复现脚本；能力差异记录（哪些降级是基座已处理的、哪些需要我方补）
- DoD：同一编程任务在两家下均完成并通过任务自带断言，两次运行仅改配置文件；一致性用例对两家全绿 → **AC-5**
- 备注（**v2.1 更新**）：**评审已决 —— AC-5 不降级**（负责人 2026-08-11 决议，章程 v2.1 §4.1：仍计为"我方实现的验收项"）。**原"若降级则把释放出的工时优先回填 P2.2"这一条款随之失效**，本包工时按原计划留在 S1，不做重排。P2.2 仍是关键路径长杆，其风险另按 S2 的风险跟踪条目处理

---

### S2 · 2026-09-14 ~ 09-27 — 通信骨架 + 记忆骨架

**P2.1 ⭐ 注册中心与按名寻址**（状态：✅ 已完成，见「完成状态速查」）
- owner：陈曦宇（backup：喻永昌）
- 依赖：P1.1
- 说明：基座的 Agent Teams 有单机 roster 与按名信箱寻址，但**明确不支持跨会话 / 跨主机寻址**（出处：基座 `docs/zh/agent/coordinator-and-swarm.mdx`，其 `to` 字段带 `@` 会被输入校验直接拒绝）。跨节点这一层是我方新建
- 交付物：注册 / 心跳 / 注销 / 查询；跨节点地址解析；智能体状态标记（在线 / 休眠 / 离线）；状态持久化；单点部署（不做高可用，章程 N-6）。**`@qianmo/registry` 已复活**（负责人 2026-08-11 决议，章程 v2.1 §5.5），在其基础上补齐。**已有**：注册 / 心跳 / 注销 / 查询、TTL 租约与过期回收（`DEFAULT_TTL_MS`）、`Bun.serve` 上的 HTTP v0 接口、可注入时钟。**本包新增**：跨节点地址解析、智能体状态标记、**状态持久化**（现有实现是纯内存表，进程重启即丢）
- DoD：双节点各注册 1 个智能体，任一节点可解析对端地址；心跳超时后状态自动转 offline；注册中心重启后节点可自动重新注册 → **AC-2 前半**

**P2.2 ⭐ 节点间传输通道**（v2.2 工作量降级，仍在关键路径上）
- owner：陈曦宇（backup：陈子轩）
- 依赖：P1.1
- 说明（**v2.2 重写**）：

  > **v2.2 修正**：v2.0 原文写「**基座起点为零**——上游已于 2026-07 删除全部远程传输层（约 45k 行）」。**这条已被代码推翻**（见 §0.1 与 `selection-m0.md` §2）：删除的是 Remote Control 的那一部分，基座现存约 5,300 行**在用**的传输代码（含 WebSocket 与 SSE 两类通道实现，以及远程 IO 与上游代理两块），且其远程 IO 类型继承自基座主循环所用的结构化 IO 抽象——**主循环在结构上已经能跑在网络传输之上**。`LAN_PIPES` / `UDS_INBOX` 连代码一并移除这一点仍属实，但它们不是判断依据。

  因此本包的工作**由「从零造一套传输栈」降级为「实现一个 Transport + 自建服务端半边」**——客户端一侧有既有形状可依，服务端一侧（基座作为客户端连出去，没有"接进来"的那一半）仍需我方新建。选型已定：**单条 wss 长连接**，消息语义用「**ack 帧 + 稍后 result 帧**」解耦（理由：语义上不能挂住一个 HTTP 请求 5 分钟；而回调式反向投递要求两端互相可达，M0 的部署形态不满足）。

  **告诫（务必保留）**：这次工作量下调**仅凭"代码存在与形状"判断，可改造性未验证**——要 S0 / 本包实际动手才知道。**不要在本包开工前就按下调后的工时安排下游排期**；若动手后发现改造成本接近重写，按下方风险跟踪条目立即报警。
- 交付物：`@qianmo/transport`——WebSocket 长连接 + TLS + 预共享密钥握手（**明确非生产级**，章程 N-3）；断线自动重连与退避；at-least-once 投递 + 接收端指纹去重
- DoD：拔网线 30 s 后自动重连且消息不丢；重复投递同一消息 3 次，接收端只处理 1 次；握手密钥错误时连接被拒并记录
- **风险跟踪**：本包若在 S2 结束时未拿出"两台机器之间能收发一条消息"的最小证据，立即向负责人报警——它的下游是 **P2.5、P3.1、P4.1** 三个关键路径任务（v2.2：下游多了一个 P2.5）。**另加一条报警口径**：若开工两周内判定"既有传输代码不可改造、需要重写"，同样立即报警——那意味着 §0.1 的关键路径要再推一次
- **测试口径（v2.2 新增）**：单机集成测试走 unix domain socket，**只有跨机 e2e 才用 TCP**——可绕开端口分配与复用上的一类非确定性陷阱（实测：两个 server 绑同一端口不报错，在 Linux 上会非确定性分流）

**P2.3 分层记忆存储 schema**（状态：✅ 已完成，见「完成状态速查」）
- owner：董宗岳（backup：李怡康）
- 依赖：P1.2
- 说明：基座有文件式项目记忆与三级上下文压缩机制（出处：基座 `docs/zh/context/project-memory.mdx`、`compaction.mdx`），但**没有阡陌定义的工作/项目/基线三层 schema，也没有基线层**。本包做映射与补齐，优先复用基座的记忆文件与注入链路，不另起炉灶
- 交付物：`@qianmo/memory`——三层表结构；条目强制字段（来源 ID、写入时间、标签、废止标记）；工作记忆 → 项目记忆的沉淀归档任务；与基座既有记忆机制的映射说明
- DoD：写入 → 归档 → 检索 round-trip 用例通过；软删除（废止）后条目不再被检索命中但仍可审计查询

**P2.4 数模竞赛场景需求文档**（状态：🟡 草案已产出、未完成，见「完成状态速查」）
- owner：李怡康（backup：喻永昌）
- 依赖：S1 期间 CUMCM 参赛记录
- 交付物：`docs/dev/scenario-mcm.md`——赛中真实痛点清单、演示分镜脚本（AC-7 的逐帧定义）、需要采集的指标、演示所需数据集规格
- DoD：全员评审通过；AC-7 的每个环节都有明确的"输入 → 期望现象 → 判据"三元组，不含"效果良好"这类无法判定的表述

**P2.5 ⭐ 跨节点 activator + 常驻 keepalive**（**v2.2 新增**；状态：✅ 已完成，见「完成状态速查」）
- owner：董宗岳（backup：陈曦宇）——**排期归属与 owner 为提案，负责人可调**
- 依赖：P1.1、P2.2
- 说明：这是**本次重推识别出的、此前完全没有任务包的必需组件**（见 `selection-m0.md` §5）。它做两件事，而这两件事**是同一个组件的两张面孔**：
  - **activator（跨节点唤醒转发）**：「**接住请求 → 唤醒目标节点 → ready 后转发**」。这一层非有不可——内核的连接排队缓冲**只在同机有效**（同机的 socket 激活方案解决不了跨机场景），而沙箱 daemon **只监听本机、不提供这一层**（已实测确认）。它的角色等同于 Knative 的 activator、等同于 `systemd-socket-proxyd` 的跨节点版
  - **keepalive（常驻保活心跳）**：周期**必须严格小于沙箱的冻结空闲阈值**，且**必须去打 daemon 的 API**——实测结论是**沙箱内自主工作对空闲判据零可见**：一个 100% CPU 忙碌的进程照样会在空闲阈值到点后被冻结，之后长时间零推进且**永不自恢复**。**"把停止阈值设为 null"不是解法，它反而把故障从"掉到停止态、下次冷启重来"升级为"永久静默停摆"**——这是本次实测最反直觉的一条，务必写进实现注释
  - 两者共用同一条通道：**都在宿主侧、都代表沙箱去打 daemon 的 API**，因此**都必须只暴露 touch / acquire、绝不暴露 destroy**。理由是硬的：daemon 的凭据无权限分级（销毁与执行同一把），**一旦这个组件把 destroy 暴露出去，AC-6(c) 立刻不成立**
- **为什么给它单独立包并给足工时**：它是 **M0 里唯一没有先例可抄、同时压着 AC-2 与 AC-6(c) 两条验收标准**的组件。P3.1 直接压在它后面（见 §0.1 新链）
- 交付物：宿主侧 activator/keepalive 组件——请求接住与排队、目标节点唤醒、ready 探测与转发、心跳调度；**能力面白名单**（只有 touch / acquire，destroy 与其他破坏性接口在代码层面不可达，不是靠配置关掉）；唤醒各阶段耗时埋点（接住 → 发起唤醒 → ready → 首字节转发），输出给 P3.1 与 P4.1 的基准报告
- DoD：
  - ① **唤醒转发闭环**：目标节点处于休眠态时，向它投递一条消息，**由 activator 接住并唤醒、目标 ready 后消息被完整转发且不丢**；连续 10 次 10/10；转发链路各阶段耗时有埋点数据
  - ② **防静默冻结**：一个在沙箱内持续自主工作（不经 API 交互）的常驻进程，在**超过冻结空闲阈值数倍的时长**内**始终未被冻结**且任务持续推进；作为对照，**关掉 keepalive 后同一场景必须复现被冻结**（负向自测——没有对照组的"它没坏"不算证据）
  - ③ **destroy 不可达**：断言该组件对外可达的能力面**不含任何破坏性接口**；构造一次直接调用销毁接口的尝试，**必须失败并留审计事件**；断言进 CI（与 P0.7 的不变式断言同一套机制）
  - ④ **崩溃与重启**：组件自身进程被 `kill -9` 后能自恢复，恢复期间被接住的请求要么被转发、要么明确失败，**不得静默丢弃**
- **风险跟踪**：本包**与 P3.1 互相咬合**，任一半不到位 AC-2 都不成立。若 S2 结束时 ① 与 ② 未同时拿出最小证据，**立即向负责人报警**并评估把 P3.1 的休眠半边后挪——**不要让两个都没做完的半成品在 S3 互相等待**

---

### S3 · 2026-09-28 ~ 10-11 — 休眠唤醒 + 编程闭环（50% 产能，国庆）

**P3.1 ⭐ 休眠与唤醒**（状态：✅ 已完成，见「完成状态速查」）
- owner：董宗岳（backup：陈子轩）
- 依赖：P1.2、P1.3、P2.1、P2.2、**P2.5**（v2.2 新增：唤醒的发起方是 activator，本包是它的另一半）
- 说明（**v2.2 重写**）：

  > **v2.2 修正（挂载方式）**：v2.0 原文写「挂载方式是**往 worker 注册表加一种 kind**」。**这条已被代码推翻**——基座的 daemon worker 入口**没有 dispatch 表**，参数检查后无条件报错并置固定退出码（出处：基座 `src/daemon/workerRegistry.ts:32-33`）。**只加一个字符串，worker 会立刻退出并被永久 parking。**
  >
  > **正确的常驻宿主是 ACP**：它已有多会话表、会话加载入口与按会话隔离，且**绕过了主入口的全部冷启动重活**——这正是常驻节点需要的形状。daemon supervisor 的**进程监督骨架**（退避重启、崩溃 parking、状态文件）仍可复用，但它不是挂载点。

  **本包拆成两半，两半的性质不同**（依据 `selection-m0.md` §2）：
  - **唤醒半边：零核心改动。**~~有四条现成通道可走——首选 MCP 的 channel 通知；备选还有异步再唤起 hook（以约定退出码触发）、cron 式文件 watch、文件变更 hook。~~ **这一半不要改基座核心**

    > **v2.15 勘误（可改造性评估实测，以代码为准）**：**结论「零核心改动」成立，但四条通道的机制写错了——它们在 ACP 宿主下一条都不通。**① MCP channel：ACP 建会话时写死 `mcpClients: []`（`src/services/acp/agent/createSessionMethod.ts:181`），整个 `src/services/acp/` 从不构造 MCP client。② 异步再唤起 hook：它 enqueue 后由 `useQueueProcessor` 排空，而该 hook 的**唯一**消费者是交互式 TUI（`src/screens/REPL.tsx`）。③ cron 文件 watch：装配点在 `src/cli/print/runHeadlessStreaming.ts`，ACP 不启动。④ 文件变更 hook：`initializeFileChangedWatcher` 唯一调用点是 `src/setup.ts:164`，而 `--acp` 是 `src/entrypoints/cli.tsx:129-134` 的 fast-path，**直接 return、根本不进 `rootAction`**，因此 `setup()` 不跑。基座自己在 `src/cli/print/headlessCron.ts:4-8` 写明了根因：「**unlike the REPL there is no queue subscriber here that drains on enqueue while idle**」。
    >
    > **正解**：ACP 自带唤醒通道，就是 `session/prompt`。外部事件 → 沙箱内 shim → 一条 JSON-RPC → 已加载会话开一个 turn。仍是零核心改动，且是 ACP 的设计意图。**这一半工时不增反略减**（不必去趟 channel 的 gate/allowlist/plugin manifest）。

  - **休眠半边：必须改核心。**~~全仓**没有通用的 idle 事件**——没有任何扩展点能告诉我们"这个会话闲下来了"。~~ 按章程 T-5 对策④，**PR 描述里必须写明"为什么扩展点不够用"**

    > **v2.15 勘误**：**结论「必须改核心」成立，但理由是错的。**基座有四个不同粒度的 idle/busy 信号，其中 `src/utils/session/sessionActivity.ts` 正是我们要的形状——一个由 API 流式与工具执行两端括起来的引用计数，`registerSessionActivityCallback(cb)` 注册回调，基座自己就拿它当沙箱保活心跳用；**在 ACP 部署下该注册槽是空的**（唯二注册方只在 `--sdk-url` CCR 路径下活）。真正的理由是**基座没有任何进程内扩展点**：插件清单能声明的只有 hooks / commands / agents / skills / mcpServers（`src/utils/plugins/schemas.ts`），**全是子进程或 Markdown，没有一条能让第三方 JS 在 occ 进程里跑**。因此哪怕只为调一次 `registerSessionActivityCallback` 也必须改核心。**这一半是「插桩」级而非「造机制」级，核心改动约 40–85 行、分散 5 个文件**——行数不是成本，每处都要写 T-5 理由、每处都是上游同步的冲突面才是。
- **休眠形态（v2.2）**：随 **D-1** 改为沙箱的 **frozen** 形态（实测：进程仍在、页被换出、状态字节级完好、端口保持监听，穿越冻结后无退化），章程 §4 的 AC-2 判据已按此修订。**D-1 已于 2026-08-12 经 P0.8 确认通过**（章程 v2.8），frozen 即为现行判据，原「按回退方案预留工时」的告诫**已不再适用**——实现可锁定在 frozen 一条路上
- 交付物：休眠（依 D-1 结论取 frozen 或温休眠）；唤醒触发源三种（消息到达 / 定时 / 手动 CLI），消息到达一路对接 P2.5 的 activator；冷启动各阶段耗时埋点（沙箱拉起 / 状态加载 / 上下文重建 / 首 token）
- **交付物追加（v2.2 新增）· 时间跳跃检测与宽限窗口**：单调时钟**在冻结期间照常前进**（实测），因此**唤醒瞬间，所有"距上次见到 X 有多久"的判据会同时越阈、一起触发**——心跳超时、租约过期、重连退避、任务截止全都会在同一毫秒判定失败。**换用单调时钟躲不掉这件事。**必须做**显式的时间跳跃检测**（例如：任一周期性 tick 的实际间隔 > 2 倍周期，即判定"刚解冻"，**重置全部截止时间**并进入一个宽限窗口），否则**常驻节点每次醒来都会自我判死一次**。另注：定时器在冻结期间**不补跑**——依赖"每 N 秒必然触发一次"的逻辑要能容忍整段空档
- DoD：**先出耗时基准报告再定实现方案**（对策 T-1）；休眠 → 唤醒 → 可响应 P95 ≤ 60 s，10 次测量成功率 10/10；**时间跳跃检测有专门用例**——构造一次跨越阈值的冻结/唤醒，断言唤醒后**无任何超时类判据误触发**、且宽限窗口结束后判据恢复正常生效；若基准显示超线，按预案将 ack 与 result 分离（ack < 5 s，**ack 的两类定义见 P1.1**）并回写 `protocol.md` → **AC-2 后半**
- **测量口径（v2.2，同步 D-3）**：上述"10 次测量"跑在**独立的基准 job** 上并**产出留档报告**，**不作为 CI 的阻塞门禁**；CI 阻塞位只放**超时兜底**（抓死锁与挂起，对抖动免疫）。章程 §4 已按此修订，**D-3 已于 2026-08-12 经 P0.8 确认通过**（章程 v2.8），此口径即为现行判据
- **交付物追加（v2.15）· 给 ACP 补一个信箱读者 —— 本包最大的一笔隐藏工作量**：章程 §341 定死最后一跳「复用基座既有的文件信箱」，`packages/adapter/src/inbound.ts` 已照此交付并验收。**但该信箱在 ACP 宿主下没有读者**：唯一能在空闲时唤醒会话的读者是 `src/hooks/useInboxPoller.ts`，它是 REPL 专有的 React hook（唯一使用点 `src/screens/repl/useReplAutomation.ts:116`）；headless 侧的 `pollTeamLeadInbox` 只在 turn 结束后调一次（`src/cli/print/headlessTurnLoop.ts:243`），空闲等待期间不轮询；**`src/services/acp/` 全目录零信箱引用**。也就是说「常驻宿主选 ACP」这个决定，把 P2.1/P2.2/adapter 已经打通并验收过的 **AC-2 最后一跳的读者拿走了**，而此前**没有任何任务包覆盖这件事**。**处置（v2.15 决策）：保信箱、给 ACP 补等价轮询器**，不改协议——理由见下方「v2.15 决策」。量级约 150–300 行 + 测试
- **交付物追加（v2.15）· 时间跳跃闸门是「覆盖面」不是「造机制」**：闸门本身**已由 P2.5 交付**（`packages/activator/src/clock.ts` 的 `TimeJumpGate`，`packages/transport/src/backoff.ts` 另有同源一套），`protocol.md` §T-2 已写成协议强制项。本包剩下的是**接到还没接的地方**：`@qianmo/*` 侧 8 处未保护（危害最高的三处是 `transport/src/client.ts` 心跳超三周期即 `socket.terminate()`、`registry/src/registry.ts` 的 `#live()` 过期即**删除**而非软判离线、`adapter/src/inbound.ts` 的 T-1 检查未过闸门），基座侧约 20 处**在 ACP 进程内可达**的判据（最高危是 `src/services/api/claude.ts` 的 90 s 流空闲看门狗，**默认开**）。这是本包最大的单项工时来源
- **阈值体检（v2.15，需在本包内解决）**：两处已知的数值撞车——`LIMITS.defaultTtlMs = 30_000` **小于** E4 实测的 34.7 s 冻结；`registry` 的 `DEFAULT_TTL_MS = 90_000` **小于** E4 实测的 97 s 冻结（后者更糟：过期走的是删除，`heartbeat()` 此后返回 `null`，**心跳无法自救、必须重新 register**）。另有两个 factor-2 检测盲区（`backoff.ts` 阈值 60 s、`keepalive.ts` 阈值 40 s，而 E4 冻结 34.7 s **检测不到**）

**P3.2 🔍 编程智能体闭环核验**（状态：✅ 已完成，见「完成状态速查」）
- owner：陈子轩（backup：董宗岳）
- 依赖：P1.3、P1.4
- 说明：**性质从"实现"变为"核验"**。接任务 → 读代码 → 改文件 → 跑测试 → 报结果这条闭环是基座的核心能力，本包不重写它
- 交付物：在团队自有仓库上的真实任务用例集；失败时的结构化输出（供 S-1 诊断消费）——若基座既有输出形状不够结构化，在此补齐
- DoD：在团队自有仓库上跑通 3 个真实小任务（如加一个函数 + 单测、修一个已知 bug、补一处类型定义），3/3 通过仓库自带测试；失败路径能产出可被 P5.1 消费的结构化结果

**P3.3 记忆检索唤醒 v0**（状态：✅ 已完成，见「完成状态速查」）
- owner：董宗岳（backup：李怡康）
- 依赖：P2.3
- 交付物：确定性检索（标签匹配 + 关键词 + 时间衰减排序，**不做向量检索**，章程 N-8）；检索结果带来源 ID 注入提示词；回答中标注引用来源
- **v2.2 补充（落实 D-6，章程 §3.2 R-2）**：在确定性检索之上叠加**小规模全量注入**，来源标注走**工具层强制引用**而非任何单一供应商的原生引用块。两个理由：① 实测纯确定性检索在对抗查询上会失手（典型失败是提问用词与条目用词不匹配，直接 0 结果），条目规模小时全量注入能把 DoD 的 5/5 从"概率"变成"结构性保证"；② 供应商原生引用块与结构化输出互斥且只此一家有，照它实现会让 **AC-4 与 AC-5（模型中立）在 S1 之后互相打架**——**AC-4 与 AC-5 的冲突就是这样解除的，不要退回原生方案**
- DoD：写入 5 条历史决策，新开无历史会话提问，命中 5/5 且标注来源 ID 与写入时间；对 3 条从未写入的伪造决策**不产生幻觉引用**；**（v2.2）来源标注在两家供应商下均生效，切换供应商不改代码**（与 AC-5 同口径） → **AC-4**

---

### S4 · 2026-10-12 ~ 10-25 — 跨节点投递打通（M0 最关键的一期）

**P4.1 ⭐ 跨节点任务消息端到端**（状态：✅ 已完成，见「完成状态速查」）
- owner：陈曦宇 + 董宗岳（联合，喻永昌兜底）
- 依赖：P2.1、P2.2、**P2.5**（v2.2 新增）、P3.1
- 交付物：完整链路——节点 A 智能体发起 → 注册中心按名解析 → 传输通道加密投递 → **activator 接住并唤醒**节点 B 上休眠的智能体 → 返回 ack → 执行任务 → 返回 result
- DoD：连续 10 次测量，ack P95 ≤ 60 s、result ≤ 5 min、成功率 10/10；链路可一键复现；**本任务包为 M0 单点风险最高项**，S4 第一周结束时若未打通端到端最小链路（哪怕只有 ack），立即启动每日站会跟踪 → **AC-2**
- **测量口径（v2.2 新增，同步 D-3）**：**"连续 10 次测量"保留**，但明确它**跑在独立的基准 job 上、产出留档报告**（同机同 job 内测基线、报相对差值），**不作为 CI 的阻塞门禁**；**CI 阻塞位只放超时兜底**。理由：延迟类静态阈值在共享 runner 上的假阳性率高到会让门禁失去判别力，**一个天天变红的门禁等于没有门禁**。章程 §4 已按此修订，**D-3 已于 2026-08-12 经 P0.8 确认通过**（章程 v2.8），此口径即为现行判据
- **v2.0 补注**：本包的前置链在 v2.0 中变长了（多了 P0.2 与 P2.2 的实际工作量），而日期未变。若 S2/S3 出现欠账，**本包是最先被挤压的**——按章程 §0.4 欠账阈值处理，不要靠 S4 加班吸收
- **v2.2 补注**：前置链**又长了一环**（P2.5），日期仍未变。P2.2 的工作量虽已下调，但下调出来的余量**已经被 P2.5 吃掉且不止**——**不要把 P2.2 的降级理解为 S4 的宽裕**
- **实施进展（v2.19，2026-08-13）· 代码完成、判据未验**：回程链路已落地——双向逻辑通道（`channelId` 受 HMAC 覆盖、断线重连重绑并重放反向 outbox）、activator 有界 task route registry（沿原请求路由回程，无第二条反向连接）、resident 在 durable read 后发 ack、在 ACP turn 终态发封闭 `task.result`。协议侧同步收缩 ack payload 并新增 `task.result` 联合与 `E_TASK_FAILED`（见 v2.19 变更记录与 `protocol.md` §4.6）。**剩余全部工作是判据本身**：按 D-3 在独立基准 job 上跑连续 10 次真机测量（ack P95 ≤ 60 s、result ≤ 5 min、10/10）并留档，以及一键复现脚本。**在那份报告出来之前，本包与 AC-2 都不得记为完成。**
- **验收完成（v2.20，2026-08-13）**：上一条列的两件事都已交付——一键复现是 `demo/p41-task-result.sh`，真机 10/10 的基准报告见 v2.20 变更记录（ack P95 **4.440 s**、result max **5.885 s**；**v2.35 勘误**：v2.20 正文与速查表记的是 **8.022 s / 12.422 s**，以其为正式记录，此处两数系早先一轮、无原始报告可核）。**三点必须一并记住**：① 判据线是 60 s / 5 min，实测低一个数量级，但**样本是固定 SSE 模型桩**——它测的是链路，不是真实模型的思考时间，别拿这组数字当「任务耗时」的基准；② 报告里的 `checks` 有八条且不合并（轮数 / 成功率 / ack P95 / result max / 每轮 frozen / 回复字段封闭 / 无越界回复 / 按名解析），任何一条为 false 即整体 false——复跑时看 `pass` 之外还要看是哪条撑着它；③ **十个样本的 P95 就是最大值**，而最大值一直是第 1 轮（冷链路建连），所以这项判据实际上是在报「最慢的那一轮」——这对 60 s 的线绰绰有余，但别把它当成稳态分位数读。

**P4.2 防循环与限流**（状态：✅ 已完成，见「完成状态速查」）
- owner：陈曦宇（backup：陈子轩）
- 依赖：P1.1、P4.1
- 交付物：
  - **防循环（v2.2 修正判据粒度，同步 D-2）**：判环粒度由「首次回访**节点**」改为「**首次回访同一处理者地址（`node/agent`）+ 同一任务标识**」，`LIMITS.maxHops` 仅作兜底。**理由不是放松验收线，是修一个会误杀合法路由的设计**——同一节点因不同目标地址被再次经过是正常路由，业界已有同样的节点粒度设计被规范级定性为 bug 的先例（依据见 `selection-m0.md` §1 D-2）。另需一并处理调研查出的两个附带缺陷：起始节点**不自我播种**、自动回复的乒乓路径**现有开关全抓不到**
  - 消息指纹去重表（带 TTL）——**v2.2：TTL 的对齐对象改为 P1.1 拆分后的「投递时限」字段**，不再对齐那个身兼两职的旧字段（旧写法正是"30 s 存活的消息等不到 60 s 回执"这一矛盾的根因）。**具体数值以 `@qianmo/protocol` 的 `LIMITS` 为唯一出处**
  - **限流**：运行时层的令牌桶——**单发送方对单目标** 60 s / 20 条。注意这与协议层 `LIMITS.ratePerMinute`（接收节点对单发送方的**每节点入站预算**）是**两层不同机制**，两者独立生效，文档与代码中不得混写或互相覆盖
  - `loop_detected` 与 `rate_limited` 审计事件
- DoD：构造 A→B→A 回环，**在首次回访同一处理者地址 + 同一任务标识时即被切断**并产生含完整 trace_id 的审计事件；**另有反向用例断言"同一节点因不同目标地址被再次经过"不被误判为环**（这是 D-2 改动的全部意义所在，没有这条用例等于没改）；运行时节流的第 21 条消息被拒并返回明确错误码；另有用例断言协议层入站预算独立生效 → **AC-3**
- **说明（v2.2；v2.11 更新）**：章程 §4 的 AC-3 已按 D-2 修订，**D-2 已于 2026-08-12 经 P0.8 确认通过**（章程 v2.8）。处理者粒度即为现行判据，原「被否决则回到节点粒度」的分支**已关闭**；上述反向用例是硬要求，不再有降级选项
- **实施记录（v2.21，2026-08-14）**：交付物逐条对上，另有三处交付时才看清的东西，写在这里而不是埋进代码注释：
  - **回复类消息必须排除在判环之外**。`ack` / `task.result` / `error` / `pong` 按 C-1 带着原任务的 `taskId` 回到请求方，这与「同一处理者地址 + 同一任务标识再次出现」**是同一个形状**。照判据字面实现会在第一条 ack 上切断 AC-2 的回程——即 P4.1 刚验收的那条链。判据本身不必改（它说的是「回环」），但实现里这条线必须显式画出来，因此区分函数落在 `@qianmo/protocol`（`isReplyType`）而不是路由包里
  - **去重表不新建**。交付物列的「消息指纹去重表（带 TTL）」`@qianmo/transport` 已有（`msgId` + `fingerprint` 两级，表项以**投递时限**过期，正是 v2.2 要求对齐的那个字段）。再造一张只会让网络有两张去重表、一份契约
  - **接线时撞出一条已存在的限制，一并记在案**：反向用例（同一任务、同一节点、**不同处理者**）在**判环层是通过的**（不写 `loop_detected`），但在 activator 与常驻节点上仍会被**回程路由**拒掉——P4.1 的 task route 以 `taskId` 为唯一键，而从沙箱回来的 `ack` 也只带这一个键，两条并发路由分不开。**这是相关性约束，不是判环**，错误码也不同（`E_BAD_ENVELOPE`，非 `E_LOOP`），`packages/activator/test/chain.test.ts` 里有一条用例把这两件事分别断言。**后果**：M0 不支持「同一任务并发派给同一节点的多个 agent」。要支持得给回程路由加处理者维度，那是 P4.1 的改动，AC-3 不要求
  - **两条边界如实记**：① 运行时令牌桶 M0 内**没有长驻的生产调用方**——agent 面的跨节点发送工具不在章程 §3 范围内，现有生产接线只有一次性的 `residentWake` CLI（每次进程新起，桶总是满的）与复现脚本；② `withHop` 的「转发前追加」半边有实现有用例，但 **M0 没有第三方中转节点**，同样无生产调用方。两条都不影响 AC-3 的判据成立（判据要的是机制存在且可验证），但谁要是据此宣称「限流已在生产链路上跑了一个月」，那是冒领
- **一键复现**：`demo/ac3-loop-rate.sh`（+ `demo/lib/ac3-{loop-rate,report-core}.ts`）。四个场景各起一对全新节点走真 transport（unix socket，按 P2.2 测试口径不用 TCP），十条 check 逐条留痕、不合并。**不需要沙箱与 daemon**，只需一把 PSK

**P4.3 权限分级与授权模型 v0**（状态：✅ 已完成，见「完成状态速查」）
- owner：陈曦宇（backup：喻永昌）
- 依赖：P4.1
- 说明：基座已有工具权限模式与审批链路可复用作本地执行侧；**跨节点身份与"消息不能替用户授权"的协议级强制是自研**
- **v2.2 补充（落实新增风险 T-7）**：跨节点消息最终会进入对端智能体的 LLM 上下文，**等价于把不可信输入喂给模型**。它相对既有攻击面有三重独立性：独立的信任边界（节点之间）、独立的放大机制（恶意内容可在互联的智能体间自我复制）、独立的失效模式（**confused deputy** 式伪授权）；其中"记忆与上下文注入"这一类直接命中阡陌的记忆系统。**判定基准必须写死：不以"模型是否被说服"作为验收依据**——业界 2026 年的一致口径是这类攻击不会被"解决"，只能提高成本，因此验收要落在**结构性阻断**（模型即使被说服，权限提升在协议与执行层也过不去）
- 交付物：智能体身份与能力清单；三级权限（只读 / 受限写 / 需用户确认）；**"消息不能替用户授权"的协议级强制点**——跨节点消息无法提升本地智能体权限等级；**（v2.2）capability 的 Ed25519 签发与校验落地**（D-8；定义在 P1.1，本包实现）——预共享密钥只作接入门禁，**授权凭据一律走每节点私钥签名**，使 C-5 由"约定"回到"结构性保证"
- DoD：越权调用被拒并留痕；**攻击用例按 T-7 扩展——不只"对端声称已获用户授权"一种注入形态**，至少覆盖：伪造授权凭据、经由记忆/上下文注入的间接指令、以及跨节点消息中夹带的越权指令，每类各 ≥ 1 条，**判据一律是"权限未被提升 + 留有审计事件"，不是"模型没有照做"**；伪造签名的 capability 被拒；用例进 CI
- **实施记录（v2.22，2026-08-14）**：DoD 逐条对上（攻击用例三族 14 条 + 1 条正向对照，全部判「等级没被抬高 + 审计有记录」；伪造签名、篡改后重签、第三方节点重放、未知签发者各有用例；越权拒绝走 `capability_denied` 审计事件），另有三处交付时才定下来的东西：
  - **回复类消息不需要授权**：`ack` / `task.result` 是**本节点自己发出的请求**的回音，要求它们出示令牌等于要求对端为我们的提问付费。`SIGNED_TASK_POLICY` 因此只抬高 `task.request` 与 `wake` 两类
  - **capability 的校验点在终点节点，不在 activator**：令牌的 `aud` 是**目标节点**，而 activator 跑在宿主上、节点段与沙箱内的常驻节点不同（P4.1 的拓扑里是 `node-b-host` 与 `node-b`），把闸门放在 activator 上会让每一条合法令牌都因 `aud` 不匹配而被拒。宿主这一跳仍有判环与限流
  - **M0 默认不强制出示令牌**，理由与边界见上方完成状态表与 `protocol.md` §10.1 的四条边界。**这条必须原样转述，不得简化成「P4.3 打开了跨节点鉴权」**——打开的是机制与可切换的策略，默认档位仍允许未签名的任务请求

**P4.4 备份与不可删除保护**（状态：✅ 已完成（本机），见「完成状态速查」）
- owner：董宗岳（backup：陈子轩）
- 依赖：P1.3
- **选型已定（v2.2，章程 §3.2 R-4）**：备份**自研，不用沙箱平台自带的归档能力**——它的归档是**移动而不是备份**（恢复成功后源对象即删，全程单副本、无时间点），对 AC-6(b) 零覆盖。**不要在本包里把它当现成方案再评估一次**
- 交付物：工作区定时快照 + 任务前快照；备份存储挂载在沙箱权限之外，智能体持只写凭据
- DoD：智能体 `rm -rf` 删除工作区后，10 min 内完整恢复且 `git status` 与删除前一致；智能体尝试删除备份被拒并留痕 → **AC-6(b)(c)**
- **实施记录（v2.23，2026-08-14）**：DoD 两条本机全绿（`demo/ac6b-restore.sh`，十一条 check 逐条留痕），另有两处需要写下来的判断：
  - **「智能体持只写凭据」不能靠文件权限实现**。POSIX 上删除权限来自目录写位，与创建是同一个位；sticky bit 只收窄到「删自己的」，而要防的正是这一种。所以只写这件事由**进程边界**承担：store 在宿主侧，沙箱只经一个没有删除动词的 socket 面够到它。这不是实现选择的偏好问题，是这条判据在文件系统上无解
  - **任务前快照在常驻节点里不等待**，理由与代价见完成状态表。要「恢复到任务开始的那一瞬」这种更强的保证，得由掌握任务生命周期的调用方 `await beforeTask`——脚本化的跑批可以，ACP turn 里不行
- **真机剩余项**：本机测到的是凭据面与动词面，**不是挂载边界**。部署时仍须把 store 目录放在沙箱挂载之外，并只把只写凭据注入沙箱（`QIANMO_BACKUP_WRITE_TOKEN`）；这一条与 P0.7 的宿主前提同类，属部署面

---

### S5 · 2026-10-26 ~ 11-08 — 调度层：诊断 / 协商 / 隧道

**P5.1 原因级诊断 v0**（状态：✅ 已完成，见「完成状态速查」）
- owner：李怡康（backup：董宗岳）
- 依赖：P3.2
- 说明：基座有成熟的错误分类与重试分类机制，其**接口形状可借鉴**，但阡陌需要的是任务级故障归因（OOM / 超时 / 依赖缺失 / 磁盘满 / 额度耗尽），与之不是同一层
- 交付物：故障分类器，输出结构化 `diagnosis` 事件，覆盖 5 类；每类附建议动作
- DoD：注入 5 类故障各 10 次，人工标注 50 条样本，**分类准确率 ≥ 80%**；输出中明确报出原因类型而非"执行失败" → **AC-7 环节 2**
- **实施记录（v2.24，2026-08-14）**：DoD 逐条对上（50 条真实注入样本、盲分类 50/50、每条判定带依据与建议动作），另有两处必须写下来的东西：
  - **`exit 137` 分不出超时与 OOM，这不是分类器能修的**。SIGKILL 谁发的都长这样，靠日志关键词在两者之间选一个就是抛硬币。修法是让**发信号的那一方当场记下事实**——我们的看门狗记 `timeoutEnforced`，宿主记 cgroup 的 `oom_kill` 增量——分类器先读事实、再读文本。这也是为什么本包的难点在**证据链**而不在规则表
  - **首轮跑批把一个真缺口抓了出来**：OOM 类只有 5/10，原因是 **Bun 撑爆内存时被系统 SIGKILL 且一个字都不写**。没有降阈值、没有加猜法，补的是一条事实（「这个信号不是我们发的」），据此得出「SIGKILL + 非本方发出 → OOM，中等置信」——中等而非高，因为运维手动 `kill -9` 长得一模一样，高置信只能来自 `oomKillDelta`
- **剩余项**：语料仍是**自造失败**。真机跑批时应把 P3.2 真实失败与 S6 压测中出现的失败并进来复算准确率——自造样本证明的是规则表自洽，不是野外覆盖率

**P5.2 资源协商协议**（状态：✅ 已完成，见「完成状态速查」）
- owner：喻永昌（backup：陈曦宇）
- 依赖：P4.3
- 交付物：四段式协商消息（请求 → 报价 → 授权 → 释放）；硬上限字段（时长、CPU、内存、`cost_limit` 恒为 0）；协商超时与拒绝路径；写入 `protocol.md`
- DoD：正常协商全流程用例通过；请求超出硬上限被拒；报价阶段超时后自动放弃且不留悬挂状态
- **实施记录（v2.25，2026-08-14）**：三条 DoD 逐条对上（全流程、超上限、报价超时无悬挂），另有三处交付时定下来的东西：
  - **硬上限是贷方的本地策略，不进 `LIMITS`**。理由不是嫌麻烦：`LIMITS` 是全网必须一致的数，而「这台机器愿意借多少」逐部署不同且与别人无关；写进去等于把本地决定升格成要改章程才能调的全网常量。**字段在协议里，数值在策略里**
  - **「用户授权」这一段不在消息里**。roadmap 原文的四段式写作「请求 → 报价 → **授权** → 释放」，落地时把「授权」定在**贷方本地**：借方声称已获授权正是 C-5 点名的 confused deputy，而 S-1 已经把远端签发的 `user-confirmed` 令牌封死。所以第三条消息是 `resource.grant`（借方接受报价），而授权发生在贷方发出报价**之前**、由本地钩子回答
  - **协商消息必须算「回复类」**，否则一次协商的第二程会被 P4.2 的判环误杀——同一个 `taskId` 在两个方向上来回走正是判环键描述的形状
- **剩余项**：本包只到「有一份租约，和一张证明它的令牌」为止。**真正把活跑在借来的资源上是 P5.3 的隧道**；两者的接缝（令牌怎么在隧道握手里出示）在 P5.3 落

**P5.3 按需加密隧道**（状态：✅ 已完成，见「完成状态速查」）
- owner：陈曦宇（backup：董宗岳）
- 依赖：P5.2
- 交付物：授权后建立隧道 → 任务在借用资源上执行 → 拆除；三种拆除路径（正常结束 / 超时 / 对端异常退出）；建立与拆除均产生审计事件
- DoD：三种拆除路径用例全部通过，拆除后无残留连接与残留凭据（`ss`/进程检查断言）；异常退出场景下 60 s 内自动拆除 → **AC-7 环节 4、6**
- **实施记录（v2.26，2026-08-14）**：三条拆除路径各有用例并全部通过；「无残留」用**再拨一次那个地址、要求拨号失败**来证明——一个 `stop()` 之后的祝愿和一次真的拨号失败不是一回事。异常退出走的是 transport 的 `onPeerDisconnect`，**socket 一关就拆**（本机实测即时，远不到 60 s）；对端不关 socket 只是不说话时，兜底是 `idleTimeoutSec`（默认 30 s，同样在 60 s 线内）。另有三处写下来的东西：
  - **不造新加密层**。N-3 把 M0 钉在 TLS + PSK 上，「按需加密隧道」里真正新增的是「按需」：协商之前没有监听，租约结束之后也没有。把这条说清楚，比声称做了个隧道加密要诚实
  - **隧道客户端关掉重连**。默认重连对节点长连接是对的，对隧道恰恰是错的——租约结束后又回来的连接就是活过了条款的租约
  - **隧道断 ≠ 租约还**。对端消失时隧道拆掉，但租约仍在贷方账上直到自己到期或被显式释放；混为一谈会让一次网络抖动提前把别人的机器还回去
- **凭据残留的诚实边界**：令牌只存在一个对象的一个字段里，拆除时该对象被释放。**这是有 GC 的运行时，「从内存里抹干净」不是代码能承诺的**，所以代码与文档都不承诺；`ss` 那一半（无残留连接）由再拨一次证明，凭据那一半只能保证不再被任何在用对象引用

**P5.4 边界用例库 v1**（状态：✅ 已完成，见「完成状态速查」）
- owner：陈子轩（backup：陈曦宇）
- 依赖：P4.2、P4.3
- 交付物：`tests/boundary/` 目录，五类边界（触发时机 / 超时 / 消息风暴 / 额度耗尽 / 异常退出）各 ≥ 1 条自动化用例；接入 P0.4 已建好的 CI 门禁
- DoD：≥ 8 条用例进 CI 且全绿；PR 模板必填项生效，无用例的边界修复 PR 被评审拒绝
- **实施记录（v2.27，2026-08-14）**：29 条用例、五类齐备、53 个 CI 分片全绿。两点写下来：
  - **「进 CI」不是把文件放进仓库就算数**。`scripts/test-shards.sh` 的分片列表原本是 `src/* packages/* tests/integration scripts`，新目录不在其中就**在 CI 里根本不跑**——而本地 `bun test` 照跑。按这条查下去发现 `demo/lib` 下的报告核心用例（P4.1 起就有）从未在 CI 里跑过，本次一并加进列表
  - **用例的价值在于它挡住什么**：这一批里有四条是明确的防倒退用例（D-2 的反向 spiral、处理器抛错要撤回去重项、隧道断不等于租约还、`costLimit` 字段必须还在）。其余的是组合覆盖，不与包内用例重复

---

### S6 · 2026-11-09 ~ 11-22 — 场景整合与压测

**P6.1 ⭐ 数模场景端到端串联**（状态：✅ 已完成，AC-7 达成，见「完成状态速查」）
- owner：李怡康（协同：全体，喻永昌主持）
- 依赖：P5.1、P5.2、P5.3、P3.2
- 交付物：完整演示脚本 + 数据集 + 一键复现 Makefile；演示环境的种子数据与重置流程
- DoD：**全程无人工修改代码或数据**，连续跑通 ≥ 10 min 完整链路（提交任务 → OOM 诊断 → 资源协商 → 用户授权 → 隧道建立 → 借用资源跑完 → 隧道拆除），连续 3 次成功 3/3 → **AC-7**
- **v2.0 补注**：演示脚本**只覆盖章程 §3 范围内的能力**。基座自带的插件市场、Artifacts、语音、Computer Use 等能力不进演示（章程 N-9、风险 P-3）——它们看起来能加分，但会让"哪些是本项目做的"这个问题在答辩现场变得无法回答
- **实施记录（v2.30，2026-08-14）**：`make -C demo p61-accept` 严格串行三轮正式运行 3/3；每轮从任务提交到 release / redial / post-teardown background 均超过 600,000 ms，20 个 worker 完成点覆盖约 510 s，七项报告 check 全 true，`ac7Eligible=true`。逐轮时长、digest、背景计数和审计计数见 v2.30 变更记录
- **证据补强（v2.31，2026-08-15）**：观测由宣称改为测量（uncaught 真处理器、skipped 差集推导）后，在提交 `87c4609b` 上重跑正式三轮 3/3 并以 `QIANMO_P61_KEEP_ARTIFACTS` 完整留档、打包归档；scenario-mcm §9 的 3/3 表以该次为正式记录。真机副本待 workbench-host SSH 恢复后补传
- **边界**：授权是贷方本地 `scripted-hook`，不是人工点击；A/B 是同一 runner 内组合的逻辑节点，unix 链路不冒称跨机 TLS；隧道只承载 A→B 工作，结果由 runner 从 B 侧 store 读取；生产常驻拓扑接线仍归 P8.2。P2.4 草案已产出但真实痛点与全员评审未完成，不随 AC-7 一并冒领

**P6.2 预测性扩容 v0**（状态：✅ 已完成，见「完成状态速查」；v2.32 记录了一处待负责人确认的 DoD 解读）
- owner：李怡康（backup：喻永昌）
- 依赖：P5.1、P2.3（基线记忆）
- 交付物：基于用量基线 + 赛事日历的提前扩容触发器；触发决策写入审计
- DoD：回放历史用量数据，能在负载峰值到来前 **≥ 30 min** 触发扩容；对平稳负载不误触发（假阳性 ≤ 1 次 / 7 天回放）

**P6.3 🔍 MCP 兼容子集核验**（状态：✅ 已完成，见「完成状态速查」）
- owner：董宗岳（backup：陈子轩）
- 依赖：P1.4
- 说明：**性质从"实现"变为"核验"**。基座已是成熟的 MCP client 与 server（出处：基座 `docs/zh/features/mcp-2026.md`）
- 交付物：接入 ≥ 2 个现成 MCP server 的实测记录；server 不可用时的降级验证
- DoD：两个 server 的工具均可被智能体发现并成功调用，用例进 CI；server 不可用时降级不崩溃

**P6.4 A2A 对齐评估报告**（状态：🟡 草案已产出、待全员评审，见「完成状态速查」）
- owner：喻永昌（backup：陈曦宇）
- 依赖：P1.1、P5.2
- 交付物：`docs/dev/a2a-gap.md`——我方协议与 A2A 的字段映射表、差距清单、M1 收敛建议（**只评估不实现**，章程 N-4）
- DoD：全员评审通过；每条差距标注"M1 收敛 / 长期保留差异 / 不适用"三选一

---

### S7 · 2026-11-23 ~ 12-06 — 稳定化与可观测性

**P7.1 边界用例库 v2 + 混沌注入**（状态：✅ 已完成，见「完成状态速查」）
- owner：陈子轩（backup：陈曦宇）
- 依赖：P5.4、P6.1
- 交付物：五类边界每类 ≥ 2 条，总数 ≥ 12 条；混沌注入脚本（随机杀进程 / 断网 / 磁盘打满 / 时钟漂移）；每双周"边界日"的执行记录模板
- DoD：≥ 12 条用例进 CI，**连续 5 次构建全绿**；混沌注入 1 小时无未捕获异常（有捕获的失败要能对应到已知边界） → **AC-8**
- **实施记录（v2.29，2026-08-14）**：边界目录共 **39 条**，`tests/boundary` 已列入 CI 分片。正式跑批 seed `2145259399`，60 分钟内完成 **177 次注入**（四类分别 42 / 47 / 45 / 43）、成功投递 **7,389 条**；每类停摆数均为 0，0 未捕获异常、0 unmapped、审计链完整，87 条被捕获失败均为真实 ENOSPC 并正确映射到 disk full。恢复最大值 103 / 513 / 112 / 103 ms，报告五条 check 全 true。**CI 五连绿已完成**：CI 先后抓到根 workspace 依赖、Knip 入口图与 watcher 测试时序三处缺口，修复于 `acf5e57` / `36ce286` / `a8b06a9`。随后同一 SHA `a8b06a9` 上五次严格串行 workflow（`31799841764` / `31800391530` / `31800890960` / `31801288270` / `31801769324`）均为 `completed / success`；至此本任务包 DoD 与 AC-8 全部通过。

**P7.2 可观测性：审计日志与全链路追踪**（状态：✅ 已完成，见「完成状态速查」）
- owner：陈曦宇（backup：陈子轩）
- 依赖：P4.1、P4.2
- 交付物：trace_id 贯穿全链路；消息全量留痕（append-only，不可改）；日志查询 CLI（按 trace_id / 智能体 / 时间窗查询）
- DoD：任取一次跨节点任务，用 trace_id 能还原完整消息链（含被丢弃、被限流、被去重的消息）；审计日志的修改尝试被拒
- **实施记录（v2.28，2026-08-14）**：两条 DoD 都跑出来了，其中第二条必须**按字面拆开说**：
  - **「修改尝试被拒」在这套实现里成立两次、不成立一次**。写入方确实改不了（`O_APPEND | O_NOFOLLOW` 的 fd，类里没有 seek / truncate / delete）；外部改动确实可检测（哈希链，断点定位到 `seq`）；**外部改动无法阻止**——有文件写权限的人可以整篇重写并重算哈希。挡住这一条要 append-only 挂载或机外见证，M0 两样都没有，所以代码和文档都不声称它。包内有一条**专门用例**把「整篇重写不会被发现」钉死，免得读套件的人以为文件不可变
  - **还原必须按 `seq` 排序，不能按时间戳**：两个节点的钟不一致，按时间排会把 ack 排到它回应的消息前面。`seq` 是这份文件自己的顺序，也是这里唯一存在的全序
- **接线现状**：六层的映射函数都已就位（路由 / 传输 / activator / 协商 / 隧道 / 备份）。**常驻节点的生产路径已完整接好**——`occ resident` 开一份 trail，路由层的拒绝与传输层的消息事件都写进去；缺哪一半都不行：只有拒绝的链会只显示出错的部分，只有投递的链会把被拒的那几条藏起来。**activator、协商、隧道、备份四层的 sink 已实现并有用例，接进各自启动路径留给 P8.2** 按演示拓扑一次性做（现在接等于对着还没定下来的拓扑猜调用点）。映射里有两处值得留意：**「过期」不是「被拒」**（一条到点的路由没有被任何人拒绝，写成 refused 会让读的人去找一个不存在的决定），以及**对端字段按层取名**（`from` / `borrower` / `sandboxName`），猜错了就是在审计行上写错地址，比留空更糟

**P7.3 性能与稳定性基线报告**（状态：🟡 aarch64 降级栏吞吐数据已入库、24 h 长跑运行中、x86 栏待真机，见「完成状态速查」）
- owner：李怡康（backup：董宗岳）
- 依赖：P6.1
- 交付物：`docs/dev/baseline-m0.md`——唤醒延迟分布、消息吞吐、内存占用、长跑稳定性（连续运行 24 h）
- DoD：数据入库并标注测量环境；作为 M1 的对比基准（章程 N-12：只测不调优，除非卡验收线）
- **v2.0 补注**：内存占用一项要特别测。基座是单进程长驻形态，其自身文档记录过长会话下的堆驻留问题与已落地的裁剪机制（出处：基座 `docs/zh/features/memory-footprint.md`）；阡陌把它变成**真常驻**，等于把这个问题从"长会话"推向"长期"。24 h 长跑的观测重点就在这里

**P7.4 ⚖️ 上游同步演练**（取代 v1.1 的"vendor 解耦验证"）（状态：🟡 演练、章程回写、BASE.md 记录已做，评审待做，见「完成状态速查」）
- owner：喻永昌 + 陈子轩
- 依赖：P0.3、全部功能包
- 说明：**目的是测出冲突成本，不是真的升级**（章程 §5.7、N-13）。M0 期间基座保持 pin 冻结
- 交付物：把上游作为 git remote，在**一次性分支**上尝试合并到某个较新 tag；产出冲突清单（哪些文件、哪类改动、身份隔离改名与常驻化改造各贡献多少冲突）、工时估算、以及"下次同步前应该怎么改代码才能少冲突"的结论；`BASE.md` 记录本次演练
- DoD：冲突清单与工时数据入库并评审；演练分支**不合入主干**；结论回写章程 §5.7 作为 M1 排期输入

---

### S8 · 2026-12-07 ~ 12-20 — 验收走查与演示固化

**P8.1 演示环境固化**（状态：🟡 本地腿完成且两台全新机器实测通过、真机腿未验证，见「完成状态速查」）
- owner：董宗岳（backup：陈子轩）
- 依赖：P6.1
- 交付物：两台节点的部署脚本、种子数据、一键重置；环境依赖清单（含 Bun 版本要求——基座对此敏感）
- DoD：在一台全新机器上，**30 min 内**从零复现完整演示环境（由未参与部署的成员实测）

**P8.2 全量验收走查**（状态：🟡 本机腿走查完成留档、真机腿与凭据腿待做，见「完成状态速查」）
- owner：喻永昌主持（全体参与）
- 依赖：全部功能包
- 交付物：AC-1 ~ AC-8 逐条录屏 + 判定记录表（含测量数据）；未通过项的根因与 M1 补做计划
- DoD：8 条全部 PASS，或未通过项 ≤ 2 条且有双签豁免决议（章程 §4）

**P8.3 ⚖️ 竞赛/答辩材料对齐**（状态：🟡 草案已产出、待双签，见「完成状态速查」）
- owner：喻永昌 + 陈曦宇
- 依赖：P8.2
- 交付物：对外表述统一口径文档（章程 §5.8）；演示视频；答辩 Q&A 预案
- DoD：负责人 + 安全 owner **双签**通过；材料中基座既有能力与本项目新增能力**分列两栏不混排**（风险 L-1）；Q&A 预案**必须包含**两类问题的书面答案——「你们自己做了什么」（用 `git diff 3380c88..HEAD` 举证；**v2.4 修正**：旧文的 `848ad8c2..HEAD` 在本仓库跑不通，见章程 §5.5「举证命令勘误」）与「基座是逆向工程产物吗」（如实陈述，章程 §5.8 第 3 条）

**P8.4 ⚖️ 软著与开源预检**（按新许可链重写）（状态：🟡 工程侧完成、NOTICE / BASE.md 已闭环、科技处咨询待人，见「完成状态速查」）
- owner：陈曦宇（backup：喻永昌）
- 依赖：P7.4、P8.3
- 交付物：第三方依赖 SBOM 与许可证清单（**范围含基座既有依赖**，这是比旧路线大得多的一张表）；许可链证据包（基座 `LICENSE` MIT、npm 公开发布记录、负责人对基座的归属关系、pin 提交与 tag）；根 `NOTICE` 与实际状态一致性核对；**学校科技处咨询记录**（议题至少含风险 L-2 与 L-5）
- DoD：无 GPL 类传染性许可项（或已隔离并记录）；`NOTICE` 与实际状态一致；许可链证据包齐备；**科技处咨询已完成并有书面或可复述的口径**——若口径要求更强隔离，立即触发范围评审（风险 L-2 对策⑤）

---

### S9 · 2026-12-21 ~ 2027-01-03 — 收尾与 M1 拆解（50% 产能，期末）

**P9.1 缺陷收敛与回归**（状态：🟡 本机口径回归三连绿、真机腿纳入前不判完成，见「完成状态速查」）
- owner：陈子轩（backup：董宗岳）
- 依赖：P8.2
- DoD：P0 / P1 级缺陷清零；全量回归（含 12 条边界用例）连续 3 次全绿

**P9.2 M0 复盘与 M1 任务拆解**（状态：🟡 复盘草案已产出、评审待人，见「完成状态速查」）
- owner：喻永昌（全体参与）
- 依赖：P8.2、P7.3
- 交付物：`docs/dev/retro-m0.md`（做对了什么 / 估错了什么 / 排期偏差数据）；M1 任务包初稿；charter 版本升级
- DoD：复盘文档评审通过；M1 首两个迭代的任务包已明确 owner 与依赖；**复盘必须回答一条**：基座路线变更实际省下/多花了多少工时，与 `base-adoption.md` 的事前估计差多少

**P9.3 文档补齐与双人签字**（状态：🟡 三件交付物已产出、签字与现场验证待人，见「完成状态速查」）
- owner：全体（喻永昌汇总）
- 依赖：全部功能包
- 交付物：每个 `@qianmo/*` 包的 README + 模块架构图；**对基座的改造点清单**（改了哪些基座文件、为什么、扩展点为何不够用——T-5 对策④的汇总）；owner / backup 双人签字表
- DoD：每个核心模块的 backup **能独立复述设计原理并接手改动**（由 owner 出 3 道问题现场验证），双人签字入库（对策 P-2）

---

### M0 依赖关系速查（v2.2 重绘）

```
P0.6 前置实验（已完成）────────────> 设计输入：P1.1 / P2.5 / P3.1
P0.1 基座导入 ─┬─> P0.3 身份隔离
               ├─> P0.4 CI 门禁
               ├─> P0.7 安全面加固 ──────────────────> [AC-6(c) 的宿主前提]
               ├─> P1.3 沙箱 ──┬─> P3.2 ──> P5.1 ──┬──> P6.2
               │                └─> P4.4 ──────────┼───────> [AC-6]
               └─> P0.2 能力盘点 ─┬─> P0.5 协议资产复核 ─┐   │
                                   ├─> P0.8 判据修订评审 ─┤   │  （D-1→P3.1、D-2→P4.2、D-3→P3.1/P4.1）
                                   ├─> P1.2 会话核验 ──────┼──> [AC-1]
                                   ├─> P1.4 模型核验 ──────┼──> [AC-5]
                                   │                        │
                                   └─> P1.1 协议 v0.1 <─────┘
                                        ├─> P2.1 注册中心
                                        └─> P2.2′ 传输通道 ──> P2.5 activator + keepalive ─┐
                                                                                            │
P1.2 ──> P2.3 记忆 ──> P3.3 ──────────> [AC-4]                                              │
P1.2/P1.3/P2.1/P2.2′/P2.5 ──> P3.1 休眠唤醒 <───────────────────────────────────────────────┘
                    │
                    └──> P4.1 跨节点端到端（前置：P2.1 / P2.2′ / P2.5 / P3.1）
                           ├─> P4.2 ──> [AC-3]
                           ├─> P4.3 ─> P5.2 ─> P5.3 ─┐
                           └─> P7.2                   │
P2.4 ────────────────────────────────────────────> P6.1 <──────────────────────────────────┘
                                                      └──> [AC-7]
P5.4 ──> P7.1 ──> [AC-8]
P0.3 ──> P7.4 上游同步演练 ──> P8.4 ──> [⚖️ 许可链与成果边界证据]
P8.1/P8.2/P8.3/P8.4 ──> M0 结项
```

**读图提示（v2.2）**：与 v1.1 相比，图的左上角从"工程地基 → 会话持久化"变成了"基座导入 → 能力盘点"；**v2.2 的净变化在右侧那条链的中段——P2.2 变短了一截，但中间插进了一个全新的 P2.5，整条链因此比 v2.0 更长而不是更短。**另有两个新的小包挂在左侧：P0.7（AC-6(c) 的宿主前提）与 P0.8（三条判据的评审，它的结论会回流到 P3.1、P4.1、P4.2）。

---

## M1：校内内测（2027H1，粗粒度）

**目标**：把 M0 的原型变成 20 ~ 50 名软院同学能真实用起来的内测系统。

> **v2.0 变更**：M1 的头号硬门槛"基座退场"**已取消**（基座是长期地基，不退场，章程 §5.7）。取而代之的是身份隔离验收与上游同步节奏确定——工作量小得多，且不再是"未完成不得进入 M2"的阻断项。这是本次路线变更在 M1 上释放出的最大一块工时。

| 方向 | 内容 | 出口判据 |
|---|---|---|
| **基座关系收敛** ⚖️ | 身份隔离改名完成并验证（阡陌节点 / occ / 官方 Claude Code 三者共存）；按 P7.4 演练数据确定上游跟随节奏并写入章程；决定是否向上游回贡（**提请评审**） | 三者共存实测通过；同步节奏有书面结论 |
| **注册发现产品化** | 最小 Web 控制台（智能体列表、状态、消息链查看）、账号体系、智能体生命周期管理 | 内测用户无需接触 CLI 即可完成注册与查看 |
| **权限模型上线** | 用户授权流（授权申请 → 用户确认 → 时效凭据 → 主动撤销）；权限审计报表 | 所有需确认动作 100% 经过用户授权链路，抽查无绕过 |
| **记忆能力上线** | 语义检索（在确定性检索之上叠加，非替换）、记忆治理（冲突消解、过期废止、跨项目隔离）。**（v2.54 补注）确定性检索接进常驻链路那一段由 P13.7 承接**（注入位置定为用户消息 sidecar，不进 system prompt）；**语义检索仍在 N-8 之内、由本行的后续包承接** | 检索命中率与幻觉引用率有量化指标并优于 M0 基线 |
| **生产级加密** | mTLS 替换预共享密钥；证书签发与轮换；密钥管理 | 解除章程 N-3；渗透自查无高危项 |
| **多租户与配额雏形** | 内测必需的租户隔离与用量配额（仍不做计费，N-1 保留） | 跨租户数据访问用例全部被拒 |
| **第二产品线验证** | 运维值守场景原型（常驻智能体的第二类应用）。**（v2.54 补注）载体为 M1 第 3 迭代 P13.6**：定时由中枢 `@qianmo/scheduler` 持有、节点只被叫醒；产出默认静默，只有 agent 显式调用 `qianmo_notify` 才打扰人 | 至少 1 个真实值守场景连续运行 7 天 |
| **稳定性目标** | 可用性 ≥ 99%（按内测时段计）、唤醒 P95 < 30 s（M0 为 60 s） | 以 P7.3 基线报告为对比基准 |
| **知识产权** | 软著申请提交；开源方案确定并执行（**前置**：M0 P8.4 的科技处口径） | 材料齐备且经学校科技处确认 |
| **边界用例库** | 从 12 条扩到 ≥ 40 条，覆盖内测暴露的真实边界 | CI 常绿，内测期新增缺陷 80% 有对应用例 |

**M1 主要风险**：真实用户带来的边界问题数量级高于自测；长期常驻下的进程稳定性与内存驻留（P7.3 的 24 h 长跑只是下限）；上游同步的真实成本仍只有一次演练数据支撑。M1 前两个迭代预留为"内测环境搭建 + 身份隔离收尾"，不排新功能。

---

### M1 第 3 迭代（暂编 S12）· 常驻 agent bot 化

> 依据 [`docs/dev/resident-botization.md`](./resident-botization.md)（v0.1-draft，评审通过才生效）。本迭代**是 M1 头两迭代
> 「不排新功能」约束之后的第一批功能实现**，承接 M1 方向表的「第二产品线验证」「记忆能力上线」
> 两行，以及「多租户与配额雏形」中与配额机制相关的那半。**`retro-m0.md` 已含 M1 首两迭代任务包草案**，
> 本节先行给出第 3 迭代的任务包框架，正式的 S10/S11/S12 迭代编号与日期在 P9.2 M1 拆解评审通过后定案。
>
> **编号说明**：`P12.1~P12.4` 已被 `docs/dev/key-distribution.md` 占用（mTLS 落地线，P11.2 设计、
> P12.x 落地包），本批次让号用 `P13.x`。两条线在 `packages/transport/src/frames.ts` 上有交集
> （都在 v1 内加可选字段），**排期不得并行**，先落者在 PR 描述里记 `frames.ts:169` 的严格版本相等约束。

| 包 | 目标 | 依赖 | DoD（判据） | owner | 估算 |
|---|---|---|---|---|---|
| **P13.1** ⚖️ **设计定稿与范围回写** | 把设计件入库并回写 roadmap / charter，**代码批次在其后** | 无 | `docs/dev/resident-botization.md` 入库并过评审；本表入 roadmap；charter §3.3 C-4 数值清单由 5 项增至 8 项并升版本号；全仓 grep 证明未与 `P12.x` 撞号 | 喻永昌 | **4–8 人时** |
| **P13.2** ⭐ **协议扩展：`notify` / 能力发现 / `LIMITS` / `E_BUSY`** | 长出唯一一个 agent 主动发起的消息类型，并给跨版本参差一条稳的路 | P13.1 | `MessageType.Notify` + `NotifyPayload` 落地且 `isReplyType` **不给它豁免**；**同一 contextId 连发 3 条 notify 第 2、3 条不被判 `E_LOOP`**（只测第 1 条等于没测）；`ReadyFrame.supportedTypes?` 在 **v1 内**加、`FRAME_VERSION` 未变（grep 断言）；缺省 ⇒ 落回 `LEGACY_MESSAGE_TYPES` 且不发 notify；`E_BUSY` 对未声明支持的对端降级为 `E_RATE_LIMITED`，并有一条用例把「旧节点会整条拒收带新码的 failed result」这个事实钉住；`LIMITS` 新增三项且 charter 一致 | 陈曦宇 | **16–28 人时** |
| **P13.3** ⭐ **队列治理与传输回执解耦（H-3）** | 让排队不再顶穿发送方 5 s 回执超时，并给队列装上上界与超时剔除 | P13.1（`E_BUSY` 依 P13.2） | `NodeTurnGate` 有界（`LIMITS.maxQueuedTurns`）、过期项出队即丢且 `work` **从未被调用**（spy 断言）、`queued` 进 `ResidentTimingStage`；**一个 turn 占门时第二条 task.request 的传输回执 5 s 内返回 Accepted**（该用例今天必红）；**协议 ack 的发出点一行未动**，仍严格晚于 `markRead`；队列满的拒绝发生在信箱写入之前（refused 不吃配额）；同一 `sessionId` 两个 turn 永不重叠，且该断言不引用门的粒度；`packages/resident` 零 mock 保持 | 董宗岳 | **20–36 人时** |
| **P13.4** ⭐ **多会话隔离 `(agent, contextId)` 与会话 GC** | 让不同远端请求者/值守作业不再共用一条上下文，并顺带关掉 G-5 / G-6 | P13.1 | `sessionKeyOf(agent, contextId)` 是全仓唯一构造点（扫描断言）；无 contextId 回退 `DEFAULT_CONTEXT` 且与今天行为字节一致；`runtime.pollAll` 在多 agent × 多 context 下有覆盖（T11 盲区②）；GC 的三类永不驱逐各一条负向用例；`session-store` 到上限先驱逐后写、**无静默截断**（G-6）；**能读旧格式 `sessions.json`**（升级不丢会话）；协议 / 适配器 / 基座**三处零改动** | 董宗岳 | **16–30 人时** |
| **P13.5** ⭐ **常驻可靠性件套（投递台账 / 终止取证 / 重启熔断 / ESTOP / 不活动早失败）** | 把「无痕丢失」「无限崩溃循环」「没有急停」三个空洞补上 | P13.1（`E_BUSY` 依 P13.2） | 回执不来的 `task.result` 进台账并在重启后重投且**带 `redelivered` 标记**；`phase=running` 哨兵能判出上一条命是被杀的；**同一条毒 `detected` 记录连续 3 次重启后 `abandoned`、节点仍服务新消息**；ESTOP 文件存在时新任务回 `E_BUSY` 且**在途从不杀**、**空文件仍算 engaged**；不活动看门狗提前失败且 `reason` 说明原因；四者读写失败一律 fail-open；**wake 常驻侧端到端用例补齐（T11 盲区③）并据此定夺 `protocol.md:175` 的文档实现差** | 陈子轩 | **20–36 人时** |
| **P13.6** ⭐ **定时反转（`@qianmo/scheduler`）与主动通知接线** | 值守场景原型的载体：定时由中枢持有、节点只被叫醒；产出默认静默，只有显式 notify 才打扰人 | P13.2、P13.5 | `dedupKey = "<jobId>:<fireAtMs>"` 幂等；两个 scheduler 实例共享 store 时 CAS at-most-once；跨 5 个周期停机后**补跑塌缩为一次**；失败退避递增；notify 端到端（agent 工具 → 中枢 → 审计链）；**中枢缺席时 notify 落台账、回来后按序排空且不重复**；**注入给 ACP 的 MCP 工具表只含 `qianmo_notify`、不含任何排程能力**（E4 结构性断言）；出站限流是**滑动窗口不是令牌桶**（用例覆盖「安静一小时后不放行一整批」）；**节点一次都不拨号**（H-2 不变式的扫描断言）；一个真实值守作业连续跑 ≥ 24 h 留档 | 李怡康 + 董宗岳 | **28–48 人时** |
| **P13.7** ⭐ **记忆接线与无人值守安全收窄** | 把 `@qianmo/memory` / `@qianmo/recall` 接进常驻链路（sidecar 位置），并给「预批准白名单没有天花板」装上天花板 | P13.4 | 注入出现在**用户消息**里、**不出现在 system prompt** 里（结构断言）；一轮内是冻结快照；作用域按 `(agent, contextId)` 分区；AC-4 的 5/5 与伪造引用负样本在常驻链路下不回归；hardline 拒绝表求值**在 allow 规则之前**且**不从会话配置读取**，file 与 shell **双面封堵各一条负向用例**；注入扫描的对象是**组装后**的完整 prompt（用「入参干净、组装后含注入」的用例证明）；远端 `text` 的分隔符被中和；**往工作树丢一个记忆目录不能劫持本节点记忆**；注入 + 队列 + 工具 schema 叠加后不触发 auto-compact；**`check:unused` 结论只在主检出或干净 clone 上取** | 董宗岳 + 陈曦宇 | **24–40 人时** |

**合计 128–226 人时**，顺序：P13.1 → P13.2 →（P13.3 / P13.4 / P13.5 可并行）→ P13.6 → P13.7。
P13.3 与 P13.4 都碰 `reader.ts`，**合并以 P13.4 先落为准**（它改的是构造参数签名）。

**本迭代明确不做**（理由见 `docs/dev/resident-botization.md` §7.1）：节点主动休眠的三帧握手（A1/A2/A3）、节点→节点 notify、
真并发（H-9）、mTLS（另线）、语义检索（N-8 保留）、计费（N-1 保留，`costLimit` 仍恒为 0）、
队列优先级、基座 system prompt 三层分层（D1/D2）。

---

## M2+ ：公测及以后（2027H2 起）

2027 下半年开放公测：面向校外用户开放注册，多租户与配额转为正式能力，注册中心从单点转为高可用部署，接入真实云资源的弹性扩容（解除 N-7），并按 P6.4 的收敛建议补齐跨厂商 A2A 互操作；同时把编程智能体之外的第三类场景（协作开发）推上线，用公测期的真实用量数据校验"节点 + 网络"飞轮是否真的转起来——判据是跨节点协作消息量占总消息量的比例持续上升。2028 进入商业化：解除 N-1，落地计费与结算、定价方案与四类收入模式。**商业化的法律前置由 M0 P8.4 与 M1 的科技处口径承接**（风险 L-2、L-5），不再是旧路线中的"基座退场"。

---

## 附：文档索引

| 文档 | 状态 | 产出任务包 |
|---|---|---|
| `docs/dev/charter.md` | ✅ v2.15 | — |
| `docs/dev/roadmap.md` | ✅ v2.58 | — |
| `docs/dev/selection-m0.md` | ✅ v1.0 | 产出：2026-08 五路调研 + 六个前置实验（**实测数据与决议记录的唯一真源**） |
| `docs/dev/base-adoption.md` | ✅ 初稿，待 P0.2 定稿（含 `selection-m0.md` §2 的修正复核） | P0.2 |
| `BASE.md`（仓库根） | ✅ 随基座导入建立 | 由负责人维护，见章程 §5.6 |
| `docs/dev/protocol.md` | ✅ 已产出（含 P5.2 的 §13 协商扩展；§14 主动通知已由 P13.2 落地，P13.3/13.5/13.7 补 §4.5.1、§14.8.1、§10.2） | P1.1（v0.1）、P5.2（协商扩展）、P13.2（notify） |
| `docs/dev/resident-botization.md` | 🟡 v0.1-draft 已入库（评审通过前不生效）；S12 P13.1–P13.7 已按其实作，分支 s12/* 线性叠放待合并 | P13.1 |
| `docs/dev/boundary-day.md` | ✅ 模板已入库 | P7.1（双周边界日的执行记录） |
| `tests/boundary/README.md` | ✅ 已入库 | P5.4（五类边界的对应关系与规矩）、P7.1（v2 与混沌的分工） |
| `docs/dev/scenario-mcm.md` | 🟡 草案已产出；赛中真实痛点与全员评审待补，P2.4 未完成 | P2.4 |
| `docs/dev/a2a-gap.md` | 🟡 v0.1 草案已入库；全员评审待做，P6.4 未完成 | P6.4 |
| `docs/dev/p6.3-mcp-conformance.md` | ✅ 已产出 | P6.3（实测记录与缺口清单） |
| `docs/dev/upstream-sync-drill.md` | 🟡 已产出并勘误；章程 §5.7 已回写（v2.9）、BASE.md 已记录，评审待做 | P7.4 |
| `docs/dev/baseline-m0.md` | 🟡 v0.2-draft 已入库（aarch64 栏吞吐正式数据；24 h 长跑运行中；x86 栏与唤醒阶梯待真机） | P7.3 |
| `docs/dev/demo-env.md` | 🟡 已入库；本地腿、两台全新机器、真机腿（08-16 首验）均通过，「未参与部署成员」形式要件待人 | P8.1 |
| `docs/dev/acceptance-m0.md` | 🟡 v0.2-draft 已入库；本机 / 凭据 / 真机三条腿走查留档（§8 / §9），剩 AC-5 qwen 半条与 AC-6(b)(c) 挂载边界 | P8.2 |
| `docs/dev/positioning-m0.md` | 🟡 v0.1-draft 已入库、两次勘误、§3.1 数字重跑并单列机器生成物；**双签前不得外发**，定稿前须再重跑一次 §3.1 | P8.3 |
| `docs/dev/sbom-m0.md` / `sbom-m0.json` | ✅ 由 `bun run sbom` 生成并入库（darwin-arm64 视角，平台包许可待各平台补） | P8.4 |
| `docs/dev/license-chain-m0.md` | 🟡 v0.1-draft 已入库（NOTICE 差异已闭环）；E-8 归属举证与科技处咨询待人 | P8.4 |
| `docs/dev/base-modifications.md` | 🟡 已入库（12 书面 / 18 依代码 / 2 待裁定——08-16 考证后，见其 §6.1） | P9.3 |
| `docs/dev/signoff-m0.md` | 🟡 v0.1-draft 已入库；签字待人 | P9.3 |
| `docs/dev/retro-m0.md` | 🟡 v0.1-draft 已入库（含 M1 首两迭代任务包草案）；全员评审待做 | P9.2 |
| ~~`docs/dev/base-analysis.md`~~ | **已废止**——研究的是旧基座（另一棵树），保留在历史提交 `67f6081`，不迁入 v2.0 文档集 | — |
