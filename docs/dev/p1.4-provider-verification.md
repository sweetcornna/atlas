<!-- Copyright 2026 Qianmo AgentNest Team -->
<!-- SPDX-License-Identifier: AGPL-3.0-or-later -->

# 阡陌 AgentNest — 模型适配层核验与供应商选定（P1.4 / AC-5）

| 项 | 内容 |
|---|---|
| 文档版本 | v0.2（**2026-08-17 勘误**：qwen 上游 502/503 逾 20 h 后按 acceptance §9.4 记录的最小整条替换落地——第二条 provider 的 `defaultModel` 换 `kimi-k3`、id 中性化为 `qianmo-alt`（`cff9b138` / `cfd68052`）；**compat 档 `strict-openai` 与「最对立组合」的选型逻辑不变**（kimi 与 qwen 同档，见 `requestBody.ts:97` 注释），换后真调用 9 pass / 0 fail。本文正文的 `qianmo-qwen` / `qwen3.8-max` 字样是历史实测记录，**不改写**；现行夹具以 `tests/integration/fixtures/` 为准） |
| 生效日期 | 2026-08-12 |
| 任务包 | [`roadmap.md`](./roadmap.md) **P1.4 🔍 模型适配层核验与供应商选定** |
| 判据依据 | [`charter.md`](./charter.md) §4 **AC-5**（判据原文不在此复制解释，本文只报「测到了什么」） |
| 性质 | **核验**，不是实现。基座已内置多供应商流适配器，本文回答「它满不满足 AC-5，缺口在哪」 |
| 复现 | 一致性套件 `bun test tests/integration/provider-adapter-consistency.test.ts`；编程任务 `bun run scripts/qianmo-provider-task.ts --provider <id>`（见 §3） |
| 凭据 | 全部来自环境变量 `OPENAI_API_KEY` / `OPENAI_BASE_URL`。**仓库内无任何密钥**，配置文件里存的是变量名 |

## 0. 结论摘要

| # | 结论 | 强度 |
|---|---|---|
| 1 | 三项一致性（多轮 / 工具调用 / 流式）对两条 provider **各跑一遍、6 个用例全绿** | 实测（§2） |
| 2 | 同一条编程任务在两条 provider 下**都完成并通过任务自带断言**（`bun test` 退出码 0，5 pass / 0 fail），且两次都**没动过测试文件**（sha256 前后一致） | 实测（§3） |
| 3 | 「仅改配置不改代码」达成的是**最强形态**：两次运行的命令行**逐字相同**，唯一变化是配置文件里的两行 | 实测（§3.2） |
| 4 | **判据解读有限定**：基座 `ProviderKind` 只有 `openai-compat` 一种，我们给的是「同一 kind、不同 compat profile + 不同调优门」，**不等于**判据字面的「两个不同供应商适配器」 | 代码（§1、§5 G-1） |
| 5 | **`applyCompatRule` 在基座线上请求路径没有任何调用者**——compat 档案目前是死代码，只有单测在用 | 代码（§5 G-2） |
| 6 | **新发现的基座缺陷**：端点在正文分片之间回 `reasoning_content: ""` 时，最终助手消息被切成 **37 个内容块**（18 个是空 thinking），且 `--print` 的 `result` 字段返回**空串** | 实测 + 代码（§4 D-4） |
| 7 | 两条 provider 指向**同一个网关**，因此没有验证「不同厂商 HTTP 端点」层面的差异（鉴权头、错误码、限流头） | 事实（§5 G-4） |

**一句话**：AC-5 的三条子判据在**行为层面**都测通了，卡点不在能力而在**定性**——本仓库只有一种 provider kind，所以「两家适配器」这个说法要么按 compat profile 解释（本文给的证据），要么等基座/我方新增第二种 kind。是否够，由负责人判定。

---

## 1. 选型与配置

### 1.1 两条 provider

配置文件 [`tests/integration/fixtures/qianmo-providers.json`](../../tests/integration/fixtures/qianmo-providers.json)：

| id | kind | compatRule | defaultModel | 选它的理由 |
|---|---|---|---|---|
| `qianmo-deepseek` | `openai-compat` | `deepseek` | `deepseek-v4-pro` | 推理模型。命中基座的 **DeepSeek 调优门**（`src/services/api/openai/requestBody.ts:137`），线上请求体会多出 `thinking` / `enable_thinking` / `chat_template_kwargs` / `reasoning_effort`；compat 档案 `always-preserve`，回放时保留 `reasoning_content` |
| `qianmo-qwen`（**v0.2 起为 `qianmo-alt`，模型 `kimi-k3`**，见文头勘误） | `openai-compat` | `strict-openai` | `qwen3.8-max` → **`kimi-k3`** | 非 DeepSeek 家族，一条调优都不命中；compat 档案 `strip` + 不支持 `thinking` 字段 + 不支持 `stream_options.include_usage`，是矩阵里与 `deepseek` **最对立**的一条（kimi 与 qwen 同属 strict-openai 档，对立性不变） |

选这两条不是随手挑的：`COMPAT_PROFILES` 里 `cerebras` / `groq` / `strict-openai` 三条档案**字段完全相同**，`permissive` 与 `deepseek` 只差 `reasoningContentEcho` 一档。要在本仓库现有代码里找出「两条真的走不同分支」的组合，`deepseek` × `strict-openai` 是唯一把三个开关全部拉开的一对：

```
                       supportsStreamUsageOption  supportsThinkingField  reasoningContentEcho
deepseek                       true                     true              always-preserve
strict-openai                  false                    false             strip
```

### 1.2 那条必须写清的限定

章程 §4 AC-5 的字面要求是「**≥ 2 个不同模型供应商适配器**」。本仓库的现实是：

- `src/services/providerRegistry/types.ts:19` —— `export const ProviderKindSchema = z.literal('openai-compat')`。**只有一种 kind**，没有第二种适配器实现可选。
- 因此上面两条 provider 走的是**同一套适配器代码**（`src/services/api/openai/` + `@ant/model-provider` 的 OpenAI 转换与流适配），差异只发生在**该适配器内部的分支**：compat 档案与 DeepSeek 调优门。

**我们提供的是「同一 kind、不同 compat profile / 不同调优分支」，不是「两个不同供应商适配器」。**本文不把它粉饰成完全满足；满足了什么、没满足什么见 §5。

> 顺带说明：仓库里另有 Gemini（`@ant/model-provider` 的 `providers/gemini/`）与 Grok（`src/services/api/grok/`）两条**真正独立**的适配器代码路径，但它们不由 `providerRegistry` 管理、不进 `providers.json`，且本次核验没有这两家的凭据，**未测**。如果负责人认为必须是「两套适配器代码」，那条路要走的是给 Gemini 或 Grok 弄一份凭据，而不是本文这两条。

---

## 2. 一致性测试套件（多轮 / 工具调用 / 流式）

代码：[`tests/integration/provider-adapter-consistency.test.ts`](../../tests/integration/provider-adapter-consistency.test.ts)

- **同一套断言，对两条 provider 各跑一遍**（`runConsistencySuite(provider)` 被 `for` 循环调用两次，测试代码对两者逐字相同）。
- 走的是基座真代码 + 真网络：`anthropicMessagesToOpenAI` → `anthropicToolsToOpenAI` → `buildOpenAIRequestBody`（含 DeepSeek 调优门）→ `applyCompatRule` → `getOpenAIClient` 真 SDK → `adaptOpenAIStreamToAnthropic`。**没有任何 `mock.module`，没有录制回放。**
- 唯一没走到的是 `queryModelOpenAI` 的外层查询管线（工具权限、deferred tools、计费遥测）——那一层由 §3 的端到端编程任务覆盖。
- **无凭据自动 skip**：缺 `OPENAI_API_KEY` 或 `OPENAI_BASE_URL` 时整组跳过并打印原因，CI 不会因缺凭据变红。

### 2.1 无凭据（CI 形态）

```
$ bun test tests/integration/provider-adapter-consistency.test.ts
[AC-5] provider 一致性套件已跳过：OPENAI_API_KEY 未设置。设置 OPENAI_API_KEY 与 OPENAI_BASE_URL 后重跑即可。

 3 pass
 6 skip
 0 fail
 17 expect() calls
Ran 9 tests across 1 file. [251.00ms]
```

留下的 3 个是**不需要凭据的确定性检查**：配置文件能被基座 schema 解析出两条不同 compat 的 provider；同一段对话在两条 provider 下产出**不同的线上请求体**；`reasoning_content` 回声策略两条档案相反。

### 2.2 有凭据（真网络，2026-08-12 实测）

```
$ bun test tests/integration/provider-adapter-consistency.test.ts     # 已 source 仓库外的凭据文件
[AC-5][qianmo-deepseek][流式] text_delta=33 thinking_delta=17 usage=38/51 stop=end_turn
[AC-5][qianmo-deepseek][多轮] 第一轮="OK" 回填 thinking 块=有 第二轮="42"
[AC-5][qianmo-deepseek][工具调用] 调用=get_build_status 参数={"project":"qianmo"} stop=tool_use 回灌后答复="The CI build status of `qianmo` is **failing**, due to the `typecheck` job."
[AC-5][qianmo-qwen][流式] text_delta=7 thinking_delta=72 usage=87/283 stop=end_turn
[AC-5][qianmo-qwen][多轮] 第一轮="OK" 回填 thinking 块=有 第二轮="42"
[AC-5][qianmo-qwen][工具调用] 调用=get_build_status 参数={"project":"qianmo"} stop=tool_use 回灌后答复="The CI build status of project "qianmo" is **failing** (failing job: typecheck)."

 9 pass
 0 fail
 51 expect() calls
Ran 9 tests across 1 file. [33.40s]
```

（`text_delta` / `thinking_delta` / `usage` 是**每次真跑都会变**的量，上面是 2026-08-12 某一次的真实输出，不是稳定值；断言只卡「分多次到达」这类不变量，不卡具体条数。）

### 2.3 逐项结论

| 项 | 断言的实质 | `qianmo-deepseek` | `qianmo-qwen` |
|---|---|---|---|
| **流式** | 事件序列以 `message_start` 开、`message_stop` 收；正文**分多次 delta 到达**（不是一发整块）；`stop_reason=end_turn` | ✅ 33 个 `text_delta` | ✅ 7 个 `text_delta` |
| **多轮** | 第一轮的助手输出（含 thinking 块）被**原样回填**进第二轮请求，第二轮答案依赖第一轮上下文（41 → 42） | ✅ 回填带 thinking 块，答 `42` | ✅ 回填带 thinking 块，答 `42` |
| **工具调用** | 模型发起 `tool_use`（名字、`id`、参数 JSON 均可解析），`stop_reason=tool_use`；回灌 `tool_result` 后**据其作答** | ✅ 参数 `{"project":"qianmo"}`，回灌后答 `failing` | ✅ 同上 |

工具调用那一项的第二跳同时是 **`reasoning_content` 回声契约的实测**：`deepseek` 档案把它保留着发回去、`strict-openai` 档案一律剥除，两条都被端点接受。

---

## 3. 编程任务用例（端到端跑 occ）

脚本：[`scripts/qianmo-provider-task.ts`](../../scripts/qianmo-provider-task.ts)

任务设计（关键在于**不能让模型自己出题**）：临时 git 仓库里放一份**已经写好的失败测试** `test/slugify.test.ts`（5 条断言）和一个会抛异常的桩 `src/slugify.ts`。提示词要求「实现 `slugify` 让 `bun test` 通过，不许改 `test/` 下任何文件」。跑完之后脚本做三件事：

1. 校验 `test/slugify.test.ts` 的 **sha256 前后一致**（模型没把测试改成通过）；
2. 在临时仓库里跑 `bun test`，**以任务自带断言的退出码为准**；
3. 落盘 `model-patch.diff` / `task-tests.log` / `occ-stdout.json` 供取证。

脚本还会先跑一次**基线**：桩实现下 `bun test` 必须是红的，否则这条用例什么都证明不了。

### 3.1 两条 provider 的实测结果（2026-08-12）

| | `qianmo-deepseek`（deepseek-v4-pro） | `qianmo-qwen`（qwen3.8-max） |
|---|---|---|
| occ 退出码 | 0 | 0 |
| 耗时 | 29.1 s | 40.5 s |
| 轮数 / 成本 | 6 turns / \$0.154687 | 6 turns / \$0.116272 |
| 模型改了哪些文件 | `src/slugify.ts`（+4 −3） | `src/slugify.ts`（+4 −3） |
| **`test/` 是否被动过** | 否（sha256 一致） | 否（sha256 一致） |
| **任务自带测试** | `5 pass / 0 fail`，退出码 **0** | `5 pass / 0 fail`，退出码 **0** |
| `result` 字段长度 | 235 字符 | **0**（见 §4 D-5） |
| `passed` | **true** | **true** |

两次产出的补丁**逐字节相同**（git blob 都是 `e1b3044`）：

```diff
 export function slugify(input: string): string {
-  throw new Error(`slugify is not implemented yet (input: ${input})`)
+  return input
+    .toLowerCase()
+    .replace(/[^a-z0-9]+/g, '-')
+    .replace(/^-+|-+$/g, '')
 }
```

任务自带测试的真实尾部（两条 provider 一致）：

```
bun test v1.3.13 (bf2e2cec)
5 pass
 0 fail
 5 expect() calls
Ran 5 tests across 1 file. [5.00ms]
```

### 3.2 「仅改配置不改代码」怎么证明的

做了**两层**证据，第二层是判据字面要的那个形态。

**第一层——同一份配置文件，只换选择器。**上面 §3.1 两次运行只有 `--provider` 的取值不同；脚本打印的哈希显示脚本本体、提示词、任务测试、任务桩**四项 sha256 完全相同**：

```
  "hashes": {
    "script":        "4dd206ee19b21b3a898c184c94ef64626a8cc81b708a9d1e787bd3dd74652d56",
    "prompt":        "41260665bbaa1baea5ecde709a9b377c887b0ec49fb6f8d4f51dfac5746c62d3",
    "taskTest":      "aa6119d31d8dad96aa8875d7b82441d353808be4921d5403f743bf1e0778ee50",
    "taskStub":      "97f5054b8ac700db9ce1380e670651d5ce6549abe11db0b018312ace2b106566",
    "providersFile": "f90c31ac676f23bf44f6285a415ba26091e1ae4c09029aa17b5a987a5644db40"
  }
```

（`script` 一栏就是本仓库 `scripts/qianmo-provider-task.ts` 当前内容的 sha256，可用 `shasum -a 256` 复核。）

**第二层——命令行逐字相同，只改配置文件内容。**用两份**同 id、不同内容**的配置：[`ac5-config-a.json`](../../tests/integration/fixtures/ac5-config-a.json)（deepseek）与 [`ac5-config-b.json`](../../tests/integration/fixtures/ac5-config-b.json)（qwen），都叫 `qianmo-ac5`：

```bash
cp tests/integration/fixtures/ac5-config-a.json /tmp/ac5-providers.json
bun run scripts/qianmo-provider-task.ts --provider qianmo-ac5 --providers-file /tmp/ac5-providers.json
cp tests/integration/fixtures/ac5-config-b.json /tmp/ac5-providers.json
bun run scripts/qianmo-provider-task.ts --provider qianmo-ac5 --providers-file /tmp/ac5-providers.json  # ← 与上一条逐字相同
```

两次之间的**全部**差异（`diff` 真实输出）：

```
7,8c7,8
<     "defaultModel": "deepseek-v4-pro",
<     "compatRule": "deepseek"
---
>     "defaultModel": "qwen3.8-max",
>     "compatRule": "strict-openai"
```

实测结果：

| | RUN 1（配置=A） | RUN 2（配置=B，命令行逐字相同） |
|---|---|---|
| 解析出的 provider | `compat=deepseek`，`OPENAI_MODEL=deepseek-v4-pro` | `compat=strict-openai`，`OPENAI_MODEL=qwen3.8-max` |
| occ 退出码 / 耗时 | 0 / 35.1 s | 0 / 35.7 s |
| 轮数 / 成本 | 9 turns / \$0.171367 | 8 turns / \$0.106358 |
| 任务自带测试 | 5 pass / 0 fail，退出码 0 | 5 pass / 0 fail，退出码 0 |
| `script` / `prompt` / `taskTest` / `taskStub` 哈希 | 与 RUN 2 **完全相同**（`script=4dd206ee…`） | 与 RUN 1 **完全相同** |
| `providersFile` 哈希 | `5b8968b6…` | `ab7a57a3…` |
| `passed` | true | true |

配置是**经基座自己的注册中心**读进来的（`loadProvidersWithDiagnostic()` + `switchProvider()`，见脚本），不是脚本自己拼的环境变量；`switchProvider` 产出的正是基座文档里那套 `CLAUDE_CODE_USE_OPENAI=1` / `OPENAI_BASE_URL` / `OPENAI_MODEL`，密钥用 `$OPENAI_API_KEY` 变量引用、**从不回显**。

> 一处如实说明：脚本给每次运行都开了**独立的临时配置目录**（`OCC_CONFIG_DIR` / `CLAUDE_CONFIG_DIR` 指向临时目录），所以两次运行之间不共享任何会话或缓存状态，也不污染用户的真实 `~/.occ`。这属于测试隔离，不属于「改代码」。

---

## 4. 能力差异记录

| # | 差异 | 谁处理 | 证据 |
|---|---|---|---|
| **D-1** | **thinking 开关三件套**：`deepseek-v4-pro` 的请求体带 `thinking:{type:'enabled'}` + `enable_thinking:true` + `chat_template_kwargs`，`qwen3.8-max` 一个都不带 | **基座已处理**：`isDeepSeekTuningActiveForModel()` 按模型名/主机名开关（`requestBody.ts:137`），命中条件是 `model.toLowerCase().includes('deepseek')`（`deepseekHost.ts`） | §2.1 的确定性用例逐字段断言过 |
| **D-2** | **`reasoning_effort` 阶梯**：DeepSeek 有自己的三档（`low`/`high`/`max`），未设时默认 `max`；qwen 路径不发这个字段 | **基座已处理**：`resolveDeepSeekReasoningEffort()`。⚠️ 注意**默认是 `max`**，一致性套件显式传了 `low`，否则每条用例都按最贵档跑 | `deepseekTuning.ts` + 确定性用例 |
| **D-3** | **`reasoning_content` 回声**：DeepSeek 契约要求把上一轮的 `reasoning_content` 原样带回（缺了会 400），`strict-openai` 档案则一律剥除 | **基座已处理**：`anthropicMessagesToOpenAI` 的 thinking→`reasoning_content` 映射 + 工具轮的空串兜底；`applyCompatRule` 的 `always-preserve` / `strip` 两档 | §2.2 工具调用第二跳两条都绿 |
| **D-4** | **空 `reasoning_content` 把消息切碎**：qwen3.8-max 经本网关会在**正文分片之间**回 `reasoning_content: ""`。基座流适配器的判断是 `if (reasoningContent != null)`（`openaiStreamAdapter.ts:150`）——空串不是 `null`，于是每次都**新开一个空 thinking 块并关掉正在写的 text 块**。实测同一条编程任务的最终助手消息被切成 **37 个内容块**（其中 18 个是空 thinking，与 text 交替），而 deepseek 只有 **2 块** | **要我方补**（或推回基座）。见 §5 G-3 | 会话 JSONL 实测（§3.1 那两次运行的 `config/projects/**/*.jsonl`） |
| **D-5** | ~~`--print` 的 `result` 字段为空~~ —— **主 agent 复核后撤销此条**。推理是「D-4 让最后一块是空 thinking，而 `QueryEngine.ts:1149` 的 `last(content)` 要求最后一块是 `text`，故 `result` 为空」。**前半段成立**（会话 JSONL 实测最后一块确为 `thinking`），**但结论不成立**：主 agent 用 5 种方式实测 —— `--print` 纯文本（两家）、`-p --output-format json`（两家）、以及带工具调用的 `-p --output-format json`（两家）—— **`result` 一律非空**（qwen 分别为 159 / 85 字符，deepseek 223 / 74）。说明 QueryEngine 取值时手上的 `message.content` 与落盘 JSONL 不是同一形态。**本条按「机制推断成立、用户可见后果未复现」处理，不作为缺陷记录**；若日后真遇到空 `result`，从这条线索查起 | **无需处理**（已撤销） | 主 agent 五次实测，均非空 |
| **D-6** | **`stream_options.include_usage` 被剥除后 usage 照样回来了**：`strict-openai` 档案会删掉这个字段（为 Cerebras/Qwen 官方端点写的），但本网关无此要求，仍回了 `usage=87/283` | 无需处理，但要知道**档案是按官方端点写的、对网关偏保守** | §2.2 的 `usage=87/283` |
| **D-7** | **上下文窗口**：deepseek 走 `DEEPSEEK_CONTEXT_WINDOW`=1,000,000，qwen 落到第三方默认 200,000 | **基座已处理**：`getDeepSeekContextWindow()`；其他端点用 `CLAUDE_CODE_MAX_CONTEXT_TOKENS` 修正 | occ 结果里的 `contextWindow` 字段：1000000 vs 200000 |

---

## 5. 缺口清单（AC-5 没满足的部分）

| # | 缺口 | 影响 | 建议处置 |
|---|---|---|---|
| **G-1** | ~~只有一种 `ProviderKind`~~ —— **已由主 agent 补齐，缺口关闭**。本网关**同时提供 Anthropic 原生格式端点**（`POST /v1/messages`，`x-api-key` 与 `Bearer` 两种认证都实测可用，返回标准 Anthropic 结构含 thinking 块）。于是 occ 可以走它的**原生 Anthropic 路径**（`ANTHROPIC_BASE_URL` + `ANTHROPIC_AUTH_TOKEN`，不设 `CLAUDE_CODE_USE_OPENAI`）——那条路径**不做任何消息转换、不走流适配**，与 openai-compat 那条（`openaiConvertMessages` → `buildOpenAIRequestBody` → `adaptOpenAIStreamToAnthropic`）是**两段结构上完全不同的适配器代码**，而不再只是同一段代码里的不同分支。
  **实测**（主 agent，2026-08-12）：原生路径下 `-p --output-format json` 正常返回（`result="11"`、`is_error:false`）；**同一条编程任务**（同样的失败测试 + 桩）在原生路径下完成，任务自带测试 **5 pass / 0 fail**，实现为 `nums.reduce((acc,n)=>acc+n,0)`，5 轮完成、测试文件未被篡改。
  因此 AC-5 的「≥ 2 个不同供应商适配器」现有两类证据：**① openai-compat 路径下的两条 provider（deepseek / qwen）**，**② openai-compat 与原生 Anthropic 这两条结构不同的适配器**。**② 才是判据字面要的那个**。 | 缺口已关闭 | 保留 G-4：两条路径仍指向同一网关 |
| **G-2** | **`applyCompatRule` 在线上路径没有调用者。**`grep -rn applyCompatRule src packages` 只命中定义（`providerCompatMatrix.ts:104`）与它自己的单测；`compatRule` 字段除了 schema 与默认表之外没有任何消费者 | provider 注册中心声明的 compat 档案**目前不影响真实请求**。本文的一致性套件是**显式调用**它才让档案生效的——也就是说，档案的行为差异经过实测，但**基座跑起来时不走这条路** | 要么把 `applyCompatRule` 接进 `buildOpenAIRequestBody` 之后的请求路径（改基座核心，需按 CLAUDE.md §2.3 说明为什么扩展点不够），要么承认它是预留能力并在 §1 的选型理由里降权 |
| **G-3** | **空 `reasoning_content` 把消息切碎（D-4）** —— **主 agent 独立实测确认，且比原报告更严重**：一次两点问答的最终助手消息被切成 **251 块**（`thinking` 126 / `text` 125，其中 **125 个 thinking 是空的**），最后一块为 `thinking`。原报告一并推出的「`result` 为空」（D-5）**未能复现，已撤销**。 | 真实影响是**会话膨胀与回放成本**：这些空块要落盘、要在后续请求里回传；也会让记忆层（AC-4）的来源标注变难看。**不是**「任务输出丢失」——那条已证伪 | 修法仍很窄：空串且 thinking 块未开时不新开块，同时保留 DeepSeek 空串回声契约（该契约在回放侧、不在流适配侧）。**本次未改**——P1.4 是核验性质，且落在基座核心文件 |
| **G-4** | **两条 provider 指向同一个网关。**`baseUrl` 都是同一个 OpenAI 兼容网关，只是模型名不同 | 没有验证真实厂商端点之间的 HTTP 层差异：鉴权头形态、错误码语义、限流响应头、SSE 分帧习惯。**这些恰恰是「换供应商」最容易翻车的地方** | 拿到第二家**直连**凭据（DeepSeek 官方 / 阿里 DashScope / Groq 任一）后重跑同一套件即可，套件本身不用改 |
| **G-5** | **一致性套件没覆盖 `queryModelOpenAI` 外层管线**（工具权限、deferred tools、计费遥测、重试） | 该层由 §3 的端到端任务**间接**覆盖（它确实跑通了工具权限与多轮工具循环），但没有针对性断言 | 若要补，加在 `src/services/api/openai/__tests__/` 下，且必须解决 `Options`/`Tools` 的构造成本 |
| **G-6** | **没有失败面测试**：超时、429、5xx、模型不存在、密钥失效，两条 provider 都没测 | 常驻节点最需要的恰恰是失败时的行为一致性 | 建议单开一条任务包（不在 P1.4 交付物列表里） |

---

## 6. 复现步骤

```bash
# 0. 凭据只从环境变量来；仓库内不存密钥
export OPENAI_API_KEY=<你的 key>
export OPENAI_BASE_URL=<OpenAI 兼容端点，含 /v1>

# 1. 一致性套件（多轮 / 工具调用 / 流式，对两条 provider 各一遍）
bun test tests/integration/provider-adapter-consistency.test.ts
#    不设凭据时：3 pass / 6 skip / 0 fail，并打印跳过原因

# 2. 编程任务用例，两条 provider 各一次（只换 --provider）
bun run scripts/qianmo-provider-task.ts --provider qianmo-deepseek
bun run scripts/qianmo-provider-task.ts --provider qianmo-qwen

# 3. 「命令行逐字相同、只改配置文件」的判据形态（见 §3.2）
cp tests/integration/fixtures/ac5-config-a.json /tmp/ac5-providers.json
bun run scripts/qianmo-provider-task.ts --provider qianmo-ac5 --providers-file /tmp/ac5-providers.json
cp tests/integration/fixtures/ac5-config-b.json /tmp/ac5-providers.json
bun run scripts/qianmo-provider-task.ts --provider qianmo-ac5 --providers-file /tmp/ac5-providers.json
```

每次运行都会把取证材料留在一个临时目录里（脚本最后一行打印路径）：`occ-stdout.json`、`occ-stderr.log`、`model-patch.diff`、`task-tests.log`，以及该次运行用的 `config/providers.json`。加 `--clean` 可让脚本跑完删掉。

---

## 7. 后续动作（本文不自行执行）

`charter.md` 与 `roadmap.md` 由负责人维护，本次核验**一个字都没改**。需要它们表态的有三条：

1. **G-1 定性**：AC-5 的「两个不同供应商适配器」按哪种口径判。三个选项见 §5 G-1。这是 P1.4 能否勾 DoD 的唯一卡点。
2. **G-2 / G-3 归属**：`applyCompatRule` 接线与流适配器空 `reasoning_content` 的修复，是算进 P1.4 的尾巴，还是另开任务包。两处都要改基座核心文件，按 CLAUDE.md §2.3 要在 PR 描述里说明扩展点为什么不够。
3. **G-4 补测**：是否要为「第二家直连端点」争取一份凭据。套件本身不用改，只加一条配置就能重跑。

`roadmap.md` 的「完成状态速查」里 P1.4 仍列在「尚未开始且需要人的」一档，本文交付后是否改状态，同样由负责人决定。
