<!-- Copyright 2026 Qianmo AgentNest Team -->
<!-- SPDX-License-Identifier: MIT -->

# 阡陌 AgentNest — M0 复盘与 M1 任务拆解

| 项 | 内容 |
| --- | --- |
| 文档版本 | **v0.1-draft** |
| 生效日期 | 2026-08-16 |
| 任务包 | [`roadmap.md`](./roadmap.md) **P9.2 M0 复盘与 M1 任务拆解**（owner：喻永昌，全体参与） |
| DoD | 复盘文档评审通过；M1 首两个迭代的任务包已明确 owner 与依赖；**必答一条**：基座路线变更实际省下 / 多花了多少工时，与 [`base-adoption.md`](./base-adoption.md) 的事前估计差多少（本文 §5） |
| 状态 | **待全员评审**。§7 的 M1 任务包是**草案**，owner 一栏一律是「建议」，实际指派待负责人定 |
| 数据口径 | 日期与提交数全部来自实跑 `git log 3380c88..HEAD`，**快照于 2026-08-16 的 HEAD `005d6888`**；结项前若还有提交，附 A 的命令原样重跑即可。文档结论一律给指针，不复制正文 |

## 本文性质

1. **本文是复盘记录 + M1 排期输入，不是范围依据，也不是排期承诺。**范围依据仍是 [`charter.md`](./charter.md) §3，排期与任务包仍是 [`roadmap.md`](./roadmap.md)，完成状态的唯一出处仍是 roadmap 的「完成状态速查」表。凡本文与这三处冲突，以它们为准。
2. **charter 版本升级不在本文范围内。**roadmap P9.2 的交付物里有「charter 版本升级」一项，那是负责人的动作（章程 §7.3）；本文只产出可评审的材料。
3. **本文不复制任何已有结论。**§2 / §3 的每一条都指向 roadmap 变更记录、章程条款或某个文档的某一节；要细节请去那里。
4. **§4 的「排期偏差」用的是提交日期，不是工时。**提交日期能证明「这件事在哪天落地」，**不能**证明「花了几个小时」——本项目从未记过工时。凡是把日历换算成人力的地方，本文一律给区间并写清换算假设（§5.3）。
5. **本文引用的两份文档在本文成稿当天仍在推进**：[`baseline-m0.md`](./baseline-m0.md) v0.2-draft（提交 `005d6888`，启用 ARM 降级方案、数据表分**栏 A x86_64 / 栏 B aarch64** 两栏，栏 B 吞吐四轮已 pass、24 h 长跑**运行中**，**栏 A 全部仍【待真机】**）、[`acceptance-m0.md`](./acceptance-m0.md) v0.2-draft（提交 `ba95527d`，含 2026-08-15 本机腿走查记录与 `demo/walkthrough.sh`）。两文定稿后若数字变动，以它们为准。

---

## 1. 一页纸摘要

| 问题 | 一句话答案 | 详见 |
| --- | --- | --- |
| M0 做完了吗 | 8 条 AC 无一条测出不达标；卡的是**能否复跑**（真机 / 凭据）与**待人流程**（评审 / 双签 / 咨询），不是能否达标 | §6 |
| 最大的排期偏差 | 计划 **2026-08-17 ~ 2027-01-03（10 迭代 / 140 天）**，实际**功能面在 2026-08-11 ~ 08-15（5 个日历天，212 个提交）内集中完成**（08-16 另有 2 条收尾提交），结项日 08-16 比计划 S0 开工日还早 1 天 | §4 |
| 最该被记住的做对的事 | 「测量而非宣称」——它至少四次抓到**本来会以绿色通过**的假证据 | §2.1 |
| 最该被记住的估错的事 | 事前能力盘点里**根本没有 P2.5（跨节点 activator）这一项**，而它是 M0 唯一无先例可抄、同时压着 AC-2 与 AC-6(c) 的组件 | §3.1 |
| 基座路线变更的工时账 | **事前估计不是工时，是六个量级词**（`base-adoption.md` §2.3 全文无一个小时 / 人日数字），因此「差多少」这一问**只能定性作答**：省下的量级与事前判断大体吻合，多花的集中在事前**没有条目**的地方 | §5 |
| M1 头两个迭代 | 按 roadmap M1 节写死的「**预留为内测环境搭建 + 身份隔离收尾，不排新功能**」拆，产出 P10.1~P10.5 / P11.1~P11.5 草案 | §7 |

---

## 2. 做对了什么

每条一个证据指针。**只列在 M0 里被证明有效的**——被证明有效的意思是：它至少改变过一次结论，或者拦下过一次错误。

### 2.1 「测量而非宣称」抓到了四次假绿 —— 本项目最值钱的一条纪律

这四次的共同形状是：**判据本身写对了，但喂给判据的观测是假的**，所以报告是绿的而系统是坏的。四次都是在落地审计里抓到、当场改成真观测后重跑：

| # | 假绿的形态 | 抓到之后 | 指针 |
| --- | --- | --- | --- |
| 1 | P6.1 的报告里 `uncaught` / `skipped` 是**硬编码字面量**，判据因此恒为真 | 改成真处理器计数与计划/派发差集推导，在 `87c4609b` 上 SHA 锚定重跑三轮 3/3 | roadmap v2.31 ① |
| 2 | 混沌注入的 `fill-disk` 往一个**空的** 1 MiB 卷追加 200 字节，每次都成功——**一次什么都没注入的注入** | 改为先真填满再写，写居然成功时主动记 `unmapped` | roadmap v2.29 ④ |
| 3 | P5.1 首轮总体 90% 看着很好，但 **OOM 只有 5/10**——Bun 撑爆内存被 SIGKILL 且零输出，文本规则无从下手 | 补的不是阈值是**一条事实**（「本进程的看门狗没发过这个信号」），重跑 50/50 | roadmap v2.24 ④ |
| 4 | AC-3 协议层预算在慢机器上判红，原因是 601 条突发跨过 100 ms 令牌回补步长——**量具的隐含假设，不是系统缺陷** | 判据改按突发用时算「顶」，同机复跑 4/4 绿（accepted=602, allowance=2） | 提交 `9736ddca` / `5b98e6e8` / `e74df9d8`；`demo-env.md` §7.5 |

同族的还有两条**判据设计**上的预防，值得一起记：P5.1 的「**被跳过的类别直接判不通过，而不是缩小分母**」（roadmap v2.24 ⑤）、P6.2 的「**探测器确实带电**」正向用例（roadmap v2.32）——都是防「弃权式假绿」的。

> **给 M1 的规则**：任何报告字段，**先问它是被测出来的还是被写上去的**。这四次里有三次，写报告的人和写判据的是同一个人。

### 2.2 「先验成本，再排期」——v2.9 立下的纪律，在 v2.15 上收回了本钱

- **立规**：P2.2 交付后逐行核实，发现「基座有 5,300 行可改造传输代码」这条降级依据**不成立**（直接 import 行数 = 0）。结论侥幸成立，理由是错的。据此定下纪律：**此后任何以「基座已有」为由下调的估算，开工第一件事先做可改造性评估**（roadmap v2.6 报警条 + v2.9 关键路径复核结论）。
- **兑现**：P3.1 开工前照此做了评估，**结论是原估算偏低**，并当场推翻两处前提（roadmap v2.15）。若没有这道前置，P3.1 会按一个建立在两个错误前提上的估算开工。

这一条的价值不在于省了多少时间，在于它把「侥幸对了」和「判断对了」分开了。

### 2.3 扩展点优先，被上游同步演练量化证实

- 基座 `src/` 总共只改了 **56 个文件**（修改 32 / 新增 24），逐文件的「为什么扩展点不够用」在 [`base-modifications.md`](./base-modifications.md) §2，三类归因在 §3。
- 演练实测：对上游 v2.46.0 三方应用，**5 文件 / 7 块冲突 = 1784 个上游 hunk 的 0.39%**；其中 **`@qianmo/*` 17 包零冲突**、**P0.3 身份隔离 9 个改动文件零冲突**（被演练评为模范样板），**冲突大头是文档**（`CLAUDE.md` / `README.md` 占 4/7 块）。指针：[`upstream-sync-drill.md`](./upstream-sync-drill.md) §3.1 / §3.2 / §5，章程 §5.7。
- 反例也被量到：唯一一处**代码语义冲突**出在「就地替换」那一族（`FreezeAwareWatchdog` 替换掉基座三处裸 `setTimeout`），因此演练的行动项③点名要把它抽成注入点（`base-modifications.md` §2.2 与 §4 的对应表）。**「加法零成本、就地替换有成本」这句话现在有数字了。**

### 2.4 边界用例库 + 混沌，分工写清楚了

- 分工写在文件头，不靠默契：**混沌跑批找没想到的组合，确定性用例防已知失效回归**；一条跑一小时才碰一次的失效，不该由随机去守（roadmap v2.29 ①）。
- 数字：边界目录 **39 条**（AC-8 要 ≥12、每类 ≥2）；60 min 正式跑批 seed `2145259399`，177 次四类注入、7,389 条成功投递、四类 `stalled` 全 0、0 未捕获 / 0 unmapped、审计链完好（roadmap v2.29 ⑤）。
- **顺带修掉一个「本地绿 + CI 绿而第二个绿没有意义」的缺口**：`scripts/test-shards.sh` 的分片名单不含新目录，`demo/lib` 的报告核心用例自 P4.1 起**从未在 CI 里跑过**（roadmap v2.27 ③）。这类缺口的价值在于它**只能被主动找**，不会自己冒出来。

### 2.5 诚实边界随处如实记 —— 而且它真的被下游用上了

M0 的每个任务包都在变更记录里留了「边界如实记」。它不是姿态，是有消费者的：

- P2.5 真机 10/10 之后立刻写明「**这批数字不能当 P3.1 的休眠唤醒基准**——被唤醒的是 94 KB 测试桩」（roadmap v2.16 ②），于是 P3.1 老老实实又测了一遍常驻 occ。
- P4.4 写明「**沙箱挂载边界本机测不到**」（roadmap v2.23 ⑦），这句话直接变成 [`acceptance-m0.md`](./acceptance-m0.md) §4.1「最可能需要豁免的一条」的事实基础。
- P6.1 写明五条边界（脚本化授权、逻辑双节点、unix 演示不冒称 TLS 等，roadmap v2.30），P8.3 的答辩口径与 13 镜分镜直接照抄它，不用重新盘。
- P7.2 把「不可改」拆成三句**只认前两句**，并**专门写一条用例**钉住第三句做不到（roadmap v2.28 ③）。

> 反向的证据是：这些边界句子后来**没有一条被推翻**。写得住的边界才是省事的边界。

### 2.6 几条工程上的小事，收益/成本比很高

| 事 | 收益 | 指针 |
| --- | --- | --- |
| `bun test --isolate` 只对 `@qianmo/*` 分片开，基座分片原样不动 | 消灭跨文件 mock/env 污染，且**在自有包上零开销**；不把 700 个基座文件翻成 isolate，避免未度量的风险与平白的基座漂移 | `selection-m0.md` §4；`base-modifications.md` §5.3 |
| CI 的 `bun-version` 从 `latest` 改读 `.tool-versions` | 堵掉「上游发新版即可在零代码改动下改变构建结果」，**直接保护 P8.1 的 30 min 复现 DoD** | `selection-m0.md` §2「缺陷」行；roadmap P0.4 追加交付物② |
| `verify` 命令组（`precheck` ≠ CI 的差集补齐） | 原判据「本地 precheck 过 = CI 过」被实测推翻后的替代品 | roadmap P0.4 DoD 第 3 条；根 `CLAUDE.md` §3 |
| 每个演示脚本都配一个 `*-report-core.ts`，判据是**机器 check** 不是眼看 | 报告能被 CI 跑、能被复核；P7.3 更进一步用它**把本地数据机器强制挡在正式表外** | roadmap v2.34 ⑤ |

---

## 3. 估错了什么

格式统一：**当时估计 → 实测 → 差多少 → 为什么错**。按「错得最贵」排序。

### 3.1 事前能力盘点里没有 P2.5 这一项（最贵的一处）

- **当时估计**：`base-adoption.md` §3.1 / §3.2 的两张能力表逐项判定了 21 项能力，**没有任何一行对应「跨节点 activator + 常驻保活」**。
- **实测**：`selection-m0.md` §5 单列一节把它识别出来——「接住请求 → 唤醒目标节点 → ready 后转发」这一层现有任务包里没有它；内核 accept queue 的缓冲**只在同机有效**，Dormice daemon 也只听本机。原文定性：「**是阡陌唯一必须自己写的基础组件，且 AC-2 直接压在它上面**」。
- **差多少**：它成了 `packages/` 里最大的一块（`packages/activator` 约 8,834 行，实跑 `git diff 3380c88..HEAD --numstat` 按目录归并），并被 roadmap v2.2 直接推上关键路径长杆位置。
- **为什么错**：能力盘点的方法是「**逐条问基座有没有**」，而 activator 是一个**基座和阡陌都没有、但拓扑要求必须有**的东西。**「基座缺什么」这个问法找不到「拓扑缺什么」。**

> **给 M1 的规则**：能力盘点做完之后，再做一遍**拓扑走查**——沿着一条真实请求从发起到落地走一遍，每一跳问「这一跳的代码在谁那里」。

### 3.2 P2.2「基座有 5,300 行可改造传输代码」

- **当时估计**：roadmap v2.2 据 `selection-m0.md` §2 把 P2.2 从「从零造」降级为「实现一个 Transport + 自建服务端半边」，并因此把它从关键路径最长杆的位置挪下来。
- **实测**：逐行核实——基座 `Transport` 钉死在 SDK 控制协议 zod 联合上（信封不可能是它的成员）、**全仓没有任何 WebSocket 服务端**（仅有的两处 `Bun.serve` 在 `SSHAuthProxy.ts`，该文件 `websocket` 零命中）、且与会话面硬耦合。**直接 import 行数 = 0**。
- **差多少**：**工时没爆，但不是因为基座帮上了忙**——是因为阡陌只需要单条 wss + PSK。真正复用的是设计（退避公式、10 分钟预算、「休眠检测阈值 = 2× 上限」、「代理不数控制帧」）。
- **为什么错**：把「**有形状相似的代码**」当成了「**可复用**」。roadmap v2.6 报警条给的定性最准确：**降级的理由不成立，降级的结论侥幸成立。**

### 3.3 P3.1「休眠改核心」的成本，以及两处方向相反的前提错误

- **当时估计**：`base-adoption.md` §3.1「休眠与唤醒」判 **无**，原文「**全部自研**。可复用的只有 supervisor 骨架，挂载方式是往 worker 注册表加一种 kind」；章程 v2.7 后改判「部分」（唤醒四条通道已核实存在）。
- **实测**（roadmap v2.15 的可改造性评估，两处前提被代码推翻，**方向相反**）：
  - 「唤醒半边有四条现成通道」——在 **ACP 宿主下一条都不通**（正解是 ACP 自带的 `session/prompt`，仍是零核心改动）；
  - 「全仓没有通用 idle 事件」——**是错的**，有四个，`sessionActivity.ts` 正是要的形状；真正的理由是**基座没有任何进程内扩展点**。
  - 更早还有一处：「往 `DAEMON_WORKER_KINDS` 加一种 kind 即可挂载」——`runDaemonWorker()` **没有 dispatch 表**，只会让 worker 以退出码 78 退出并被永久 parking（`selection-m0.md` §2）。
- **最大的一笔是 roadmap 此前零覆盖的**：常驻宿主选 ACP，把 P2.1 / adapter 已验收的 **AC-2 最后一跳的读者拿走了**（`useInboxPoller` 是 REPL 专有），得给 ACP 补一个读者。
- **差多少**：净效应是**增量**（v2.15 原话）。落到代码上，P3.1 是基座改动最大的一族：**16 个修改 + 13 个新增**（`base-modifications.md` §1 归因族表）。
- **为什么错**：三态判定（已有/部分/自研）是「**基座给了什么**」，**不是工时折扣**——章程 v2.7 已经把这句话写进条文，但写进条文和排期时真的不打折是两件事。

### 3.4 Dormice API：按「合理形状」写的假设全错

- **当时估计**：P2.5 首版按 REST + 「`touch` 方法」+ 状态 `running` 实现。
- **实测**（roadmap v2.8，主 agent 真机核实）：API 是 `POST /{methodName}` + JSON body 的 **RPC 而非 REST**；**`touch` 方法根本不存在**（31 个真实方法里没有）；状态取值是 `active` 不是 `running`。保活改用实测会刷新 `lastActiveAt` 的 `acquireSandbox`。
- **差多少**：一次返工，外加**一处防护丢失如实记录**——真实 API 中保活与唤醒是同一操作，原类型窄化保证不再成立（`destroy` 不可达这条核心性质不受影响）。
- **为什么错**：**对第三方 API 的形状做了推断而不是核实**，且这个推断进了已交付代码。同一天定下的纪律（`selection-m0.md` §6「凡文档与代码冲突，以代码为准」）在这里应当扩写成「凡未核实的第三方接口，不得进已交付代码」。

### 3.5 「跨节点传输基座给的是零」—— 一次来回

- **当时估计**：`base-adoption.md` §3.2 判「**基座起点为零**」，理由是远程控制传输层已于 2026-07 整体删除（约 45k 行）。据此 roadmap v2.0 把 P2.2 推上关键路径长杆。
- **中途推翻**：`selection-m0.md` §2 以代码推翻——删的只是 `src/bridge/`，基座仍有数千行在用的传输代码，`RemoteIO extends StructuredIO` 意味着主循环已能跑在网络传输上。章程 v2.2 据此把 C-3 由「自研」改判「部分」。
- **再纠回**：见 §3.2——**可复用的直接 import 行数是 0**。
- **差多少**：结论上，`base-adoption.md` 的「零」在**可复用代码**这个意义上是对的；错的是它给的**理由**（「45k 行被删所以是零」）恰好也不准确。**两次判断都是对结论对、理由错。**
- **为什么错**：两次都用**行数**当可复用性的代理指标。行数既证明不了「有」，也证明不了「没有」。

### 3.6 AC-3 量具的隐含假设（判据对，量具错）

- **当时估计**：协议层入站预算的判据是「打满 `LIMITS.ratePerMinute = 600` 后第 601 条回 `E_RATE_LIMITED`」，实现上默认 601 条突发能在令牌回补步长内打完。
- **实测**：慢机器（Debian 13）上 601 条突发**跨过了 100 ms 回补步长**，于是 accepted 变成 602，判红。
- **差多少**：一次「系统没坏、报告说坏了」的假红，加上一次被撤回的错误修法（`c7a02607` 冻结接收方时钟 → `7e83649a` 撤回，改为按突发用时算「顶」）。
- **为什么错**：**判据把「600 条」当成了绝对上限，而令牌桶的语义是速率**。写判据时脑子里那台机器是开发机。

### 3.7 P8.1 runbook 漏了两样东西，都是在全新机器上才暴露的

- **当时估计**：干净 clone 实测 **87 s**（热 bun 缓存 + 本地 clone + 开发机），距 30 min 预算 20 倍余量。
- **实测**：第一台全新机器（Debian 13 x86_64、冷缓存、公网）**2 min 40 s**，runbook 补 bun / node / make 装法与最低内存（提交 `da98d86f`）；第二台全新机器（2 vCPU / 1973 MB / 无 sudo）净 **105 s**、含操作者往返墙钟 **8 min 19 s**，**唯一硬卡点是 bun 官方安装器要 `unzip`**，minimal 镜像不带、无 sudo 装不上（提交 `840b934d`，`demo-env.md` §2.1 / §4.1 busybox 垫片 / 故障表）。
- **为什么错**：「我这台机器上能跑」与「一台什么都没有的机器上能跑」之间隔着一整份依赖清单。**这正是 DoD 写「由未参与部署的成员实测」的原因**——那条形式要件至今仍待人（§8）。

### 3.8 举证命令 `848ad8c2..HEAD` 跑不通（写进答辩预案之后才发现）

- **当时估计**：章程 v1.x ~ v2.3 一直用 `git diff 848ad8c2..HEAD` 作为成果边界的举证命令，且它已经写进 P8.3 的答辩 Q&A。
- **实测**：`848ad8c2` 是**上游仓库**的 SHA，本仓库经零改动快照导入、不含上游历史，实跑报 `unknown revision`。章程 v2.4 全面勘误为 `3380c88`。
- **还有一层**（roadmap v2.35 ⑥(b)）：**本机主检出里 `848ad8c2` 居然能解析**——它是 pack 中不被任何 ref 可达的悬空对象，干净 clone 里不存在。**结论不变，但本机预演会误以为旧命令能跑，还会给出另一组数字。答辩机必须用干净 clone。**对象来源已坐实：P7.4 演练用的 worktree 与主检出共享对象库，上游 fetch 的对象因此进了主 pack（`BASE.md` 勘误 `a626eb95`、`upstream-sync-drill.md` 勘误 `856d0ff8`）。
- **为什么错**：**举证命令从未被实跑过**。一条写进答辩预案的命令，和一条被跑过的命令，是两种东西。

### 3.9 其余几处，一句话各记一条

| 处 | 当时 → 实测 | 指针 |
| --- | --- | --- |
| `precheck` = CI | 实测 `precheck` 不含 `check:cycles` / `check:unused` / `check:bundle`，且 `check:fix` **会写文件** | `selection-m0.md` §2「修正」行；roadmap P0.4 DoD |
| 会话恢复入口 | `--continue` 无持久化索引、成本随历史线性增长、会撞 10 s 线；必须钉死 `--resume` | `selection-m0.md` §2 最后一行 |
| AC-4 用 Anthropic 原生引用块 | 与结构化输出**互斥**（同用报 400），且只在一家存在，与 AC-5 模型中立直接冲突 → 改「全量注入 + 工具层强制引用」 | `selection-m0.md` D-6 |
| 防火墙加在 `DOCKER-USER` | 实测无效——容器访问宿主本机端口走 **INPUT 链**；改为 INPUT 为主、`DOCKER-USER` 作纵深 | `selection-m0.md` D-5 落地记录 |
| `check:unused` 在 worktree 里的红色 | git worktree 特有的假阳性，**已骗过两个子代理**（其中一个的对照基线也在同一个 worktree 里） | 根 `CLAUDE.md` §3.1 |
| E6：per-test 超时不杀子进程 | **会杀**，Bun 显式打印 `killed 1 dangling process` | `selection-m0.md` §3 |

---

## 4. 排期偏差数据

### 4.1 口径声明（读下面任何一个数字之前先读这段）

1. **提交日期 ≠ 工时。**本项目没有工时记录。下表的「实际」列是**该任务包最后一次落地提交的日期**（从提交主题里的 P 编号归类，实跑 `git log 3380c88..HEAD`），它回答「哪天落地」，不回答「花了多久」。
2. **计划列取自 roadmap「迭代总览」表**（S0 08-17 ~ S9 01-03），偏差按「计划迭代**结束日** − 实际落地日」计。
3. **口径不可通约**：计划是按「5 人 × 60% 有效产能」估的（roadmap §0 第 1 条），实际是「主开发单线程 + AI 协作工具」（章程 v2.3 P-1 补注已预见并明确写下「本次不重排日期」）。**两者不是同一种人力单位，任何「快了 N 倍」的说法都是拿两把不同的尺子相除。**
4. 本文只统计**有交付物落地**的任务包。评审、双签、咨询等待人项不计入（它们的状态在 §6 / §8）。

### 4.2 计划 vs 实际（按迭代）

| 迭代 | 计划日期 | 计划产能 | 本期任务包 | 实际落地日 | 提前（天） |
| --- | --- | --- | --- | --- | --- |
| S0 | 08-17 ~ 08-30 | 100% | P0.1~P0.8 | 08-12（P0.2 除外，**未完成**） | **18** |
| S1 | 08-31 ~ 09-13 | 80%（数模国赛） | P1.1~P1.4 | 08-13（P1.3 最晚） | **31** |
| S2 | 09-14 ~ 09-27 | 100% | P2.1~P2.5 | 08-12（P2.4 仍为草案，草案落在 08-15） | **46** |
| S3 | 09-28 ~ 10-11 | 50%（国庆） | P3.1~P3.3 | 08-13 | **59** |
| S4 | 10-12 ~ 10-25 | 100% | P4.1~P4.4 | 08-14 | **72** |
| S5 | 10-26 ~ 11-08 | 100% | P5.1~P5.4 | 08-14 | **86** |
| S6 | 11-09 ~ 11-22 | 100% | P6.1~P6.4 | 08-15（P6.4 仍为草案） | **99** |
| S7 | 11-23 ~ 12-06 | 100% | P7.1~P7.4 | 08-15（P7.3 栏 B 至 08-16） | **113** |
| S8 | 12-07 ~ 12-20 | 100% | P8.1~P8.4 | 08-15（四包均为首版 / 待人项未闭） | **127** |
| S9 | 12-21 ~ 01-03 | 50%（期末） | P9.1~P9.3 | 08-15（P9.3 文档面）/ 08-16（本文） | **140** |

**三个直接读得出的事实：**

1. **整个 M0 的功能面在计划 S0 开工之前就做完了。**第一个提交 `18eac781` 在 **2026-08-11 09:11**，计划 S0 开工日是 **08-17**；结项文档（本文）落在 **08-16**，比计划开工日还早一天。
2. **偏差是单调递增的**（18 → 140 天），说明不是某一期赶工，而是**全程以同一个远高于计划的节奏在跑**。
3. **三个低产能窗口（S1 尾数模国赛、S3 国庆、S9 期末）一个都没有真正落到执行上**——它们对应的日历还没到。**这意味着 P-1「产能断崖」这条风险在 M0 里从未被真正测试过**，它对 M1 依然有效，不能因为 M0 没出事就下调。

### 4.3 每日提交直方图

实跑 `git log 3380c88..HEAD --format='%ad' --date=format:'%Y-%m-%d' | sort | uniq -c`（含 merge 提交）：

```
2026-08-11  ███████████                                18   +3,783  −1,296
2026-08-12  ██████████████████████████████████████████ 94  +29,263  −1,275
2026-08-13  █████████████████                          28  +13,036    −733
2026-08-14  ██████████████████                         30  +16,590    −163
2026-08-15  ██████████████████████████                 42  +49,167    −261
2026-08-16  █                                           2   +1,647     −86
                                                      ───
                                              合计     214
```

（08-16 那两条是 P8.2 的一键走查驱动与 P7.3 栏 B 正式数据，本文自身尚未提交。）

- **08-12 的 94 个提交是 S0 全部 + S1/S2/S3 大部分**：那天落地了 P0.1/P0.3/P0.4/P0.5/P0.7/P0.8/P1.1/P1.2/P1.4/P2.1/P2.2/P2.3/P2.5/P3.3，外加两次章程升版（v2.4~v2.8）。
- **08-15 的 +49,167 行有一大半不是手写代码**：`docs/dev/sbom-m0.json` 一个文件就是 **+30,921 行**（`bun run sbom` 生成物）。见 §4.5 的口径提醒。
- 工作面总计（实跑 `git diff --shortstat 3380c88..HEAD`，2026-08-16）：**471 files changed, 111,253 insertions(+), 1,366 deletions(-)**，**214 个提交**。

### 4.4 关键路径的实际耗时

关键路径（roadmap §0.1 v2.2 新链）：`P0.1 → P0.2 → P1.1 → P2.2′ → P2.5 → P3.1 → P4.1 → P6.1`

| 环节 | 计划所在迭代（结束日） | 实际达成 | 依据 |
| --- | --- | --- | --- |
| P0.1 基座导入与环境跑通 | S0（08-30） | 08-12 | roadmap v2.10（干净 clone + `--frozen-lockfile` + precheck 9217 pass / 0 fail） |
| **P0.2 基座通读与能力盘点** | S0（08-30） | **未完成** | roadmap「尚未开始且需要人的」 |
| P1.1 协议 v0.1 | S1（09-13） | 08-12 | 速查表 P1.1 行 |
| P2.2′ 传输通道 | S2（09-27） | 08-12 | 速查表 P2.2 行（附报警） |
| P2.5 activator + keepalive | S2（09-27） | 08-12（DoD ① 真机 10/10） | roadmap v2.16 |
| P3.1 休眠与唤醒 | S3（10-11） | 08-13 | roadmap v2.17 |
| P4.1 跨节点端到端（AC-2） | S4（10-25） | 08-13 | roadmap v2.20 |
| P6.1 场景端到端（AC-7） | S6（11-22） | **08-15**（P6.1 全部落地提交与 roadmap v2.30 提交 `ca8c07f7` 均在 08-15 07:39–07:40；同日 08:58 `d999c93f` SHA 锚定重跑为正式记录）。**注**：roadmap 变更记录 v2.30 行的日期栏写 08-14，与提交时间不符，本文以 git 为准 | roadmap v2.30 / v2.31；`git log --date=iso` |

**关键路径实际跨度：08-11 09:11（首个提交）→ 08-15 07:40（AC-7 达成）= 5 个日历天（约 94 小时）**；计划跨度（S0 开工日 08-17 → S6 结束日 11-22）= **97 天**。

> **一处必须如实指出的执行偏离**：关键路径的第二环 **P0.2 至今未完成，而它下游的全部环节都完成了**。这不是流程失守被忽略，而是它的**功能被替代了**——`selection-m0.md` 的五路调研 + 六个前置实验（P0.6）在 08-12 之前就产出了对能力盘点的一批修正，随后每个任务包开工时的逐条代码核实（v2.6 / v2.8 / v2.15 …）又补上了剩下的部分。**留在 P0.2 名下没做的，是「全员通读 + base-adoption.md 定稿 + 章程 §3 三态逐项落实」这个可评审的产物形态**，它同时也是 P-2（单点知识）对策的一环，因此不能算自动关闭。处置见 §8。

### 4.5 一条给答辩材料的口径提醒（不是偏差数据，但同源）

[`positioning-m0.md`](./positioning-m0.md) §3.1 的实跑数字锚在 `37dc4eb1` 的前一个提交：**191 提交 / 429 文件 / +69,812 / −1,362**，并且已经主动分解了「40.3% 是测试」。截至 2026-08-16 的 HEAD，同一条命令给出 **214 提交 / 471 文件 / +111,253 / −1,366**。

- 差额约 **+41,441 行**里，`docs/dev/sbom-m0.json` 一个**机器生成物**就占 **30,921 行（约 75%）**。
- **建议（不在本文处置）**：`positioning-m0.md` 定稿前重跑一次，并在行数分解里**为「机器生成物」单列一行**。否则「新增 10.9 万行」这个数字会在答辩现场把一个 SBOM 说成成果。

---

## 5. 必答题：基座路线变更的工时账

> roadmap P9.2 的 DoD：**复盘必须回答一条**：基座路线变更实际省下 / 多花了多少工时，与 `base-adoption.md` 的事前估计差多少。

### 5.1 先说清楚：事前估计不是工时，是六个量级词

这一问的前半截需要一个前提检查，而检查结果改变了作答形式。**`base-adoption.md` 全文没有任何以小时或人日计的估计。**（实跑 `grep -n "工时\|人日\|人时\|小时" docs/dev/base-adoption.md` 只命中 4 行，其中 3 行是别处的表述，1 行是 G-4 的「工程与合规工时」这个**受益方**标签，不是数值。）

事前估计的全部内容是 §2.3 收益表的**量级列**与 §2.4 代价表的**影响列**，逐字如下：

| 事前条目 | 原文量级 / 影响（逐字） |
| --- | --- |
| G-1 法律状态从「无有效许可」变为 MIT | **决定性** |
| G-2 产品线 ① 的躯干直接到手 | **大** |
| G-3 成熟工程门禁直接继承 | **中，且几乎零成本** |
| G-4 省掉旧路线的全部隔离机制建设 | **中** |
| G-5 归属可控 | **中，长期看是大的** |
| G-6 身份隔离机制现成 | **小到中** |
| C-1 失去「100% 自研」这个简单叙事 | 影响 **高** |
| C-2 失去 `no-vendor` 这类一句话自证清白的机制 | 影响 **中到高** |
| C-3 逆向工程从「在我方之外」变成「在我方之内」 | 影响 **中**（性质变化） |
| C-5 失去「随时可换基座」的选项性 | 影响 **中** |
| C-6 认知负担与故障面显著上升 | 影响 **中 / 高概率** |
| C-7 依赖面与许可核查面变大 | 影响 **低到中** |

并且 `base-adoption.md` §7 局限第 3 条自己写明：「**未做运行时实测**……**本文因此不含任何我方实测的性能数据**」。

**因此「差多少」这一问只能定性作答，不能给出「事前估 X 小时、实际 Y 小时、差 Z」这种形式的答案。**给一个伪精确的数字比不给更糟——它会被当成测出来的。下面按「省 / 多花 / 打平」三栏逐条对照，每条都指到能核的证据。

### 5.2 逐条对账

#### ✅ 确实省了（与事前量级大体吻合）

| # | 事前 | 实际发生了什么 | 判定 |
| --- | --- | --- | --- |
| S-1 | **G-2「大」** —— 会话持久化（AC-1） | 工作性质确实从「自研持久化层」降为「核验 + 补齐」。落到代码上，P1.2 只改了基座 **2 个文件 + 1 个新增测试**（`logAssembly.ts` / `transcriptLoader.ts` / `logAssemblyTieBreak.test.ts`，见 `base-modifications.md` §2.6、§2.8），加一个 `demo/ac1-restart.sh` | **省，量级吻合**。但核验本身**抓到一个基座缺陷**（`--resume` 时间戳并列丢尾部消息），这笔是「省」里附带的额外产出，不是额外成本 |
| S-2 | **G-2「大」** —— 多模型适配（AC-5） | 基座既有包只改 **1 个文件 +16/−1**（`openaiStreamAdapter.ts` 空 `reasoning_content` churn，`base-modifications.md` §5.1）。我方工作是选定两家 + 跑一致性套件 | **省，量级吻合** |
| S-3 | **G-2「大」** —— 编程闭环（AC-7 的 P-1 那一环） | P3.2 用的是基座本体的闭环能力，我方写的是**任务夹具 + fail-closed 沙箱 + 凭据隔离 + 结果 envelope**（roadmap v2.18） | **省，量级吻合** |
| S-4 | **G-2「大」** —— 上下文压缩 | `base-adoption.md` §3.1 判「已有 / 缺口：无（M0 直接用）」。M0 全程没有为它写过一行代码，也没有一条变更记录提到它 | **省，且是最干净的一笔**（唯一一项真正零成本的继承） |
| S-5 | **G-4「中」** —— 省掉隔离机制建设 | 旧路线的 `fetch-base.sh` / vendor hook / CI `vendor-guard` / `no-vendor` 作业**一件都没建**。替代物是记录性的（`BASE.md` + git 基线 + 上游同步），成本显著更低 | **省，量级吻合**。章程 §5.3 的「旧机制废止清单」是它的账本 |

#### ➖ 打平 / 略高于事前（量级判对，细节偏乐观）

| # | 事前 | 实际发生了什么 | 判定 |
| --- | --- | --- | --- |
| E-1 | **G-3「中，且几乎零成本」** —— 工程门禁 | 门禁本身确实是移植而非重建，但**「几乎零成本」不成立**：P0.4 得连修三件继承来的缺陷（flaky 用例、CI `bun-version: latest` 与 `.tool-versions` 脱节、`--isolate`），P5.4 又发现**分片名单不含新目录导致 `demo/lib` 从未在 CI 里跑过**，P7.1 的 CI 五连绿是在修完**三处**真实缺口（根 workspace 漏声明、Knip 入口图、watcher 固定等待）之后才拿到的 | **低成本但非零**。且这些缺陷**都是继承来的**，不是本项目引入的——这一点在申报口径上应当讲清楚 |
| E-2 | **G-6「小到中」** —— 身份隔离现成 | 沿 `paths.ts` 派生这条路确实是现成的，但实做是 **9 个修改 + 3 个新增**，清点出 **26 处**带身份字面量的生产代码位置（1 处真正打穿身份切换、3 处保护清单取错范围、22 处有理由保留），并抓到一处真实泄漏（阡陌态可自动改写 occ 的 `~/.occ`，内含凭据） | **落在「小到中」的中**。**回报是超额的**：这一族在同步演练里 9 文件 0 冲突，被评为模范样板 |
| E-3 | **C-6「中 / 高概率」** —— 认知负担与故障面 | 事前判「高概率」，实际就是发生了：`check:unused` 在 worktree 里的假阳性**骗过两个子代理**（根 `CLAUDE.md` §3.1）、mock 卫生的进程全局语义、`paths.ts` 派生纪律 | **判对了**。对策（把标准交给 CI）也确实起了作用——三类踩坑里有两类是被门禁抓住的 |

#### ➕ 确实多花了（事前低估或没有条目）

| # | 事前 | 实际发生了什么 | 判定 |
| --- | --- | --- | --- |
| M-1 | **§2.5「产品线 ② 一分未得」** | **这句话判对了**，但中途被 `selection-m0.md` §2 以「5,300 行在用传输代码」推翻、又被 P2.2 实施逐行纠回（直接 import = 0）。**这次来回本身是一笔可避免的成本**：它导致 roadmap v2.2 的关键路径重推、v2.6 的报警、v2.9 的复核三份文档动作 | **传输层未省**。事前结论对、理由不准；中途的推翻理由错、结论也错 |
| M-2 | **事前没有这一项** —— 跨节点 activator | 见 §3.1。`packages/activator` 约 8,834 行，`packages/` 里最大的一块，且被推上关键路径长杆 | **最大的一笔漏估。**它不属于「基座路线变更多花的」——**它是能力盘点方法本身的漏项**，换哪个基座都会漏 |
| M-3 | **§3.1「休眠与唤醒：无 / 全部自研」→ 章程 v2.7 改判「部分」** | 见 §3.3。可改造性评估结论「**原估算偏低**」，净效应是增量；P3.1 成为基座改动最大的一族（16 修改 + 13 新增） | **多花**，且**「部分」这个三态改判在此处没有带来任何工时折扣**——章程 v2.7 自己就写死了这一点 |
| M-4 | **C-1「高」+ C-2「中到高」** —— 举证成本 | 实际产出：`positioning-m0.md` 943 行、`base-modifications.md` 252 行、`license-chain-m0.md` 291 行，外加举证命令勘误一次（章程 v2.4）与「本机 pack 里 `848ad8c2` 竟可解析」这个陷阱（roadmap v2.35 ⑥(b)） | **事前判「高」是判对的**，这是路线变更**最实在的一笔新增经常性成本** |
| M-5 | **C-7「低到中」** —— 依赖面与许可核查面 | 实际：`scripts/sbom.ts` **1,590 行**（零新依赖，自己写的 CycloneDX 生成器），1,419 条 lockfile 条目 → **1,089 个唯一组件**；外加两件待处置（`@anthropic-ai/claude-agent-sdk` 专有授权 devDep、`vendor/audio-capture/` 六个 `.node` 无源码无 LICENSE 无溯源） | **多花，事前偏低**。判「低到中」，实际是**中**——而且它的尾巴（E-8 归属关系、科技处口径）**根本不是工程能关掉的** |
| M-6 | **C-5「中」** —— 失去「随时可换基座」的选项性 | 事前只给了定性。演练把它变成了数字：单次同步 **10–20 人时（中位 ~14 h，1.5–2.5 人日）**，建议节奏 **每 2–4 周 / 每 5–8 个 release 一次** | **多花，且是长期经常性的**。按建议节奏**推算**年化约 **13~26 次 × 10–20 h = 130~520 人时/年**（此为按演练区间做的**算术外推，非实测**）。事前完全没有这个量级的概念 |

#### 一处**M1 才兑现**的省

roadmap M1 节写明：M1 的头号硬门槛「基座退场」**已取消**，「这是本次路线变更在 M1 上释放出的最大一块工时」。**M0 内它一分钱也没兑现**——它是一张要到 M1 才能贴现的票据，因此不计入本节的 M0 账。

### 5.3 结论（可以直接念的三句话）

> **第一，「省下/多花多少工时」这一问在本项目里无法给出数值答案**——事前估计是六个量级词，事后没有工时记录。任何数值都是造出来的。
>
> **第二，定性对账的结果是：省下的都在产品线 ①（会话持久化、多模型适配、编程闭环、上下文压缩）与工程门禁，量级与事前的「大 / 中」大体吻合；多花的集中在事前根本没有条目的地方**——跨节点 activator（能力盘点方法的漏项）、被两次修正的休眠改造、以及举证 / SBOM / 许可链这一整条「说清楚自己做了什么」的成本。
>
> **第三，路线变更引入了一笔事前没有量级概念的长期经常性成本：上游同步。**演练把它量成了 10–20 人时/次；按建议节奏**算术外推**，年化约 **130~520 人时 ≈ 16~65 人日**，即 M1 起每年三到十三个人周的固定开销（区间这么宽是因为节奏本身还没定稿）。**这笔账事前是空的，现在有数字了，M1 排期必须把它当成一条常设任务而不是一次性动作。**

### 5.4 给 M1 的方法论修正（这一节的真正产出）

1. **量级词不是估计。**M1 的能力盘点如果还写「大 / 中 / 小」，就必须同时写「按什么类比得出」——例如「与 P0.3 同量级（9 文件改动 + 一个派生层）」。可类比的锚点现在已经有 17 个包 + 56 个基座文件了。
2. **能力盘点之后加一遍拓扑走查**（§3.1）。
3. **「基座已有」永远不折扣工时，先做可改造性评估**（v2.9 纪律，M0 已验证有效，M1 继续）。
4. **凡未实跑的命令不得写进对外材料**（§3.8 的教训）。
5. **上游同步进常设任务**（§5.3 第三句）。

---

## 6. M0 出口自评

**判据原文在章程 §4，完成状态的唯一出处是 roadmap 速查表，逐条证据在 [`acceptance-m0.md`](./acceptance-m0.md)。本节不复制它们，只做出口口径的收敛。**

### 6.1 对照章程 §4 的八条 AC

| AC | 当前判定（取自 `acceptance-m0.md` §1 总览表，v0.2-draft） | 卡在哪 |
| --- | --- | --- |
| AC-1 | 脚本部分 **PASS-已走查（本机）**；**判据②仍待凭据**（脚本内恒 SKIP） | 凭据腿 + 该判据只有叙述、无留档报告（同文 §4.3） |
| AC-2 | **PASS-待复跑**（需 Dormice + gVisor 真机） | 真机腿（`workbench-host` SSH 不可达） |
| AC-3 | **PASS-已走查（本机）**，十条 check 全 true | — |
| AC-4 | **PASS-待复跑**（需真实 provider 凭据） | 凭据腿（一条命令即可补齐） |
| AC-5 | **PASS-待复跑**（需凭据；G-4 定性限定仍在） | 凭据腿 + G-4 表述待负责人书面结论（同文 §4.4） |
| AC-6 | (a) **待真机复跑**；(b)(c) **PASS-已走查（本机）** | 真机腿；**沙箱挂载边界本机测不到**——同文 §4.1 列为「最可能需要豁免的一条」，两种定性供负责人选 |
| AC-7 | **PASS-已走查（本机）** 3/3 | 五条诚实边界不变（脚本化授权、逻辑双节点等） |
| AC-8 | **PASS-已走查（本机）** | CI 五连绿已于 08-16 在 `d162fe72` 重取 5/5（同文 §4.5）；重取过程抓出并修掉一处 Linux 专属的 watcher 用例竞态（roadmap v2.37） |

**按章程 §4 豁免规则的自评**：**0 条测出不达标**，因此**目前不需要动用「豁免 ≤ 2 条」的额度**；真正的风险是「证据已在、复跑受阻」与「子项未在目标环境验证」两类，处置方式完全不同——这两类的区分见 `acceptance-m0.md` §3，**本文不替负责人裁定**。

### 6.2 对照章程 §8 的 M0 出口条件

M0 出口条件原文两句：「AC-1 ~ AC-8 全部通过（豁免 ≤ 2 条），演示可复现；上游同步演练完成、`BASE.md` 记录完整」。

| 出口条件 | 自评 | 依据 |
| --- | --- | --- |
| AC-1 ~ AC-8 全部通过（豁免 ≤ 2） | **待走查现场定**（见 §6.1） | `acceptance-m0.md` |
| 演示可复现 | **本地腿 ✅**（两台全新机器实测，§3.7）；**真机腿 ❌ 脚本未验证** | `demo-env.md` §7.1；DoD 的「由未参与部署的成员实测」形式要件仍待人 |
| 上游同步演练完成 | ✅ 已完成，数据入库 | `upstream-sync-drill.md`；章程 §5.7 已回写（v2.9） |
| `BASE.md` 记录完整 | ✅ 演练记录、上游公开 URL 与「上游对象经 worktree 进入主检出 pack」勘误已由负责人授权写入（`0c2f3b17`、`a626eb95`）；**演练结论评审仍待全员** | `BASE.md`「同步记录」；章程 §5.7 |

### 6.3 任务包层面的未闭项（**不要和 AC 豁免混在一起数**）

下列任务包的 DoD 未满足，但**都不是 AC 未通过**（`acceptance-m0.md` §4.6 已对其中四个如此定性）：

- **P0.2** 全员通读与能力盘点定稿（关键路径上唯一未闭环节，§4.4）
- **P2.4** 赛中真实痛点补写 + 全员评审 · **P6.4** 全员评审 —— 两份仍是草案
- **P7.3** 栏 A 正式数据待真机（栏 B 状态见 §1 第 5 条）
- **P7.4** 演练结论评审（`BASE.md` 记录已于 08-16 写入）
- **P8.1**（全新机器由成员实测的形式要件 + 真机腿未验证）· **P8.2**（真机腿/凭据腿/录屏）· **P8.3**（双签）· **P8.4**（科技处咨询 + E-8）
- **P9.3** 签字与现场验证
- **P9.1「缺陷收敛与回归」**：本文成稿时它不在 roadmap 速查表里（按该表规矩视为未开始）；roadmap v2.36 已补行——DoD 后半「全量回归连续 3 次全绿」于 08-16 在 HEAD `005d6888` 实测（`bun test` 三次连跑 9985 pass / 0 fail，另加同日 `precheck` 一次），前半「P0/P1 级缺陷清零」按 `acceptance-m0.md` §1 与速查表**无未闭 P0/P1 缺陷登记**。它仍不判完成：真机腿两条 AC 复跑（§8.4）纳入回归之前，「全量」只是本机口径。

---

## 7. M1 任务包初稿（草案 · 待评审 · owner 待负责人定）

### 7.1 拆包依据与三条硬约束

| 输入 | 说明 |
| --- | --- |
| roadmap **M1 节**（10 个方向 + 出口判据） | 方向清单的出处 |
| 章程 **§8** M1 出口条件 | 三件硬的：注册发现/权限模型/记忆能力对内测用户上线；生产级加密替换；**身份隔离改名完成并验证 + 上游同步节奏确定** |
| 章程 **§5.7「M1 排期输入（v2.9）」** | 五条减冲突候选（全部标「候选 · 未排期」） |
| 各文档标 M1 的待办 | `a2a-gap.md` §4 的 **11 条 M1 收敛项**、`p6.3-mcp-conformance.md` 的 G-1~G-5、`capability` 的 mTLS/TOFU 收敛条件、`backup` 的保留策略与真机挂载、`audit` 的机外见证、`capacity` 的真实供给、`baseline-m0.md` §7 的 B-6 等 |

**三条硬约束，先写在前面：**

1. **roadmap M1 节写死：「M1 前两个迭代预留为『内测环境搭建 + 身份隔离收尾』，不排新功能。」**下面两个迭代的任务包**一律不含新功能实现**——mTLS、语义检索、Web 控制台、多租户全部排在候选池（§7.4），它们的**设计与前置**可以进 P11。
2. **M0 的待人项是 M1 的开工前置**（§8）。特别是 P8.4 的科技处口径——章程 §8 把它写成 M1「知识产权」方向的**前置**。
3. **本节的 owner 一律是「建议」**，按章程 §7.1 的**辅助方向**给（主开发仍是负责人）；实际指派待负责人定。

### 7.2 M1 第 1 迭代（暂编 S10）· 身份隔离收尾与基座关系收敛

| 编号 | 目标 | 依赖 | DoD | 建议 owner（方向） | 估算区间 | 估算依据 |
| --- | --- | --- | --- | --- | --- | --- |
| **P10.1** ⚖️ **身份隔离三者共存正式验收** | 阡陌节点 / occ / 官方 Claude Code 三者在同一台机器上共存互不干扰，出一份可复现的验收报告 | P0.3（已完成） | 三者共存实测通过并留档（配置根 / 凭据 / 缓存 / 项目资产四类互不读写）；**零处绕过 `paths.ts` 直接拼路径**的全仓 grep 自查入库；报告进 `docs/dev/` | 陈子轩（测试与 CI） | **6–12 人时** | P0.3 已有子进程探针测试（`identityIsolation.test.ts` + `identityProbe.runner.ts`，268 行）；本包是把它扩成三方共存的正式验收，不是重做 |
| **P10.2** ⚖️ **上游同步节奏定稿 + 首次真实同步** | 把演练的「建议节奏」变成章程条款，并真做一次同步（不是演练） | P7.4 评审闭环（§8）；P10.3 建议先做 | 节奏写入章程 §5.7 并升版本号；**同步真实合入**、`bun run verify` 全绿；`BASE.md` 由负责人记录；本次冲突数与工时**实记**，与演练的 0.39% / 10–20 h 对照 | 陈曦宇（通信与安全）+ 负责人（`BASE.md`） | **10–24 人时** | 演练实测单次 10–20 人时（`upstream-sync-drill.md` §4）；首次真做要加一次 CI 全量与回归，取上沿 |
| **P10.3** **减冲突整改三件套** | 把演练点名的三条整改做掉，量出下次同步的冲突面下降 | P7.4（数据已在） | ① `CLAUDE.md`/`README.md` 改薄为指针（演练测算可消 **57%** 冲突块）；② `FreezeAwareWatchdog` 等常驻替换**抽成注入点**（`base-modifications.md` §2.2 三行）；③ `scripts/unused-budget.json` 拆「基座基线 + 阡陌增量」。**判据是可测的**：在 P10.2 的真实同步上冲突块数下降，且下降量与①的测算对得上 | 陈子轩（测试与 CI） | **8–16 人时** | ①是文档搬运；②按 `base-modifications.md` §2.2 只有 3 个文件、其中 1 个是演练里唯一的代码语义冲突；③是一个 JSON 拆两段 + 门禁脚本改读 |
| **P10.4** **真机腿补测（M0 欠账清账）** | 把 M0 卡在真机上的三件补齐：AC-2 复跑、AC-6(a) 复跑、P7.3 栏 A 正式数据 | `workbench-host` 恢复**或**第二台 Dormice 主机装好（需 sudo，§8） | AC-2 十轮全绿留档；AC-6(a) 五条全绿留档；`baseline-m0.md` 栏 A 的三块数据（24 h 长跑 / n=100 唤醒双阶梯 / T3 吞吐正式表）填完并判读通过 | 董宗岳（常驻运行时与沙箱） | **12–30 人时**（**不含**等机器的时间） | 三件都有现成量具与一键脚本（`demo/p41-task-result.sh`、`demo/ac6a-sandbox.sh`、`demo/p73-baseline.sh`）；成本主要在环境准备与 24 h 长跑的看护 |
| **P10.5** **P0.2 补齐：能力盘点定稿** | 关掉关键路径上唯一没关的那一环（§4.4） | 无（材料已齐） | `base-adoption.md` 定稿（把 `selection-m0.md` §2 的每条修正写成「采纳 / 部分采纳 / 不采纳」的书面结论，不采纳的给代码依据）；章程 §3 每项「基座起点」有改造入口，无「待定」；每位成员能口头回答本方向的「基座给了什么、缺了什么」 | 全体（负责人汇总） | **8–16 人时** | 修正条目在 `selection-m0.md` §2 只有 8 行；章程 §3 的三态自 v2.7 起已无「待定」，本包主要是**把已发生的复核补成可评审的产物** |

### 7.3 M1 第 2 迭代（暂编 S11）· 内测环境搭建（仍不排新功能）

| 编号 | 目标 | 依赖 | DoD | 建议 owner（方向） | 估算区间 | 估算依据 |
| --- | --- | --- | --- | --- | --- | --- |
| **P11.1** **内测环境形态设计与最小落地** | 把 M0 的「演示环境」升级成「20~50 人能连的内测环境」，先出形态设计再落最小可用 | P10.1、P10.4 | 设计文档过评审（节点数、账号最小形态、配置根与审计链分区、数据保留、回滚）；`demo/env/` 的真机腿脚本**在真机上跑通一次**（M0 欠账，`demo-env.md` §7.1）；一键起两节点 + 注册中心的内测拓扑 | 董宗岳（常驻运行时与沙箱）+ 李怡康（场景/数据） | **20–40 人时** | 本地腿已实测（87 s / 105 s 两台机器）；真机腿脚本已写未验；主要成本是设计评审与真机首跑 |
| **P11.2** ⚖️ **生产级加密与密钥分发的设计（只设计，不实现）** | 为 mTLS 替换 PSK 铺路，同时解掉 `OPEN_POLICY` 的收敛条件 | P10.1 | 设计文档过评审：证书签发 / 轮换 / 托管形态、**TOFU 还是带外分发**、`--trust <node>=<publicKey>` 的替代路径、`SIGNED_TASK_POLICY` 切默认的时点与回滚；解除章程 N-3 的**条件**写清楚（N-3 本身由评审解除，不在本包） | 陈曦宇（通信与安全） | **12–24 人时** | 收敛条件已经写在 `packages/capability/src/policy.ts` 的模块注释里（`a2a-gap.md` G-27 与 `protocol.md` §10 转述过）；本包是把它扩成方案，不是从零设计 |
| **P11.3** **备份的真机边界与保留策略** | 把 AC-6 最可能需要豁免的那条彻底关掉，并补 M0 明确不做的保留/轮转 | P10.4；`acceptance-m0.md` §4.1 的裁定 | store 放到沙箱挂载之外并**在真机上证明够不到**；保留策略与轮转（M0 按 N-12 不做）落地在**宿主侧工具**，不落在入站路径上的任何方法 | 董宗岳（常驻运行时与沙箱） | **10–20 人时** | 补做成本被 `acceptance-m0.md` §4.1 评为「低」（P0.7 已有宿主加固脚本与绑定不变式断言）；保留策略是新写 |
| **P11.4** **审计的机外见证（选型 + 最小验证）** | 补上 P7.2 明确做不到的第三句：「外部改动无法阻止」 | P7.2（已完成） | 选型报告（append-only 挂载 / 机外见证服务 / 定期锚定摘要 三选一，给成本与失效模式）；选定方案跑一次最小验证：**人为整篇重写并重算哈希后仍能被发现** | 陈曦宇（通信与安全） | **10–20 人时** | P7.2 已把这条缺口写成一条专门用例（roadmap v2.28 ③），判据现成；成本在选型与部署 |
| **P11.5** **M0 遗留评审与文档收口** | 把 M0 拖过来的四份草案 / 待评审文档一次性收掉 | P10.5 | P2.4（赛中真实痛点补写 + 全员评审）、P6.4（全员评审 + 三个判断点定夺 + G-18 处置）、P7.4 演练结论评审、`base-modifications.md` 的 **7 行「待改动人确认」转 ✅**、`base-modifications.md` §6.2 指出的**章程 §5.5 措辞不符**由负责人裁定 | 全体（负责人汇总） | **10–20 人时** | 全是评审与确认，没有实现；`base-modifications.md` §6.1 已经把 7 行整理成 5 个问题 |

### 7.4 M1 候选池（**不排进头两个迭代**，待排期评审）

按 roadmap M1 节的方向归并，逐条给出处，**本节只登记不承诺**：

| 方向 | 候选项 | 出处 |
| --- | --- | --- |
| 注册发现产品化 | 最小 Web 控制台、账号体系、智能体生命周期管理 | roadmap M1 节 |
| 权限模型上线 | 用户授权流（申请 → 确认 → 时效凭据 → 主动撤销）、权限审计报表 | roadmap M1 节 |
| 记忆能力上线 | 语义检索（叠加而非替换）、记忆治理（冲突消解 / 过期废止 / 跨项目隔离） | roadmap M1 节；章程 N-8 在 M1 解禁 |
| 生产级加密 | mTLS **实现**、证书签发与轮换、密钥管理 | roadmap M1 节；前置 P11.2 |
| 多租户与配额雏形 | 租户隔离与用量配额（仍不计费，N-1 保留） | roadmap M1 节 |
| 第二产品线验证 | 运维值守场景原型，连续运行 7 天 | roadmap M1 节 |
| A2A 对齐收敛 | **11 条 M1 收敛项**：G-1 Agent Card / G-2 taskId 映射 / G-6 TTL 扩展 / G-8 判环 / G-9 来源标注 / G-14 取消 / G-15 INPUT_REQUIRED / G-17 AUTH_REQUIRED / G-18 错误码信息泄漏 / G-23 内容分片 / G-27 强制签名。**三个判断点未定夺**（含「M1 建网关算不算越过 N-4」），G-15 与 G-17 **共用一个机制不能只做一半** | `a2a-gap.md` §4 / §5 / §6 |
| MCP 部署与覆盖 | **G-2 roots 通告整体顶掉 filesystem server 命令行允许目录**（部署真坑，M0 只走部署文档）、G-1 只覆盖 legacy 纪元、G-3 只覆盖 stdio、G-4 默认工具超时 ≈27.8 h、G-5 `packages/mcp-client` 平行实现未测 | `p6.3-mcp-conformance.md` §5 |
| 扩容 | `capacity` 的**真实资源供给**（M0 只到 `auditOnlyExecutor`，N-7 保留到 M2+）；「21 天 = 14 热身 + 7 计分」的解读待负责人确认 | roadmap v2.32 |
| 性能 | `baseline-m0.md` §7 的观察项，其中 **B-6「被限流比被投递贵 ×500」是本轮唯一可能真该在 M1 动手的一条**（先复测确认） | `baseline-m0.md` §7 |
| 并发 | 同节点多 agent 真并发（M0 用节点级 turn 串行规避，**M1 若要真并发须回到基座核心**） | roadmap v2.15 决策 Q3 |
| 协议 | 消息指纹的跨实现规范化（M0 依赖 `JSON.stringify` 键序，只保证「同一发送方实现对同一逻辑消息重发」可识别） | `protocol.md` §7.2「诚实的局限」 |
| 上游关系 | 是否向上游**回贡**通用性改动（章程 §5.7 保持开放，演练未提供材料）；「先找上游现成间接层」成文为改造规则（若采纳，落点是章程 §7.2⑦，须升版本号） | 章程 §5.7 |
| 知识产权 | 软著申请提交；开源方案确定并执行（**前置：M0 P8.4 的科技处口径**） | roadmap M1 节；章程 §8 |
| 边界用例库 | 从 39 条扩到 ≥ 40 条并覆盖内测暴露的真实边界；**边界日从模板变成有记录**（`boundary-day.md` 目前只有模板） | roadmap M1 节；`acceptance-m0.md` 附表 |

### 7.5 关于估算的一句话

上表的「估算区间」**全部按「主开发 + AI 协作」这一 M0 实际口径给**，锚点是 M0 里同类任务包的实际规模（文件数 / 行数 / 已有量具），**不是**按章程 §6.3 的「5 人 × 60% 有效产能」口径。**两种口径不能混用**——若 M1 的分工回到多人并行，这些数字要重估。另：M0 的三个低产能窗口（数模国赛 / 国庆 / 期末）一个都没被真正测试过（§4.2），**P-1 产能断崖对 M1 依然满格有效**。

---

## 8. 仍需人的清单（汇总在此，别散落）

按「谁做」分组。**这张表是 M1 开工的前置检查表**。

### 8.1 全员评审（5 项）

| 事 | 对象 | 阻塞什么 |
| --- | --- | --- |
| P0.2 能力盘点定稿评审 | `base-adoption.md` + 章程 §3 三态 | 关键路径上唯一未闭环节（§4.4）；建议并入 P10.5 |
| P2.4 全员评审 | `scenario-mcm.md`（另需补写赛中真实痛点） | P2.4 的 DoD |
| P6.4 全员评审 | `a2a-gap.md`（含三个判断点与 G-18 处置） | P6.4 的 DoD；A2A 收敛项能否进 M1 |
| P7.4 演练结论评审 | `upstream-sync-drill.md` + 章程 §5.7 的节奏建议 | 同步节奏定稿（章程 §8 的 M1 出口条件之一） |
| **本文（P9.2）评审** | `docs/dev/retro-m0.md` | P9.2 的 DoD 第一条 |

### 8.2 双签（2 项）

| 事 | 签字人 | 状态 |
| --- | --- | --- |
| P8.3 对外口径与答辩材料 | 负责人 + 安全 owner（章程 §5.8 第 4 条） | **签前不得外发**；「为何改核心」的归因已改指向 `base-modifications.md`（`8012b300`），那里还有 7 行待改动人确认 |
| 根 `NOTICE` 的修订 | 同上（对外法律文本） | 六条差异**已按负责人授权先行落地**（`f288afb4`：举证命令勘误至 `3380c88`、第五节补 SBOM / 预编译二进制 / ripgrep / 专有 devDep 披露；`0c2f3b17`：`BASE.md` 补上游公开 URL）；**双签补记待做**，签前 NOTICE 仍以「已修订、待签」对待 |

### 8.3 只有负责人能做的（5 项，其中第 1、4 项已于 08-16 落地）

1. ~~**`BASE.md` 追加 P7.4 演练记录**~~ —— **已写入**（`0c2f3b17` + 勘误 `a626eb95`，负责人授权），章程 §8 这一条出口条件已满足；剩演练结论评审（§8.1）。
2. **E-8：对基座的归属关系材料**——`license-chain-m0.md` §2 判定为「**仓库内无任何证据**，工程侧取不到」，是许可链证据包的最大缺口，也是科技处最可能追问的一组。
3. **科技处 / 知识产权办公室咨询**（议题模板在 `license-chain-m0.md` §5，至少含 L-2 与 L-5；口径必须是「书面或可复述」，「他们说应该没问题」不算完成）。
4. ~~**`vendor/audio-capture/` 的许可归属说明与 `@anthropic-ai/claude-agent-sdk` 专有 devDep 的披露方式**~~ —— **已决**并写进 NOTICE 五（`f288afb4`）：`.node` 溯源到上游同样只有二进制（Rust + napi-rs / cpal，无源码无 LICENSE，随基座 MIT 分发，如实披露）；`claude-agent-sdk` 保留 + 披露（仅 `import type`、`dist/` 零命中）。剩双签（§8.2）。
5. **一批待裁定项**：`acceptance-m0.md` §4.1（AC-6(b)(c) 判 PASS 还是豁免）/ §4.2（真机不可达时的走查形态）/ §4.4（AC-5 的 G-4 表述）（§4.5 的 CI 重取已于 08-16 做掉，不再待裁）；`base-modifications.md` §6.2（章程 §5.5「不改基座核心」措辞与 `teammateMailbox.ts` 改动不符）；roadmap v2.32（capacity「21 天 = 14 热身 + 7 计分」的解读）；**charter 版本升级**（P9.2 交付物，不由本文做）。

### 8.4 需要环境或凭据的（3 项）

| 事 | 需要什么 | 阻塞 |
| --- | --- | --- |
| 真机复跑 | `workbench-host` 恢复 SSH，**或** sudo 装第二台 Dormice + gVisor 主机 | AC-2 / AC-6(a) 复跑、P7.3 栏 A 三块正式数据、`demo/env/remote/` 首次验证 |
| 凭据腿 | 一份可用的模型供应商凭据 | `acceptance-m0.md` §1 的原话是「凭据三条（**一条命令**）」：AC-4 用 `source <凭据文件> && demo/walkthrough.sh --only ac4`，AC-5 用 `--only ac5,ac5e2e`。**AC-1 判据② 另需一次现场真调用并留档**（脚本内恒 SKIP，同文 §4.3） |
| P8.1 的形式要件 | 一位**未参与部署的成员**在全新机器上实测 30 min runbook | P8.1 的 DoD（技术上已由两台全新机器实测过，缺的是「由成员做」这个形式要件） |

### 8.5 现场活动（2 项）

| 事 | 排法 | 指针 |
| --- | --- | --- |
| **P8.2 走查现场 + 录屏** | 本机腿已于 2026-08-15 走查并留 transcript；缺真机腿、凭据腿、真人录屏（13 镜分镜已备） | `acceptance-m0.md` §7 / §8；`positioning-m0.md` 分镜表 |
| **P9.3 双人签字与现场验证** | 17 个包、每包 3 道题、**建议一次 90 分钟**；backup 先不看 README 作答，owner 只判「能不能接手」 | `signoff-m0.md` §1 / §3（含每人担子表：陈曦宇 5 答 6 出、陈子轩 5 答 0 出、喻永昌 3 答 3 出、李怡康 2 答 2 出、董宗岳 2 答 6 出） |

---

## 附 A · 本文核到的数字与其命令

| 数字 | 命令 / 出处 |
| --- | --- |
| 214 个提交 | `git log 3380c88..HEAD --oneline \| wc -l`（快照于 HEAD `005d6888`） |
| 471 文件 / +111,253 / −1,366 | `git diff --shortstat 3380c88..HEAD` |
| 每日提交 18 / 94 / 28 / 30 / 42 / 2 | `git log 3380c88..HEAD --format='%ad' --date=format:'%Y-%m-%d' \| sort \| uniq -c` |
| 首个提交 `18eac781` @ 2026-08-11 09:11 | `git log 3380c88..HEAD --format='%h %ad %s' --date=iso \| tail -1` |
| 各包首/末落地日 | 从提交主题里的 `P<n>.<n>` 归类统计（§4.2） |
| 基座 `src/` 56 文件 | `git diff --name-only 3380c88..HEAD -- src \| wc -l`；逐文件在 `base-modifications.md` §2 |
| `docs/dev/sbom-m0.json` +30,921 行 | `git diff --numstat 3380c88..HEAD \| sort -rn \| head` |
| `packages/activator` ≈8,834 行 | `git diff --numstat 3380c88..HEAD` 按目录归并 |
| 17 个 `@qianmo/*` 包 | `signoff-m0.md` §1 逐个列名；`positioning-m0.md` §3.2 |
| 0.39% 冲突率 / 10–20 人时 | `upstream-sync-drill.md` §3.1 / §4；章程 §5.7 |
| `base-adoption.md` 无工时数字 | `grep -n "工时\|人日\|人时\|小时" docs/dev/base-adoption.md`（4 行命中，无一是数值估计） |

## 附 B · 本文发现的文档不一致（**本文只登记；处置栏记录 08-16 审计时的处置结果**）

| # | 不一致 | 建议处置人 |
| --- | --- | --- |
| B-1 | `acceptance-m0.md` §5.5 记「roadmap 文头依据栏停在 charter v2.4」、§5.6 记「『尚未开始且需要人的』清单仍列 P0.1」——**这两条都已被 roadmap v2.35 修掉**，acceptance 未回写 | ~~P8.2 定稿时~~ **已处置**（08-16 于两节加「已修」注） |
| B-2 | roadmap 变更记录表的**行序**：v1.0 ~ v2.29 升序，之后是 v2.35 / v2.34 / v2.33 / v2.32 / v2.31 / v2.30 **降序**。不影响内容，但会让人以为漏了几版 | ~~roadmap 下次升版时~~ **已处置**（v2.36 全表改回升序） |
| B-3 | `positioning-m0.md` §3.1 的举证数字锚在旧提交（191/429/+69,812），当前 HEAD（`005d6888`）是 214/471/+111,253，**差额的约 75% 是机器生成的 SBOM**。行数分解表建议为「机器生成物」单列一行 | ~~P8.3 定稿时（§4.5）~~ **已处置**（08-16 重跑并单列「机器生成物」，见 `positioning-m0.md` §3.1；定稿前仍须再跑一次） |
| B-4 | `base-modifications.md` §6.2 已指出：章程 §5.5 写「复用基座既有的文件信箱，**不另造、不改基座核心**」，而 P3.1 改了 `teammateMailbox.ts`（+33/−3，向后兼容的加法）。**字面不符，会在答辩现场被问出来** | ~~负责人（须回写章程并升版本号）~~ **已处置**（章程 v2.10 于该句加补注，原句保留） |
| B-5 | ~~根 `NOTICE`（45–46 / 133–134 行）与 `README.md` 仍写旧举证命令 `git diff 848ad8c2..HEAD`~~ —— **成稿时已过期**：`f288afb4`（08-16）已勘误，NOTICE 现只在「锁定提交」栏保留 `848ad8c2` 作上游 pin，并说明举证一律用 `3380c88` | 已处置；双签补记见 §8.2 |
| B-6 | `session-persistence-review.md` §0 第 7、8 条已过期（已由 `acceptance-m0.md` §5.4 登记，该文本身未回写） | ~~P8.2 定稿时~~ **已处置**（08-16 于两行加过期注） |

---

*本文随 P9.2 产出，v0.1-draft，待全员评审。评审通过后的修订直接改本文并升版本号；M1 任务包一经排期评审确认，其正文迁入 `roadmap.md` 的 M1 节，本文只保留指针。*
