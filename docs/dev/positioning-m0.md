# 阡陌 AgentNest — M0 对外表述统一口径与答辩材料

| 项 | 内容 |
|---|---|
| 文档版本 | **v1.0** |
| 撰写日期 | 2026-08-15 / 签署 2026-08-17 |
| 覆盖阶段 | M0 · S8 任务包 **P8.3**（竞赛/答辩材料对齐） |
| 撰写 | 喻永昌 |
| 状态 | **已双签，可对外**（负责人 2026-08-17 决议宣布双签通过，签署记录见 §6）。对外使用仍须逐字采用 §1 的场景版本、避开 §1.4 禁用表述 |
| 依据 | 范围与法律边界见 [`charter.md`](./charter.md) §5.2 / §5.4 / §5.5 / §5.8、§6 L-1 / L-2 / L-5；任务包与 DoD 见 [`roadmap.md`](./roadmap.md) P8.3；基座能力判定与建议话术见 [`base-adoption.md`](./base-adoption.md) §3 / §6 |

**变更记录**

| 版本 | 日期 | 说明 |
|---|---|---|
| v0.1-draft | 2026-08-15 | 初稿。落实 P8.3 三项交付物的文字部分：统一口径（§1）、两栏能力表（§2）、举证材料（§3，全部实跑）、答辩 Q&A 预案 16 问（§4）、演示视频分镜脚本（§5）。**双签未进行**（§6），DoD 未闭环。同时记录三处需负责人与安全 owner 亲自裁定的事实性问题（§3.5） |
| **v1.0** | **2026-08-17** | **双签通过，升定稿**。负责人 2026-08-17 决议宣布双签通过（含安全 owner 会签；本表为决议记录口径，与本项目历次「负责人决议」同一形式，可复核），状态改「已双签，可对外」。§3.5 三处待裁定的收口情况：① 举证命令勘误已于 08-15 落地、改后 NOTICE 文本随本次决议一并签认；②（claude-agent-sdk 专有 devDep）已于 roadmap v2.36⑥ 决为**保留 + 披露**；③（vendor/audio-capture 溯源）已于同处补齐披露。除本表与文头状态行外内容零改动 |

---

## 0. 本文的性质与读法

1. **本文是草案，不是已批准的口径。**P8.3 的 DoD 是「负责人 + 安全 owner 双签」，双签栏在 §6，**尚未签署**。在签署之前，§1 的场景版本、§4 的书面答案、§5 的旁白，一律不得对外使用。
2. **本文不复制章程与 roadmap，只给指针**（`CLAUDE.md` §1.1⑧「指针不复制」）。**唯一的例外是 §1.1 中章程 §5.8 的那句标准表述**——它本身就是口径，逐字引用才有意义。
3. **§3 的每一个数字都来自本文作者在本仓库实跑的只读命令**，命令与原始输出贴在附录 A。凡未实跑者一律标注来源文档，不写数字。
4. **本文不作任何法律判断。**许可效力问题属章程风险 L-2，须经学校科技处/知识产权办公室（roadmap P8.4）。答辩现场同样不得自行给出法律结论（见 §4 Q8 的回答纪律）。
5. **本文只覆盖「怎么说」，不覆盖「录什么」。**§5 是分镜脚本，不是视频；实际录制、剪辑与敏感信息核查（章程 L-3 对策④）不在本文范围。

---

## 1. 统一口径

### 1.1 标准表述（章程 §5.8 原文，逐字引用）

> **"阡陌基于团队负责人自有的开源项目 open-claude-code（MIT 许可）构建，并在其上实现常驻化改造与智能体通信网络。"**

这一句是**基准句**。以下三个场景版本都是它的展开，**不得改写它的三个要件**：① 基座是团队负责人自有的开源项目；② 许可是 MIT；③ 本项目的新增工作是常驻化改造与智能体通信网络。

### 1.2 四条强制要求

章程 §5.8 在标准表述之后列了四条强制要求，本文不复制其文本，只给编号与落点：

| 条 | 要点（关键词） | 在本文的落点 |
|---|---|---|
| 第 1 条 | 必须同时说清成果边界；被问「写了多少」时用 `git diff 3380c88..HEAD` 回答 | §1.3 三个版本各自的边界句；§3.1 实跑数字；§4 Q1 |
| 第 2 条 | 不得暗示与 Anthropic 存在授权、合作或背书关系；商标声明与根 `NOTICE` 一致 | §1.4 禁用表述清单；§4 Q2 / Q9 / Q10 |
| 第 3 条 | 被问及基座的逆向工程溯源时，如实陈述，不回避、不粉饰 | §4 Q2（书面答案全文） |
| 第 4 条 | 所有对外材料由负责人与安全方向 owner 双签后方可发出 | §6 双签栏；本文头「签前不得外发」 |

### 1.3 三个场景版本

**每一版都在自身文字内满足第 1、2 条**（边界说清、不暗示关联）；第 3 条是**追问时的义务**，第 4 条是**材料的发布闸门**——两者不由句子本身承担，各版下方逐条标出落点。

#### ① 一句话版（30 字量级，用于名片式介绍、海报副标题、答辩 PPT 首页脚注）

> **阡陌是一个云端常驻智能体交流网络，基于团队负责人自有的 MIT 开源项目 open-claude-code 构建，新增常驻化改造与跨节点通信网络。**

| 强制要求 | 本版如何满足 |
|---|---|
| 第 1 条 边界 | 「基于……构建，新增……」把既有资产与新增工作分成两个动词，不含任何「自研全部」的暗示 |
| 第 2 条 无关联 | 全句不出现 Anthropic / Claude / Claude Code；出现的是 open-claude-code 这个**项目名** |
| 第 3 条 溯源 | 本版过短不含溯源；**使用本版时必须准备好 §4 Q2 的完整答案**，被追问即给出，不得以「就是个开源项目」搪塞 |
| 第 4 条 双签 | 本版随本文一并双签后方可印制 |

#### ② 摘要版（100 字量级，用于报名表「项目简介」栏、评审材料摘要、新闻稿首段）

> **阡陌（AgentNest）面向「云端常驻智能体交流网络」，目标是让智能体长期驻留、持有记忆并彼此可寻、可唤、可协作。项目基于团队负责人自有的开源项目 open-claude-code（MIT 许可）构建：该基座提供单机编程智能体的躯干（会话持久化、模型适配、编程闭环等既有能力），本项目在其上新增沙箱内常驻与休眠唤醒、跨节点注册寻址与传输、防循环与两层限流、能力签发与授权、不可删除备份、审计哈希链与资源协商隧道等，并以数学建模竞赛场景做端到端验证。本项目与 Anthropic 无关联、未获其背书。**

| 强制要求 | 本版如何满足 |
|---|---|
| 第 1 条 边界 | 「该基座提供……本项目在其上新增……」是一句话里的两栏；括号内举的既有能力与新增能力**逐条对应 §2 的表**，不越栏 |
| 第 2 条 无关联 | 末句是根 `NOTICE` 第四节商标声明的同义压缩（不逐字复制 NOTICE，避免两份文本漂移） |
| 第 3 条 溯源 | 本版不含溯源陈述；随材料附 §4 Q2 的书面答案，或在同一页脚注给出「基座性质见 NOTICE 第三节」的指针 |
| 第 4 条 双签 | 同上 |

#### ③ 答辩开场版（30 秒口播，约 130 字，用于答辩/路演开场）

> **各位老师好。阡陌要解决的问题是：现在的智能体是会话内工具，窗口一关就消散，彼此之间也只能靠人转述。我们做的是让智能体常驻、有记忆、并且彼此可寻可唤。**
>
> **先把技术来源交代清楚：阡陌基于团队负责人自有的开源项目 open-claude-code 构建，它以 MIT 许可发布、已在 npm 公开，是本项目开始之前就存在的资产。它给了我们单机编程智能体的躯干；本项目做的是常驻化改造和跨节点通信网络这两件它没有的事。两部分的边界可以当场用 git 举证。本项目与 Anthropic 无关联、未获其背书。**

| 强制要求 | 本版如何满足 |
|---|---|
| 第 1 条 边界 | 「是本项目开始之前就存在的资产」+「两部分的边界可以当场用 git 举证」——主动交底并给出可验证承诺（承诺兑现见 §3.1 与 §5 镜 11） |
| 第 2 条 无关联 | 末句显式否认；全段不出现「官方」「授权」「合作」 |
| 第 3 条 溯源 | 本版**主动**说出「负责人自有 / MIT / npm 公开」三项事实但未说逆向溯源——**若评委追问「这个开源项目是怎么来的」，立即转 §4 Q2 全文，不得跳过、不得改口** |
| 第 4 条 双签 | 口播稿同属对外材料，随本文双签 |

**开场版的使用纪律**：技术来源必须在开场 30 秒内主动说出，不等评委问。章程 §6.2 L-2 对策①的原话是「回避比事实本身伤害更大」——把它前置到开场，就没有「被抓到」这回事。

### 1.4 禁用表述清单

以下说法**一律不得出现**在任何对外材料、口播、问答、演示旁白、代码注释截图的讲解中。左列是禁用说法，右列是它为什么禁以及替代说法。

#### A. 会暗示与 Anthropic 存在授权/合作/背书的说法

| ❌ 禁用 | 为什么禁 / ✅ 替代 |
|---|---|
| 「Anthropic 官方 / 官方版本 / 官方 CLI」用于描述本项目或基座 | 章程 §5.4 禁止项第 1 行。基座与本项目均非官方。✅ 说「社区实现」「第三方实现」 |
| 「获得 Anthropic 授权 / 许可 / 支持 / 认证」 | §5.8 第 2 条。不存在此类授权。✅ 不说 |
| 「与 Anthropic 合作 / 联合 / 生态伙伴」 | 同上。✅ 说「与 Anthropic 无关联、未获其背书」 |
| 「基于 Anthropic 的 Claude Code 开发」 | 极易被读成「从 Anthropic 取得代码或授权」。✅ 说「基于团队负责人自有的开源项目 open-claude-code 构建」 |
| 「MIT 许可意味着 Anthropic 允许我们这么做」 | 章程 §5.2② 第 1 条：MIT 是基座作者对**其自身还原与原创工作**的授权，不构成 Anthropic 的授权。✅ 见 §4 Q8 |
| 指着屏幕上的 `You are Claude Code, Anthropic's official CLI for Claude` 说「你看我们就是 Claude Code」 | 章程 §5.2③ 与根 `NOTICE` 第四节：这类字符串承载协议不承载品牌，保留是技术兼容需要，**不构成关联性主张**。✅ 见 §4 Q9 |
| 「Claude Code 中国版 / 国产 Claude Code」 | 同时踩商标与关联暗示两条。✅ 说「云端常驻智能体交流网络」 |

#### B. 会把基座既有能力计入本项目成果的说法

| ❌ 禁用 | 为什么禁 / ✅ 替代 |
|---|---|
| 「我们自研了 XX 万行代码」（含基座行数） | 章程 §5.4 禁止项末行 + L-1。基座导入是一次提交 3,911 文件 / 820,207 行，**不属本立项周期**。✅ 说「导入基座之后本项目新增 472 文件、+111,782 行，其中 31,093 行是工具生成的 SBOM」，并主动说明其中的构成（§3.1） |
| 「100% 自研 / 完全自主可控 / 全栈自研」 | 与 `base-adoption.md` §2.4 C-1 直接冲突（旧洁净室路线才有这个叙事，已随路线变更失去）。✅ 说「基座是既有资产，本项目的新增工作是……」 |
| 「我们从零实现了一个编程智能体」 | 编程闭环是基座既有（章程 §3.5 P-1 判「已有」）。✅ 说「编程闭环由基座提供，我方工作是在自有仓库上的真实任务核验（P3.2）」 |
| 把插件市场 / Artifacts / 语音 / Computer Use 写进成果或演示 | 章程 N-9 明确不纳入 M0 验收与演示；`base-adoption.md` §3.3 专门警告「功能表很长会产生什么都有了的错觉」。✅ 一律不提，被问到就说「基座自带、本项目未纳入验收与演示范围」 |
| 「深度改造 / 大量重写 / 海量代码」 | `base-adoption.md` §6 第一行：不要用「大量」「深度」这类无法验证的词。✅ 给数字与命令 |

#### C. 在知识产权审查中不利、或会被认定为不实的措辞

| ❌ 禁用 | 为什么禁 / ✅ 替代 |
|---|---|
| 「抄 / 照搬 / 移植」官方 Claude Code | 章程 §5.4 禁止项末行明列。✅ 说「从公开行为中提炼设计结论，不复现表达」 |
| 「反编译 / 破解 / 脱壳」 | 与基座自述的 reverse-engineered restoration 不是一回事，且措辞更不利。✅ 用基座自述原词「逆向复原（reverse-engineered restoration）」，见 §4 Q2 |
| 「法律上没问题 / 已通过法律审查 / 合规无风险」 | 章程 §6.2 L-2 对策③：申报前须咨询科技处，**且咨询尚未完成**（roadmap P8.4 未开始）。自行下结论即违规。✅ 见 §4 Q8 回答纪律 |
| 把 🟡 未完成项说成已完成 | L-1「轻则被质疑夸大」。当前 🟡 项见 §2 与 §4 Q5。✅ 逐项如实 |
| 把本机数据说成真机数据；把仪器校准数据填进正式表 | roadmap P4.4 / P7.3 已把这条写成机器强制（report-core 六条 check）。✅ 见 §4 Q5、Q15 |
| 「两台机器跨机演示」用于描述 AC-7 | roadmap v2.30 如实记：AC-7 的 A/B 是**同 runner 内的逻辑节点**。✅ 见 §4 Q14 |

---

## 2. 两栏能力表：基座既有 vs 本项目新增

**读表须知（三条，缺一条就会误读）**

1. **左栏是 open-claude-code v2.38.3 在导入提交 `3380c88` 那一刻就有的能力**，右栏是 `3380c88` 之后本项目做的。两栏**不混排、不合并单元格**。
2. **「基座起点」列照抄章程 §3 的既有判定**（`已有` / `部分` / `自研`），本文**不重新判定**，只给指针。章程 §3.6（E-1~E-4）本身没有这一列，相应行标「§3.6 无此列」。
3. **右栏带 🟡 的是未完成项**，状态取自 roadmap「完成状态速查」（该表是完成状态的唯一出处）。**未标 ✅ 的一律不得作为成果宣称。**

| # | 能力域 | 基座起点 | 左栏：基座既有（occ v2.38.3，导入即有） | 右栏：本项目新增（`3380c88` 之后） | 状态 / 指针 |
|---|---|---|---|---|---|
| 1 | 会话持久化与恢复 | **已有**（章程 §3.2 R-1） | append-only JSONL 会话记录、`--continue` / `--resume` / `--fork-session` 恢复入口 | 在其上核验 AC-1 三条判据并修一个基座既有缺陷：`--resume` 锚点在时间戳并列时静默丢尾部消息（`fad809bc`） | ✅ P1.2 / AC-1 |
| 2 | 上下文压缩 | 章程 §3 无对应行；`base-adoption` §3.1 判「已有」 | MicroCompact / Session Memory Compact / API 摘要三级 | **无新增**（M0 直接用） | — |
| 3 | 多模型适配 | **已有**（§3.1 A-2） | 流适配器 + provider 档案 + 分层模型设置 | 选定两条 provider 路径并跑一致性套件；补齐「Anthropic 原生端点 vs openai-compat 是两段结构不同的适配器」的证据 | ✅ P1.4 / AC-5（保留缺口 G-4，见 §4 Q7） |
| 4 | 沙箱执行环境 | **部分**（§3.1 A-1） | 工具权限模式与审批链路、hooks；**无容器化沙箱** | `@qianmo/sandbox`：Dormice + gVisor（`runsc`）新出生契约、路径白名单、超时强杀、CPU 节流、内存上限、持久审计 | ✅ P1.3 / AC-6(a) 真机 5/5 |
| 5 | 分层记忆 | **部分**（§3.2 R-2） | 文件式项目记忆、四类分类法、`MEMORY.md` 索引与召回管线 | `@qianmo/memory`：三层即三张表、双时间轴（`revoke` / `invalidate` 两个独立操作）、沉淀归档先写目标再封存源；与基座定性为**上层封装 + 共用文件格式** | ✅ P2.3 |
| 6 | 记忆检索唤醒与来源标注 | **部分**（同 §3.2 R-2） | 记忆注入系统提示词的链路 | `@qianmo/recall`：确定性检索 + 小规模全量注入；来源标注走工具层强制引用（引用解析不到即无法被引用） | ✅ P3.3 / AC-4 |
| 7 | 休眠与唤醒 | **部分**（§3.2 R-3，**v2.7 由「自研」改判**） | 唤醒半边有骨架；**休眠半边无扩展点** | `@qianmo/resident` + 常驻 ACP 宿主 + durable 文件信箱读者 + 时间跳跃闸门 + 冻结感知看门狗；沙箱冻结形态 | ✅ P3.1（AC-2 后半） |
| 8 | 跨节点 activator + 常驻 keepalive | **章程 §3 无此行**；roadmap 现状基线记「必须自研，无先例可抄」 | 无 | `@qianmo/activator`：两张面孔共用一个只有 `touch`/`acquire`/`status` 的端口，`destroy` 三层守卫代码层面不可达 | ✅ P2.5（四条 DoD 全部真机） |
| 9 | 注册与按名寻址 | **部分**（§3.3 C-2） | Agent Teams 单机 roster + 按名文件信箱（同主机跨进程可用，**跨主机不可用**） | `@qianmo/registry`：复合键 `<node>/<agent>`、`publicKey`、心跳租约、状态持久化、按节点段核对公钥 | ✅ P2.1 |
| 10 | 节点间传输通道 | **部分**（§3.3 C-3，v2.2 修正） | 拨号侧传输代码 | `@qianmo/transport`：五类帧 + PSK 握手 + 指数退避重连 + 两级去重 + 稳定 `channelId` 重绑重放 | ✅ P2.2 **附报警**：roadmap v2.6 逐行核实「基座有 5,300 行可复用」这一降级依据**不成立**，直接 import 行数 = 0 |
| 11 | 消息协议与信封 | **自研**（§3.3 C-1） | 有结构化协议消息类型，但无信封 / hop / trace / 生命周期状态机 / 错误码表 | `@qianmo/protocol` + `protocol.md`：信封、地址、两条时限、`LIMITS` 唯一出处、`isReplyType`、`advanceTraceparent` | ✅ P1.1 |
| 12 | 防循环与两层限流 | **自研**（§3.3 C-4） | 信箱有存储配额，无跳数与环路概念 | `@qianmo/router`：判环键 `(处理者地址, taskId)` 首次回访即切断；两层限流两个类两套键两个码 | ✅ P4.2 / AC-3（十条判据全绿） |
| 13 | 权限分级与授权 | **部分**（§3.3 C-5） | 工具权限模式与审批链路 | `@qianmo/capability`：每节点 Ed25519、紧凑令牌、nonce 重放表、三级权限；`user-confirmed` 只认本节点签发 | ✅ P4.3（边界：默认 `OPEN_POLICY`，见 §4 Q6） |
| 14 | 备份与不可删除保护 | **自研**（§3.2 R-4） | 无 | `@qianmo/backup`：只增不改、**全包无删除路径**、只写/只读两把凭据、恢复前核对 sha256 | ✅ P4.4（**本机**全绿；真机挂载待部署，见 §4 Q5） |
| 15 | 审计与全链路追踪 | **部分**（§3.3 C-6） | 会话 JSONL 本身 append-only | `@qianmo/audit`：一份追加式哈希链文件、六层映射、`occ audit` 三种查法 + `--verify` | ✅ P7.2（**外部改动无法阻止**，如实记，见 §4 Q6） |
| 16 | 原因级诊断 | **部分**（§3.4 S-1） | 错误分类与重试分类机制 | `@qianmo/diagnosis`：五类成因 + 诚实的 `unknown`，规则式、结构化事实先于文本 | ✅ P5.1（50 条盲分类 50/50） |
| 17 | 资源协商 | **自研**（§3.4 S-2） | 无 | `@qianmo/negotiation`：四段式、字段封闭 payload、三处状态各有不依赖对端的出口 | ✅ P5.2 |
| 18 | 按需加密隧道 | **自研**（§3.4 S-3） | 无 | `@qianmo/tunnel`：连接只在租约期间存在、三条拆除路径、「无残留」由再拨一次并要求失败证明 | ✅ P5.3 |
| 19 | 预测性扩容 | **自研**（§3.4 S-4） | 无 | `@qianmo/capacity`：双通路（日历先验 / 同槽中位数+MAD）、冷却与窗口抑制、被抑制的判断也写审计 | ✅ P6.2（一处 DoD 解读待负责人确认） |
| 20 | 编程闭环 | **已有**（§3.5 P-1） | 接任务→读码→改文件→跑测试→报结果 | 在 Atlas 团队仓库上做真实任务核验 3/3，统一失败 envelope 供诊断消费 | ✅ P3.2 |
| 21 | MCP 兼容 | **已有**（§3.5 P-3） | MCP client / server | 接两个第三方现成 server 实测（发现/调用/互不串扰/三类降级），10 条用例进 CI；五条缺口入档 | ✅ P6.3 |
| 22 | A2A 对齐评估 | **自研**（§3.5 P-4） | 无 | `a2a-gap.md`：15 概念面逐字段映射、27 条差距三选一 | 🟡 **P6.4 未完成**——草案已产出，**全员评审未进行** |
| 23 | 数模竞赛场景 | **自研**（§3.5 P-2） | 无 | `demo/p61-e2e.sh` 六环节端到端，seed 6101 正式三轮 3/3 | ✅ P6.1 / AC-7；🟡 **P2.4 场景需求文档未完成**（赛中真实痛点待补、全员评审未做） |
| 24 | 边界用例库 | 章程 §3.6 无此列（E-3） | 无 | `tests/boundary/` 39 条五类齐备 + 60 分钟混沌跑批 + CI 五连绿 | ✅ P5.4 + P7.1 / AC-8 |
| 25 | 性能与稳定性基线 | 章程 §3.6 无此列（E-4） | 基座有堆驻留裁剪机制与相关文档 | 量具（`demo/lib/p73-*`、`occ resident --mem-sample`）与方法节入库；两个作废级测量坑已钉死 | 🟡 **P7.3 未完成**——正式 24 h / n=100 唤醒 / T3 吞吐**数据待真机** |
| 26 | 身份隔离与 CI 门禁 | 章程 §3.6 无此列（E-2） | 基座已做过一次与官方 CLI 的用户态隔离，路径唯一真源 `paths.ts` 已建 | `identity.ts` 派生层（三身份共存）；保护清单改取三身份并集；删发布类 workflow、CI 钉死 Bun 版本、新增 `verify` 命令组 | ✅ P0.3 + P0.4 |
| 27 | 上游同步 | 章程 §5.7 | 上游有完整 git 历史与 tag | 对上游 v2.46.0 三方应用演练：5 文件 7 冲突块（0.39%）、合并后 11311 测试全绿、工时估算 10–20 人时 | 🟡 **P7.4 未完成**——评审、章程 §5.7 回写、`BASE.md` 记录三项待做 |
| 28 | 本地节点部署脚本 | **自研**（§3.1 A-3） | 无 | **尚未开始**（roadmap P8.1） | ⬜ 未开始 |

**两栏表的一句话总结（可直接用于材料）**：基座给了**单机编程智能体的躯干**（第 1/2/3/20/21 行的「已有」）；本项目做的是**它没有的两件事**——把单机进程改造成沙箱内可休眠可唤醒的常驻节点（第 4/7/8 行），以及从零建一张跨节点的通信协作网络（第 9~19 行）。

---

## 3. 举证材料（全部实跑，命令与原始输出见附录 A）

### 3.1 主口径数字

实跑日期 **2026-08-16**，仓库 `/Users/cornna/project/atlas`，分支 `s4/p4.2-loop-and-rate`，`HEAD = dbecf008`（**首版锚在 08-15 的 `6bada14c`：191 提交 / 429 文件 / +69,812 / −1,362**；本次重跑的差额里约 **75%** 是 08-15 入库的机器生成物 `docs/dev/sbom-m0.json`，故分解表自本版起把「机器生成物」单列一行——**定稿前必须再重跑一次**，命令在附录 A）。

| 问题 | 命令 | 结果 |
|---|---|---|
| 本项目的工作面有多大 | `git diff 3380c88..HEAD --shortstat` | **472 files changed, 111,782 insertions(+), 1,366 deletions(-)** |
| 其中 `packages/` 部分 | `git diff 3380c88..HEAD --stat -- packages/ \| tail -1` | **270 files changed, 44,139 insertions(+), 1 deletion(-)** |
| 其中 `@qianmo/*` 十七个新包 | `git diff 3380c88..HEAD --shortstat -- packages/{17 个包目录}` | **268 files changed, 44,007 insertions(+), 0 deletions** |
| 其中基座既有包 | `git diff 3380c88..HEAD --name-status -- packages/@ant …` | **2 files**（`packages/@ant/model-provider` 的一处 provider 兼容修复 + 一个新增测试），合计 +132 行 |
| 工作记录有多少条 | `git log 3380c88..HEAD --oneline \| wc -l` | **217 个提交** |
| 基座的贡献边界（对照量） | `git show --stat 3380c88 \| tail -1` | **3,911 files changed, 820,207 insertions(+), 34 deletions(-)** |
| 导入前的两个立项提交 | `git show --stat 67f6081 / 74f7a22 \| tail -1` | `67f6081`（立项初始化）**77 files, +4,487**；`74f7a22`（撤下洁净室脚手架）**38 files, −3,072** |

> **`packages/` 目录名的一处提示**：`@qianmo/*` 是**包名**不是目录名——十七个包位于 `packages/<domain>/`（如 `packages/protocol/`，其 `package.json` 的 `name` 为 `@qianmo/protocol`）。因此 `git diff … -- 'packages/@qianmo/*'` **返回空**，答辩现场不要用这条路径，用附录 A 中列全目录的那条。

**行数≠成果：111,782 行的构成（本项目必须主动说出的分解）**

| 构成 | 插入行数 | 占比 | 说明 |
|---|---|---|---|
| **机器生成物**（`docs/dev/sbom-m0.json` 30,921 + `sbom-m0.md` 172，由 `bun run sbom` 生成） | **31,093**（2 文件） | **27.8%** | **不是成果，是证据的载体**——答辩只说「SBOM 已入库、1,089 组件、传染性许可零」，不把这三万行算进任何「新增代码」口径 |
| 测试代码（`*.test.ts` / `__tests__/` / `tests/` / 包内 `test/`） | **28,414**（141 文件） | **25.4%** | 含 39 条边界用例、各包单元与集成用例 |
| 生产与工具代码（`packages/` 非测试 25,405 + `src/` 非测试 2,834 + `scripts/` 3,568 + `demo/` 非测试 10,609） | **42,416** | **37.9%** | `demo/` 那 10,609 行是**可复现验收脚本与量具**、`scripts/` 里 1,590 行是 SBOM 生成器，都不是产品代码；**产品代码 = `packages/` + `src/` ≈ 28,239 行** |
| 文档（`docs/`，不含上一行的生成物） | **9,149** | 8.2% | 章程、roadmap、协议、各评估报告、验收 / 基线 / 复盘等结项文档的增量 |
| 根配置 / CI / `.claude` | **710** | 0.6% | 含删除 `publish-npm.yml`（−135 行） |

**这张表必须主动出示。**只报「十一万行」而不报它有近三成是机器生成的 SBOM、四分之一是测试、又有一大块是验收脚本，就是章程禁用表述清单 B 的「无法验证的夸大」。**扣掉机器生成物之后的口径是 80,689 行**——对外若只能报一个数，报这个，并且说明它是怎么扣出来的。正确的说法见 §4 Q1。分解命令：`git diff --numstat 3380c88..HEAD` 按路径归类（测试 / `docs/` / `packages|src|scripts|demo` / 其余），生成物两文件单列。

### 3.2 新增 workspace 包清单（17 个 `@qianmo/*`）

来源：逐个读 `packages/*/package.json` 的 `name` 字段（附录 A）。三十个 `packages/` 子目录里，**十七个**的包名前缀是 `@qianmo/`，其余为基座既有（`@open-claude-code/*` 五个、`@ant/*`、五个 napi 原生包、`cloud-artifacts`）。

| 包名 | 目录 | 文件 / 插入行 | 承担的能力域（§2 行号） |
|---|---|---|---|
| `@qianmo/protocol` | `packages/protocol/` | 16 / 3,257 | 11 |
| `@qianmo/registry` | `packages/registry/` | 10 / 2,308 | 9 |
| `@qianmo/transport` | `packages/transport/` | 23 / 4,128 | 10 |
| `@qianmo/activator` | `packages/activator/` | 31 / 8,707 | 8 |
| `@qianmo/resident` | `packages/resident/` | 28 / 3,288 | 7 |
| `@qianmo/adapter` | `packages/adapter/` | 16 / 2,723 | 9（AC-2 最后一跳） |
| `@qianmo/memory` | `packages/memory/` | 17 / 2,589 | 5 |
| `@qianmo/recall` | `packages/recall/` | 15 / 1,966 | 6 |
| `@qianmo/router` | `packages/router/` | 12 / 1,504 | 12 |
| `@qianmo/capability` | `packages/capability/` | 13 / 1,658 | 13 |
| `@qianmo/backup` | `packages/backup/` | 15 / 2,208 | 14 |
| `@qianmo/audit` | `packages/audit/` | 7 / 982 | 15 |
| `@qianmo/diagnosis` | `packages/diagnosis/` | 8 / 879 | 16 |
| `@qianmo/negotiation` | `packages/negotiation/` | 9 / 1,304 | 17 |
| `@qianmo/tunnel` | `packages/tunnel/` | 7 / 864 | 18 |
| `@qianmo/capacity` | `packages/capacity/` | 15 / 2,514 | 19 |
| `@qianmo/sandbox` | `packages/sandbox/` | 9 / 951 | 4 |
| **合计** | | **251 / 41,830** | |

**这十七个包全部是新增，`3380c88` 之后创建，删除行为 0。**其中 `@qianmo/protocol` 与 `@qianmo/registry` 的设计资产来自导入**之前**的立项提交 `67f6081`（章程 §5.5 已记为原样复活），亦属本项目工作面。

### 3.3 改动过的基座核心文件清单（`src/` 共 56 文件，其中**修改 32 / 新增 24**）

`git diff 3380c88..HEAD --stat -- src/ | tail -1` → **56 files changed, 5,152 insertions(+), 154 deletions(-)**。

**按目录汇总**

| 目录 | 修改（M） | 新增（A） | 主要归因 |
|---|---|---|---|
| `src/config/` + `src/constants/` | 3 | 3 | P0.3 身份隔离派生层 |
| `src/services/acp/` | 9 | 0 | P3.1 常驻宿主：ACP 侧三个扩展方法 |
| `src/services/api/` | 3 | 0 | P3.1 冻结感知看门狗替换 |
| `src/services/qianmo/`（**全新目录**） | 0 | 9 | 常驻节点、节点身份、审计接线、沙箱审计 |
| `src/cli/handlers/`（阡陌子命令） | 0 | 7 | `occ resident` / `resident-wake` / `occ audit` |
| `src/cli/transports/` | 2 | 0 | P3.1 看门狗替换 |
| `src/utils/`（permissions / sandbox / session / sessionStorage / agents / settings） | 9 | 3 | P0.3 保护清单并集、P1.2 `--resume` 缺陷、P3.1 activity |
| `src/commands/` + `src/skills/` | 3 | 0 | P0.3 硬编码路径文案 |
| `src/entrypoints/` | 1 | 0 | 子命令注册 |
| `src/QueryEngine.ts` | 1 | 0 | P3.1 输入受理回调 `onInputAccepted`（**勘误**：原写「看门狗替换」不成立，见 `base-modifications.md` §2.4） |
| `src/__tests__/` | 0 | 1 | 入站受理用例 |
| `src/services/mcp/`、`src/services/skillLearning/` 的测试 | 2 | 0 | 基座既有 flaky / 固定等待修复 |

**逐文件：为什么改核心而不走扩展点**

理由**一律取自该文件所属提交的提交信息**（`git log 3380c88..HEAD -- <file>`，全文见附录 A.3）。三态标注：

- ✅ = 提交信息**明确**回答了「为什么必须改这个基座文件 / 扩展点为何不够用」
- 🟡 = 提交信息给出了**实质理由**，但未逐字回答「扩展点为何不够用」
- ⚠️ = **提交信息未说明，待补**（不编造）

| 文件 | 提交 | 改核心的理由（取自提交信息） | 判定 |
|---|---|---|---|
| `src/config/paths.ts` | `189268d6` | 身份派生层要成为「唯一信号源」，而路径常量的唯一真源就在本文件；且它在启动期 keychain prefetch 路径上，不能加模块初始化开销 | 🟡 |
| `src/constants/brand.ts` | `189268d6` | 同上；`BIN_NAME` / `PRODUCT_NAME` / `DISPLAY_NAME` 改走 `byIdentity`，`brand.ts` 只 import `identity.ts` | 🟡 |
| `src/config/__tests__/paths.test.ts` | `189268d6` | 随上两项同步的测试 | 🟡 |
| `src/commands/plugin/ManagePlugins.tsx` | `06ad1423` | 三处用户可见文案**硬编码在本文件里**写死 `.claude/settings*.json`，那是官方 CLI 的目录、本构建从不写它 | ✅ |
| `src/commands/sandbox-toggle/sandbox-toggle.tsx` | `06ad1423` | 同上 | ✅ |
| `src/skills/bundled/ultracode.ts` | `4f262147` | **明确作答**：workflow-engine 引擎「单独发布、零 host 依赖」，自己不能 import `src/config/paths.ts`，所以只能修在宿主侧本文件 | ✅ |
| `src/utils/settings/mdm/constants.ts` | `47878025` | 决定「MDM 策略域不随身份切换」并写明三条理由（只读策略非各身份状态 / 切换会静默逃逸管理员策略，安全默认须 fail closed / 阡陌节点是同一套代码的另一种模式） | ✅ |
| `src/utils/permissions/filesystem.ts` | `6c6eef5b` | 实测缺口：阡陌态可自动改写 occ 的 `~/.occ`（内含凭据）。保护清单的语义**本就该是「全部身份的并集」**，而该清单的定义就在本文件 | ✅ |
| `src/utils/sandbox/sandbox-adapter.ts` | `6c6eef5b` | 同一泄漏的读方向：credentials 拒读表改用 `getProtectedUserConfigDirectories()` | ✅ |
| `src/QueryEngine.ts` | `4fe8cd1e` | **勘误**：与看门狗无关——新增可选配置 `onInputAccepted`，在 transcript 落盘后 await 它（「输入已进上下文」这一刻只有 QueryEngine 知道，基座无对应 hook）；书面依据待补，见 `base-modifications.md` | ⚠️ |
| `src/cli/transports/WebSocketTransport.ts` | `4fe8cd1e` | **勘误**：`registerSessionActivityCallback` 回调签名由 `() => void` 变为 `(active: boolean) => void` 的连带修改（+2/−2，加 `if (active)` 保持基座原语义）；书面依据待补 | ⚠️ |
| `src/cli/transports/ccrClient.ts` | `4fe8cd1e` | 同上（连带修改） | ⚠️ |
| `src/services/api/claude.ts` | `4fe8cd1e` | 同上 | 🟡 |
| `src/services/api/gemini/client.ts` | `4fe8cd1e` | 同上 | 🟡 |
| `src/services/api/openai/responsesAdapter.ts` | `4fe8cd1e` | 同上 | 🟡 |
| `src/services/acp/agent/AcpAgent.ts` | `4fe8cd1e` + `4b06f672` | 「ACP 侧补三个扩展方法（输入受理、输入状态、会话活跃度），让宿主能确定性地知道这条输入真的进了模型的上下文，而不是靠时间猜」；`4b06f672` 进一步把 input-status 改回只读查询 | 🟡 |
| `src/services/acp/agent/createSessionMethod.ts` | `4fe8cd1e` | 同上（ACP 扩展方法接线） | 🟡 |
| `src/services/acp/agent/internalAccessors.ts` | `4fe8cd1e` | 同上 | 🟡 |
| `src/services/acp/agent/promptFlow.ts` | `4fe8cd1e` | 同上 | 🟡 |
| `src/services/acp/agent/sessionLifecycle.ts` | `4fe8cd1e` | 同上 | 🟡 |
| `src/services/acp/agent/sessionTypes.ts` | `4fe8cd1e` | 同上 | 🟡 |
| `src/services/acp/entry.ts` | `4fe8cd1e` | 同上 | 🟡 |
| `src/services/acp/__tests__/agent.test.ts` | `4fe8cd1e` + `4b06f672` | 随上述 ACP 改动同步的测试 | 🟡 |
| `src/utils/session/sessionActivity.ts` | `fdbbc2e3` + `4fe8cd1e` | `fdbbc2e3` 明确：新加的两处 `activityCallback` 没走 `CLAUDE_CODE_REMOTE_SEND_KEEPALIVES` 这道基座既有闸门，开关关着也往远端发包——**修的方向是回到基座既有扩展点** | ✅ |
| `src/utils/sessionStorage/logAssembly.ts` | `fad809bc` + `4b06f672` | `fad809bc`：**基座既有缺陷**——`findLatestMessage` 用严格 `>`，时间戳并列时锚点粘在第一条，`--resume` 静默丢尾部消息，正是 AC-1 / AC-2 最难现场诊断的失效方式 | ✅ |
| `src/utils/sessionStorage/transcriptLoader.ts` | `4b06f672` | `loadSessionFile` 原从全局状态推项目目录，导致只读查询必须改全局会话指针；改为允许显式传入项目目录（缺省行为不变） | ✅ |
| `src/services/skillLearning/__tests__/throttleAndCircuitBreaker.test.ts` | `e86d901b` | 基座既有 flaky：用例缺 `timeoutMs`，前序文件泄漏凭据时会真连网并超时变红 | ✅ |
| `src/utils/__tests__/claudemd.projectDirs.test.ts` | `6bada14c` | 基座既有断言把 `readdir` 枚举顺序当成契约，CI 的 ext4 上会假红 | ✅ |
| `src/entrypoints/cli.tsx` | `11c0a622` + `4fe8cd1e` | 两个提交都只陈述做了什么（注册 `occ audit` / `occ resident` 子命令），**未说明为什么必须改本文件、扩展点为何不够用** | ⚠️ **待补** |
| `src/utils/agents/teammateMailbox.ts` | `4fe8cd1e` | 提交信息提到常驻节点复用「文件信箱」，但**未说明本文件为何需要改动** | ⚠️ **待补** |
| `src/utils/__tests__/teammateMailbox.test.ts` | `4fe8cd1e` | 同上 | ⚠️ **待补** |
| `src/services/mcp/__tests__/configWatcher.test.ts` | `a8b06a9a` | **提交正文为空**，只有标题「watcher 正向用例等待真实文件事件」，未说明改核心的理由 | ⚠️ **待补** |

**统计：32 个被修改的基座 `src/` 文件中，✅ 12 个 / 🟡 18 个 / ⚠️ 待裁定 2 个**（勘误：初稿为 17 / 4，其中三行「看门狗替换」归因经逐 diff 复核不成立，转为待补；2026-08-16 逐条考证后 5 行升级、仅会话活跃度签名两行留待裁定；逐文件理由与核实记录见 `docs/dev/base-modifications.md`，那里是 P9.3 改造点清单的真源，本表此后不再单独维护）。

**本节是 roadmap P9.3「对基座的改造点清单」的原料，如实标注。**四条 ⚠️ 与十七条 🟡 都需要在 P9.3 汇总时由改动人补写「扩展点为何不够用」（章程 T-5 对策④要求写在 PR 描述里）。另：roadmap v2.33 / `upstream-sync-drill.md` §5 行动项 ③ 已建议把 `FreezeAwareWatchdog` 这类替换**抽成注入点**而不是在基座文件里就地替换——上表 6 条 🟡 的看门狗改动正是那条建议的对象，本次同步演练中唯一的代码语义冲突也出自这里。

### 3.4 为什么基线是 `3380c88` 而不是 `848ad8c2`

**结论先行**：对外举证一律用 **`3380c88`**（本仓库的基座导入提交），**不用 `848ad8c2`**（上游 open-claude-code 的 pin 提交）。依据是章程 **v2.4「举证命令勘误」**（§5.5 末条，另见 §5.8 第 1 条括注、§6.2 L-1 对策②、`base-adoption.md` §2.4 C-1 与 §5 R-2 的同款勘误，以及 roadmap v2.4 与 P8.3 条目）。

理由（章程 §5.5 的判断，本文只转述不复制）：本仓库是**零改动快照导入**，不含上游 git 历史；`848ad8c2` 是**上游仓库**的提交 SHA，在一份正常克隆的本仓库里不是有效 git 对象，`git diff 848ad8c2..HEAD` 会报 `unknown revision`。上游溯源以根 `BASE.md` 的记录 + 与上游仓库比对为准，不经本仓库 git 对象。

**答辩现场的三条纪律**：

1. 只敲 `git diff 3380c88..HEAD` / `git log 3380c88..HEAD` / `git show --stat 3380c88`。
2. 若评委手里的材料写着 `848ad8c2..HEAD`（旧写法；根 `NOTICE` 与 `README.md` 已于 2026-08-15 勘误，见 §3.5 ①），当场说明这是已勘误的旧写法并改用 `3380c88`，**不要现场试跑旧命令**。
3. 不要把 `3380c88` 说成「上游的 commit」。它是**本仓库**的导入提交；上游 pin 是 `848ad8c2` = tag `v2.38.3`，两者是两个仓库里的两个对象，`BASE.md` 记录其对应关系。

### 3.5 ⚠️ 三处必须由负责人与安全 owner 亲自裁定的事实（本文不擅自处理）

> 本节只陈述实跑发现的事实与冲突，**不改动任何被指涉的文件**（本次交付只新增本文档）。

#### ① ✅ 已勘误 —— 根 `NOTICE` 第二节曾写着已勘误的举证命令

**处置（2026-08-15，负责人授权，随 P8.4 一并处理）**：`NOTICE` 中文「二、基座溯源」节末与英文 section 2 节末的举证命令已改为以本仓库导入提交 `3380c88` 为基线（工作面 / 工作记录 / 基座贡献边界三条命令 + 导入前的 `67f6081`、`74f7a22`），并各补一句「本仓库经零改动快照导入、不含上游 git 历史」的说明；`848ad8c2` 作为**上游 pin 的 SHA 陈述**保留。`README.md` 基座说明段同批修正。逐条依据见 [`license-chain-m0.md`](./license-chain-m0.md) §3.2 D-1。

- **原问题**：`NOTICE` 中文第二节与英文第 2 节均写有 `git diff 848ad8c2..HEAD` 作为成果边界的举证命令（中文原句：「pin 提交 848ad8c2 之后的全部提交为阡陌团队的工作」），与章程 v2.4 的勘误结论**不一致**。
- **为什么必须处理**：`NOTICE` 是随仓库分发的对外文件，也是 roadmap **P8.4 DoD** 明列的核对对象（「根 `NOTICE` 与实际状态一致性核对」）。答辩材料若引用 NOTICE 的这一句，就会把已知跑不通的命令带上台。
- **仍要做的**：改后文本属对外法律文本，**仍需负责人 + 安全 owner 双签确认**（章程 §5.8 第 4 条）；本次改的是事实性勘误，未改动任何口径实质。**本文正文各处引用 `NOTICE` 时不再写行号**（改动会使行号漂移，用节名定位）。

#### ② 本工作副本里 `848ad8c2` **实际可解析**——与章程「不是有效 git 对象」的表述在本机不符

实跑（附录 A.1）：

```
git cat-file -e 848ad8c2                 → exit 0
git rev-parse 848ad8c2                   → 848ad8c2c8daca9f5aa2410da555553e07700f5d
git diff 848ad8c2..HEAD --shortstat      → 464 files changed, 70377 insertions(+), 555 deletions(-)
```

同时核实：`git remote -v` 只有 `origin`（`github.com/sweetcornna/atlas`）；`git for-each-ref --contains 848ad8c2`、`git branch -a --contains`、`git tag --contains` **全部为空**；`git rev-list --all --count` = **194**（= 191 + 导入提交 + 两个立项提交），即该对象**不可从任何 ref 到达**，是留在 pack 里的悬空对象。

- **这意味着什么**：章程的**结论仍然正确且必须遵守**——评审方拿到的克隆或压缩包里**不会有**这个对象（悬空对象不参与 clone/fetch 传输），旧命令在他们机器上照样跑不通。但**在这台机器上试跑会「意外成功」**，且给出的是**另一组数字**（464/+70,377/−555，因为它把 `3380c88` 相对上游多出的 39 个 atlas 独有立项文档也算了进去——该差异已由 `upstream-sync-drill.md` §2 独立核实过）。
- **风险**：预演时若有人在本机试跑 `848ad8c2..HEAD` 并看到它「能跑」，可能据此认为章程 v2.4 的勘误是错的，进而在正式材料里改回旧命令——那会在评审方机器上当场失败，且两套数字对不上。
- **需要裁定的事**：(a) 是否要求答辩机在演示前做一次干净 clone（这也能顺带证明 §5 镜 11 的可复现性）；(b) 这个悬空对象的来源是否需要查明并记入 `BASE.md`（`upstream-sync-drill.md` §6 记载演练「主检出未加 remote、未 fetch、无上游对象」，与本次实测不一致——**该演练记录的其余部分本文未做复核**）。**`BASE.md` 由负责人维护，本文不碰**（`CLAUDE.md` §2.4）。

#### ③ 四个基座核心改动缺「扩展点为何不够用」的书面理由

见 §3.3 表末四条 ⚠️。章程 T-5 对策④ 要求改核心的 PR 描述里必须注明该理由，roadmap P9.3 要把它汇总成「对基座的改造点清单」。**这四条现在没有出处可引**，答辩若被问到「你们改了基座哪些地方、为什么」，这四个文件回答不出理由。建议在 P9.3 之前由改动人补写。

---

## 4. 答辩 Q&A 预案（16 问）

**通用回答纪律（每一问都适用，下面各问只写附加纪律）**

- 只说能当场举证或有文档指针的话；不确定就说「这条我给不出结论，材料里有 X 文档 / 会后补」。
- 不用「大量 / 深度 / 海量 / 基本上 / 差不多」这类不可验证的词（`base-adoption.md` §6）。
- 不替科技处、不替法务下结论（章程 §6.2 L-2 对策③）。
- §1.4 的禁用表述清单在问答中同样有效，且问答是最容易破功的场合。

---

### Q1 ⭐「你们自己做了什么？写了多少代码？」

**书面答案**

> 先说边界：open-claude-code 是我们团队负责人在本项目立项**之前**就已有的开源项目，以 MIT 许可发布、已在 npm 公开。它在 2026-08-11 以零改动快照导入本仓库，那一次提交是 3,911 个文件、82 万行——**那部分不是本立项周期的成果**。
>
> 从导入之后算起，本项目的工作面是 **217 个提交、472 个文件、新增 111,782 行、删除 1,366 行**，一条 `git diff 3380c88..HEAD --shortstat` 就能当场列出来。
>
> 但我想主动说清楚：**十一万行不等于十一万行的成果。**先扣掉 **31,093 行机器生成的 SBOM**（软件物料清单，工具生成、只是证据载体），剩 80,689 行；其中 **28,414 行是测试代码**，9,149 行是文档，约 10,600 行是可复现的验收脚本与测量量具，还有一个 1,590 行的 SBOM 生成器。真正的产品代码约 2.8 万行，主体是 **17 个新增的 `@qianmo/*` workspace 包**（44,007 行，含各自的测试），加上对基座 `src/` 的 56 个文件改动（其中 32 个是修改既有文件、24 个是新增）。
>
> 按能力域讲，新增的是基座**没有**的两件事：把单机进程改造成沙箱内可休眠可唤醒的常驻节点，以及从零建一张跨节点的通信协作网络——协议信封、注册寻址、传输、防循环与限流、能力签发、审计哈希链、资源协商与按需隧道。基座给的是单机编程智能体的躯干。这两栏在材料里是分开列的，没有混排。

**举证指针**：§3.1（数字与命令）、§3.2（17 个包清单）、§3.3（基座改动清单）、§2（两栏表）。附录 A 有原始输出。

**回答纪律**

- **必须主动先扣掉机器生成物、再说出测试占比**这两件事，不要等评委追问。先说的是坦诚，被问出来的是心虚。
- 不说「自研十一万行」——那会把 SBOM、测试与文档全说成「自研代码」。说「新增 111,782 行」，然后立刻给分解。
- 不说「100% 自研」「完全自主可控」。
- 不说「基座只是个脚手架」——章程 §5.7 明写基座是长期地基、不退场。

---

### Q2 ⭐「你们的基座是逆向工程产物吗？」

**书面答案**（这一问**如实、不回避、不粉饰**，章程 §5.8 第 3 条）

> 是的，我如实说明。
>
> **第一，基座是什么。**open-claude-code 在它自己的 README 里自述为 Anthropic Claude Code 的「社区版」——以**逆向复原（reverse-engineered restoration）**的代码为基座、由社区维护和优化的衍生版本，并记录了它 fork 自另一个还原工程。这是它的公开自述，我们没有转述得更轻。
>
> **第二，许可链是怎么回事，以及它到哪儿为止。**基座以 MIT 许可发布，版权声明是 open-claude-code contributors，已在 npm 公开发布，包名 `@sweetcornna/open-claude-code`。我们保留了它的 LICENSE 与版权声明。但必须说准一层：**这份 MIT 是基座作者对其自身还原与原创工作的授权，它不构成、也无法构成 Anthropic 对被还原表达的授权。**这两件事我们在根 NOTICE 第三节里是分开写的，NOTICE 明确写了「本声明不就该授权与被还原表达之间的关系作出任何法律判断」。
>
> **第三，商标与关联关系。**「Claude」「Claude Code」「Anthropic」是 Anthropic, PBC 的商标。本项目与 Anthropic **无关联、未获其背书、未获其赞助**，也不主张对这些商标的任何权利。这一条在根 NOTICE 第四节里，与我们对外说的完全一致。
>
> **第四，为什么代码里还留着 Anthropic 的字样。**基座刻意保留了一批**承载协议而非品牌**的字符串——系统提示词的一段固定前缀、`claude-code/<version>` 的 User-Agent、OpenTelemetry 的 service.name、`CLAUDE.md` 这类跨工具约定的文件名、以及 `CLAUDECODE=1` 这个子进程环境变量。改动它们会以难以诊断的方式破坏功能，所以我们做身份隔离改名时同样不动。**这些标识的技术性保留不构成任何关联性主张**，NOTICE 第四节逐条列了它们。
>
> **第五，我们没有把这件事当成已经解决的问题。**「MIT 能否覆盖上游被还原的表达」这个问题，我们在立项文档里是作为一条**开放的法律风险**记着的，正在按计划咨询学校科技处和知识产权办公室，咨询结论出来之前我不会替他们给答案。如果口径要求更强的隔离，我们的立项文档里预留了触发范围评审的退路。

**举证指针**：根 `NOTICE` 第三、四节；根 `LICENSE`；`BASE.md`；章程 §5.2②（三条纪律）、§6.2 **L-2**（风险、影响「极高」、对策五条）、**L-5**（已解除，2026-08-11 三语 README 许可段修正）；`base-adoption.md` §5 **R-1**（四条有出处的事实 + Q-1~Q-4 + 「不粉饰的判断」）与 §6（三类高频提问的建议骨架）。

**回答纪律**

- **不说**「已经没有法律问题了」——`base-adoption.md` §5 R-1 原文点名纠正这种理解。
- **不说**「MIT 说明 Anthropic 同意了」。
- **不说**「反编译」「破解」「脱壳」；用基座自述的原词「逆向复原 / reverse-engineered restoration」。
- **不说**「这个我们也不清楚」——基座归属在我方，说不清楚比说清楚更糟。
- **不主动展开**基座与官方产品的具体相似度；我们从未做过逐行比对，也不应当做（`base-adoption.md` §7 第 4 条）。被追问就说：我们没有做过这项比对，也不打算做，基座的性质以它自己的公开自述为准。
- 这一问**只由负责人或安全 owner 回答**，其他成员不接。

---

### Q3「为什么不直接用 Anthropic 官方的 Claude Code 或者官方 SDK？」

**书面答案**

> 三个理由，按重要性排。
>
> **第一，我们要改的东西在扩展点之外。**阡陌的核心是把一个单进程 CLI 改造成沙箱内可休眠、可被跨节点唤醒的常驻节点。这需要动进程内的会话生命周期、流看门狗、信箱读者这些内部机制——比如基座那条 90 秒的流空闲看门狗默认开着，沙箱冻结一次就会把正常的流判成挂死，我们必须让它跨过时间跳跃后重新计时。这类改造在一个闭源产品或一个只给外部接口的 SDK 上做不了。我们的评估结论是：唤醒那半边有现成通道，**休眠那半边没有任何进程内扩展点**。
>
> **第二，许可与归属。**我们需要一个可以入库、可以修改、可以随材料分发、也可以开源的地基。open-claude-code 是 MIT，且归属在团队负责人手里——不会因为第三方仓库删库、改许可或断更而受影响。
>
> **第三，成本与验收线不能互换。**换基座抬高的是起点，不是验收线。我们的八条验收标准在换基座时**一条判据都没有放松**，后来修订的两条（休眠态定义、判环粒度）是把定义改精确，不是把线放松。
>
> 顺带说一句纪律：我们对官方 Claude Code 的参考只取**结论**不取**表达**——不复制它的源码或提示词原文，这一条在立项章程里是强制条款。

**举证指针**：章程 §5.1（选它的三条理由）、§5.4（允许与禁止表）、§4 v2.0/v2.2 说明（判据未放松）、§6.1 **T-5**（改造优先走扩展点）；roadmap v2.15 ②（「基座没有任何进程内扩展点」这一结论的出处）；`base-adoption.md` §2.2 新前提对比表。

**回答纪律**

- 不说官方产品的坏话，也不做功能对比评判——我们没有做过评测。
- 不说「官方不让用 / 官方封了我们」这类无据陈述。
- 不把「MIT 可分发」说成「我们比官方更自由」。

---

### Q4「你们和 A2A、MCP 是什么关系？是不是重复造轮子？」

**书面答案**

> 两个规范，答案不一样。
>
> **MCP：我们是它的使用者，已经实测接入。**基座本来就带 MCP client，我们做的是核验——接了两个第三方现成的 MCP server（filesystem 与 memory，版本精确 pin），实测覆盖工具发现、调用的写读闭环（还用 `node:fs` 绕开 MCP 通道复证过）、两台同时在线互不串扰、以及三类降级（进程不存在、启动即退出、在飞被杀后自动重连自愈），10 条用例进了 CI 分片、零 mock。我们也如实记了五条没覆盖到的缺口，其中一条是部署真坑：**occ 的 roots 通告会整体顶掉 filesystem server 命令行上的允许目录**。
>
> **A2A：不是重复造轮子，是不在同一层。**我们做了逐字段的对齐评估，结论是——A2A 是「一个客户端调用一个不透明智能体服务」的**应用层 API**；阡陌协议是「一个节点把一个信封投递到另一个节点」的**网络层**。所以映射表里大部分「无对应」不是缺口，是层次差：A2A 规范里根本不存在跳数、TTL、环路检测、去重键这些字段，而它们正是我们 AC-3 依赖的核心。我们免费对齐的是三处概念：任务状态机名、`taskId` / `contextId` 分离、Agent Card 形状；`trace_id` 采用 W3C `traceparent` 格式。
>
> 真正的差距我们也列了三条，最主要的一条是**我们没有能力描述面**——任何 A2A 客户端连发现我方智能体都做不到，这是 M1 的收敛点。
>
> 需要说明的是：**A2A 对齐评估这份文档目前是草案，全员评审还没进行，所以这个任务包在我们自己的表上是未完成状态。**

**举证指针**：`docs/dev/a2a-gap.md`（§1 一句话结论、§2 层次对照、§3 十五个概念面映射、§4 二十七条差距、§5 M1 收敛建议、§6 三个待定夺判断点）；`docs/dev/p6.3-mcp-conformance.md`（§0 八条结论、§5 缺口 G-1~G-5）；章程 **N-4**（不实现完整 A2A 协议栈）、§3.3 C-1（自研 + 概念对齐 A2A 的决议）。

**回答纪律**

- 不说「我们兼容 A2A」——章程 N-4 明确只做评估不实现，说兼容即不实。
- 不说「A2A 不如我们的协议」——是层次差，不是优劣。
- **必须说出 P6.4 是 🟡 未完成**（草案 + 全员评审未做）。
- 引用 A2A 规范时说明这是公开标准的正常引用，不构成与 Linux Foundation 或 A2A 项目的关联主张。

---

### Q5 ⭐「哪些验收标准还没过？哪些是本机跑的、不是真机？」

**书面答案**

> 分两部分说：验收标准（AC）与任务包（P）。
>
> **八条验收标准的当前状态**（依据我们自己的完成状态表，它是完成状态的唯一出处）：
>
> | AC | 状态 | 环境 | 需要说明的边界 |
> |---|---|---|---|
> | AC-1 进程重启续答 | ✅ | 真实模型补测 | — |
> | AC-2 跨节点唤醒休眠智能体并回执 | ✅ 10/10 | **真机**（GCP + Dormice + gVisor） | ack P95 8.022 s、result max 12.422 s，判据 60 s / 5 min |
> | AC-3 环路即时切断 + 两层限流 | ✅ 十条判据全绿 | 本机 | 协议层按真实 600 条预算打满 |
> | AC-4 项目记忆跨会话检索 | ✅ 10/10、伪造 0/6 | 真实 provider | — |
> | AC-5 模型中立 | ✅ | 真实 provider | **缺口 G-4：两条 provider 路径仍指向同一网关**，未验证不同厂商 HTTP 层差异 |
> | AC-6(a) 沙箱越权被拒 | ✅ 5/5 | **真机** | — |
> | AC-6(b)(c) 删库可恢复 / 备份不可删 | ✅ 十一条全绿 | **本机** | **沙箱挂载边界本机测不到**（两侧同进程），真机部署仍需把备份 store 放在沙箱够不到的位置 |
> | AC-7 场景端到端 3/3 | ✅ | 本机 | **授权是脚本化本地同意不是真人点击；A/B 是同一个 runner 内的逻辑节点**（真实包 / 信封 / unix socket / 子进程，但不是跨机生产拓扑） |
> | AC-8 边界用例进 CI | ✅ 39 条 + CI 五连绿 | CI | — |
>
> **四个任务包处于未完成（🟡）状态，我逐条说**：
>
> 1. **P2.4 数模场景需求文档**：赛中真实痛点待按原始记录补写，全文待全员评审。
> 2. **P6.4 A2A 对齐评估**：草案已产出，**全员评审未进行**。
> 3. **P7.3 性能基线报告**：量具与方法节入库，但**正式的 24 小时长跑、n=100 唤醒阶梯、吞吐正式表全部待真机恢复**。本地跑出的数字只作仪器校准，我们的报告核心有六条 check 把它**机器强制**挡在正式表外——macOS 没有 cgroup 通道就直接判 `pass=false`。
> 4. **P7.4 上游同步演练**：演练已执行、数据已入库，但评审、章程回写、`BASE.md` 记录三项待做。
>
> 另外 **P0.1 的服务器 `dor doctor` 与 P0.2 的全员通读定稿**仍未闭环，S8/S9 的任务包（演示环境固化、全量走查、软著预检、复盘）尚未开始。

**举证指针**：roadmap「完成状态速查」（唯一出处）；各行的边界原文见 roadmap v2.20（AC-2 真机数据）、v2.23（AC-6(b)(c) 本机边界）、v2.30（AC-7 诚实边界）、v2.34（P7.3 待真机）、v2.13（AC-5 的 G-4）。

**回答纪律**

- **不把本机说成真机**，也不把「本机全绿」说成「已验收」。表里的「环境」列必须念出来。
- 不把 🟡 说成 ✅。四个未完成项要主动报，不要等问。
- 不说「基本上都过了」；说具体条目与具体环境。
- 被追问「那 AC-6(b)(c) 到底算不算过」时：判据本身通过，**沙箱挂载这一维本机测不到**，真机部署时补——这就是完整答案，不要加修饰。

---

### Q6 ⭐「你们安全上最弱的一环是什么？」

**书面答案**（这一问答得越具体越可信）

> 我列四条，按我自己认为的严重程度排。
>
> **第一，跨节点消息构成 prompt injection 攻击面，而且这类攻击只能提高成本、不会被解决。**跨节点消息最终会进入对端智能体的 LLM 上下文，等于把不可信输入喂给模型。它有三重独立性：独立的信任边界、独立的放大机制（恶意 prompt 可在互联 agent 之间自复制）、独立的失效模式（消息事实上替用户做了授权）。我们在立项文档里把它记为技术风险 T-7，并且**把判定基准写死了：不以「模型是否被说服」验收**——验收必须落在结构性约束上（授权链、来源标记、能力边界），「这次没被骗到」不算通过依据。落地上，`user-confirmed` 这一级权限**只认本节点私钥签发的**，远端签得再好也拒，而且这条判断在验签之前就做。
>
> **第二，M0 的默认策略允许未签名的任务请求。**默认 `OPEN_POLICY` 下，可选的只是「是否强制出示令牌」——**出示了就一定全程校验**，但可以不出示。原因是 M0 没有密钥分发机制，`--trust <node>=<publicKey>` 是手工配的、没有 TOFU，在拿不到公钥之前强制签名只会让整张网安静下来。强制版本 `--require-signed-tasks` 已经实现并在集成用例里跑通，M1 上 mTLS 之后切默认。
>
> **第三，审计日志的「不可改」只成立两句，第三句不成立。**写入方改不了（fd 是 `O_APPEND | O_NOFOLLOW`，类里没有 seek / truncate / delete）；外部改动可检测（哈希链断点能定位到具体序号）；但**外部改动无法阻止**——有文件写权限的人可以整篇重写并重算哈希。挡住它需要 append-only 挂载或机外见证，M0 两样都没有。我们没有在代码或文档里声称它不可变，而且专门写了一条用例把这句钉住，免得读套件的人误以为文件是不可变的。
>
> **第四，M0 的加密是 TLS + 预共享密钥，够演示不够上线。**不做 PKI、不做证书签发与轮换、不做端到端加密，这是立项时就写下的非目标，M1 换 mTLS。另外 PSK 是对称的——所以 capability 我们没有用它签，改成每节点 Ed25519 自签，否则任何持钥节点都能伪造任意节点签发的「用户已确认」凭证。

**举证指针**：章程 §6.1 **T-7**（三重独立性 + 判定基准）、§2.2 **N-3**（生产级加密非目标）、§3.3 **C-5**（Ed25519 决议与 N-3 的界线）；roadmap v2.22 **P4.3**（S-1 / S-3 / `OPEN_POLICY` 边界）、v2.28 **P7.2**（「不可改」三句）；`packages/capability/test/authorization-invariants.test.ts`（会变红的扫描）。

**回答纪律**

- **不说**「我们的系统是安全的」「已经做了完整的安全防护」。
- **不说**「prompt injection 我们已经解决了」——判定基准明确写了这类攻击不会被解决。
- 不把 M1 的计划说成 M0 的现状（mTLS、密钥分发都还没有）。
- 举第三条时把「三句只认两句」完整念出来，不要只念前两句。

---

### Q7「模型中立怎么证明？会不会只是换个 API key？」

**书面答案**

> 判据是我们自己定的，比「换 key」严格：同一条编程任务用例，在两个不同模型供应商适配器下都要完成并通过任务自带的测试断言；两次运行**只改配置文件、不改任何代码**；另外还要跑一致性测试套件——多轮、工具调用、流式三项，对两家全绿。
>
> 实测结果：一致性三项对两条 provider 各跑一遍，零 mock 零回放；同一条编程任务两家各跑通、任务自带测试各 5 pass；「仅改配置」这一条我们是用**两次逐字相同的命令行**证明的。
>
> 更强的一条证据是路径本身不同：其中一条走的是 Anthropic 原生端点（不做消息转换、不走流适配器），另一条走 openai-compat 路径——这是**结构上不同的两段适配器**，不是同一段代码换个 base URL。
>
> **但我要主动说一条缺口**：这两条路径目前仍指向**同一个网关**，所以我们没有验证不同厂商在 HTTP 层的差异（限流响应、错误体格式、连接行为这些）。这条缺口在我们的记录里编号 G-4，是保留项。
>
> 另外，模型中立这件事不只体现在适配器上。我们的原因级诊断是**规则式而不是模型式**，三条理由之一就是模型中立——把模型调用放在失败路径上，而失败路径最可能正是因为模型不可用；记忆检索的引用工具也是纯 JSON Schema、零供应商名，两家转换后声明逐字相同。

**举证指针**：章程 §4 **AC-5** 判据；roadmap 完成状态速查 **P1.4** 行（含 G-1 关闭、G-4 保留）；`docs/dev/p1.4-provider-verification.md`；roadmap v2.24 **P5.1**（诊断规则式的三条理由）、**P3.3**（工具声明两家逐字相同）。

**回答纪律**

- **必须主动说 G-4**（同一网关）。这是最容易被追问出来的一条，先说是坦诚，被问出来是隐瞒。
- 不说具体供应商的优劣或价格。
- 不说「支持所有模型」——判据只承诺 2 家（章程 T-4 对策④）。

---

### Q8 ⭐「许可链讲一下：这套东西你们有权拿来参赛/申报/开源吗？」

**书面答案**

> 我把已经确定的和还没确定的分开说。
>
> **已经确定的部分（有文件可查）**：基座 open-claude-code 以 **MIT** 发布，根 LICENSE 的版权行是 open-claude-code contributors；它已在 npm 公开发布，包名 `@sweetcornna/open-claude-code`；它归属于我们团队负责人；锁定版本是 tag v2.38.3 对应的提交，记录在根 `BASE.md` 里。本仓库整体**沿用 MIT**，包括 17 个 `@qianmo/*` 包；这些包的自有 TypeScript 源文件都带统一的版权头与 SPDX 标识（实测覆盖 **217 / 217** 个 `.ts` 文件）。MIT 允许使用、复制、修改、合并、发布、再分发，条件是保留版权与许可声明——我们保留了。
>
> **曾经有过、现在已解决的一条**：基座 README 的许可段以前写着「仅供学习研究用途」，与 MIT 正文不含用途限制不一致。2026-08-11 已由基座作者在基座侧修正三语 README，用途限定移除、三语的商标声明统一。这条风险在我们的记录里编号 L-5，已标记解除。
>
> **还没确定、我不替任何人下结论的一条**：MIT 是基座作者对**其自身还原与原创工作**的授权，它能否覆盖上游被还原的表达，这是一个法律问题。我们把它记为风险 L-2，影响评级写的是「极高」，对策第三条是**申报前主动咨询学校科技处和知识产权办公室**。这项咨询是我们计划中的任务包，**目前尚未完成**。在拿到他们的书面或可复述口径之前，我不会说「法律上没问题」。而且我们预留了退路：如果口径要求更强的隔离，就触发范围评审。
>
> 完整的第三方依赖 SBOM 与许可证清单、许可链证据包、以及 `NOTICE` 与实际状态的一致性核对，是另一份专门的产出。

**举证指针**：根 `LICENSE`、根 `NOTICE`（第一、二、三、五节）、根 `BASE.md`；章程 §5.2①、§5.5（版权头已决为「加」，覆盖两包 15 个 `.ts`；license 字段已改 MIT）、§6.2 **L-2** / **L-5**；`base-adoption.md` §5 R-1 的 Q-1 与 Q-2；roadmap **P8.4**（SBOM、证据包、科技处咨询、NOTICE 核对）。

> **许可链的专门产出**（2026-08-15 更新：文件已入库）：P8.4 的交付物落在 [`license-chain-m0.md`](./license-chain-m0.md)（证据包核对表 E-1~E-9、`NOTICE` 差异清单 D-1~D-6、非开源授权项人工核读、科技处议题模板与回填表）与 [`sbom-m0.md`](./sbom-m0.md) / [`sbom-m0.json`](./sbom-m0.json)（`bun run sbom` 生成）。**答辩前仍须核对未决事项一栏**（该文 §6）：科技处咨询与 E-8 归属举证尚未完成，本问只能回答到上面这一层。

**回答纪律**

- **绝对不说**「法律上没问题」「已经过法律审查」「合规无风险」。科技处咨询未完成（roadmap P8.4 未开始）。
- 不说「MIT 覆盖一切」。
- 不说「Anthropic 也是开源的所以没关系」。
- 这一问**只由负责人或安全 owner 回答**。

---

### Q9「你们演示的界面/日志里为什么有 Anthropic 的字样？」

**书面答案**

> 那是承载**协议**而不是品牌的技术标识，基座刻意保留、我们做改名时同样不动。具体是几类：系统提示词里的一段固定前缀（供应商侧的提示缓存与行为调优按这段文本命中）、`claude-code/<version>` 这个 User-Agent、OpenTelemetry 的 service.name、`CLAUDE.md` 这类跨工具生态约定的文件名，以及 `CLAUDECODE=1` 这个子进程环境变量。改动它们会以难以诊断的方式破坏功能。
>
> **保留这些标识是技术兼容需要，不构成任何关联性主张。**商标声明在根 NOTICE 第四节，逐条列了这些字符串，并写明本项目与 Anthropic 无关联、未获其背书。

**举证指针**：根 `NOTICE` 第四节；章程 §5.2③ 及其末尾的「技术上保留 ≠ 对外宣称」；`base-adoption.md` §5 R-1 的 **Q-3**（明确预告演示时屏幕上可能真的出现这些字符串，需专门准备说法）。

**回答纪律**

- 不说「那只是个字符串没关系」——要说清它为什么必须保留。
- 不借这些字符串开玩笑，不说「你看我们跟官方一样」。
- 录屏前按 §5 的分镜纪律检查画面，能不入镜就不入镜；入了镜就按本答案解释，**不要临时切窗口回避**。

---

### Q10「这是不是把别人的产品改个名就来参赛？」

**书面答案**

> 不是，我从三个角度回答。
>
> **归属上**：基座不是「别人的产品」，是我们团队负责人自有的开源项目，MIT 许可、npm 公开发布。这一点可查。
>
> **成果边界上**：我们从来没有把基座的能力算作本项目的成果。材料里基座既有能力与本项目新增能力是**分列两栏**的，两栏对应的 git 边界是导入提交 `3380c88`——之前是基座，之后是我们，可以当场 `git log` / `git diff` 举证。这是我们自己在立项文档里写下的强制要求，不是被问到才补的。
>
> **工作性质上**：我们做的不是改名。改名只是其中很小的一部分——身份隔离那一包，让阡陌节点、occ 本体、官方 CLI 三者能在同一台机器上共存互不干扰。主体工作是基座**没有**的两件事：沙箱内的常驻与休眠唤醒，以及一张跨节点通信协作网络（17 个新包）。基座在跨主机这一维上给我们的是零——它的远程传输层在上游已被整体删除，我们的跨节点传输是从零自研的，不是「把某个开关打开」。

**举证指针**：§2 两栏表；§3.1 / §3.2 / §3.3；章程 §6.2 **L-1** 对策④⑤；`base-adoption.md` §5 R-1 的 **Q-4**（明确写「必须事前准备，不能现场发挥」）、§3.2（基座在跨主机维度为零）；`CLAUDE.md` §1.2（`LAN_PIPES` / `UDS_INBOX` 代码已被删除，跨节点传输是从零自研）。

**回答纪律**

- 语气不要防御。这一问 `base-adoption.md` 早就预判到了，我们有准备。
- 不反问评委、不说「你可以自己去看代码」；直接把命令敲出来。
- 不说「改名只是个小工作」的同时贬低 P0.3——它解决的是真实的越权读写缺口（见 §3.3 `6c6eef5b` 行）。

---

### Q11「十一万行，五个人两个月？是不是 AI 生成的？」

**书面答案**

> 先纠正一个数：新增 111,782 行里，**31,093 行是工具生成的 SBOM**，**28,414 行是测试代码**，9,149 行是文档，约 10,600 行是可复现的验收脚本与量具，真正的产品代码约 2.8 万行。
>
> 关于人：本阶段的开发主要由项目负责人本人承担，其余四位成员转为辅助开发——每个方向有一位辅助人作为第二知情人，负责该方向的设计评审、测试协助与知识备份。这个分工调整写在我们的立项章程里，不是事后补的说法。
>
> 关于工具：我们确实使用 AI 编码工具协助开发，这在立项文档的产能假设里是写明的——开发集中后的实际吞吐取决于「主开发单线程 + AI 协作工具」。我们没有把这件事藏起来。
>
> 关于质量，我们不靠自述，靠门禁：所有代码走 PR + 至少一人评审，不允许直推主干；`typecheck + lint + test` 零错误是硬门禁；另有循环依赖棘轮、mock 卫生棘轮、未使用导出棘轮三个双向严格的自动门禁。最终在同一个提交上**严格串行跑了 5 次 CI 全绿**，每次都是前一次成功后才派发下一次。

**举证指针**：§3.1 构成表；章程 §7.1（v2.3 分工调整）、§6.3 **P-1** 的 v2.3 补注（产能口径）、§7.2 条款 5；roadmap v2.29（CI 五连绿的五个 run id）；`CLAUDE.md` §3（门禁命令）。

**回答纪律**

- 不否认使用 AI 工具，也不夸大它的贡献比例——我们没有测量过这个比例，**不要给数字**。
- 不说「五个人一起写的」——章程 v2.3 已把分工改为主开发 + 四个辅助方向，说不实的分工会与章程冲突。
- 不用行数论证工作量强度；用门禁与验收数据论证质量。

---

### Q12「知识都集中在一个人身上，这个风险你们怎么看？」

**书面答案**

> 这是一个我们**知情选择**的代价，写在立项文档里，不是没人提过。分工调整之后知识集中度**实际上升**了，我们的对策是三条：设计文档先行不放松（无设计文档的功能 PR 不予合入）；每个方向的辅助人须能复述该方向核心模块的设计原理，结项时双人签字确认；以及最重要的一条——**把知识落在可查的产物上**，文档与代码即真源，降低对任何个人在场的依赖。
>
> 落到实处：设计与评估文档都在 `docs/dev/` 下（章程、路线图、协议、基座采用报告、选型决议、各评估与实测记录），每个 `@qianmo/*` 包的关键决策与**被否决的方案**都写在 roadmap 的变更记录里，验收全部有一键复现脚本。

**举证指针**：章程 §6.3 **P-2**（含 v2.3 补注）、§7.1 / §7.2；roadmap **P9.3**（双人签字 + 对基座的改造点清单）。

**回答纪律**

- 不淡化这条风险，不说「其他人也都懂」。
- 不承诺 P9.3 的双人签字已经完成（它属 S9，尚未开始）。

---

### Q13「和现有产品比，你们的创新点到底在哪？」

**书面答案**

> 一句话：**把「会话内工具」变成「网络里的常驻节点」，并把这件事的工程边界全部量出来。**
>
> 具体三条，每条都有可验证的判据：
>
> 1. **常驻与休眠唤醒**：智能体在沙箱冻结态（进程不占用可分配内存、不消耗 CPU 配额）被跨节点唤醒，60 秒内回 ack、5 分钟内回结果。真机实测 10/10，ack P95 是 8.022 秒。
> 2. **跨节点的路由与治理层**——这一层是现有智能体互操作规范里没有的：信封、跳数、两条独立时限、判环键、两级去重、两层限流、审计哈希链。我们的 A2A 对齐评估的结论正是「不在同一层」，A2A 规范里根本不存在这些字段。
> 3. **爆炸半径控制不靠人工审批，靠结构**：备份包里**根本没有删除方法**（不是带守卫的删除）；`user-confirmed` 权限只认本节点私钥签发；协商的四条消息里**没有任何字段**能表达「我这边用户同意了」。
>
> 我们也很清楚哪些不是创新：编程闭环、会话持久化、模型适配是基座既有的，我们没把它们算进来。

**举证指针**：§2 两栏表右栏；章程 §1.2 核心判断、§1.6 安全设计哲学；`a2a-gap.md` §1 / §2；roadmap v2.20 / v2.22 / v2.23 / v2.25。

**回答纪律**

- 不说「首创」「国内首个」「填补空白」——我们没有做过全面的竞品调研。
- 不把基座能力列进创新点。

---

### Q14「演示里的两个节点是真的两台机器吗？」

**书面答案**

> 分两个演示说，答案不一样，我逐个讲。
>
> **AC-2 跨节点唤醒（`demo/p41-task-result.sh`）：是真机。**目标节点跑在真实的 Dormice + gVisor 沙箱里，每一轮都先确认沙箱处于 frozen 状态再投递；宿主上同时跑 activator 和真的注册中心；发送方是一条只拨出、不监听的传输客户端。我们还做了独立核验，不采信宿主的回执——进沙箱取常驻侧的时间戳，每条 ack 都晚于沙箱内的持久化读取，全程只有一条进沙箱的链路。
>
> 另外我们做过一次**真实跨机跨架构**验证：甲骨文云的 ARM 机器到 GCP 的 x86 机器，走公网，PSK 握手 233 毫秒，10 条投递 82 毫秒，重发的 2 条被正确判重且没漏进应用层。
>
> **AC-7 场景端到端（`demo/p61-e2e.sh`）：不是。**这一条我要如实说明——A、B 两个节点是**同一个 runner 进程内的逻辑节点**。它们用的是真实的包、真实的信封、真实的 unix socket 和真实的子进程，但**不是跨机的生产拓扑**；里面的用户授权是**脚本化的本地同意**，不是真人点击；unix socket 那段是 WebSocket + PSK，我们不把它叫 TLS。生产接线归后面的演示环境固化任务包。

**举证指针**：roadmap v2.20（AC-2 真机与独立核验）、v2.12（跨机跨架构实验）、v2.30（AC-7 的「诚实边界不藏」原文）；roadmap **P8.1**（演示环境固化，未开始）。

**回答纪律**

- **不把 AC-7 说成跨机演示。**这是最容易被抓、也最伤信任的一处。
- 不说「效果一样」。说清楚哪些是真的（包、信封、socket、子进程）、哪些不是（拓扑、真人授权、TLS）。

---

### Q15「性能数据呢？唤醒要多久、能扛多大吞吐、内存占多少？」

**书面答案**

> 我把**已经测到的**和**还没测到的**分开说。
>
> **已经测到的（都是验收判据里的，有真机数据）**：跨节点唤醒到 ack，P95 8.022 秒（判据 60 秒），result 最大 12.422 秒（判据 5 分钟），10/10；沙箱冻结/解冻这一层的开销可以忽略（p50 37 毫秒）；常驻节点在冻结沙箱内被唤醒到首个内容，P95 约 4.3 秒；按名解析 8–11 毫秒；备份恢复 20 毫秒（预算 10 分钟）。
>
> **还没测到的（我不能拿本机数字充数）**：正式的性能与稳定性基线报告——24 小时长跑、n=100 的唤醒延迟阶梯、正式吞吐表——**全部待真机恢复**，目前是未完成状态。我们已经把量具、方法和文档骨架做完了，也把两个会让报告作废的测量坑钉死了：在 Bun 下 `process.memoryUsage().heapUsed` 是个冻结常量（三次采样都是 0.20 MB，而同期 RSS 从 27 涨到 106 MB），所以采样必须走 `bun:jsc heapStats`；而 `heapSize` 是滞后指标，判读必须按多点斜率。
>
> 更重要的是，我们把「本地数据不许进正式表」做成了**机器强制**——报告核心有六条 check，macOS 上没有 cgroup 通道就直接 `pass=false`。所以就算有人想拿本机数字填表，也填不进去。
>
> 另外声明一条范围：M0 的非目标里写明**只测基线、不调优**，除非某项指标卡住验收。

**举证指针**：roadmap v2.20 / v2.17 / v2.16 / v2.23（各项真机数字）、**v2.34 与 P7.3 行**（待真机项与两个测量坑）；`docs/dev/baseline-m0.md`（v0.1-draft，数据表标【待真机】）；章程 **N-12**（不做性能优化）。

**回答纪律**

- **不引用本机校准数字**当作性能结论。
- 不说「性能很好 / 很快」；说数字和它的判据上限。
- 主动说 P7.3 是 🟡 未完成。

---

### Q16「M0 之后呢？这东西打算怎么发展、会商业化吗？」

**书面答案**

> M0 只回答一个问题：「常驻 + 交流」这条路在工程上走得通吗。它的产出是**可演示、可复现、可测量的原型，不是可交付用户的产品**。
>
> 后面三个里程碑：M1 校内内测（2027 上半年），把注册发现、权限模型、记忆能力对内测用户上线，把 M0 的 TLS + 预共享密钥换成 mTLS，完成身份隔离改名的验证，并确定上游同步节奏；M2 开放公测；M3 才谈商业化。
>
> 需要说明的是：**M3 商业化有一个明确的前置条件**——我们前面提到的两条法律口径必须已经取得书面结论。这不是场面话，是写在里程碑出口条件里的。

**举证指针**：章程 §8 里程碑总览（M3 的前置条件行）、§2.1（M0 产出定性）、§2.2 **N-10**（不做商业化动作）。

**回答纪律**

- 不谈估值、不谈融资、不谈定价。
- 不承诺 M1/M2 的具体功能日期。
- 不说「已经可以投入使用」。

---

## 5. 演示视频脚本（分镜表）

> **本节是脚本，不是视频。**实际录制、剪辑、字幕与敏感信息核查（章程 §6.2 L-3 对策④「演示录屏发布前做敏感信息核查」）不在本文范围，由 P8.1 / P8.2 承接。
>
> **AC 证据全部取自 roadmap「完成状态速查」**；命令路径均已 `ls` 核实存在。

### 5.1 总体建议

| 项 | 建议 | 理由 |
|---|---|---|
| 主片总时长 | **8–10 分钟** | 竞赛评审的实际观看耐受；八条 AC 全跑一遍的真实耗时远超此数，必须剪 |
| AC-7 的处理 | **主片给 60 秒剪辑版 + 单独出一支完整版**（不剪、不加速、时间戳连续；正式三轮实测每轮约 600 s，故完整版 ≥ 10 分钟） | AC-7 判据要求「一次 ≥ 10 min、全程无人工修改代码或数据的连续演示」——**剪过的版本不能作为该判据的证据**，完整版才是 |
| 加速处理 | 允许倍速，但**必须在画面上标出倍速倍数**；AC-7 完整版**不得加速** | 加速而不标注等于伪造耗时 |
| 真机镜头 | 标注 🖥️ 的镜头**必须在真机上录**（GCP + Dormice + gVisor），画面里要看得到 `state=frozen` 那一行 | 本机录制的画面冒充真机，是 §1.4 C 类禁用项 |
| 凭据镜头 | 标注 🔑 的镜头需要真实模型凭据，录制前后按 L-3 做脱敏核查 | — |
| 画面纪律 | 录前把 `~/.occ`、`~/.qianmo`、shell history、浏览器标签清出画面；**若 Anthropic 相关协议字符串入镜，按 §4 Q9 在旁白里一句带过，不要临时切窗口** | 切窗口比出现字符串更像在藏东西 |
| 字幕纪律 | 所有数字字幕必须与 roadmap 完成状态速查一致；🟡 项若入镜必须打「未完成」角标 | — |

### 5.2 分镜表（主片）

| 镜 | 时长 | 对应 AC / 任务包 | 命令（实际路径） | 镜头要给到的输出行 | 旁白（一句） | 标注 |
|---|---|---|---|---|---|---|
| **0** | 30 s | — | 无（口播 + 标题卡） | 标题卡打出 §1.3③ 开场版全文 | 「阡陌基于团队负责人自有的开源项目 open-claude-code 构建，它以 MIT 许可发布、是本项目开始之前就存在的资产；本项目做的是常驻化改造和跨节点通信网络。」 | 口播稿即 §1.3③，**不得即兴改写** |
| **1** | 40 s | **AC-1**（P1.2） | `bash demo/ac1-restart.sh` | `kill -9` 那一行；重启后的 `session_id` 与重启前**同一串**；末尾 `PASS=… FAIL=0 WARN=0` | 「多轮任务途中直接 kill -9，重启后不重放历史继续追问，答案仍引用只存在于重启前上下文的细节。」 | 🔑 需真实模型凭据 |
| **2** | 45 s | **AC-4**（P3.3） | `bun test tests/integration/qianmo-memory-recall.test.ts` | 5 条决策的命中行（含记忆条目 **ID 与写入时间**）；3 条伪造决策的 **引用数 0** | 「写入的决策能被新会话检索到并标注来源；对没写过的伪造决策，引用数是零——引用解析不到就无法被引用。」 | 🔑 |
| **3** | 40 s | **AC-5**（P1.4） | 并排展示两次**逐字相同**的命令行 + `docs/dev/p1.4-provider-verification.md` 的结论段 | 两条命令行逐字相同的对比；两家各自的 `5 pass` | 「同一条编程任务，两个供应商适配器都跑通，两次运行只改配置文件、命令行一个字符都没变。」 | 🔑；**字幕必须带一行「缺口 G-4：两条路径仍指向同一网关」** |
| **4** | 50 s | **AC-6(a)**（P1.3） | `bash demo/ac6a-sandbox.sh` | 白名单外写入被拒的那一行；超时强杀 / CPU 节流 / 内存 OOM 三行；审计文件权限 `0600` | 「沙箱内向白名单外路径写入被内核拒绝，超时被强杀，全部留下审计。」 | 🖥️ **需真机**（Dormice + gVisor） |
| **5** | 70 s | **AC-2**（P4.1，M0 最关键一条） | `bash demo/p41-task-result.sh` | 每轮开头的 `state=frozen`；`ack p50 2.490s / P95 8.022s`；`result max 12.422s`；`link.opened=1`；`10/10` | 「对端处于冻结态——进程不占内存也不吃 CPU；按名解析、投递、唤醒、回执，十轮全中，ack 的 P95 是 8 秒，判据是 60 秒。回程走的是同一条连接，没有第二条链路。」 | 🖥️ **需真机**；`link.opened=1` 那一行是「没有第二条连接」的唯一证据，**必须给到** |
| **6** | 50 s | **AC-3**（P4.2） | `bash demo/ac3-loop-rate.sh` | 回环在**第 2 跳**被切（同屏给出 `maxHops=8`，证明不是兜底救的场）；`loop_detected` 恰 1 条且带 trace-id；运行时层第 21 条被拒；协议层第 601 条 `E_RATE_LIMITED` | 「环路在首次回访同一处理者时就被切断，不是等跳数上限兜底；两层限流各走各的键、各回各的错误码。」 | 本机可录 |
| **7** | 45 s | **AC-6(b)(c)**（P4.4） | `bash demo/ac6b-restore.sh` | `rm -rf` 之后的空目录；恢复后 `git status --porcelain` 与 `HEAD` 与删除前**逐字一致**；只写凭据的 DELETE **405**；`gitStatusWasNonTrivial: true` | 「智能体删掉自己的工作区，由它无删除权限的备份恢复；它想删备份时，那个方法根本不存在。」 | 本机；**字幕必须带「本机验证，沙箱挂载边界真机部署时补」** |
| **8** | 60 s | **AC-7**（P6.1）剪辑版 | `bash demo/p61-e2e.sh`（主片用剪辑，完整版单独出片） | 六个环节各给 8–10 秒：任务提交 → **`oom` 原因级诊断**（不是「执行失败」）→ 协商 request/offer/grant → 授权后建隧道 → 20/20 worker 跑完 → release 后**同址再拨失败** | 「节点 A 真实 OOM，拿到的是原因级诊断而不是一句失败；向 B 协商借资源，授权后建隧道，跑完自动拆除——再拨同一个地址会失败。」 | 本机；**字幕必须带「A/B 为同 runner 内逻辑节点；授权为脚本化本地同意」** |
| **9** | 40 s | **AC-8**（P7.1） | `ls tests/boundary/` + `bash demo/chaos-inject.sh --seed 2145259399`（截取报告尾部） | 五类边界文件名 + 39 条计数；混沌报告的 `177 次注入 / 7,389 条投递 / 0 停摆 / 0 未捕获 / 0 unmapped`；CI 五连绿的五个 run id | 「五类边界 39 条用例进 CI，60 分钟混沌跑批四类故障全覆盖，随机带种子可原样重放。」 | 本机；跑批用录好的片段，不现场等 60 分钟 |
| **10** | 35 s | **P7.2 审计** | `occ audit --trace <id>` 然后 `occ audit --verify` | 同一 trace 下四条消息四种命运（接收 / 被去重 / 被处理器拒 / 被限流）；`--verify` 的链完整输出 | 「一次任务的全过程，包括被拒的部分，按序号还原；哈希链可校验——但我们只声称写入方改不了、外部改动可检测，不声称不可篡改。」 | 本机；**旁白必须念出「不声称不可篡改」这半句** |
| **11** | 45 s | **成果边界举证**（章程 §5.5 / L-1） | `git show --stat 3380c88 \| tail -1`；`git log 3380c88..HEAD --oneline \| wc -l`；`git diff 3380c88..HEAD --shortstat`；`ls packages/` | 三条命令的输出各给 5 秒：`3911 files / 820,207 insertions`（基座）；`191`；`429 files / +69,812 / −1,362`；17 个 `@qianmo/*` 包名 | 「导入提交之前是基座，之后全部是本项目。八十二万行是基座的，六万九千行是我们的——其中四成是测试。」 | **必须在干净 clone 上录**（见 §3.5②）；只敲 `3380c88`，**不敲 `848ad8c2`** |
| **12** | 30 s | — | 无（口播 + 边界卡） | 边界卡逐条打出：AC-6(b)(c) 本机 / AC-7 逻辑节点 / AC-5 同网关 / P7.3 待真机 / P6.4 与 P2.4 待评审 | 「最后把边界说清楚：这几条是本机不是真机，这几条还没完成。与 Anthropic 无关联、未获其背书。」 | 这一镜**不得删**——删掉它，前面十一镜全部变成夸大 |

**主片合计：8 分 20 秒**（30+40+45+40+50+70+50+45+60+40+35+30 = 535 秒）。留 60–90 秒余量给转场与字幕停留，落在 9–10 分钟内。

### 5.3 未入主片的素材（备查/备问）

| 素材 | 命令 | 用途 |
|---|---|---|
| 常驻节点休眠唤醒细节 | `bash demo/p31-resident-wake.sh` | 被追问 AC-2 前半（P3.1）时播 🖥️ |
| activator 唤醒转发 | `bash demo/ac2-wake-forward.sh` | 被追问「唤醒那一跳」时播 🖥️ |
| 诊断分类器 50/50 | `bash demo/p51-diagnosis.sh` | 被追问原因级诊断时播 |
| 编程闭环 3/3 | `bun run verify:p32` | 被追问 P-1 核验时播 🔑 |
| 基线量具 | `bash demo/p73-baseline.sh` | **不入片**——P7.3 🟡 未完成，本机数据只作仪器校准 |

---

## 6. 双签栏

**双签已于 2026-08-17 通过**（负责人决议宣布，含安全 owner 会签；本节保留原核对清单与勾选状态作签署时点的记录）。

签署时逐条确认状态：

- [x] §1.3 三个场景版本的文字**逐字**核对，确认无 §1.4 禁用表述
- [x] §2 两栏表的「基座起点」列与章程 §3 逐行一致，未新增判定
- [x] §2 右栏 🟡 项与 roadmap「完成状态速查」一致（签署同日 roadmap v2.41 已把 P2.4 / P6.4 / P7.4 按决议关闭，P7.3 仍在途——引用 §2 时以速查表现行状态为准）
- [x] §3 的全部数字与附录 A 的原始输出一致
- [x] §3.5 三处待裁定事项已有处置决定（① 举证命令 08-15 勘误落地、改后文本随本次签认；② claude-agent-sdk 已决**保留 + 披露**（roadmap v2.36⑥）；③ vendor/audio-capture 溯源已披露（同处））
- [x] §4 Q2 / Q8 两问的书面答案随本次决议一并确认
- [ ] §5 的真机标注（🖥️）与凭据标注（🔑）与实际录制条件一致 —— **录屏尚未录制**，此条与下一条在录制完成时补勾；在此之前分镜脚本可对外、录屏成品不存在
- [ ] 演示录屏的敏感信息核查已完成（章程 §6.2 L-3 对策④）—— 同上，随录屏做

| 角色 | 姓名 | 签署 | 日期 | 备注 |
|---|---|---|---|---|
| 项目负责人 | 喻永昌 | ☑ 已签 | 2026-08-17 | 负责人决议宣布 |
| 安全方向 owner | 陈曦宇 | ☑ 已签 | 2026-08-17 | 会签随同一决议记录（决议记录口径，可复核） |

---

## 附录 A：命令与原始输出

> 全部为**只读** git 命令，执行于 2026-08-15，工作目录 `/Users/cornna/project/atlas`，分支 `s4/p4.2-loop-and-rate`，`HEAD = 6bada14c`（`fix(test): legacy 目录只读断言不再依赖 readdir 顺序`）。

### A.1 成果边界与基线核对

```console
$ git cat-file -t 3380c88
commit

$ git log -1 --format='%h %ad %s' --date=short 3380c88
3380c883 2026-08-11 feat: 导入 open-claude-code v2.38.3（848ad8c2）作为基座 —— 零改动快照 + BASE.md 溯源

$ git diff 3380c88..HEAD --shortstat        # 2026-08-16，HEAD dbecf008（08-15 首版：429 / 69812 / 1362）
 472 files changed, 111782 insertions(+), 1366 deletions(-)

$ git log 3380c88..HEAD --oneline | wc -l   # 08-15 首版：191
     217

$ git show --stat 3380c88 | tail -1
 3911 files changed, 820207 insertions(+), 34 deletions(-)

$ git show --stat 67f6081 | tail -1
 77 files changed, 4487 insertions(+)

$ git show --stat 74f7a22 | tail -1
 38 files changed, 3072 deletions(-)
```

**关于 `848ad8c2`（见 §3.5②，本机实测与章程表述不符，结论不变）**：

```console
$ git remote -v
origin	https://github.com/sweetcornna/atlas.git (fetch)
origin	https://github.com/sweetcornna/atlas.git (push)

$ git cat-file -e 848ad8c2 ; echo "exit=$?"
exit=0

$ git rev-parse 848ad8c2
848ad8c2c8daca9f5aa2410da555553e07700f5d

$ git diff 848ad8c2..HEAD --shortstat
 464 files changed, 70377 insertions(+), 555 deletions(-)

$ git for-each-ref --contains 848ad8c2        # 空
$ git branch -a --contains 848ad8c2           # 空
$ git tag --contains 848ad8c2                 # 空
$ git rev-list --all --count
194
```

→ 该对象存在于本机 pack 中但**不可从任何 ref 到达**（194 = 191 + `3380c88` + `67f6081` + `74f7a22`），因此不会出现在任何克隆里。**对外一律用 `3380c88`。**

### A.2 目录与包

```console
$ git diff 3380c88..HEAD --stat -- packages/ | tail -1
 253 files changed, 41962 insertions(+), 1 deletion(-)

$ git diff 3380c88..HEAD --shortstat -- packages/activator packages/adapter packages/audit \
    packages/backup packages/capability packages/capacity packages/diagnosis packages/memory \
    packages/negotiation packages/protocol packages/recall packages/registry packages/resident \
    packages/router packages/sandbox packages/transport packages/tunnel
 251 files changed, 41830 insertions(+)

$ git diff 3380c88..HEAD --stat -- src/   | tail -1
 56 files changed, 5152 insertions(+), 154 deletions(-)
$ git diff 3380c88..HEAD --stat -- docs/  | tail -1
 15 files changed, 4823 insertions(+), 807 deletions(-)
$ git diff 3380c88..HEAD --stat -- demo/  | tail -1
 62 files changed, 9662 insertions(+)
$ git diff 3380c88..HEAD --stat -- tests/ | tail -1
 19 files changed, 5177 insertions(+)
$ git diff 3380c88..HEAD --stat -- scripts/ | tail -1
 10 files changed, 2445 insertions(+), 7 deletions(-)
```

**⚠️ 一条会踩空的写法**：`git diff 3380c88..HEAD --stat -- 'packages/@qianmo/*'` **输出为空**——`@qianmo/*` 是包名不是目录名（目录是 `packages/<domain>/`）。答辩现场用上面列全目录的那条。

**包名清点**（`packages/*/package.json` 的 `name` 字段，前缀为 `@qianmo/` 者共 17 个）：

```
@qianmo/activator  @qianmo/adapter   @qianmo/audit     @qianmo/backup
@qianmo/capability @qianmo/capacity  @qianmo/diagnosis @qianmo/memory
@qianmo/negotiation @qianmo/protocol @qianmo/recall    @qianmo/registry
@qianmo/resident   @qianmo/router    @qianmo/sandbox   @qianmo/transport
@qianmo/tunnel
```

基座既有包（不计入本项目新增）：`@open-claude-code/{agent-tools,builtin-tools,mcp-client,tool-runtime,workflow-engine}`、`@ant/model-provider`、五个 `*-napi` 原生包、`cloud-artifacts`。

**版权头覆盖率**（章程 §5.5「版权头已决为加」的实测核对，用于 §4 Q8）：

```console
# 遍历 17 个 @qianmo/* 包的全部 .ts，检查前 3 行是否含 SPDX-License-Identifier
total .ts=217   with SPDX header=217
```

**行数构成的统计口径**（`--numstat` + 按路径分桶，见 §3.1 构成表）：

```console
# 测试文件（*.test.ts / __tests__/ / tests/ / 包内 test/）
files=140 insertions=28121
# packages/ 非测试
files=163 insertions=23257
# src/ 非测试
files=36  insertions=2834
# 根 + .github + .claude（bun.lock 为二进制式差异，不计行）
files=14  +591 −393
```

### A.3 基座 `src/` 改动与提交理由

```console
$ git diff 3380c88..HEAD --name-status -- src/ | awk '$1=="M"' | wc -l
      32
$ git diff 3380c88..HEAD --name-status -- src/ | awk '$1=="A"' | wc -l
      24
```

理由来源（每个被修改文件的所属提交，全文已在 §3.3 逐条引用）：

```console
$ for f in $(git diff 3380c88..HEAD --name-status -- src/ | awk '$1=="M"{print $2}'); do
    echo "### $f"; git log 3380c88..HEAD --format='  - %h %s' -- "$f"; done
```

涉及的 13 个提交：`189268d6`（身份派生层）、`06ad1423`（路径文案）、`4f262147`（ultracode 提示）、`47878025`（MDM 策略域决定）、`6c6eef5b`（保护清单三身份并集）、`4fe8cd1e`（常驻宿主接线）、`fdbbc2e3`（activity 走 keepalive 闸门）、`4b06f672`（input-status 只读化）、`fad809bc`（`--resume` 锚点缺陷）、`11c0a622`（审计与 `occ audit`）、`a8b06a9a`（watcher 用例）、`e86d901b`（H7 flaky）、`6bada14c`（readdir 顺序）。

---

*本文档随 roadmap P8.3 产出，定稿于负责人 + 安全 owner 双签。任何对成果边界数字的更新，直接重跑附录 A 的命令并改本文，不在其他文档另存一份（`CLAUDE.md` §1.1⑧）。*
